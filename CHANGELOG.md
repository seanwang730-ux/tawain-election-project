# Changelog

All notable changes to `test.html`（台灣立委選舉地圖）記錄於此。版本號從本檔案建立時（v1.0）開始編號；再更早的歷史沒有正式版本紀錄，只能從備份檔案回推大致內容（見下方 v1.0 說明）。

格式參考 [Keep a Changelog](https://keepachangelog.com/)。

---

## v1.0 — opencode 深色主題重構（基準版本，回推自備份比對）

在本檔案開始記錄版本之前，`test.html` 由 opencode 進行了一次大規模視覺重構，把原本的淺色主題改成深色科技風主題。這是後續所有版本的起點。

- 整體主題：淺色（`#f0f2f5` 背景）→ 深色（`#0b0d12` 背景），加入 Orbitron 字體、青色霓虹發光效果、玻璃感（backdrop-filter blur）
- 版面：頂部橫向 `#toolbar`（工具列＋ticker＋席次卡）拆解，改成左側可收合側邊欄 `#left-rail`（新增 `#sidebar-toggle`／`#sidebar-content`／`toggleSidebar()`）
- 標題從「2020 區域立委地圖」改成「台灣立委選舉儀表板 / Taiwan Election Dashboard」
- 移除多處標題後綴的席次數字（如「73席」「34席」「113席」）
- **已知造成的功能退化（後續版本逐一修復）：**
  - 「藍綠對決」(`toggle2Party()`) 的 UI 觸發元件遺失，函式變成無法從介面呼叫的死程式碼
  - `#nat-view-wrap`（全台鄉鎮市區／全台村里按鈕的顯示/隱藏邏輯）7 處呼叫全部遺失，按鈕變成常駐顯示不再依畫面層級切換
  - 部分折疊區塊（`collapseSection`）、圖例（`hist-legend`）被移除或簡化

---

## v1.1 — 修復 opencode 重構造成的功能退化

- 還原「藍綠對決」切換：在情境推演工具列補回 `<select id="sim-sub-select">`，重新接回 `toggle2Party()`，並在 `updateSimGating()` 恢復其 enable/disable 邏輯
- 還原 `#nat-view-wrap` 顯示/隱藏邏輯：在 `goED()`／`goTC()`／`goVill()`／`goVillAll()`／`goVillAllNational()`／`goTCAllNational()` 六個函式中補回對應的 `.style.display` 呼叫，讓「全台鄉鎮市區／全台村里」按鈕重新依畫面層級正確顯示

## v1.2 — 政黨輪替相關 UI 整併與簡化

- 「政黨輪替熱區」checkbox 改成按鈕型態（`.btn`/`.on` 樣式），改名為「翻盤選區」，搬進「政黨輪替分析」折疊面板內，位置在「翻盤鄉鎮市區」上一層
- 「查看翻盤鄉鎮市區」移除「查看」二字 → 「翻盤鄉鎮市區」
- 移除「翻盤鄉鎮市區」旁的手風琴三角形（`flip-accordion-icon`／`toggleFlipAccordion()`），「翻盤村里」改成常駐顯示的平行按鈕，不再需要展開
- 「政黨輪替分析」面板字體縮小，主題色從黃色 `#d4a017` 改成跟資訊欄一致的青色 `rgba(40,200,200,0.7)`（含 `#flip-analysis-box .btn` 邊框色）

## v1.3 — 棒狀圖標題整併、村里層 bug 修復

- ED／2020／2016 三個年份層級的「總統大選」標題與「總統/總統票」列標籤合併成一行（標籤直接顯示「總統大選」），移除重複標題
- ED／TC／VC 三個層級的「不分區政黨票」標題與「政黨票」列標籤合併，統一顯示「不分區政黨票」
- **修復重大 bug**：`vcCapsuleRow()`（村里層區域立委／政黨票長條圖）數字標籤誤用「原始票數」直接接「%」顯示（如 103 票顯示成「103.0%」），改回使用正確算出的百分比
- 村里層棒狀圖順序從「政黨票→總統大選」改成「總統大選→政黨票」，與 TC/ED 層一致（區域立委→總統大選→政黨票）；`histVCBox` 拆成 `_vcDistRow`／`_vcPartyRow` 兩個獨立變數以便插入總統大選區塊
- 三個層級的「棒狀圖摘要區」與「候選人長條圖列表」之間新增分隔線（共用常數 `SEC_DIVIDER`）
- 移除 ED／TC 層多餘的「${年份} 區域立委歷史得票」子標題（與上方勝選卡片重複）
- 「選舉人數」badge 改名為「預估選舉人數」，tooltip 補上估算公式說明（＝有效票數÷投票率）

## v1.4 — 毛玻璃風格統一

- 「⊞ 全選區村里」系列按鈕（約 14 處）與「全選區村里總覽／↩ 返回鄉鎮層」bar，從純色綠底改成半透明毛玻璃風格（`rgba(27,158,75,0.18)` + `backdrop-filter: blur`），與其餘深色主題按鈕風格一致
- 「逐鄉鎮進入 ▶」文字改成「點擊地圖進入鄉鎮市區 ▶」（6 處）

## v1.5 — 版面重構：浮動毛玻璃面板 + 席次卡搬移

這是最大幅度的一次版面調整，把原本「左／中／右三塊拼接」的版面改成「地圖鋪滿整頁，左右資訊欄浮動疊加」的單頁面設計。

- `#layout` 改為 `position: relative`，`#main`/`#mp`（地圖）改為 `position: absolute; inset: 0`，鋪滿整個畫面
- `#left-rail`（左側工具列）與 `#ip`（右側資訊面板）從固定寬度的 flex 欄位，改成 `position: absolute` 的半透明毛玻璃浮動面板，疊在地圖上方
- 側邊欄收合（☰）現在會把 `#left-rail` 真正縮成 28px 窄條，而不是留著空的 250px 欄位
- 4 張席次卡（區域立委推演／不分區政黨推演／原住民席次／立法院總席次）從左側欄搬到右上角新元件 `#seat-summary-float`，位置在中英文切換下方、資訊面板上方；預設收合成一行數字（`區域73 不分區34 原民6 總113 ▾`），點擊展開成橫排四卡；卡片內數字透過新函式 `updateSeatSummaryStrip()` 自動同步既有的 `updateHemicycle()` 資料，不重複邏輯

### v1.5 修復（同批次使用者回報的問題）
- `#map-title`（地圖標題）被 `#left-rail` 的浮動玻璃背景蓋住 → 把 `#map-title` 的 `z-index` 從 1 提高到 20，高於所有浮動面板
- `#left-rail` 原本固定拉到畫面底部（`bottom: 0`），4 張卡搬走後底下留下一大塊空的玻璃色塊 → 改成 `height: auto`（`max-height` 限制），面板只跟著內容高度縮放
- 側邊欄收合時 `#nav-wrap`（全台／全台鄉鎮市區）沒有跟著隱藏，內容被硬擠進 28px 窄條 → 新增 `.sidebar-collapsed #nav-wrap { display: none }`

## v1.6 — 浮動版面後續修復：跑馬燈定位、收合鈕改圓形、地圖置中

- **跑馬燈（`#ticker`）重疊左側工具列與標題**：根因是 `#mp`（地圖容器）改成鋪滿全寬後，原本相對於「扣掉側欄後的地圖區」的 `left:16px` 變成相對於「整個畫面」，直接疊到左側工具列跟標題的位置上。修法：`left` 從 `16px` 改成 `266px`（250px 側欄寬 + 原本 16px 間距），恢復原本「跑馬燈在地圖區左上角」的相對位置
- **側邊欄收合鈕改成圓形浮動按鈕**：從原本佔滿側欄寬度的長條按鈕，改成懸浮在地圖標題下方的 30px 圓形按鈕（`position: absolute; top:44px; left:14px`），收合/展開時都固定顯示在同一位置
- **修復圓形按鈕跟地圖/標題重疊、按鈕被黑色地圖蓋住的問題**：根因是 CSS 冷知識——`#left-rail` 有 `backdrop-filter`，會讓它變成內部 `position: fixed` 子元素的新 containing block（原本應該是相對整個視窗，結果變成相對 `#left-rail` 自己的框），導致按鈕定位跟疊層都跑掉。修法：把 `<button id="sidebar-toggle">` 從 `#left-rail` 內部搬出來，變成 `#layout` 的直接子元素（跟 `#map-title`同一層級），並把定位方式從 `position: fixed` 改成 `position: absolute`，徹底避開這個 containing-block 陷阱；同時加上 `outline: none` 避免瀏覽器預設的金黃色 focus 外框
- **全台地圖偏右、左側留白過多**：地圖鋪滿全寬後，`zoomTo()`／`fitFeats()`（縮放置中函式）仍然用整個 `#mp` 寬度的正中央（`W/2`）當置中目標，但左右其實各被 `#left-rail`／`#ip` 浮動面板遮住一塊，導致地圖在「看得到的範圍」裡明顯偏右。新增共用函式 `mapVisibleCenterX(W)`，即時讀取 `#left-rail`／`#ip` 目前的實際寬度（會隨側欄收合/展開、資訊面板顯示與否動態變化），把置中目標從 `W/2` 改成扣掉兩側遮擋後「看得到的範圍」的正中央，`zoomTo()`／`fitFeats()` 都套用

## v1.7 — 席次卡數字對齊、綜合／第三勢力 PVI 卡片一致性

- **總席次卡「113」數字偏左**：根因是 `#region-hemi-svg`／`#pl-hemi-svg`／`#total-hemi-svg` 三個半圓圖 SVG 都沒有設定 `viewBox`（只有 `#ind-svg` 有）。沒有 `viewBox` 的 SVG 在被 CSS（`max-width:100%`）縮小時不會等比例縮放，而是直接被裁切，導致視覺內容跟置中的數字標籤對不齊——`#total-hemi-svg`（220px，三者最寬）在浮動版面裡最容易被擠壓，因此症狀最明顯。修法：三個 SVG 都補上對應的 `viewBox="0 0 寬 高"`，讓它們跟 `#ind-svg` 一樣正確等比縮放
- **第三勢力PVI模式的卡片比綜合PVI模式小**：根因是這 4 張卡在切換模式時，「原住民席次」卡（第3張）跟「立法院總席次」卡（第4張）會被整個換成動態清單內容——`updateMismatchedCard()`（綜合PVI）把第3張換成「錯估席次」清單、`updatePVIMarginCard()`（綜合PVI）把第4張換成「黨派投票指數」清單；但 `updateThirdPVICards()`（第三勢力PVI）只換第4張（第3張維持原本較矮的原住民席次卡），而且兩種模式下清單實際筆數常常不一樣長。這些清單容器原本只設了 `max-height:125px`（上限），沒有下限，內容少的時候卡片就會跟著縮小。修法：4 處清單容器（第三勢力PVI的兩種子模式、綜合PVI的兩張卡）都補上 `min-height:125px`，讓卡片高度固定，不再因為模式或清單筆數不同而忽大忽小
- **右上角收合條的簡寫標籤現在會跟著模式變化**：`updateSeatSummaryStrip()` 新增標籤同步邏輯，讓收合條的「區域／不分區／總」文字比照卡片標題的邏輯，在綜合PVI／第三勢力PVI模式下改成對應的簡稱（如「開票」「PVI預測」「黨派指數」「熱區」），並支援中英文切換；同時因為 comp/third 模式會把第3、4張卡的原始數字元素整個換掉，收合條抓不到來源數字時會顯示「—」而不是留著切換模式前的舊數字

## v1.8 — 第三勢力PVI子分頁按鈕失效、綜合PVI三大棒狀圖、卡片尺寸終極修法

- **「不分區席次推演」「關鍵少數」按鈕點不動**：根因是 `#third-sub-toolbar` 巢狀在 `#ticker` 底下，而 `#ticker` 有 `pointer-events:none`（讓滑鼠事件穿透到下面的地圖），子項目卻沒有把它加回來，導致這兩顆按鈕看得到但點不到。修法：`#third-sub-toolbar` 補上 `pointer-events:auto`，覆蓋掉繼承自 `#ticker` 的 `none`
- **刪除綜合PVI模式下的「區域立委／總統／不分區」三大棒狀圖**：TC層、村里層其實已經在切到綜合PVI模式時排除這三大棒狀圖（改顯示選區分析／歷史開票等其他內容），只有ED層2024（預設）路徑還是不分模式一律顯示。修法：`showEDPanel()` 裡的 `_hist24inner`（= `histBoxHtml(ed)`，內含區域立委/總統大選/不分區政黨票三大棒狀圖）改成 `mode === 'comp'` 時不產生，連帶把後面接的分隔線也一併省略，避免留下一條沒有上文的孤線。2020／2016 的綜合PVI分頁本來就只有候選人明細（`_compHistCandBarsHtml`），沒有這三大棒狀圖，不用另外處理
- **第三勢力PVI卡片還是比綜合PVI小（含熱力圖模式）**：先前 v1.7 只針對「清單型」卡片內容補了 `min-height:125px`，但沒有涵蓋所有情境——例如第三勢力PVI「不分區席次推演」子模式的第3張卡其實會被換成漢迪法模擬圖（badges + svg，不是清單），跟政黨熱力圖模式（dpp/kmt/tpp等）根本不會觸發任何卡片替換、維持原始內容——這些情境都不吃到 v1.7 的清單容器修法，高度還是會因模式不同而不一致。改成更根本的作法：直接在 `.stats-card` 這個共用 class 上設 `min-height: 168px`，不管換到哪個模式（歷史勝選／綜合PVI／第三勢力PVI 任一子模式／政黨熱力圖），4張卡跟整排的高度都固定一致，不再需要針對每個模式個別調整

## v1.9 — 玻璃樣式補完、綜合PVI預設畫面、學術聲明與操作指引

- **「↩ 錯估席次」「↩ 黨派投票指數預測席次」按鈕改毛玻璃樣式**：這兩顆點進特定選區後用來「返回」卡片預設內容的按鈕，一直是舊版淺色主題殘留（`border:#d1d5db;background:#f5f5f5`），沒跟著整體深色玻璃化。已改成跟其他按鈕一致的青色玻璃樣式（`rgba(255,255,255,0.06)` + `backdrop-filter:blur`）
- **「↩ 黨派投票指數預測席次」縮寫成「↩ 預測席次」**
- **公式折疊區塊改毛玻璃**：`getCompFormulaHtml()`／`getThirdFormulaHtml()`（點開「公式」看到的完整計算式）也是同一批淺色主題殘留（`background:#fafafa;border:#ccc;color:#222`），沒人發現過。已改成跟外層 `#pvi-note` 一致的深色半透明玻璃樣式，並補上 `backdrop-filter:blur`
- ~~綜合PVI點進選區後預設顯示「預測席次」而非「鄉鎮市區分類」：移除點選區時自動呼叫 `updateCardsForED(ed)`~~ **v1.10 診斷有誤，已在 v1.10 revert**——這個修法把整個「查看鄉鎮市區分類／選區趨勢」功能連同「↩」按鈕一起拿掉了；真正的根因記錄在 v1.10
- **新增綜合PVI學術聲明 + 操作指引（`#comp-onboard-modal`）**：第一次切換到「綜合黨派投票指數」模式時，會先跳出全螢幕玻璃彈窗，內容依序為：
  1. 研究性質聲明（純學術嘗試、非替特定政黨拉票、資料來源為中央選舉委員會與 data.gov.tw、不影響投票意向與選舉結果），需點擊「我知道了，繼續」才會往下走
  2. 五步操作指引（綜合PVI概念說明含＊黨派投票指數 權重混合...＊等既有公式說明文字、權重滑桿、公式折疊、地圖顏色、席次卡），每一步用空白鍵（或按鈕）往下一步，按 Esc 可以中途關閉（但不會真的切換到綜合PVI模式，等於維持鎖定）
  在使用者按空白鍵/按鈕走完全部步驟之前，`setMode('comp')` 不會真的執行——直接把判斷寫在 `setMode()` 內部，不管是點按鈕還是切換年份下拉選單觸發，都會擋下來
  完整看完一次後用 `localStorage`（`pvi_comp_onboarded`）記住，之後切換模式不會再跳出；工具列新增一顆青色圓形「i」按鈕，可以隨時重新叫出這個聲明/指引（不影響鎖定狀態）
- **修復第三勢力PVI「不分區席次推演」子模式卡片仍偏寬/偏矮的殘留問題**：`#dhondt-hemi-svg`（漢迪法模擬圖）也缺 `viewBox`（跟 v1.7 修的三個半圓圖同一種問題），已補上；另外把 `.stats-card` 的 `flex:1` 明確寫成 `flex:1 1 0`（寬度完全由 flex-grow 比例決定，不受各自內容大小影響），並把 `overflow:hidden` 從只套用在 comp/third 模式移到 `.stats-card` 本身全域套用，確保任何模式下卡片內容都不會把卡片本身撐寬撐高

## v1.10 — v1.9「移除自動切換」診斷錯誤修正、i 按鈕移位

- **v1.9 誤刪「查看鄉鎮市區分類／選區趨勢」功能，已還原**：上一版為了讓綜合PVI「點選區後預設顯示預測席次」，直接刪掉 `showEDPanel()` 對 `updateCardsForED(ed)` 的自動呼叫，結果連帶把整個切換功能跟「↩」按鈕都拿掉了，使用者完全看不到、按不到。已還原 4 處 `updateCardsForED(ed)` 呼叫，功能恢復原樣（點選區會顯示鄉鎮市區分類/選區趨勢，可按「↩」返回預測席次/錯估席次）
- **真正的根因：綜合PVI／第三勢力PVI 互相切換時卡片內容沒有重置，殘留舊模式的資料**：`setMode()` 裡原本只有「離開 comp/third 兩個模式時」才會呼叫 `restoreCardsFromED()`（把第2、3張卡重置），條件寫成 `m!=='comp'&&m!=='third'`——但如果是**在 comp 跟 third 之間互相切換**（例如原本在第三勢力PVI選區A，直接切到綜合PVI），這個條件恆為假，卡片就會維持第三勢力模式殘留的「鄉鎮市區分類（不分區）」內容，不會換成綜合PVI自己的內容。這就是「點進去綜合pvi應該先顯示預測席次而不是鄉鎮市區分類」真正的成因。修法：把條件改成 `m!==mode`（只要跟*目前*模式不同就重置，涵蓋 comp↔third 互切），配合 `setMode()` 後段本來就會呼叫的 `updateHemicycle()`（會依照新的 `mode` 重新產生第2張卡標題與第3、4張卡內容），卡片就能正確反映剛切換進去的模式
- **「i」按鈕移到中英文切換按鈕左邊**：原本放在工具列「綜合黨派投票指數」按鈕旁邊，改成跟 `#lang-toggle` 同一個新的浮動容器 `#top-right-controls`（`position:fixed;top:10px;right:12px`），i 按鈕在左、EN/中文切換在右

## v1.11 — 跑馬燈logo大小不一、卡片裁切、單一其他候選人不折疊、常駐聲明

- **跑馬燈無黨籍logo比其他政黨大**：查了 `logoSVGs` 才發現所有政黨徽章圖檔本身其實都宣告同樣的 `14px×14px`（無黨籍也不例外），但外層 `.tk-logo` 容器只有 `10px×10px`，且沒有強制縮放——各政黨圖檔一律用自己宣告的尺寸渲染，只是剛好都一樣大所以平常看不太出來；同時「其他政黨」用的是內嵌 SVG（15px×15px），跟其他用 `<img>` 的政黨圖檔尺寸還是有微小落差。改法：`.tk-logo` 容器放大到 16px×16px，並加上 `.tk-logo img, .tk-logo svg { width:100% !important; height:100% !important; object-fit:contain }`，不管各政黨圖檔本身宣告多大，一律強制縮放成跟容器一樣大，確保完全一致；跑馬燈數字 `.tk-num` 字體也從 10px 放大到 13px
- **「黨派投票指數預測席次」半圓形席次圖被裁切、數字排版跑掉（含關鍵少數的同款卡片）**：根因是 `.stats-card` 被加了 `overflow:hidden`（v1.9 為了不讓清單型卡片撐開版面而加，`body.mode-comp/mode-third .stats-card` 這條規則更早就有），但清單型卡片（`.pvi-body`/`.mismatch-body`）自己已經有 `max-height` + `overflow-y:auto` 會捲動，外層再裁一次沒有必要，反而把非清單卡片（如半圓形席次圖）該顯示的部分連帶切掉。已移除 `.stats-card` 的 `overflow:hidden`，改成只在 `.card-title` 這個共用標題 class 加上 `overflow:hidden;text-overflow:ellipsis`，讓標題文字過長時自己安全截斷，不會影響卡片其餘內容
- **長條圖候選人折疊：只有「其他1位」時不應該折疊**：8 處「未達門檻候選人自動收合成『其他 N 位候選人』」的邏輯（ED/TC/VC 各層、各年份），原本只要有任何一位小額候選人就會被收進折疊列。改成 `minor.length === 1` 時直接照一般候選人的樣式顯示那一位候選人，`minor.length > 1` 才收合成「其他 N 位候選人」
- ~~左側工具列底部新增常駐紅字聲明，不受切換模式影響，隨時可見~~ **v1.12 修正為只在綜合PVI模式顯示**，見下方
- 第三勢力「政黨票戰略熱區排行」比綜合PVI「黨派投票指數」卡片窄/寬的問題：檢查了兩者卡片外層與清單列的 HTML/CSS 結構，寫法完全相同，找不到程式碼層級的尺寸差異來源；`.stats-card` 的 `flex:1 1 0` 已經確保 4 張卡寬度只由 flex-grow 比例決定。這次沒有找到新的成因，麻煩重新整理後再確認一次是否還會發生，如果還在的話麻煩截圖或更精確描述差異（例如是卡片本身寬度不同，還是卡片內部某個元素的寬度不同）

## v1.12 — 聲明彈窗鎖定失效、半圓圖置中方式改用flex、按鈕併行、TC層「最多票」文字

- **修復綜合PVI聲明彈窗「其實沒有強制跳出」的 bug**：根因是 `closeCompOnboarding()` 不管是「真的第一次進入綜合PVI被強制看完」還是「用 i 按鈕主動點開預覽」，只要走完所有步驟都會寫入 `localStorage`，等於使用者只要點過一次 i 按鈕看完預覽，就會被誤判成「已同意」，之後不管是不是第一次進入綜合PVI都不會再強制跳出。修法：只有*真正被強制*（非 i 按鈕觸發）走完流程才會寫入 localStorage；同時把 key 從 `pvi_comp_onboarded` 改成 `pvi_comp_onboarded_v2`，讓先前測試時已經誤寫入的舊記錄失效，可以重新測試一次完整流程
- **紅字聲明改成只在綜合PVI模式顯示**：`#disclaimer-note` 預設 `display:none`，在 `setMode()` 裡跟著 `mode-comp` 一起切換顯示/隱藏，離開綜合PVI就不會再看到
- **「黨派投票指數預測席次」半圓形圖仍偏左/被裁切，改用更穩健的置中方式**：`.hemi-center-lbl`（半圓圖中央的數字）原本用 `left:50%;transform:translateX(-50%)` 置中，改成 `left:0;right:0;display:flex;justify-content:center`——不需要抓元素自己的寬度，理論上更不容易跑位；另外 `.svg-container svg` 補上明確的 `width:100%` 與 `overflow:visible`，確保半圓圖不會被意外裁掉一部分。這是這個問題第三次回報，這次做了比之前更徹底的處理，麻煩再測一次
- **「↩ 錯估席次」「↩ 預測席次」按鈕移到跟標題同一行的右手邊**：原本這兩顆返回按鈕各自佔一整行、疊在標題正上方，改成跟「選區趨勢 DPP/(DPP+KMT)」「鄉鎮市區分類」標題同一行，標題靠左、按鈕靠右（`justify-content:space-between`）
- **TC層（鄉鎮）2024歷史勝選模式的勝選徽章文字從「勝選」改成「最多票」，跟村里層一致**：`winBadge(s, wpct, subLevel)` 這個共用函式本來就有 `subLevel` 參數控制文字（true＝「最多票」，false/未傳＝「勝選」），村里層呼叫時有正確傳 `true`，但TC層呼叫漏了這個參數，一直顯示「勝選」（鄉鎮層級不是選區實際當選判定，顯示「最多票」比較準確）。補上 `true` 參數修正

## v1.13 — 學術聲明改版、卡片對齊第四次修正、陣營篩選新功能

- **學術聲明全文改版**：彈窗第一步（聲明）跟左側常駐紅字，內容都換成新版全文（純學術性空間資料視覺化與科學模型研究、資料來源與清洗時間、GIS對齊與四捨五入可能造成的微幅偏差說明、智慧財產與 GitHub Issue／Email 回報管道），日期更新為 2026 年 7 月。`[GitHub Issue / 你的 Email]` 這段目前照你給的文字原樣保留成佔位符，之後有實際的 repo 連結或聯絡信箱要記得換掉
- ~~半圓形圖跟數字對齊，第四次修正：`.svg-container { flex:1 }`~~ **v1.14 revert，這個修法讓容器比實際圖案大一圈，反而讓數字跟圖案分得更開，已改回**
- **「淺綠 PVI +7.1%」「淺綠 開票 +7.0%」背景色點亮**：`classBadgeHtml()`（PVI／開票差距徽章共用函式）背景色透明度從 0.28 調到 0.45，顏色更飽和明顯
- **「綜合黨派投票指數 權重調整」標題置中**
- **「不分區政黨」文字改成亮藍色**：從 `#1555b4`（深藏青藍）改成 `#3b82f6`（跟站內其他「亮藍」用色一致）
- **「↩ 錯估席次」「↩ 預測席次」按鈕跟標題同一行，第二次修正**：加上 `flex-wrap:nowrap` 明確禁止換行，並讓標題本身可以在空間不夠時用 `text-overflow:ellipsis` 安全截斷，而不是把按鈕擠到下一行
- ~~新增右上角「陣營篩選」切換鈕...直接複用既有的 `getRegionHemiData()` 陣營分類邏輯~~ **v1.14 發現這個分類邏輯是錯的，已重寫，見下方**

## v1.14 — 陣營席次計算修正、彈窗新增陣營選擇頁、對齊修法revert

- **半圓形圖對齊 v1.13 的修法讓情況更糟，已 revert**：`.svg-container { flex:1 }` 會讓容器吃滿卡片剩餘空間、比圖案本身大一圈，「數字貼容器底部」跟「圖案在容器中置中」的基準點就對不上，反而讓數字整組偏移。改回原本貼齊內容大小的寫法，靠 `.stats-card` 的 `justify-content:space-between` 統一處理留白。這個問題已經修過四次還沒完全解決，如果還有殘留位移麻煩附截圖，用文字描述比較難精準定位
- **「淺綠 PVI +7.1%」「淺綠 開票 +7.0%」背景色再點亮一次**：透明度從 0.45 再提到 0.65
- **陣營席次計算邏輯是錯的，已重寫**：v1.13 用 `getRegionHemiData()` 的 `camp` 欄位去數「泛綠／泛藍」席次，但這個函式其實是照黨籍/勢力分桶（`green`/`tsr`/`npp`/`other`/`blue`），第三勢力或無黨籍當選人一律分到自己的桶、不算進 green 或 blue，導致 2024 年泛藍少算了鍾東錦（無黨籍，苗栗）這一席（漏算後是36，應該是37）。改成新函式 `countRegionSeatsByCamp(yr)`，直接逐選區找出實際當選人姓名，比對既有的陣營覆蓋表（2024用 `PVI_BACKED_MAP`、2020用 `COMP_BACKED_MAP`、2016用 `BACKED_MAP_2016`）——這些表原本就是全站計算PVI在用的陣營覆蓋表，這次只是套用到「數席次」這個新用途，沒有另外發明分類規則。同時把 `鍾東錦：kmt` 補進 `PVI_BACKED_MAP`（之前沒有任何一張表收錄他），2024泛藍席次修正後是 37（36+1），泛綠維持36，兩者相加73，跟總席次數對得起來
- **不分區政黨票（預測席次）泛綠「13」的疑慮**：這個數字來自 `HIST_PL_SEATS_2024 = {green:13, blue:13, white:8}`，是2024年民進黨不分區實際獲得的13席（中選會公告數字），不是逐選區當選人統計，本身沒有「陣營覆蓋表」可以套用——不分區席次是直接照各政黨自己的政黨票得票比例分配，不會有「第三勢力候選人算進哪個陣營」的問題。如果你認為這個數字不對，麻煩告訴我你預期的數字是多少，或是想要的計算方式（例如是不是想把民眾黨不分區席次也按候選人光譜切一部分算進泛綠/泛藍？），我才能對症下藥
- **左側常駐聲明加上 ⚠️ 警示圖示，整塊改成紅框毛玻璃卡片樣式**（背景 `rgba(239,68,68,0.08)` + 紅色邊框 + `backdrop-filter:blur`，不只是純文字）
- **i聲明彈窗新增最後一頁「陣營選擇」**：在原本的操作指引後面新增一頁，用三顆按鈕（泛藍／中間／泛綠）取代原本的「下一步」，選擇後直接完成整個聲明流程並套用該陣營篩選——等於一次解決「同意聲明」跟「設定初始陣營偏好」兩件事。選「中間」時，收合條的「開票」「預測席次」改成兩行同時顯示藍/綠兩個陣營的數字（例如「藍37」換行「綠36」），不用再切換
- **陣營篩選鈕补上「中間」第四個狀態**：循環改成 總→泛綠→泛藍→中間→總，跟彈窗最後一頁的三選項一致
- **關於「i聲明沒有強制跳出」**：重新檢查過 `setMode()` 裡的判斷邏輯跟 `closeCompOnboarding()` 的寫入時機，程式碼看起來是對的（只有真正首次進入且 `localStorage` 沒有 `pvi_comp_onboarded_v2` 記錄時才會跳過強制彈窗）。如果重新整理後還是沒有跳出來，麻煩用無痕視窗（或先清一次瀏覽器對這個檔案的 localStorage）測一次，排除舊記錄或分頁快取的干擾；如果無痕視窗還是不會跳，麻煩告訴我，我再往其他方向查

## v1.15 — 三角形變色、「原民/錯估」標籤範圍修正、找到「泛藍泛綠預測13」真正的原因

- **全台鄉鎮市區折疊三角形**（`#nat-accordion-icon`）顏色從紫色 `#9b6bc4` 改成跟資訊面板一致的青色 `rgba(40,200,200,0.7)`
- **「原民」→「錯估」標籤修正**：之前這段邏輯被寫在陣營篩選（泛綠/泛藍/中間）的分支裡面，導致沒有另外選陣營時（`campFilter` 為預設值）還是顯示「原民」。改成獨立判斷，只要 `mode === 'comp'` 就換成「錯估」，不管有沒有另外篩選陣營
- **找到「泛藍泛綠預測席次都是13，跟圖表完全不一樣」的真正原因**：v1.14 誤以為這是不分區政黨票的官方數字（13/13，中選會公告），但實際檢查 `refreshPLHemiDots()` 才發現，綜合PVI模式下卡2「黨派投票指數預測席次」畫的**根本不是不分區政黨票結果**——它把73個選區各自的 PVI 正負值畫成73個點（`is73` 分支），圓心數字也顯示「73」不是「34」，跟卡1的「選舉結果」是同一種73選區的圖，只是一個是實際當選結果、一個是PVI模型預測結果。之前陣營篩選的「預測席次」數字卻是拿 `getPLSimSeats()`（不分區政黨票官方13/13/8）去套，兩個完全是不同資料源，難怪跟圖表對不起來。改成新函式 `countCompPVISeatsByCamp(yr)`，邏輯跟 `refreshPLHemiDots()` 的 `is73` 分支完全一致（同一批73選區、同樣算法算出每區 PVI 值），PVI>0算泛綠、PVI<0算泛藍，這樣陣營篩選顯示的數字才會跟卡2實際畫出來的73個點是同一件事
- **確認「i聲明沒有強制跳出」是瀏覽器 localStorage 殘留，不是程式錯誤**：用無痕視窗測試後確認彈窗有正常強制跳出，證實這個分頁在先前測試時已經寫入過 `pvi_comp_onboarded_v2`，不需要再改程式

## v1.16 — 卡2半圓形圖真正找到尺寸不一致的原因、「接近」徽章文字顏色修正

- **半圓形圖「沒有居中、沒有一樣大小」修過五次終於抓到根本原因**：`#pl-hemi-svg` 的 HTML 原始 `viewBox` 寫死是 `"0 0 160 80"`（不分區34席版面用的座標系統），但綜合PVI模式下 `refreshPLHemiDots()` 的 `is73` 分支改用跟卡1同一份 `REGION_SEATS` 座標（用 `buildHemiSeats(..., 90, 80, ...)` 算出來，中心點 x=90、y基準=80，是照 180×90 的座標系統算的），JS只有把 `width`/`height` 屬性改成180/90，卻從來沒有跟著把 `viewBox` 也改成 `"0 0 180 90"`——等於用90年的圖釘座標畫在還是160寬的畫布設定上，圖案內容自然對不齊、也填不滿卡2的框，跟卡1（viewBox本來就是180×90，兩者天生對得起來）看起來大小/置中都不一樣。修法：`is73` 分支額外加 `.attr('viewBox','0 0 180 90')`，34席分支加回 `.attr('viewBox','0 0 160 80')`，兩個分支都跟著各自用的座標系統切換viewBox
- **「接近 PVI +0.9%」「接近 開票 -0.9%」這類徽章的「接近」文字顏色問題**：`classBadgeHtml()` 原本 `|pvi|<=1` 時文字寫死用純黑 `#222`，第一次修法改成用分類自己的顏色（灰色 `#757575`），但實測後發現還是不對——因為徽章背景 `bg` 是 `pviColor(pvi)` 疊 0.65 透明度蓋在整個網站的深色底（`#0b0d12`）上，淡色（如 pvi 接近0時算出來接近白色）疊上深色底、透明度只有0.65，實際看到的並不是淺色，而是被深色背景拉深後的中間調，這種背景不管黑字或灰字對比都不夠。既然其他分類（深綠/淺綠/淺藍/深藍）本來就統一用白字且都沒問題，代表深色主題下這個特例邏輯根本不必要，直接砍掉 `|pvi|<=1` 的特殊分支，全部分類都固定用白字（跟其餘分類一致），順便清掉因此變成死碼的 `clr` 變數

## v1.17 — 折線圖超框、切換模式/年份後鄉鎮村里卡片殘留舊內容

- **綜合PVI模式下「區域立委趨勢」等折線圖超出卡片框框**：`distTrendChartHtml()`／`tcTurnoutTrendHtml()`／`distTrendChartHtmlThird()`／`distTrendChartHtmlPR()` 這五個折線圖函式的 SVG 都是寫死 `width="340"`（或290/其他固定像素值），但外層卡片（`.stats-card`）寬度是彈性的（`flex:1 1 0`，實際寬度看視窗/側欄大小而定）——卡片比340px窄的時候，SVG就直接溢出卡片邊界。改成 `viewBox` + `width:100%;height:auto;max-width:${W}px`（標準SVG響應式寫法），卡片變窄時圖表會照比例縮小塞進去，不會再超框；卡片夠寬時仍維持原本設計大小，不會被拉爆
- **切換模式或年份後，鄉鎮／村里卡片還留著切換前的舊內容**：這是這次「2020/2016跟2024排版不一樣」問題的真正原因，不是年份資料本身的問題。在鄉鎮層 hover 一個鄉鎮時（綜合PVI／第三勢力PVI模式），最上排4張卡會被暫時換成「催票率／村里分類／區域立委趨勢」，用 `_tcCardsActive` 這個旗標記錄「目前是不是換過」；離開這個畫面時要靠呼叫 `restoreCardsFromTC()` 把卡片換回來、旗標歸位。但 `goTC()`／`goVill()`／`goVillAll()`／`goVillAllNational()`／`goTCAllNational()` 這五個換頁函式都漏了呼叫這個還原函式（只有 `goED()` 和 `showEDPanel()` 兩處原本有呼叫），所以只要「曾經在綜合PVI/第三勢力PVI模式下 hover 過某個鄉鎮或村里」，之後不管怎麼切換模式或年份、切到哪個選區，只要重新進入鄉鎮/村里層，都會看到上一次殘留的舊卡片內容，跟目前實際模式/年份對不起來。五個函式都補上 `restoreCardsFromTC()`，統一跟 `goED()`/`showEDPanel()` 的寫法一致

## v1.18 — 歷史勝選2020/2016鄉鎮村里層：候選人長條圖缺漏、排序錯誤、總統/政黨票資料年份誤植

再深入確認「排版不一樣」問題後，發現除了上一版的卡片殘留還有三個真正的內容/資料問題（不是單純排版）：

- **歷史勝選2020，鄉鎮層完全沒有候選人長條圖**：`showTCPanel()` 的 `winnerYear==='2020'` 分支只組出「區域立委/總統大選/不分區政黨票」三大棒狀圖（`histBoxHtml2020()`），從來沒有像2024版本（`_tc24CandFull`）一樣把候選人逐一得票列出來（傅崐萁/蕭美琴/黃啓嘉…）。補上候選人長條圖（沿用既有的 `_compHistCandBarsHtml()`，跟ED層/2016層共用同一套major/minor收合邏輯）＋有效票/投票率，順序照2024版本放在三大棒狀圖後面
- **歷史勝選2020，鄉鎮層與村里層的「總統大選」「不分區政黨票」兩個bar其實是2024年的資料，年份完全對不起來**：這兩個bar原本呼叫 `getTCPresVote()`/`getTCPartyVote()`（鄉鎮層）或直接讀 `vs.rg/vs.pg` 等欄位（村里層），但這些函式/欄位都是從 `VC_SIM` 的**頂層**欄位算的，而頂層欄位就是2024年資料（2020專屬資料是另外包在 `VC_SIM[vc].v20` 底下，且只有候選人得票 `.cands`，沒有總統/政黨票子欄位）。實際去查資料檔才發現真正的2020村里級政黨票資料表 `PL20_VC` 早就存在（且有真實數字），只是這兩處都沒有拿來用。修法：不分區政黨票改用新寫的 `getTCPartyVote2020()`（鄉鎮層，逐村里加總`PL20_VC`）／直接讀 `PL20_VC[vc]`（村里層）；總統大選目前沒有村里級2020資料表，改傳 `null` 讓 `histBoxHtml2020()` 內建的ED級 `PRES_VOTES_2020[ed]` fallback接手，至少年份是對的（不會再是2024的數字）
- **歷史勝選2020，村里層的三大棒狀圖跟長條圖順序反了**：原本是「候選人長條圖→有效票/投票率→總統大選bar→不分區政黨票bar」，改成跟2024版本（`_vcHistBox24 + SEC_DIVIDER + _vc24Full`）一致的「三大棒狀圖（`histBoxHtml2020()`）→候選人長條圖→有效票/投票率」
- **歷史勝選2016，鄉鎮層完全沒有三大棒狀圖也沒有候選人長條圖**：`showTCPanel()` 的 `winnerYear==='2016'` 分支其實已經正確算好了年份對的資料（`getTCPresVote2016()`/`getTCPartyVote2016()`，這兩個函式本身沒問題，是真的2016資料），但算完之後完全沒有放進輸出的HTML裡（`_tcPresData16w`/`_tcPLData16w` 算完就沒再用到，是死變數），只顯示一個「最多票」標籤就結束。補上 `histBoxHtml2016(...)` 三大棒狀圖＋候選人長條圖＋有效票/投票率，跟2020鄉鎮層/2024版本的內容結構一致。（2016村里層原本就有完整內容，這次沒有動）

## v1.19 — 高雄圓餅圖鄉鎮層抓錯2020資料key、16個村里缺2020資料改顯示鄉鎮層退而求其次

- **高雄市第8選舉區前鎮區、第6選舉區（鹽埕/鼓山等）、第7選舉區鳳山區，在歷史勝選2020模式的鄉鎮市區層都沒有圓餅圖**：逐一比對 `TC_2020` 實際存的資料key後發現，`redrawPies()`／`redrawSwing()` 裡用來查2020資料的 `_kh24tk`／`_kh24sk` 這兩張表，內容其實是抄了2016年的舊選區編號（鹽埕/鼓山2016年是屬於「第5選舉區」，前鎮2016年是「第7選舉區」，鳳山2016年是「第8選舉區」），但2020年這幾個里的選舉區編號其實已經跟現在(2024)一樣了（鹽埕/鼓山→第6、前鎮→第8、鳳山→第7），只有2016年的編號不同。用錯的舊表去查`TC_2020`當然查不到資料，圓餅圖就整個不畫。修法：改用跟`_pie24`（已經驗證是對的「現在」選區對照表）一樣的內容
- **全台約16個村里（如霧臺鄉佳暮村、屏東市公園里、東港鎮數個里、坪林區/龍崎區「石曹里」、仁愛鄉德鹿谷村等）在2020模式顯示「無2020選舉資料」，但2024正常**：直接用JSON解析`VC_SIM`逐筆檢查後確認，這些村里在資料裡**真的完全沒有`v20`欄位**（不是查詢邏輯抓錯位置），推測是2020年之後才從其他村里拆分出來的最小行政單位，2020年源頭資料本來就沒有細到這個村里層級可以拆。這16個是真實的資料缺口，不是bug，沒辦法無中生有生資料出來；但至少可以退而求其次——改成去查該村里所屬鄉鎮的2020鄉鎮層級資料（`TC_2020`）並顯示，加一行橘色提示文字說明「此村里無2020村里級資料，改顯示所屬鄉鎮層級資料」，比之前完全空白好
- **屏東縣的圓餅圖/村里範圍問題還在查**：屏東的情況原本猜比高雄複雜（一度以為2024也變回3個選舉區），後來你確認2020、2024其實都只有2個選舉區（第十屆起維持至今），3個選舉區是2016（第七屆~第九屆）的舊制。詳見v1.20
- **臺南市第5/6選舉區**：確認仁德/歸仁/關廟/龍崎這4個區的村里在2024確實是分屬兩個選舉區（跟高雄苓雅區類似），但目前資料庫裡完全找不到任何一份「這4個區裡哪些村里屬於第5、哪些屬於第6」的清單（`VC_ED`只顯示全部都是第5），沒有這份清單就沒辦法正確分割，這部分需要你確認有沒有其他資料來源

## v1.20 — 屏東縣2020/2024選區從根本上修正（不只是圓餅圖，整個鄉鎮/村里導航都受影響）

你確認了關鍵事實：**屏東縣從第十屆（2020）起選區「數量」就從3個併成2個，2020、2024都是同一套2個選舉區的分法，只有2016（第七~九屆）是3個選舉區的舊制**——
- 第1選舉區（2020/2024）：里港鄉、高樹鄉、三地門鄉、霧台鄉、九如鄉、鹽埔鄉、長治鄉、內埔鄉、瑪家鄉、屏東市、麟洛鄉
- 第2選舉區（2020/2024）：萬丹鄉、泰武鄉、竹田鄉、萬巒鄉、潮州鎮、新園鄉、崁頂鄉、南州鄉、新埤鄉、來義鄉、東港鎮、林邊鄉、佳冬鄉、枋寮鄉、春日鄉、枋山鄉、獅子鄉、牡丹鄉、車城鄉、滿州鄉、恆春鎮、琉球鄉

拿這份真實名單去對照 `ED_GJ`（全台選舉區地圖本體）發現：**`ED_GJ` 本身完全正確**，屏東縣就只有2個polygon（「第1選舉區」「第2選舉區」），沒有第3選舉區這個形狀存在。問題出在**鄉鎮/村里層級**的資料：`data_map_v64.js` 裡每個鄉鎮自己記錄的「ed」欄位（`TC_GJ`、`VC_ED`）還停在2016年3個選舉區的舊分法，而且從來沒有人幫屏東寫過像高雄那樣的「新舊選區對照表」去修正。

實際影響範圍比原本以為的「圓餅圖沒顯示」嚴重很多——只要點進「屏東縣第2選舉區」，因為底層20個鄉鎮裡有20個的舊`ed`標籤跟現在對不上（16個鄉鎮舊標籤還寫著已經不存在的「第3選舉區」、潮州/竹田/萬巒/泰武這4個舊標籤寫著「第1選舉區」），比對 `f.properties.ed === curED` 全部失敗，這些鄉鎮/村里會直接從地圖上消失，不是只有圓餅圖的問題，是整個鄉鎮市區/全選區村里導航都受影響。

修法：從你提供的真實名單建了一份完整的「屏東鄉鎮代碼→現在(2020/2024通用)選舉區」對照表（`_pt24`／`_pt24v`／`_pt24p`／`_pt24pv`／`_pt24s`／`_pt24sv`／`_pt24b`，依原本高雄表的既有命名慣例，在每個原本只有高雄對照表的地方旁邊加一份），補進 `goTC()`、`goVill()`、`goVillAll()`（村里filter與鄉鎮邊界filter兩處）、`redrawPies()`（鄉鎮層與村里層兩處）、`redrawSwing()`（鄉鎮層與村里層兩處）共9個位置，跟高雄現有的表並存、互不影響（代碼開頭不同，10013開頭是屏東、64000開頭是高雄）。2016模式的路徑沒有動，本來就是走另一條正確的 `TC_2016`/`TC_TO_ED_2016` 邏輯

## v1.21 — 臺南市第5/6選舉區同樣修正（含東區跨區村里分割）

你補了臺南第5/6選舉區的真實名單，包含2020/2024現行的6個選舉區完整範圍，以及2016（第九屆）只有5個選舉區的舊制範圍。比對後跟屏東是同一種bug：`ED_GJ` 本身沒問題（臺南就是6個選舉區的形狀），但鄉鎮/村里層的 `ed` 欄位一樣停在2016年5個選舉區的舊分法——仁德/歸仁/關廟/龍崎（舊分法都算「第5選舉區」）其實整區都改劃進現在的「第6選舉區」；永康區（舊法也算第5）改劃進「第4選舉區」；新化/新市（舊法算第2）也改劃進「第4選舉區」；安平/中西（舊法算第3或第4）改劃進「第5選舉區」；南區（舊法算第4）改劃進「第5選舉區」；官田區（舊法算第1）改劃進「第2選舉區」。另外東區本身現在**跨第5/6兩個選舉區**，用你給的45個里名單（16個里屬第5、29個里屬第6）逐一比對出村里代碼，程式邏輯上跟高雄苓雅區的村里級拆分完全同一招（`_tn5east` 這個Set）

修法：建了臺南鄉鎮代碼→現在選舉區對照表（`_tn24`／`_tn24v`／`_tn24p`／`_tn24pv`／`_tn24s`／`_tn24sv`／`_tn24b`），跟屏東的表用一樣的命名邏輯加在同樣9個位置的旁邊；東區的村里級拆分（`_tn5east`／`_tn5eastPv`／`_tn5eastSv`）比照苓雅的做法，只加在村里層級的4個位置（鄉鎮層級單一polygon沒辦法拆，這點跟苓雅在TC層的處理方式一致，不算新問題）

## v1.22 — 新竹縣第1/2選舉區也是同一種病（全選區村里圓餅圖跑不出來）

跟屏東/臺南一樣的病，但起因更單純：新竹縣以前（2020以前）全縣只有1個選舉區「新竹縣選舉區」，2020起才第一次分成2個選舉區，但13個鄉鎮的 `ed` 屬性完全沒有一個更新過、全部還停在「新竹縣選舉區」這個現在已經不存在的舊名字——不像屏東/臺南是「編號錯置」，這次是完全沒編號，所以只要點進第1或第2選舉區，比對永遠對不上，兩個選舉區都會整個空掉。

這次不用麻煩你查真實範圍——`ED_2020` 剛好從2020年就已經有「新竹縣第1選舉區」「新竹縣第2選舉區」分開的紀錄（不像屏東2020年連續兩屆都改變選舉區數量），直接逐鄉鎮比對 `TC_2020` 真實資料key就能還原正確分法：
- 第1選舉區：新埔鎮、關西鎮、湖口鄉、新豐鄉、芎林鄉、尖石鄉
- 第2選舉區：竹東鎮、橫山鄉、北埔鄉、寶山鄉、峨眉鄉、五峰鄉
- 竹北市本身跨第1/2選舉區（人口太多），用2020/2024兩年候選人姓名逐村里比對出12個里屬第1選舉區、19個里屬第2選舉區（`TC_STATS`/`TC_2020` 都各自有兩筆分開的竹北市資料可以互相驗證，不是用猜的）

修法：跟屏東/臺南同樣的模式，建了新竹鄉鎮對照表（`_hc24`／`_hc24v`／`_hc24p`／`_hc24pv`／`_hc24s`／`_hc24sv`／`_hc24b`）加在同樣9個位置，竹北市的村里級拆分（`_hc1zhubei`／`_hc1zhubeiPv`／`_hc1zhubeiSv`）比照苓雅/臺南東區的做法

## v1.23 — 新北市／桃園市：6個人口最多的區跨2~3個選舉區，全部修正

順便系統性掃了一次全國每個選舉區，比對「真的存在的選舉區形狀（`ED_GJ`）」跟「每個鄉鎮自己記錄的ed屬性」，發現新北市（第3/4/6/7/8選舉區）、桃園市（第3/4選舉區）也是同一種病，但更複雜——這6個選舉區底下完全沒有任何一個鄉鎮的ed屬性對得上，因為板橋區、三重區、中和區、新莊區（新北）、桃園區、中壢區（桃園）這6個全國人口最多的區，本身就跨2個選舉區（不是簡單併區/拆區，是同一個區的村里被切成兩半分別劃給不同選舉區），鄉鎮層級的資料本來就不可能表示這種村里級的切法。這個問題你還沒回報過，是系統性掃描順便發現的。

你提供了桃園市（現行6個選舉區）跟新北市（現行12個選舉區）完整的真實範圍，逐村里比對出以下6組跨選舉區的大區：
- 桃園區（跨第1/4選舉區）：15里屬第1、67里屬第4
- 中壢區（跨第3/6選舉區）：76里屬第3、12里屬第6
- 三重區（跨第2/3選舉區）：16里屬第2、103里屬第3
- 新莊區（跨第4/5選舉區）：75里屬第4、9里屬第5
- 板橋區（跨第6/7選舉區）：65里屬第6、61里屬第7
- 中和區（跨第8/9選舉區）：76里屬第8、17里屬第9

比對過程中發現你給的名單裡有少數幾個里名跟資料庫實際村里名不完全一致（多半是「舘/館」等異體字，或極少數명稱誤植），逐一寫程式比對「真實村里總數」與「兩個選舉區加起來的村里數」是否完全吻合（不能有遺漏也不能有重複），核對到完全一致才採用，其中「立言里」（新莊）、「公舘里」（板橋）兩個是實際存在但沒被列在你的名單裡的村里，依照命名規律/異體字比對後歸類。除了這6個跨區大區以外，新北/桃園其他鄉鎮的ed屬性本身都已經是正確的（逐一核對過你給的完整範圍都對得上），不需要額外修正。

修法：跟之前幾個縣市不同，這次改成在檔案前面統一定義一份 `_splitVcSets`／`_splitVcMatch()`（因為板橋一組就要存61個里代碼，繼續用之前「每個地方各複製一份表」的寫法會讓檔案多出太多重複資料），只在4個村里層級的地方（`goVill()`／`goVillAll()`／`redrawPies()`村里分支／`redrawSwing()`村里分支）各加一行呼叫 `_splitVcMatch()`；新北市/桃園市不需要鄉鎮層級的對照表（不像屏東/臺南/新竹，這兩個縣市除了這6個大區以外都是對的）

## v1.24 — 全台村里/全台鄉鎮市區的滑鼠經過黃色框框，是完全獨立、沒被前幾版動過的另一段程式碼

你回報「全台村里地圖mode 黃色框框（滑鼠經過時顯示選區範圍）2024抓的是2016的範圍，高雄/臺南/新竹縣有重劃的也都不對」——這個黃色框功能（`goVillAllNational()`/`goTCAllNational()` 的 `mouseover`）是完全獨立於前6版一直在修的 `goTC`/`goVill`/`goVillAll`/`redrawPies`/`redrawSwing` 之外的另一段程式碼，之前都沒碰過，所以同一種病（直接讀 `VC_ED[vc].ed`／`f.properties.ed` 這個stale欄位）在這裡完整重現了一次。

順便把前6版分散在各函式裡的縣市對照表整理成2個全域函式方便重複使用：
- `_splitVcSets`／`_splitVcMatch()`：擴充進苓雅、臺南東區、新竹竹北市（原本只有新北/桃園的6個跨區大鎮），現在9個跨選舉區的村里級拆分全部集中在同一份表
- `resolveCurrentEd(tc8, vc)`：新函式，把高雄/屏東/臺南/新竹的鄉鎮對照表也整合成同一份全域表 `_countyEd24`，給定tc8（跟vc，若有）就回傳「現在真正屬於哪個選舉區」的正確答案

黃色框框的兩處 `mouseover` 都改用 `resolveCurrentEd()` 決定要畫哪個選舉區的外框，不再直接信任 `VC_ED`/`TC_GJ` 裡的舊選區標籤。全台鄉鎮市區層（`goTCAllNational`）因為滑鼠經過的是整個鄉鎮（沒有村里細節），遇到板橋/三重/中和/新莊/桃園區/中壢區這種跨區大鎮時仍然沒辦法精準框到單一選舉區（跟同一批函式其他地方在TC層級的既有限制一樣），但全台村里層（`goVillAllNational`，村里級，才是你這次主要回報的）已經完全精準。順手也拿掉了這段程式碼裡一行忘記移除的除錯用 `console.log`

## v1.25 — 歷史勝選2020/2016：拿掉多餘的標題/圖例文字

依你的要求，`histBoxHtml2020()`／`histBoxHtml2016()`（兩個共用函式，ED/鄉鎮/村里層在歷史勝選2020/2016模式下都會呼叫到）拿掉：
- 「2020 歷史真實得票／2016 歷史真實得票」標題整行，以及底下「■ 民進黨 ■ 國民黨 ■ 民眾黨 ■ 時代力量 ■ 其他」圖例整行
- 「總統大選」後面的「（蔡/韓/宋）」「（蔡/朱/宋）」小字副標

另外拿掉v1.18新增的候選人長條圖區塊標題（內容/數字都保留，只拿掉標題文字）：
- 鄉鎮層「本鄉鎮候選人得票」（2020、2016都有）
- 村里層「2020 區域立委歷史得票」「2016 區域立委歷史得票（村里）」
- 選區層（ED，全台選區總覽時）2016版的「2016 區域立委歷史得票」

只處理「歷史勝選」(winner mode)，綜合PVI／第三勢力PVI模式下用到同名文字的地方（例如綜合PVI模式ED層收合區塊「查看歷史開票」裡的「2020/2016 區域立委歷史得票」）沒有動，因為那些是不同的UI脈絡（本來就摺疊起來，標題有其導覽作用）

## v1.26 — 陣營篩選鈕循環回「總」時PVI預測顯示「—」

`updateSeatSummaryStrip()` 開頭那段「數字：跟現有的4張卡同步」邏輯，是直接讀 `pl-hemi-center`（卡2半圓形圖中間的「73」那個數字節點）的文字內容回填收合條，這是「總」狀態唯一沒有自己重新算一次數字的地方——泛綠/泛藍/中間三個狀態都各自呼叫 `countCompPVISeatsByCamp()` 直接算出正確數字，只有「總」偷懶去讀DOM文字。問題是只要曾經hover過任何一個鄉鎮/村里（綜合PVI模式下這會把卡2整個換成催票率清單），`pl-hemi-center` 這個節點就從DOM上被整組拿掉了，之後不管hover結束後有沒有換回來，只要陣營篩選鈕剛好circle回「總」，就讀不到這個節點，落到函式自己寫的 fallback「—」。

修法：幫「總」狀態也補上跟其他三個陣營狀態一樣的邏輯，直接呼叫 `countRegionSeatsByCamp()`/`countCompPVISeatsByCamp()` 算出泛綠+泛藍相加的總數直接填入，不再依賴卡片DOM節點還在不在

## v1.27 — 摺疊區塊標題顏色、綜合PVI 2024 ED層加上「歷史開票」摺疊、拿掉2020/2016摺疊內多餘標題

- **`collapseSection()` 摺疊標題文字顏色從黑改白**：`color:#222` 是深色主題改版前留下的舊值，這個函式是全站共用（村里分類/區域立委趨勢/查看歷史開票等等全部摺疊區塊都是同一個函式），改成 `rgba(255,255,255,0.85)` 之後全部一起變亮，不只是綜合PVI/第三勢力PVI模式那幾個
- **綜合PVI（不含第三勢力PVI，先維持現狀）2024模式的ED層候選人得票清單，加上「歷史開票」摺疊包裹**：原本2024模式候選人清單是直接展開顯示，跟2020/2016模式（本來就摺疊在「歷史開票」裡）不一致，改成一致，預設收合
- **拿掉綜合PVI模式ED層「歷史開票」摺疊區塊內多餘的「2020 區域立委歷史得票」「2016 區域立委歷史得票」標題**：跟上一版（v1.25）拿掉歷史勝選模式的同名標題是同一件事，這次是comp mode版本。確認過綜合PVI模式在鄉鎮/村里層並沒有這個標題（那两层直接用`cmpBoxHtml()`呈現，本來就没有多一層標題），只有ED層需要處理

## v1.28 — 區域立委趨勢折線圖，深綠/深藍的點跟數字看不清楚

`distTrendChartHtml()`（區域立委趨勢DPP/(DPP+KMT)折線圖）的顏色分級，數值離50%越遠（越深綠/越深藍）配色反而越暗（`#1b5e20`深綠、`#0d47a1`深藍），這兩個顏色同時用在圓點填色跟數字文字上，在深色底的面板上對比度太低，比中間淺色那幾級（`#66bb6a`、`#64b5f6`）還難看清楚，剛好是最極端、理論上最需要一眼看到的資料點最不明顯。改成 `#22c55e`（亮綠）跟 `#3b82f6`（亮藍，跟站內其他「亮藍」用色一致），中間幾級的淺綠/淺藍沒有動（那些原本對比度就夠）

## v1.29 — 2016鄉鎮層候選人長條圖順序錯亂（范雲/呂欣潔等排在最前面）

`_compHistCandBarsHtml()`（v1.18新增，鄉鎮層2020/2016候選人長條圖、以及ED層綜合PVI模式歷史開票摺疊區塊共用的函式）完全沒有依票數排序，直接照原始資料陣列的順序渲染——2016年的候選人原始資料陣列通常是照「候選人抽籤號次」排列，不是照得票率，導致像范雲、呂欣潔這類候選人只因為抽籤號次靠前就被排到長條圖最上面，不管實際得票是多是少。村里層用的是另一段有排序的程式碼（`sortedVC16`），所以你才會看到「鄉鎮層亂、村里層正常」的落差。

這個問題不是2016專屬——`_compHistCandBarsHtml()`同時也是2020鄉鎮層、以及綜合PVI模式ED層「歷史開票」摺疊區塊在用的同一個函式，只是完全沒排序，2020鄉鎮層理論上會有一模一樣的問題，只是你可能剛好還沒特別注意到。檢查過其他候選人長條圖的地方（ED層2024/2020/2016預設版本、鄉鎮層2024預設版本、村里層2020/2016）都各自有自己的 `.sort()`，只有這一個函式漏掉，修好這一個函式就同時修好它所有的呼叫點（鄉鎮層2020/2016、ED層綜合PVI 2020/2016）

## v1.30 — 2020全選區村里模式，臺北市松山/中正/士林區在跨區的兩個選舉區之間分不清楚

你回報「臺北市第3/7選舉區的全選區村里模式，松山區有分不開的問題，pie圖跟激戰選區都是對的」。查證後發現：松山區（跟中正區、士林區一樣）本身就跨兩個選舉區（松山跨3/7、中正跨5/8、士林跨1/2，`ED_GJ`本身是對的，只是這三個區在鄉鎮層級沒有單一選舉區可以標記），這點跟這幾週修的高雄苓雅/臺南東區/新竹竹北市是同一種情況——但這次幸運的是，`VC_ED`／`VC_SIM`村里級的`ed`欄位本身分得完全正確（不像臺南東區那樣全部都錯標成同一區），所以理論上不需要額外建對照表。

真正的bug在 `goVillAll()` 用來加速判斷2020模式選區歸屬的 `_tc2020toED`（鄉鎮代碼→2020年TC_2020實際資料key）這張表——它是用「後面蓋掉前面」的方式從`TC_2020`的key建出來的，遇到像松山區這種**一個鄉鎮代碼在TC_2020裡對應兩筆不同選舉區資料**的情況，就會把整個鄉鎮武斷歸類成其中一個（剛好是第7選舉區，因為它在原始資料裡排在後面），導致點選第3選舉區時松山區20個真正屬於第3區的村里全部消失，點第7選舉區時卻把松山區全部33個村里（該歸第3的也算進來）都塞進去，兩個選區「分不開」。pie圖／激戰選區之所以沒事，是因為那兩個功能的程式碼從來沒有用過這條`_tc2020toED`捷徑，本來就是直接讀`VC_ED`/`VC_SIM`逐村里判斷，這次臺北市剛好證明這個資料來源沒問題

修法：讓建立`_tc2020toED`的迴圈同時記錄「這個鄉鎮代碼在原始資料裡出現過不只一個選舉區」（`_tc2020Ambiguous`），凡是被標記為有歧義的鄉鎮，`goVillAll()`／`buildFlipCacheVill()`都改成跳過這條捷徑，直接退回逐村里判斷（`VC_ED`/`VC_SIM`）。這是通用修法，不只修松山/中正/士林，之後全台如果還有其他我沒發現的跨區鄉鎮也會自動正確處理，不用再一個一個手動列表

## 2020鄉鎮層總統大選棒狀圖，不同鄉鎮/村里數字都一樣（已知限制，非bug，需要你提供資料）

這是v1.18就講過的已知限制，這次確認一下：全台目前**完全沒有村里級或鄉鎮級的2020年總統大選得票資料**（`VC_SIM`頂層的`pres_g/pres_b/pres_w`是2024年資料，`v20`子物件只有立委候選人得票，沒有總統得票子欄位），只有選區(ED)級的`PRES_VOTES_2020`。v1.18當時把總統大選bar改成"傳null讓histBoxHtml2020用ED級資料當fallback"，所以同一個選區內，不管點哪個鄉鎮/村里，總統大選棒狀圖顯示的都是整個選區的同一組數字——這不是沒更新，是本來就只有這個精細度的資料可以顯示。如果你有村里或鄉鎮級的2020總統大選得票資料（蔡英文/韓國瑜/宋楚瑜三組數字），可以提供給我，我可以比照`PL20_VC`的方式加進去；沒有的話這個限制目前沒辦法繞過去

## v1.31 — 綜合PVI「加權（相對全國平均）」「區域立委板塊移轉」藍色不夠亮

`cmpBoxHtml()`（hover選區/鄉鎮/村里時跳出的PVI資訊小方塊）裡「加權（相對全國平均）」跟「2016→2020／2020→2024區域立委板塊移轉」這兩行，數字是負值（偏藍/國民黨）時用的是`#1555b4`，跟這次連續修過好幾次的其他地方（折線圖深藍、不分區政黨文字）是同一個偏暗的舊藍色，在深色底面板上不夠亮。統一改成`#3b82f6`（站內既有的「亮藍」慣用色），權重滑桿即時更新那段程式碼（`cpSpan.style.color`）也是同一組顏色，一併改掉

## v1.32 — 不分區政黨票超過4個分類時，第5個數字（灰色其他）跟長條圖對不上

你附的截圖清楚看到：「區域立委」列4個分類（綠/藍/白/灰）數字對齊得很好，但「不分區政黨票」列有5個分類（民進黨/國民黨/民眾黨/時代力量/其他），第5個「其他」數字被擠到獨立一行、偏到最左邊，跟它自己灰色的長條圖區塊（在最右端）完全對不上。

根因：`.hist-nums` 這個class本身是寫死 `grid-template-columns: 1fr 1fr 1fr 1fr`（固定4欄）的CSS grid，`plBar2020()`／`plBar2016()`（不分區政黨票）在分類數等於或少於4個時看起來沒事，一旦到5個分類（比如同時有時代力量估算值又有剩餘其他），第5個項目就會被grid自動擠到新的一行、也就跟它對應的長條圖區塊對不齊了。同時檢查了 `regionBar()`（區域立委列，候選人多的時候一樣會超過4個）——這個函式本來是想用 `style="flex-wrap:wrap"` 處理超出的狀況，但 `.hist-nums` 本身是 `display:grid` 不是 `flex`，`flex-wrap` 對grid容器完全沒作用，所以候選人數一多（例如混亂選區有5位以上候選人）也會出現一樣的對不齊問題，只是還沒被你抓到。

修法：`plBar2020()`／`plBar2016()`／`regionBar()`（3個地方，ED/鄉鎮/村里三層共用同一份）都改成用行內樣式依實際分類/候選人數量動態決定 `grid-template-columns:repeat(N,1fr)`，不再寫死4欄，數字永遠跟它自己的長條圖區塊對齊

## v1.33 — 加入2016總統大選鄉鎮/村里級真實得票資料

你提供了中選會官方2016總統大選得票數一覽表（`總統-A05-2`鄉鎮級、`總統-A05-3`村里級，涵蓋全部22縣市）。寫程式逐縣市解析這批xls檔案，把「鄉鎮/村里名稱」比對成資料庫既有的村里代碼（沿用NM對照表），過程：

- 鄉鎮層368個全部比對成功，加總後跟全國已知2016總統大選總票數（蔡英文6,894,744／朱立倫3,813,365／宋楚瑜1,576,861）完全吻合，確認解析正確
- 村里層約7800個中，7604個直接比對成功；其餘約200個主要是2016年當年開票所把好幾個現在已經獨立記錄的村里合併回報（連江縣/屏東市/東港鎮最多），這種情況讓合併範圍內每個村里都指向同一組合併後的數字（不是分不出來就丟掉，至少讓使用者查得到當年那個開票區的真實結果）；剩不到20個是村里名稱本身對不到（推測是2016年之後被整併掉的村里，或極少數罕見字造成的編碼問題），這些留白，會自動退回選區級數字

資料存成 `PRES_TC_2016`（368個鄉鎮）／`PRES_VC_2016`（7604個村里），格式 `[朱立倫,蔡英文,宋楚瑜,有效票]`，放在原本 `PL16_TC`/`PL16_VC` 旁邊

**改成逐村里加總、依現在的選區歸屬分（不是死板照鄉鎮總表）**：`getTCPresVote2016()` 原本設計是要從 `VC_SIM[vc].v16.pres_g` 讀真實資料，但 `v16` 這個子物件其實從來沒有總統票欄位，所以這段邏輯永遠不會命中，一路都是fallback用全國單一數字（`HIST_PRES_VOTES_2016`）硬套用在每個鄉鎮/選區上，跟2020總統票的狀況一模一樣。現在改成優先逐村里加總 `PRES_VC_2016`，鄉鎮村里比對不到才退回鄉鎮總表 `PRES_TC_2016`，再不行才退回選區級近似值。ED層（`showEDPanel`）跟村里層（`showVCPanel`，含目前的主要分支跟legacy fallback分支）也都比照修正，不再各自散落沒接上真實資料的死程式碼

## v1.34 — 加入2020總統大選鄉鎮/村里級真實得票資料

同樣方式處理你提供的2020年官方資料（`總統-A05-2`鄉鎮級、`總統-A05-3`村里級）。這次比對率更好：鄉鎮層368個全部成功，村里層約7750個中7694個直接比對成功（99.3%），加總後跟全國已知2020總統大選總票數（蔡英文8,170,231／韓國瑜5,522,119／宋楚瑜608,590）完全吻合。剩餘約56個未比對到的村里，模式跟2016一樣（主要是屏東市/東港鎮/連江縣等地當年開票所合併回報，或極少數村里後來被整併掉），一樣用「合併範圍內每個村里指向同一組數字」處理，比對不到的留白退回選區級數字。

資料存成 `PRES_TC_2020`（368個鄉鎮）／`PRES_VC_2020`（7694個村里），格式 `[宋楚瑜,韓國瑜,蔡英文,有效票]`（2020候選人號次跟2016不一樣，注意順序）。

新增 `getTCPresVote2020()`（仿照 `getTCPresVote2016()` 的寫法：逐村里加總`PRES_VC_2020`，比對不到才退回鄉鎮總表，再不行才退回選區級`PRES_VOTES_2020`），鄉鎮層與村里層（`showTCPanel`／`showVCPanel`）都接上真實資料，不再是v1.18當時因為沒有資料而寫的null/ED級fallback。**注意**：既有的 `getTCPresVote()`（沒有年份後綴的那個）本來就是給2024模式用的，`VC_SIM`頂層的`pres_g/pres_b/pres_w`欄位對2024來說是正確資料，這次沒有動它，只新增了2020專用的版本，避免搞混

ED層（`showEDPanel`）2020模式本來就是直接呼叫 `histBoxHtml2020(ed)`（沒有另外傳presData），內建fallback本來就會抓到真實的選區級 `PRES_VOTES_2020[ed]`，這部分原本就是對的，這次沒有需要修正的地方

## v1.35 — 2016歷史勝選單一鄉鎮村里模式，跨區鄉鎮（松山/中正/北投/中和/三民等）會多畫出不屬於這個選區的黑色村里

你附的截圖（臺北市第3選舉區→松山區）看到：進入松山區的村里畫面時，明明選的是第3選舉區，卻混進了實際屬於第7選舉區的村里（例如慈祐里），而且這些村里呈現黑色（不是正常的勝選顏色）。這是跟這幾週修的「跨區鄉鎮」同一類問題，但發生在不同的程式碼、只在2016模式、只在「點進單一鄉鎮的村里畫面」（`goVill()`，麵包屑「選區›鄉鎮」）這個路徑，2020模式因為走的是另一段（這次新增的`_splitVcMatch`那條路）完全沒事，「全選區村里」（`goVillAll()`）用的是另一個函式，邏輯本來就是對的。

根因：`goVill()` 處理2016年跨區鄉鎮（松山/中正/北投/新北中和/高雄三民…只要TC_2016裡同一個鄉鎮代碼對應兩筆不同選舉區資料的都算）的邏輯，本來就有算出每個村里「是否真的屬於目前選區」（用候選人姓名比對，變數`_in`，邏輯本身是對的，也正確存進了`_vSplitInVc`這個Set），但過濾函式最後寫的是 `return true`，等於算完`_in`卻沒有拿來用，不管是不是真的屬於這個選區，全部村里照樣被畫出來。程式下面雖然還有一段用`_vSplitInVc`把不屬於的村里改成透明色的保護機制，但顯然沒有可靠地擋住（`_vSplitInVc`在某些情況下可能是空的，一空整組保護就形同虛設，所有村里退回正常上色邏輯，對不屬於這個選區的村里來說，正常上色邏輯找不到資料就變黑色）。

修法：把 `return true` 改成 `return _in`，從根本的資料過濾這關就把不屬於目前選區的村里排除掉，不再依賴下游那層「不夠可靠」的透明化保護。跟`goVillAll()`的既有寫法（本來就是 `return _isIn`）現在邏輯一致了

**修完後你回報還是有問題**：黑色的村里格是不見了，但整塊松山區還是有一大塊深色色塊。原因是`goVill()`裡還有另一層背景：`gBg.append('path')...class('bg-fill')`，畫的是`TC_GJ`裡**整個鄉鎮**的輪廓（不分選區），本來是設計給正常單一選區的鄉鎮鋪底用的深色色塊（CSS `.bg-fill{fill:#121a28}`）。之前因為所有村里（含不屬於這個選區的）都畫在上面剛好蓋住整個鄉鎮範圍，所以看不出這層背景的存在；這次改成只畫真的屬於這個選區的村里之後，不屬於的那一半範圍沒有村里畫在上面遮住，這層鄉鎮輪廓背景就露餡了，變成一整塊深色色塊，比之前的黑色村里格更大、更明顯。

修法：只有在`_vSplitInVc`有內容（代表這個鄉鎮2016年真的跨了兩個選區）時才跳過這層背景，不要讓它畫出來；正常沒有跨區的鄉鎮不受影響，鋪底效果照舊

## v1.36 — 2024/2020歷史勝選+綜合PVI單一鄉鎮村里圓餅圖，會混進其他跨區鄉鎮同選區的村里（鼓山顯示苓雅、仁德/歸仁顯示東區、竹東/湖口顯示竹北市）

你回報：2024和2020模式（歷史勝選、綜合PVI都有）點進鼓山區的村里圓餅圖畫面時，會多畫出苓雅區（實際跨第5/6選舉區的鄉鎮）的村里圓餅圖，但反過來點進苓雅區看不會多出鼓山區的；同樣的問題也發生在臺南仁德/歸仁顯示東區的村里、新竹縣竹東/湖口顯示竹北市的村里。這個不對稱現象（A看得到B，B看不到A）是關鍵線索：鼓山、仁德、歸仁、竹東、湖口都是「整鄉鎮完整屬於同一選區」的一般鄉鎮，苓雅/東區/竹北市則是「同一個鄉鎮代碼底下的村里分屬兩個不同選區」的跨區鄉鎮。

根因在`redrawPies()`負責畫村里層圓餅圖的過濾邏輯（`goVill()`點進單一鄉鎮時，`level`實際上是`'vill'`，走的正是這段村里層程式碼，不是我原先以為的`'tc'`層）。這段程式碼原本為了正確處理跨區鄉鎮的村里所屬選區判斷，加了一段呼叫`_splitVcMatch(tc8, curED, vc)`的邏輯，只要這個函式回傳的不是`null`（代表這個村里屬於一個已知的跨區鄉鎮），就直接`return`這個結果——問題是這個`return`是從整個外層`.filter()`的callback直接跳出，完全跳過了後面本來用來限制「只顯示目前鄉鎮的村里」的關鍵一行`return villAllMode || vc.startsWith(curTC)`。

結果：當你在`goVill()`單一鄉鎮模式下看鼓山區（`curTC='64000020'`，`villAllMode=false`）時，過濾函式跑到苓雅區（跨區鄉鎮，`tc8='64000080'`）的某個村里，`_splitVcMatch`發現這個村里「所屬的選區」剛好等於`curED`（鼓山所在的同一個選區），就直接回傳`true`——但完全沒檢查這個村里的`vc`是不是真的以`curTC`（鼓山的代碼）開頭，於是苓雅的村里就被誤畫進了鼓山的畫面。鼓山本身不是跨區鄉鎮，沒有這條`_splitVcMatch`早退路徑，所以永遠會乖乖走到最後那行的鄉鎮範圍檢查，才會呈現「鼓山看得到苓雅、苓雅看不到鼓山」這種不對稱結果。

修法：把`_splitVcMatch`的判斷結果拆成兩種情況分開處理，不再讓它直接跳出filter——回傳`false`（明確不屬於這個選區）時才直接排除；回傳`true`時，還是要接著檢查`villAllMode || vc.startsWith(curTC)`才能決定是否真的畫出來，這樣才能同時滿足兩種需求：`goVillAll()`（全選區村里模式，`villAllMode=true`）想要選區內所有跨區鄉鎮的村里都顯示；`goVill()`（單一鄉鎮模式，`villAllMode=false`）則只顯示真正屬於目前這個鄉鎮的村里，即使其他跨區鄉鎮的村里剛好也屬於同一個選區也不該顯示。

順便檢查了同檔案裡另外兩處也用到`_splitVcMatch`的地方：`redrawSwing()`的村里層過濾在呼叫`_splitVcMatch`之前，已經有`if (!villAllMode && !vc.startsWith(curTC)) return false;`先擋掉不屬於目前鄉鎮的村里，本來就沒有這個問題；`goVillAll()`裡的那處呼叫是設計給「顯示整個選區所有村里」用的，本來就不該限制鄉鎮範圍，維持直接`return`是正確的，都不需要修改。

## v1.37 — 補上2020/2016歷史勝選鄉鎮/村里層的當選打勾標記；「預估選舉人數」改名「推算選舉人數」

**當選打勾（✔）補齊**：2024模式的候選人得票長條圖，當選人姓名前面會有一個綠色✔，其他人字體變灰、長條變半透明——這個視覺效果原本只接在2024年資料上，2020和2016完全沒有。逐層檢查後發現：
- ED（選區）層：2020、2016都已經有了（之前就做過），不用動
- TC（鄉鎮）層：2020、2016都是透過共用函式`_compHistCandBarsHtml(candsArr, tot, yr)`畫候選人長條，這個函式完全沒有打勾邏輯——這是這次真正要補的地方
- VC（村里）層：2016已經有（`showVCPanel`裡有自己的一套`isWinnerMode`/`winnerName`邏輯），但2020是另一段獨立的inline渲染程式碼，也完全沒有打勾——這是另一個要補的地方

`_compHistCandBarsHtml`加了第4個參數`winnerName`（不傳或傳null時完全不影響既有行為，因為這個函式也被綜合PVI模式的「歷史開票」摺疊區塊共用，那邊使用者沒有要求打勾，不能因為這次改動而多長出打勾）。TC層在winnerYear==='2020'呼叫時傳入`ED_2020[s.ed].wn`、在winnerYear==='2016'時傳入`ED_2016[_ed16tcw].wn`（`_ed16tcw`已經是mapEDNameTo2016()轉換過的2016選區名）；VC層2020的inline渲染直接補上跟其他地方一致的`isElected`/`ns`/`check`/`op`四件套邏輯，同樣用`ED_2020[_ed20].wn`當作真正的選區當選人。

打勾用的「當選人」全部都刻意查**選區層級**的`.wn`（ED_2020/ED_2016），不是TC/VC本地資料自己的`.wn`——因為TC/VC自己的`.wn`代表的是「這個鄉鎮/村里得票最多的人」（對應旁邊「最多票」徽章），跟「這個候選人有沒有真的贏得整個選區」是兩回事，必須分開，跟原本ED層、VC層2016版的既有寫法保持一致。

而且只在`mode==='winner'`（純歷史勝選）時才啟用打勾，`dpp`/`kmt`等政黨熱力圖子模式不受影響，維持原樣。

**改名**：「預估選舉人數」→「推算選舉人數」（英文 Est. Voters → Derived Voters）。原因：這個數字是用「有效票數÷(1-廢票率)÷投票率」從官方公布的投票率、廢票率反推出來的，不是真正意義上的「預測/估計」，改成「推算」更準確反映這是個回推計算值。

（原本也考慮幫2020模式加一個「推算選舉人數 vs 2016」的對照徽章，但2016的ED_2016/TC_2016資料完全沒有投票率/廢票率欄位，沒辦法用同樣公式反推2016的選舉人數，你確認後決定先不做這個——後續在v1.38補上了。）

## v1.38 — 補齊2016立委選舉投票率/廢票率資料；修正「推算選舉人數」公式在ED/TC/VC三層不一致；加上2020對照2016的推算選舉人數徽章

**發現一個我自己講錯的地方**：上一版（v1.37）改名的時候，我以為ED層的「推算選舉人數」徽章公式已經包含`÷(1-廢票率)`這一項、只有TC/VC層少算——後來仔細看程式碼才發現搞錯了：ED層那個徽章實際用的變數是`_edElv24`/`_edElv20`，這兩個從頭到尾都**沒有**`(1-廢票率)`這一項；我當時看到的`÷(1-廢票率)`其實是同一個函式裡「鄉鎮市區分類」摺疊區塊用的另一個變數`_edElig`，兩個變數長得像但不是同一個。所以v1.37我幫ED層title文字補上的公式說明，反而變成「講的公式」跟「實際算的公式」對不起來。這次三層（ED/TC/VC）都統一補上`÷(1-廢票率)`，讓公式、title說明、實際計算三者一致。這個誤判是我自己的疏忽，不是你的問題。

**2016立委投票率/廢票率資料**：你提供的`各投票所得票明細及概況-2016/立委-A05-2V-(縣市).xls`（22個縣市檔案，每個檔案依選舉區分成不同分頁，欄位有效票數A/無效票數B/投票數C/選舉人數G/投票率H都是逐鄉鎮、逐村里的官方數字），我寫了python腳本解析：
- 投票率(tr) 直接取欄位H
- 無效票比例(ir) = B÷C×100（欄位沒有直接給，用無效票數B除以投票數C自己算）
- ED層總計行取自每個分頁最上面的「總計」列；TC層取每個鄉鎮的小計列；VC層取每個村里的資料列
- 解析出73個選舉區、368個鄉鎮、7604個村里的tr/ir，全國加總反推的投票率66.58%，跟2016年區域立委實際投票率（官方公布66.58%）完全吻合，驗證解析正確
- 跟之前解析2016總統大選資料一樣，遇到約215個因為原始報表把多個村里合併成一列報票（屏東市、東港鎮、連江縣等）而配對不到的村里，比照舊做法：用頓號拆開合併列，能對到代碼的村里就分配同一組數字，對不到的維持沒有tr/ir（極少數，不影響整體）
- 資料以`TR_ED_2016`/`TR_TC_2016`/`TR_VC_2016`三個新增的const表存放，載入後用`Object.assign`直接把`tr`/`ir`欄位併進既有的`ED_2016`/`TC_2016`/`VC_2016`物件裡——這樣原本程式碼裡好幾處早就寫好、但因為沒有2016投票率資料而一直沒作用的計算（例如`showTCPanel`的2016投票率徽章`turnoutBadgeHtml(s16tc.tr, s16tc.ir)`、`updateCardsForTC`的2016推算選舉人數、`thirdBoxHtml`的2016版本）全部自動跟著生效，不用額外改動

**順便修正一個2016村里面板的舊bug**：`showVCPanel`的2016主要分支（`VC_2016`有資料時），投票率徽章原本寫的是`turnoutBadgeHtml(info.tr, info.ir)`——`info`是`VC_ED[vc]`，也就是**2024年**的資料，在2016面板上顯示2024的投票率，年份對不起來（這是舊code，不是這次新增的）。現在2016終於有自己的投票率資料了，改成`turnoutBadgeHtml(_vc16.tr, _vc16.ir)`（`_vc16`=`VC_2016[vc]`），另外沒有村里級2016資料、退回舊版`VC_SIM.v16`資料的那個fallback分支，因為`_v16`本來就沒有tr/ir，改成用`_v16.tr`/`_v16.ir`（會是undefined，直接不顯示徽章）而不是繼續顯示錯誤的2024數字。

**「推算選舉人數 vs 2016」徽章**：仿照既有「vs 2020」徽章的寫法，在ED/TC/VC三層的2020模式面板都加上這個徽章，公式一樣是`有效票數÷(1-廢票率)÷投票率`，只是拿2020年算出來的選舉人數對比2016年算出來的選舉人數。ED層原本2020分支的標題列沒有徽章顯示區（跟2024分支不一樣），這次順便補上跟2024分支一致的flex版面。

## v1.39 — 修2024村里層打勾bug（重劃選區失效）；泛藍/泛綠英文翻譯；陣營篩選按鈕切語言不即時更新；2016候選人黨籍勘誤

**2024村里層打勾功能其實是壞的**：你說「2024 village level 增加勾勾功能」，但程式碼裡其實早就寫了打勾邏輯（`showVCPanel`裡的`isWinnerMode24`那段），問題出在當選人判斷用的是`ED_STATS[info.ed]`——`info`是`VC_ED[vc]`，它的`.ed`欄位在**重劃選區**的村里（今年會遇到這問題的地方主要是本季一直在修的高雄/屏東/臺南/新竹縣/新北市/桃園市這些split鄉鎮）可能還殘留舊選區名稱，`ED_STATS`（2024當年資料）裡查不到這個舊名稱，`edStats`變成null、`winnerName`變成null，結果是**每個候選人都判定「未當選」**，打勾整個消失、大家都變灰色半透明。同一個函式下面其實已經有寫好的修正值`_vc24Ed`（優先用`VC_SIM`的即時選區歸屬，`VC_ED.ed`只是備援），但打勾那段沒有用到它。這次把打勾邏輯改成用同一套修正過的選區名稱（`_vc24EdForWinner`），問題應該只出現在重劃選區的村里，非重劃地區原本就正常。

**泛藍/泛綠翻譯**：陣營篩選按鈕、開場選陣營的按鈕，英文原本直接寫「Blue」/「Green」，你說應該翻成「Lean KMT」/「Lean DPP」比較精確（不是在講顏色，是在講政治傾向），已改。

**陣營篩選按鈕切換語言不會立刻更新**：`#camp-toggle-btn`（陣營篩選收合條上那顆按鈕，顯示「總／泛藍／泛綠／中間」）的文字是在按鈕被**點擊**的當下用`btn.textContent = T(zh, en)`直接寫死賦值的，不是用`data-zh`/`data-en`屬性掛在DOM上、讓全域`applyLang()`統一重新翻譯的那一套系統。所以你切換語言（例如中→英再切回中）的時候，`applyLang()`跑過一輪、其他所有文字都更新了，但這顆按鈕因為壓根不在`applyLang()`的處理清單裡，文字停留在切換語言前按鈕最後一次被點擊時的語言，要再點一次按鈕本身（讓campFilter狀態改變一次）才會刷新。這次在`applyLang()`裡加了一段，直接讀目前的`campFilter`狀態、重新設定按鈕文字，跟其他按鈕一樣即時同步。

**2016候選人黨籍勘誤**：你列了幾個名字，我用votetw.com／自由電子報2016選舉專題等來源逐一查證：
- 鄭村棋：原本寫「人民民主黨」，這個黨名是誤植，官方登記的正確名稱是「**人民民主陣線**」，已修正
- 張育華（你打成「張玉華」，資料庫裡查無此人，但「張育華」確實存在且跟鄭村棋同樣是人民民主陣線在高雄市第8選舉區的候選人，判斷是你筆誤，已用「張育華」加入）
- 董建一：機車黨的分類其實原本就是對的（`code:'mkp'`），問題出在`logoSVGs`裡完全沒有`mkp`這個key，所以黨徽圖示顯示不出來——已用你提供的`emblems/中華民國機車黨黨徽.png`補上
- 吳忠錚：原本完全沒分類，查證確實是健保免費連線（臺北市第1選舉區），已加入
- 溫國銘：原本完全沒分類，查證是勞動黨（彰化縣第2選舉區），已加入（沿用既有的`lp`勞動黨代碼跟黨徽）
- 李婉鈺：原本村里級資料裡是`"p":"other"`（其他），查證新北市第7選舉區的李婉鈺確實是無黨籍，已加入`ind`分類修正

**順便查到並修好一個更嚴重的資料損毀**：檢查「還有哪些candidate沒分類」時發現`ED_2016`/`TC_2016`/`VC_2016`裡溫國銘的名字本身就是壞的——217筆資料裡有110筆姓名寫成亂碼替代字元（Unicode U+FFFD `�`），另外106筆寫成簡體/日式異體字「温」而不是正體「溫」，只有1筆是對的。應該是原始資料建置時期（不是這次session做的，是更早以前）的編碼轉換問題造成的姓名損毀。已經全部統一改成正確的正體「溫國銘」，順便也修好一個對照英文姓名表裡同一筆資料的殘留亂碼（`"� Guo-ming"`→`"Wen Guo-ming"`）。全檔案搜尋確認沒有其他U+FFFD殘留。

**追加分類**：順便系統性比對了`ED_2016`/`VC_2016`裡所有還沒被`KNOWN_PARTY_2016`覆蓋、被歸類成「其他」的候選人名單（不是只查你點名的幾個），另外查到5位：林獻山、楊悟空（泛盟黨）、林麗容（正黨）、鄭振源（台灣未來黨）——這3個黨之前完全沒出現在系統裡，新增了對應的顏色分類（沒有黨徽圖檔，先用純色塊）。確認`VC_2016`（最細顆粒度的資料）裡已經沒有任何「其他」分類的候選人名字是漏網之魚了。

## v1.40 — 「推算選舉人數 vs 2016」在重劃選區算出離譜數字（+50%/-48%）；機車黨/人民民主陣線黨徽；台灣基進候選人長條圖顯示成民進黨綠；村里全選區按鈕配色；吳忠崢姓名勘誤

**「推算選舉人數 vs 2016」在重劃選區算出離譜的數字，你抓得完全正確**：高雄市第6選舉區顯示+50.2%/+105,827人、新竹縣第1選舉區顯示-48.3%/-191,376人，四年內選舉人數不可能有這種變化。根因正是你猜的：v1.38實作這個徽章時，2016對照值直接查`ED_2016[mapEDNameTo2016(ed)]`——用選區名稱字串去對照2016年的舊資料，但「高雄市第6選舉區」這個名字2016年根本不存在（2016年高雄市選區劃分完全不同，第5-8選舉區都是2020年以後才有的新選區），`mapEDNameTo2016()`把它對應到某個舊選區名，查到的是**完全不同地理範圍**的舊資料；新竹縣的情況更明顯：2016年新竹縣只有1個選舉區（涵蓋全縣），2020年才拆成2個，所以「新竹縣第1選舉區」對照到的2016資料其實是**整個新竹縣**的選舉人數，比實際上只涵蓋半個縣的2020年「第1選舉區」多了快一倍，才會出現-48%這種崩盤式的數字。

修法：不能再用「選區名稱查表」這種方式比較兩個邊界不同的選區，改成**逐村里重新加總**——每個村里的2016推算選舉人數（用村里自己的tr/ir/tot算，村里的地理範圍本身不會因重劃而改變）先算好，再用`resolveCurrentEd(tc8, vc)`（這學期一直在用、專門處理高雄/屏東/臺南/新竹縣/新北市/桃園市這些split鄉鎮的既有函式）判斷這個村里「現在（2020/2024年）」真正屬於哪個選舉區，逐村里歸戶加總成`_ED_ELIG_2016`/`_TC_ELIG_2016`兩個查表，取代原本的`ED_2016[mapEDNameTo2016(ed)]`直接查表。修完後驗算：高雄市第6選舉區變成+0.55%（低於1%顯示門檻，直接不顯示徽章，屬正常）、新竹縣第1選舉區變成+2.37%、新竹縣第2選舉區+9.75%、高雄市第5選舉區-0.1%，數字終於落在合理範圍。沒有重劃的其他選區不受影響（`resolveCurrentEd`對他們回傳null，直接fallback用原本村里自帶的選區欄位，等於還是原本的加總方式）。

**機車黨/人民民主陣線黨徽**：`logoSVGs`裡本來完全沒有`mkp`（機車黨）、`pdmp`（人民民主陣線）這兩個key，所以就算候選人黨籍分類是對的，圖示也不會顯示（可能因此讓你覺得鄭村棋「還是無黨籍」——其實黨籍名稱早就對了，只是沒有圖示）。這次用你給的`emblems/中華民國機車黨黨徽.png`、`emblems/People_Democratic_Party_logo.png`補上這兩個key，鄭村棋、張育華、董建一都會正確顯示黨徽了。

**2020歷史勝選台中市第2選舉區，台灣基進候選人（陳柏惟）長條圖顯示成民進黨綠色**：根因在`regionSegs2020()`這個2020年區域立委長條圖的顏色判斷函式——裡面的`COMP_BACKED_MAP`為了PVI計算方便，把陳柏惟這類「非典型技術性歸類」候選人算作dpp陣營（這是正確、刻意的設計，PVI數字不能動），但`_eff()`只針對蘇震清/洪慈庸/陳癸佑/林昶佐/趙正宇這幾位有特別覆寫成灰色顯示，陳柏惟沒被排除、就直接落到`COMP_BACKED_MAP`的'dpp'分支，長條圖跟著PVI計算邏輯一起顯示成綠色。系統裡其實早就有`KNOWN_PARTY_2020['陳柏惟'] = {code:'tsr', name:'台灣基進', color:'#B33A1E'}`這筆正確資料，只是被前面的dpp判斷擋住、永遠輪不到它生效。比照傅崐萁的既有寫法（`if (c.n === '傅崐萁') return 'other';`），加一行`陳柏惟`的同款覆寫，讓他跳過dpp/kmt/tpp快速通道，落到後面呼叫`getCandidateDisplay()`取得他真正的台灣基進顏色跟名稱這條路徑上。PVI計算完全沒動，只改了長條圖顯示。

**村里「全選區村里總覽／返回鄉鎮層」按鈕配色**：原本是綠色主題（`rgba(27,158,75,...)`背景、`#4ade80`文字），跟資訊面板本身的深色系（`rgba(18,24,38,0.55)`背景、近白色`rgba(255,255,255,0.92)`文字）不一致，這次改成跟資訊面板容器`#ip`同一套配色。

**吳忠錚姓名勘誤**：查證健保免費連線在臺北市第1選舉區的候選人正確寫法是「吳忠**崢**」，不是「吳忠**錚**」（兩個字形近但不同），資料庫裡115筆全部改正，`KNOWN_PARTY_2016`裡的key也同步改名。

## v1.41 — 系統性補齊2020歷史勝選候選人黨籍分類（94位）

你問「2020 hist mode還有一堆沒有更新logo的候選人」，順便要我查有沒有分類錯的。跟上次2016一樣，先確認`KNOWN_PARTY_2020`裡目前用到的18個黨籍代碼在`logoSVGs`裡都有對應圖示（結果：都有，沒有缺漏），所以問題不是「分類對但沒圖示」，是「根本沒被分類」——逐一比對`ED_2020`裡所有被標記成`"p":"other"`、但沒有出現在`KNOWN_PARTY_2020`裡的候選人姓名，抓到97位（扣掉傅崐萁/洪慈庸/趙正宇這3位本來就有`getCandidateDisplay()`裡的特殊處理，剩94位真的完全沒分類）。

這次沒有用web search一個個查，而是直接找到更權威、更有效率的資料源：你之前給的`立委所得票明細及概況-2020/區域立委-A05-2-得票數一覽表(縣市).xls`（跟2016年A05-2同款格式），每個選區分頁的表頭第3列就是`"(號次)\n候選人姓名\n推薦政黨"`格式，寫python腳本把22個縣市、所有分頁的這個表頭列全部解析出來，直接對到94位候選人裡的每一位，100%全部找到對應的政黨名稱（不是網路查證的間接資訊，是官方開票報表本身的登記資料）：

- **65位登記為「無」（無黨籍）**：包含蔣月惠、黃宏成台灣阿成世界偉人財神總統這種知名的獨立參選人，原本因為沒被分類，會落到含糊的「其他」灰色群組；現在正確歸類成`ind`/無黨籍
- **29位有實際政黨名稱**：
  - 何倍爾、林名哲、臧汝興、許育嘉、黃博仁 → 勞動黨（沿用既有`lp`代碼跟黨徽）
  - 李建輝 → 台灣維新（沿用既有`twr`代碼）
  - 林麗容 → 正黨（沿用v1.39幫2016年同一個人新增的`zpp`代碼——她2016跟2020都有參選）
  - 蔣祖棋 → 司法正義黨（發現系統裡其實早就有`jjp`這個代碼跟黨徽了，只是這位候選人沒被連結上，直接沿用，沒有另外新增重複代碼）
  - 其餘23位分屬22個2020年才第一次出現在系統裡的小黨（中華愛國同心黨、中華婦女黨、中華聯合黨、臺灣前進黨×2人、中華照生黨、台灣革命黨、勞工黨、聾國黨、金門高粱黨、圓黨、興中同盟會、中國生產黨、中華文化復興在理黨、台灣動物保護黨、民生公益、言論自由聯盟、中華新住民黨、天宙和平統一家庭黨、台灣整復師聯盟工黨、中國和平統一黨、台灣民意黨），新增對應代碼跟顏色

**黨徽圖示**：檢查`emblems/`資料夾發現裡面其實已經放了好幾個新政黨的圖檔（應該是更早之前就下載好、但一直沒接上系統），比對出6個檔名清楚對應得上的，直接接上：中華婦女黨(`Chinese_Women's_party_logo.jpg`)、中華聯合黨(`中華聯合黨.jpg`)、台灣動物保護黨(`TAPA_logo.png`)、台灣整復師聯盟工黨(`台灣整復師聯盟.jpg`)、中華愛國同心黨(`Flag_of_the_Chinese_Concentric_Patriotism_Alliance_(Party).svg.png`)、勞工黨(`Emblem_of_Labour_pt.svg`，跟勞動黨的`Labor_Party_(Taiwan)_emblem.svg.png`是不同檔案，兩個黨名字很像但是不同政黨，沒有搞混）。其餘16個新政黨檔名不夠明確對應（怕貼錯，比錯誤的圖示更保守的做法是先只用純色塊），暫時只有顏色分類、沒有圖示。

沒有動任何既有分類，只新增了原本完全空白（落到「其他」）的94個名字。

## v1.42 — 台灣維新候選人被誤植成台灣基進（2020/2024共18人）；地圖右下角圖例被資訊面板蓋住

**台灣維新（twr）被誤植成台灣基進（tsr）**：你點名林獻山、陳愷寧、翁語含、李柏融、楊時睿、朱翊銘這幾位，查證後發現根因是`KNOWN_PARTY_2020`/`KNOWN_PARTY_2024`這兩張年度覆寫表裡，有各自獨立、完全重複輸入過一次、但寫錯政黨的紀錄——系統裡本來就有一張年份無關的「基礎表」`KNOWN_PARTY_MAP`，這幾位在`KNOWN_PARTY_MAP`裡本來就正確寫著台灣維新，但`KNOWN_PARTY_2020`/`KNOWN_PARTY_2024`各自又「重複建了一次檔」，這次卻寫成台灣基進，而`getCandidateDisplay()`的查表順序是先查`KNOWN_PARTY_MAP`拿到正確值，最後才查年度表覆寫掉——覆寫的優先權比較高，所以錯誤的年度表值蓋過了正確的基礎表值。

不只你點名的幾位，系統性比對兩張表發現同樣問題共有18筆：2020年10位（吳文凱、曾楷耀、朱智德、李柏融、林獻山、沈宜璇、王金山、翁語含、陳愷寧、黃兆呈）、2024年8位（劉佩玲、徐百弟、朱翊銘、楊時睿、羅丹、陳愷寧、陳長志、黃一哲，陳愷寧兩年都中）。修法：把這18筆錯誤的年度表紀錄直接刪掉（不是改成twr，因為`KNOWN_PARTY_MAP`裡本來就有正確答案，刪掉多餘、錯誤的覆寫，讓查表自然掉回基礎表拿到正確的台灣維新分類），沒有動任何其他分類。這個修正同時也解決了「政黨熱力圖也跟著錯了」的問題——熱力圖模式（`mode==='tsr'`等）的顏色深淺是用`getCandidateDisplay()`回傳的`code`去分類統計，同一份查表邏輯，年度表修好之後熱力圖自然一起正確。

**地圖右下角圖例被資訊面板蓋住**：你說改版前地圖右下角有圖例（淺色=五五波/深色=差距大的亮度說明、激戰鄉鎮市區村里的斜線圖示），改版後不見了。查了`renderMapLegend()`這個函式（負責畫`#map-legend`這個「固定顯示、不隨模式切換而消失」的圖例）發現內容其實從頭到尾都還在跑、也還是有把亮度說明跟激戰區域圖示畫出來，根本原因不是內容被刪掉，是**位置被蓋住了**：`#map-legend`的CSS原本是`right: 10px`，這是改版前「資訊面板佔專屬版面欄位、地圖本身縮窄」時代寫的，那時候地圖右下角剛好就在資訊欄左邊、不會重疊；改版後資訊面板`#ip`變成用`position:absolute`浮動蓋在地圖上面（寬288px、`right:0`、z-index更高、背景不是全透明），地圖容器本身還是佔滿全寬，`#map-legend`的`right:10px`就變成剛好被浮動的資訊面板蓋住、完全看不到。

修法：把`#map-legend`的`right`從`10px`改成`298px`（288px面板寬度+10px間距），讓它移到資訊面板正左邊、地圖實際看得到的右下角——就是你說的「地圖的落下腳跟資訊欄的交界處」。內容本身（亮度圖例、激戰區域斜線圖示）完全沒動，只是移到看得見的地方。

## v1.43 — 政黨熱力圖工具列改成跟歷年分析/綜合PVI同款「按鈕+年份」版面；「歷史勝選」改名「歷年分析」；政黨熱力圖背景改黑色＋圖例文字改白

**政黨熱力圖工具列版面統一**：原本歷年分析/綜合PVI/第三勢力PVI三個模式的工具列都是「左邊一個按鈕（切模式）＋右邊一個年份下拉（切年份）」的藥丸形雙欄版面，但政黨熱力圖只有單獨一個下拉選單（`#party-heatmap-select`，用來選民進黨%/國民黨%/民眾黨%等），完全沒有年份切換的介面——雖然程式碼底層其實早就支援政黨熱力圖跟著`winnerYear`切2016/2020/2024（`setWinnerYear()`裡有明確判斷「如果目前模式是熱力圖就不要強制跳回歷年分析模式」），只是工具列從來沒放年份選單讓你點。這次比照另外三個模式的版面，把原本單獨的下拉包進同款雙欄容器，右邊新增`#heatmap-year-select`（2016/2020/2024），選擇年份時呼叫既有的`setWinnerYear()`（跟其他三個模式共用同一個年份狀態）。同時在`setWinnerYear()`／`setCompYear()`裡補上這個新選單的同步邏輯，確保切年份時四個模式的年份選單顯示值一致。

**改名**：「歷史勝選」→「歷年分析」（英文 History → Yearly Analysis）。

**政黨熱力圖背景改黑色**：政黨熱力圖模式的地圖容器`#mp`背景原本是`window.isHeatmap ? '#dde3ec' : '#05050a'`（熱力圖用淺灰藍`#dde3ec`、其他模式用近黑`#05050a`），這個判斷式出現在5個不同的重繪函式裡，全部統一改成一律`#05050a`。這裡只改的是最外層容器背景（海洋/空白區域的顏色），縣市層級用來襯托熱力圖漸層色的`.bgf`淺色底圖（`#edf1f5`）完全沒動——那是刻意設計來讓淺色（低得票率）的縣市色塊在底圖上還看得出輪廓，跟這次改的容器背景是兩回事，不會影響熱力圖本身的可讀性。

**圖例文字改白**：background改黑之後，`renderMapLegend()`（畫地圖右下角固定圖例）原本用來判斷文字要深色還淺色的`isLightBg`變數裡有把熱力圖模式(`isHM`)算進「淺背景」，導致文字被畫成深灰`#333333`——現在背景已經統一改黑，這個判斷跟著拿掉`isHM`，熱力圖模式的圖例文字（「亮度=投票人數多寡」「激戰區域」「漸層=無黨籍或他黨候選人獲政黨支持」）自動變回淺色`#cccccc`可讀。另外資訊面板裡（`renderLegend()`）政黨熱力圖各子模式（民進黨%/國民黨%/民眾黨%/時代力量%/台灣基進%/無黨籍%/其他%）共用的「淺色=低得票 / 深色=高得票」說明文字，原本是中灰`#aaa`，也一併加亮到接近白色`rgba(255,255,255,0.85)`。

## v1.44 — 政黨熱力圖面板誤顯示綜合PVI區塊；TC層染色被2024資料閘門擋住；跨黨候選人漸層顏色解析不一致（顯示灰色而非該黨顏色）；親民黨/台聯黨補色

**政黨熱力圖面板不該出現「區域黨派投票指數／政黨黨派投票指數／加權（相對全國平均）／板塊移轉」**：這些文字是`cmpBoxHtml()`（綜合PVI專用區塊）的輸出。ED/TC/VC三層的資訊面板都各自有一個`isWinner ? '' : cmpBoxHtml(...)`這種三元判斷式，決定要不要顯示PVI區塊——問題是判斷式只檢查`mode==='winner'`，沒有把政黨熱力圖模式（`dpp`/`kmt`/`tpp`/`npp`/`tsr`/`ind`/`other`）也算進「不顯示」的那一邊，所以熱力圖模式（不等於winner，也不等於comp）就掉進「顯示cmpBoxHtml」的分支。三層都改成`(isWinner || 熱力圖模式) ? '' : cmpBoxHtml(...)`，比照這學期稍早処理2020年打勾標記時用過的同款判斷式寫法。

**2016時代力量第七區/台南第四區等TC層沒有染色**：根因在`tcColor()`裡熱力圖模式的判斷式排在`const s = TC_STATS[key]; if (!s) return '#e0e0e0';`這行「2024資料存在檢查」後面——`TC_STATS`只有2024年的資料，用的key是`目前選區名__鄉鎮代碼`這種複合鍵，理論上每個現存鄉鎮都應該找得到，但只要有任何原因查不到（複合鍵對不上、或該鄉鎮在`TC_STATS`裡缺漏），就會在還沒進到`heatmapSimple()`（熱力圖真正的染色邏輯，本身有自己的年度感知，2016查`TC_2016`、2020查`TC_2020`）之前就先回傳灰色`#e0e0e0`短路掉了。修法：把熱力圖模式的判斷搬到這個2024資料存在檢查**之前**，讓熱力圖模式完全不受`TC_STATS`拖累，直接用`heatmapSimple()`自己的年度資料源判斷。

**跨黨候選人（親民黨/台聯黨/社民黨范雲/無黨籍鄭永金等）漸層顏色解析錯誤**：你點名台北市第4選舉區的親民黨候選人、臺中市第5選舉區的台聯黨候選人、臺北市第6選舉區的范雲、新竹縣選舉區的鄭永金，這幾位在民進黨熱力圖模式下都應該顯示「該候選人自己政黨顏色的漸層」（對應圖例「漸層=無黨籍或他黨候選人獲政黨支持」），但都顯示灰色。根因是`heatmapSimple()`裡有兩段各自獨立計算候選人「已知黨籍」的迴圈：第一段（算票數加總、判斷這個候選人算不算這個熱力圖模式的票）正確地依年度查`KNOWN_PARTY_2016`/`KNOWN_PARTY_2020`/`KNOWN_PARTY_2024`；但第二段（決定漸層要顯示哪個政黨的顏色）卻只查年份無關的`KNOWN_PARTY_MAP`基礎表，完全沒套用年度覆寫表，也沒有套用`isCandidateForHeatmapMode()`裡「范雲2016年前算社民黨」這類特殊名單覆寫——導致「這個候選人算不算數」判斷對了，但「該顯示什麼顏色」用了不同的、通常查不到正確政黨代碼的邏輯，就退回灰色預設值。

修法：把`isCandidateForHeatmapMode()`開頭那段「已知黨籍」解析邏輯抽成獨立的`_resolveCandKnownParty(name, year)`函式，`heatmapSimple()`的兩段迴圈都改成呼叫這個共用函式，確保「算不算票」跟「顯示什麼顏色」用的是同一套、完全一致的黨籍判斷。

**順便補上兩個一直缺的顏色**：`pctFill()`（依政黨代碼決定漸層顏色深淺的函式）本來就有`dpp`/`kmt`/`tpp`/`npp`/`tsr`/`sdp`/`np`各自的專屬色階，但漏了`pfp`（親民黨）跟`tsu`（台聯黨）——這兩個黨的候選人就算前面的邏輯不一致問題修好、`coopParty`正確解析出`'pfp'`/`'tsu'`，也還是會落到`pctFill()`的預設灰階分支。這次補上親民黨（橙色系）跟台聯黨（卡其色系）的專屬色階。

**村里資料缺漏清單**：你要我整理一份2016/2020缺資料的村里清單，已經比對`VC_ED`（全部7713個村里）跟`VC_2016`／`VC_SIM[vc].v20`的涵蓋範圍，找出113個完全沒有2016資料、16個沒有2020資料的村里（其中9個兩年都缺），清單另外傳給你，等你有空抓資料。台南市特別集中（113個裡超過一半在台南），懷疑是同一批A05-3村里級報表的合併報告模式，跟之前處理過的屏東市/東港鎮同類問題。

另外你點名的「彰化路上村」（芳苑鄉路上村，10007230009）查證後其實**有**2016村里級資料（洪遊江/鄭汝芬/陳朝容/張益勝/洪宗熠5位候選人都在），不在缺資料清單裡——如果修完上面幾個bug之後它還是沒有正確染色，麻煩之後測試時特別回報一下，可能是另一個還沒抓到的問題。

## v1.45 — 2016熱力圖背景/文字還是黑底黑字；無黨籍/其他熱力圖漏了漸層圖例；2016/2020棒狀圖補分隔線；推算選舉人數比對邏輯抓到資料不完整導致灌水的鄉鎮（暫時隱藏，非formula錯誤）；范雲2016漸層顯示灰色（gsdp沒有配色）

**2016熱力圖漸層文字還是黑的**：上一版修`isLightBg`時只拿掉了`isHM`，忘記這個判斷式裡還留著`winnerYear === '2016'`——查證後2016年模式（不管winner還是熱力圖）地圖背景本來就沒有另外设定淺色，一路都是黑底，這個`winnerYear === '2016'`條件本身就是誤植，這次整個拿掉，只剩`mode === 'comp' || mode === 'third'`兩個真正還是淺色背景的模式。

**無黨籍％／其他％熱力圖漏了漸層圖例**：`renderMapLegend()`裡判斷要不要顯示「漸層＝無黨籍或他黨候選人獲政黨支持」這行的`heatmapModes`陣列寫的是`['dpp','kmt','tpp','npp','tsr']`，漏了`ind`跟`other`兩個熱力圖子模式，這兩個模式下這行說明完全不會出現。補上這兩個。

**2016/2020棒狀圖補分隔線**：你說2024的「區域立委／總統大選／不分區政黨票」三個長條圖中間有分隔線，2020/2016沒有——查證`histBoxHtml2020()`／`histBoxHtml2016()`裡總統大選、不分區政黨票兩段的`.hist-row`確實從頭到尾都沒有分隔線樣式（2024對應的`tcPresVoteBarHtml()`／`vcPresVoteBox`兩處本來就有`margin-top:6px;padding-top:4px;border-top:1px solid rgba(255,255,255,0.06)`），這次比照2024補上，2020/2016的三段長條圖現在有一致的分隔線。

**推算選舉人數比對抓到「資料不完整導致灌水」的問題（不是公式錯誤）**：你懷疑2016→2020的推算選舉人數計算有問題，我把所有鄉鎮/選區的2020 vs 2016推算選舉人數全部算一次比對，抓到35個漲跌幅超過12%的異常，其中**臺南市的鄉鎮佔了絕大多數**（+128.8%、+94.7%、+80.7%…），這些鄉鎮剛好都是上一版列在「缺2016資料清單」裡的重災區——不是公式算錯，是這些鄉鎮的2016村里資料本來就殘缺不全（例如臺南市第5選舉區永康區只有11/20個村里有2016資料），少算的村里直接讓2016年的分母被低估，兩年一比就灌出離譜的漲幅。連江縣則是相反方向的異常（-79%等），這是另一個已知限制：2016原始報表把好幾個村里合併成一筆報票，每個村里都分配到「整組合併後」的相同人數，同一個鄉鎮裡好幾個村里名字加總起來會重複計算，導致2016的分母被高估。

修法：新增覆蓋率追蹤——逐村里加總2016推算選舉人數的同時，也記錄「這個鄉鎮/選區實際有算到2016資料的村里數」跟「總村里數」，覆蓋率低於80%時，`推算選舉人數 vs 2016`徽章直接不顯示（比顯示錯誤的灌水數字安全）。連江縣那種「資料都有、但合併報告造成重複計算」的狀況覆蓋率檢查抓不到，這是資料本身精細度的限制，不是能用程式修的問題，等你之後補資料再看能不能處理。

**范雲2016漸層顯示灰色**：查證她2016年在`KNOWN_PARTY_2016`裡的代碼其實是`gsdp`（綠黨社民黨聯盟，她2016年的正式參選聯盟名稱，比對`isCandidateForHeatmapMode()`裡原本寫的社民黨`sdp`特殊名單覆寫更精確——年度表本來就會覆寫掉特殊名單，這個優先順序是對的），但`pctFill()`（決定漸層顏色深淺的函式）根本沒有`gsdp`這個代碼的配色，直接落到預設灰階，這是跟親民黨/台聯黨一樣的「有正確分類但沒配色」漏洞，這次補上萊姆綠色系。

**彰化路上村的時代力量張益勝**：你這次補充是路上村，查證候選人是張益勝（時代力量）——這個村里的原始2016資料裡他確實只拿到**0票**（同村里總投票數380票裡，另一位候選人洪遊江也只拿2票，是個很小的村里，得票個位數/0票在小村里並非不合理）。這種狀況下熱力圖邏輯是正確地顯示「這裡沒有時代力量得票」（回傳白色`#ffffff`，不是灰色），不是bug——如果你查證這個0票數字本身是原始資料誤植，麻煩另外告訴我正確票數，我可以個別修正這一筆。

## v1.46 — 用官方A05-2/A05-2V原始檔重新解析2016/2020村里級候選人得票，補上缺資料村里

你說`各投票所得票明細及概況-2016/`跟`立委所得票明細及概況-2020/`的A05-2都是區域立委資料，要我直接重灌重算。實際動手重新解析之後，有個重要發現要先說明。

**2016**：用`立委-A05-2V-(縣市).xls`（22個縣市，跟上次抓投票率/廢票率用的同一批檔案）重新解析候選人得票，逐村里比對出7604筆——跟現有`VC_2016`（7600筆）幾乎一樣。驗算全國區域立委得票（民進黨5,382,973票、國民黨4,622,784票）**跟現有資料一字不差**，證實現有`VC_2016`本來就是從同一份官方檔案建置的，不是解析有誤。跟上次找到的「113個村里完全缺2016資料」清單比對，新解析只多救回**5個**村里（10013270005霧臺鄉佳暮村、67000070033麻豆區清水里、67000190024善化區蓮潭里、67000270019仁德區文賢里、67000370048中西區西和里），其餘108個之所以缺資料，是官方報表本身把好幾個村里的投開票所合併成一列申報（前幾輪處理過的屏東市/東港鎮模式），不是解析方式的問題，重灌同一份來源檔案沒辦法變出這些村里單獨的數字。

**2020**：用`區域立委-A05-2-得票數一覽表(縣市).xls`重新解析（這份是投開票所逐筆層級，不是預先彙總好的村里層級，要自己依「村里別」欄位分組加總各投開票所的票數才能算出村里總計）。解析出7694個村里，驗算全國得票（民進黨6,319,805票、國民黨5,626,319票）跟現有`ED_2020`（民進黨6,315,031票、國民黨5,621,116票）幾乎一致（差距0.08%，屬於少數幾個無法比對的村里名造成的正常誤差）。跟「16個村里缺2020資料」清單比對，救回**5個**（10013010004/10013010015屏東市大同里/楠樹里、10013270005霧臺鄉佳暮村、10018010008/10018020004新竹市東區下竹里/北區中央里），其餘11個一樣是投開票所合併申報造成，無法從這份資料補齊。

**判斷跟做法**：既然驗算證實現有7600+7697筆資料本來就是從這批官方檔案建置、數字完全吻合，整批「重灌」會是拿幾乎一樣的東西去換掉現有已經跑過這麼多輪測試、驗證過的資料，風險（可能introduce新的格式/邊界案例bug）跟效益（只多10個村里）不成比例。改成**只新增這10個原本完全缺資料的村里**（2016的5個直接補進`VC_2016`，2020的5個補進`VC_SIM[vc].v20`），其餘7600多筆原封不動，不去動已經驗證過沒問題的東西。霧臺鄉佳暮村（10013270005）兩年份都補了，但原始資料本身2016/2020都是0票（可能是极小/整併過的投開票所），補上之後格式上「有資料」了，但顯示出來還是0票，不是真的多了有意義的內容。

## v1.47 — 用你查到的村里沿革資料，補上南投仁愛鄉德鹿谷村（舊名合作村）2016資料

你查到德鹿谷村（10008130017）是2021年由「合作村」正名而來，回頭查2016年南投縣仁愛鄉的原始報表，「合作村」這個名字確實還在（我的自動比對用的是現在的村里名清單，查不到已經改掉的舊名，所以之前才會漏掉）。這個村里很小，2016年只有8張有效票（馬文君6票、張國鑫2票，投票率28.57%），已經補進`VC_2016`。

你另外查到的其他幾組（東港鎮4里從興東里拆出來、臺南市中西區/北區好幾組里在2018年合併改名）比對後發現比預期複雜：
- **東港鎮4里（內關帝/東新/新街/東光）**：確認是從單一個「興東里」拆出來的，但我沒有拆分前細到投開票所層級的村界資料，沒辦法把興東里的總票數精確拆成4份——如果要補，只能4個里都填一樣的「興東里整體」數字，這樣可能會誤導（看起來像4個里各自的數字，其實是同一份母村的重複）。先沒有動，等你確認要不要接受這種近似值。
- **臺南市中西區/北區合併案例**：查證比想像中複雜——像五條港里要合併的「神農里」，2016原始報表裡雖然有這個名字，但是在完全不同的選舉區（第1選舉區，不是中西區所在的第3選舉區），應該是撞名到別的鄉鎮的神農里，不是同一個；北華里要合併的「長北里」、兌悅里要合併的「新港里」等好幾個名字，翻遍臺南市整份2016報表完全找不到，可能是2018年之前臺南市還經歷過其他輪次的村里調整，你查到的這批「2018年」只是其中一段歷史，不是全部。這幾個貿然用村名比對容易配錯（尤其撞名的情況），先沒有動，如果你能查到更完整、含年份的臺南市村里代碼沿革表（不只是名稱，最好有村里代碼或投開票所代碼的對照），會更好核對。

你後來找到`kiang/tainan_basecode`這個GitHub repo的村里調整對照工具，裡面`data.json`確實是村里調整紀錄，但比對後發現記錄的是「村里保留原名、只跟鄰居微調界線」這種案例（307筆，跟本次缺資料的76個村里完全沒有重疊），不是全部改名合併的那種案例——需要往`cunli.json`/`cunli_new.json`（新舊完整村里邊界GeoJSON）做地理空間比對才可能抓到，你決定先擱置，之後有更直接的對照表再處理。

## v1.49 — 歷年分析模式新增「i」按鈕導覽（研究聲明／地圖染色邏輯／推算選舉人數公式）

右上角的「i」按鈕原本點下去一律顯示綜合PVI專用的導覽內容（`startCompOnboarding`，介紹PVI權重滑桿、公式折疊區塊等），在歷年分析模式點擊看到的內容完全對不上（畫面上根本沒有那些東西），你要求歷年分析模式要有自己專屬的聲明稿跟操作指引。

新增`WINNER_ONBOARD_STEPS`，三步：(1) 研究性質聲明（跟常駐聲明同一份文字，另外補充2016/2020部分村里資料因行政區劃調整無法百分之百對齊的說明）、(2) 地圖染色邏輯（顏色＝當選政黨、深淺＝領先差距、年份下拉切換說明）、(3) 推算選舉人數公式（有效票數÷(1-廢票率)÷投票率，以及涵蓋率不足時徽章會自動隱藏的說明）。

把原本寫死指向`COMP_ONBOARD_STEPS`的`renderCompOnboardStep()`／`advanceCompOnboarding()`改成讀取新的`_activeOnboardSteps`變數，新增`startWinnerOnboarding()`（跟`startCompOnboarding()`同款寫法，只是把`_activeOnboardSteps`指向`WINNER_ONBOARD_STEPS`）跟`startInfoOnboarding()`（依`mode`判斷該叫哪一個），「i」按鈕的`onclick`改成呼叫`startInfoOnboarding()`。沒有改動既有comp模式的行為（含強制彈窗、localStorage記錄同意狀態的邏輯都原封不動），單純讓歷年分析模式也有自己對得上內容的導覽視窗可以點開看。

## v1.50 — 歷年分析導覽補三步（圓餅圖/激戰區域、政黨輪替分析、全選區村里）；政黨熱力圖收合條改顯示該黨2024真實席次分布

**導覽補三步**：上一版`WINNER_ONBOARD_STEPS`只有三步（研究聲明/染色邏輯/推算選舉人數），你要求把左側工具列其他幾個功能也講清楚，這次補上：
- 圓餅圖與激戰區域：說明進入鄉鎮層級後才會出現的「圓餅」「激戰區域」兩個勾選框各自的作用
- 政黨輪替分析：說明可展開的摺疊區塊裡「搖擺地圖」「翻盤選區」「翻盤鄉鎮市區」「翻盤村里」四個功能
- 全選區村里：說明鄉鎮層資訊面板的「⊞ 全選區村里」按鈕跟「↩ 返回鄉鎮層」的用途

現在導覽總共六步，最後一步（推算選舉人數）維持在最後，按鈕文字「開始使用」。

**政黨熱力圖收合條改顯示真實席次分布**：你的構思是——政黨熱力圖模式下選了某個政黨（例如民眾黨），原本綜合PVI模式專用的陣營切換鈕（`camp-toggle-btn`）跟收合條四張卡摺疊簡寫，應該套用同一套邏輯改成顯示「這個政黨」的真實席次分布：區域幾席、不分區幾席、原民幾席、總共幾席，沒有的類別顯示`--`，另外保留一個「總」的狀態可以切回全部113席。

新增`REAL_SEATS_2024`（2024年各黨真實當選席次，區域/不分區/原民/總共，資料來源中選會公告結果，加總驗證：區域73+不分區34+原民6=113，各黨總席次跟公開報導一致：國民黨52、民進黨51、民眾黨8、無黨籍2、時代力量與台灣基進皆為0）跟`HEATMAP_PARTY_NAME`（政黨代碼→中英文名稱對照）。

`cycleCampFilter()`（陣營切換鈕的點擊處理）新增熱力圖模式分支：點擊時在「顯示這個政黨」跟「顯示總113席」兩個狀態間切換（不是原本comp模式的泛綠/泛藍/中間/總四態循環，因為你確認熱力圖模式一次只會顯示一個政黨，沒有「中立」或「多黨並列」的需求）。`updateSeatSummaryStrip()`新增熱力圖模式分支，直接讀`REAL_SEATS_2024[mode]`（或`heatmapSeatView==='total'`時讀全國113席總表）填入四張卡，0用`--`表示。`setMode()`切換到熱力圖模式時會重置成預設顯示該政黨、同步按鈕文字。

## v1.48 — 常駐免責聲明只在綜合PVI模式顯示，補上歷年分析（勝選）模式也要顯示

`#disclaimer-note`（那個「※本頁面純屬學術性空間資料視覺化與科學模型研究...」的固定小提示框）原本只在`mode==='comp'`（綜合PVI）時顯示，`setMode()`裡`_dn.style.display = (m === 'comp') ? '' : 'none';`這行沒有把`winner`（歷年分析，原「歷史勝選」）算進去，導致使用者一開始進頁面預設在歷年分析模式時完全看不到這個聲明。改成`mode==='comp' || mode==='winner'`都顯示，兩個模式都會出現，其他模式（third/熱力圖/sim）維持原樣不顯示。

## v1.51 — 修正v1.50的誤解：政黨席次選擇是歷年分析模式自己的功能，不是政黨熱力圖，補上強制導覽跟導覽最後一步選政黨

v1.50把「選政黨後右上角按鈕/收合條四張卡顯示該黨真實席次」這個功能做在政黨熱力圖模式（`mode`是`dpp`/`kmt`等的時候），但你要的其實是**歷年分析（`mode==='winner'`）模式自己的功能**，跟政黨熱力圖是兩回事，這次全部重做並跟熱力圖模式的既有邏輯（保留不動）分開。

**強制導覽**：比照綜合PVI模式「第一次進入時強制彈出導覽，選完才能真的進去」的寫法，`setMode()`裡新增`if (m === 'winner') { 沒看過(pvi_winner_onboarded_v1) 就強制show導覽 }`，讓歷年分析（本來就是預設進頁面看到的模式）也有跟綜合PVI對等的強制聲明+操作指引，不再只能透過手動點「i」才看得到。

**導覽最後一步改成選政黨**：`WINNER_ONBOARD_STEPS`原本6步全部是純說明、沒有選項，這次把最後一步改成跟綜合PVI「最後一個小問題」同樣的isChoice機制，但選項換成「國民黨／民進黨／民眾黨／無黨籍／其他／總」6個（原本comp mode是「泛藍／中間／泛綠」3個）。選擇畫面（`#comp-onboard-choice-row`）改成由JS依步驟的`choices`陣列動態產生按鈕，不再寫死3顆藍/中/綠按鈕，comp模式跟winner模式的選項互不影響、各自定義在自己的step物件裡。

**新增獨立狀態`_histSeatParty`**：跟`campFilter`（comp模式陣營篩選）、`heatmapSeatView`（熱力圖模式總/該黨切換）三者完全分開，互不干擾。歷年分析模式下：
- 選完導覽最後一步的政黨（或之後點右上角按鈕循環切換：總→國民黨→民進黨→民眾黨→無黨籍→其他→總...），收合條四張卡（`updateSeatSummaryStrip()`新增`mode==='winner'`分支）顯示該黨2024真實當選席次（區域/不分區/原民/總，沒有席次顯示`--`，跟熱力圖模式共用同一張`REAL_SEATS_2024`資料表）
- 按鈕本身同步顯示該黨顏色（新增`HIST_PARTY_BTN_STYLE`色票：國民黨藍、民進黨綠、民眾黨青、無黨籍灰、其他紫），用`applyHistSeatPartyToButton()`統一套用
- 修掉一個順便發現的潛在bug：按鈕的政黨顏色是用inline `style.color`設定的，如果沒有在其他三個地方（`chooseCampAndFinish`、`cycleCampFilter`的熱力圖分支跟comp分支、`setMode`的熱力圖同步區塊）也清空這個inline樣式，從歷年分析切到綜合PVI/熱力圖模式時，按鈕的文字顏色會被殘留的inline樣式蓋掉CSS class該有的顏色（inline样式優先權比class高）。四個地方都補上`style.color = '';`重置。

## v1.52 — 選了國民黨，按鈕卻顯示藍字的「總」

你點導覽最後一步的國民黨，按鈕顏色對了（藍色）但文字錯了（顯示「總」不是「國民黨」）。查出來是`applyLang()`（切換語言時同步畫面文字用的函式，在v1.43加過一段camp-toggle-btn的文字校正邏輯）在**每次`setMode()`跑完的最後都會被呼叫**，而`chooseHistPartyAndFinish()`選完政黨後會呼叫`closeCompOnboarding(true)`，裡面又會呼叫`setMode('winner')`，等於選完政黨的下一瞬間，`setMode()`結尾的`applyLang()`就跟著跑了一次——但`applyLang()`裡那段v1.43寫的校正邏輯只認得`campFilter`（綜合PVI專用的陣營篩選狀態），根本不知道有`_histSeatParty`這個新變數存在，看到`campFilter`是`null`就把按鈕文字覆寫回「總」，顏色因為`applyLang()`沒有動`style.color`所以沒被蓋掉，才會出現「藍字的總」這種文字跟顏色對不起來的狀況。

修法：把`applyLang()`裡這段校正邏輯改成依目前`mode`判斷該讀哪個狀態來源——`mode==='winner'`時改成直接呼叫`applyHistSeatPartyToButton()`（跟歷年分析模式其他地方共用同一個函式，不會再各自維護一份邏輯）、政黨熱力圖模式讀`mode`本身+`heatmapSeatView`、其餘（綜合PVI）才維持原本讀`campFilter`的寫法。三套狀態來源分流之後，不會再互相蓋掉。

## v1.53 — 收合條數字依政黨變色；地圖黑底混一點政黨色

**收合條四張卡數字變色**：`HIST_PARTY_BTN_STYLE`（政黨→顏色對照表）補上每個政黨的數字顏色，無黨籍改用比較明確的淺灰`#9ca3af`（原本是`rgba(255,255,255,0.8)`，跟「總」的白色太接近不好區分），「總」固定白色`#ffffff`。`updateSeatSummaryStrip()`裡歷年分析模式跟政黨熱力圖模式兩個分支，四個數字（`ss-region`/`ss-pl`/`ss-ind`/`ss-total`）現在都會套用對應政黨的顏色。

順便補上跟按鈕顏色同一類的殘留bug防護：非歷年分析/非熱力圖模式（綜合PVI/第三勢力）那段原本只有更新文字、沒有重置`style.color`，如果剛好前面在別的模式把數字上過色，切過來會殘留顏色蓋掉，這次也補上`style.color = ''`重置。

**地圖黑底混政黨色**：新增`mapBgColor()`，歷年分析模式下選了政黨時，回傳一個混了該黨顏色的深色（不是純黑`#05050a`，例如國民黨偏深藍`#070d18`、民進黨偏深綠`#071809`），選「總」或其他模式維持原本純黑。把檔案裡7處寫死`mp.style.background = '#05050a'`的地方全部改成呼叫這個共用函式，不用逐一手動維護每個層級渲染函式各自的顏色判斷。

## v1.54 — 歷年分析：政黨背景色加深看得出來；已經看過導覽的人每次進頁只問最後一題；選了政黨後地圖只留該黨贏的地方染色、其餘變黑

**背景色太淡看不出來**：v1.53加的`HIST_PARTY_BTN_STYLE`裡`bg`色票混色比例太保守（例如國民黨`#070d18`，跟純黑`#05050a`幾乎分不出來），這次大幅加深混色比例，改用更明顯的深藍`#0d2040`（國民黨）、深綠`#0d2c14`（民進黨）等，跟純黑肉眼能明確分辨。

**已經看過導覽的人，每次重新整理頁面進歷年分析模式只問最後一題**：原本的邏輯是「看過(`pvi_winner_onboarded_v1`)就完全不再顯示任何導覽」，你要的是老用戶每次重新整理頁面後，第一次進歷年分析模式時，還是要跳出「選政黨」這最後一題（不用重播整個6步導覽），選完之後同一次頁面存活期間就不再問。新增`_winnerPartyAskedThisLoad`（跟`pvi_winner_onboarded_v1`不同，這個是純JS變數不寫localStorage，重新整理頁面就重置）跟`startWinnerOnboarding()`新增的`startIdx`參數，讓`setMode()`可以直接從導覽的最後一步（`WINNER_ONBOARD_STEPS.length - 1`）開始顯示，不用從頭跑。

**選了政黨後，地圖只留該黨贏的地方染色，其餘變黑**：這是你這次要的核心功能——選國民黨，地圖上非國民黨贏的選區/鄉鎮/村里全部變黑；選民眾黨，因為2024民眾黨區域立委0席，理論上全黑，符合預期。

找到`winMarginFill(party, margin, realColor)`這個函式——不管2016/2020/2024、ED/TC/VC哪個層級，歷年分析模式的地圖染色最終都會呼叫到這一個共用函式決定顏色，所以只要在這裡攔截一次，不用個別修改`edColor()`/`tcColor()`/`vcColor()`各自的十幾條分支。新增`_normPartyForHistFilter()`把傳進來的`party`（可能是`dpp`/`green`/`kmt`/`blue`/`tpp`/`white`等別名，或`npp`/`tsr`/`ind`/`other`等其他代碼）正規化成`_histSeatParty`能比對的6種之一（`dpp`/`kmt`/`tpp`/`ind`/其餘都算`other`），比對不符就直接回傳一個接近黑色的顏色`#0c0c0e`，符合就照原本邏輯正常染色。

另外新增共用函式`redrawCurrentLevel()`——依目前`level`/`curED`/`curTC`/`villAllMode`判斷該呼叫`goED()`/`goTC()`/`goVill()`/`goVillAll()`/`goTCAllNational()`/`goVillAllNational()`中的哪一個重繪，但不像`setWinnerYear()`裡同款邏輯那樣會重置`curED`/`curTC`/`curVC`——單純重新畫一次目前畫面、套用新選的政黨濾鏡，不會把使用者導覽狀態清掉。點右上角按鈕循環切換、或在導覽最後一步選政黨完成時，都會呼叫這個函式讓地圖立即反映新選的政黨。

## v1.55 — 苗栗一（陳超明）歷年分析染色修正為無黨籍；隱藏遮罩改深灰

**苗栗縣第1選舉區在「選政黨」染色功能下歸類錯誤**：查證後，苗栗一2016/2020/2024三屆的實際當選人都是`陳超明`（不是你口誤/記錯的鍾東錦——`鍾東錦`這個名字在全部票數資料裡完全查不到，只出現在`PVI_BACKED_MAP`那張表，應該是之前哪次對話把苗栗縣長跟這位立委搞混了）。`陳超明`2016/2020代表國民黨參選（原始資料`p`/`wp`都是`kmt`），但2024他沒拿到國民黨提名、以無黨籍身分參選（黨禮讓支持），2024原始資料裡`ED_STATS`/`TC_STATS`存的`wp`/`winner_p`欄位是`"other"`。

`getCandidateDisplay()`早就有針對他的特例（2024回傳`code:'ind'`、顯示「無黨籍」），候選人資訊卡/棒狀圖一直都顯示對的。但v1.54新增的「選政黨只留該黨染色」功能，`edColor()`/`tcColor()`/`vcColor()`傳給`winMarginFill()`的`party`參數在2024分支是直接讀原始的`s.wp`/`info.winner_p`（也就是`"other"`），沒有經過`getCandidateDisplay()`校正，導致選「無黨籍」時苗栗一沒有染色、選「其他」時卻染了色。

修法：ED/TC/VC三個2024分支原本都已經呼叫`winnerCandidateDisplay()`算出`wd`（拿它的`.color`給`winMarginFill()`畫圖），這次改成`wd`存在時優先傳`wd.code`（已經套用過陳超明等候選人層級校正的黨籍代碼）給`winMarginFill()`當`party`參數，取代原始的`s.wp`/`info.winner_p`。只影響歷年分析模式的染色/篩選判斷這一條路徑，`PVI_BACKED_MAP`（`鍾東錦`/`陳超明`都在裡面，PVI計算用的陣營覆蓋表）完全沒動，跟綜合PVI模式的計算邏輯分開算，符合你的要求。

**隱藏遮罩顏色改深灰**：v1.54「選了政黨後其餘選區變黑」用的`#0c0c0e`太接近純黑背景色，選區邊框糊在一起分不清楚。改成`#333338`深灰，蓋掉原本染色但邊框輪廓依然看得出來（邊框顏色是另外畫的、不受這個fill值影響）。

## v1.56 — 地圖黑底改科技感光暈（格線太老套、雷達環也怪，最後改成純光暈層次+呼吸動畫）

v1.53加的`mapBgColor()`原本回傳一個扁平的純色hex（例如國民黨`#0d2040`），直接蓋掉`#mp`本來CSS寫的`radial-gradient(ellipse at 40% 50%, #141b2b 0%, #0a0d16 70%, #05070c 100%)`——結果選了政黨之後反而變成死板的單色塊，連原本預設（沒選政黨）都被拉平成純黑`#05050a`，質感比原本CSS的漸層還差。

改法：`HIST_PARTY_BTN_STYLE`每個政黨補一個`glow`（比`bg`更亮更飽和的核心色），`mapBgColor()`改成回傳完整的多層`background`字串——中心是該黨`glow`色的光暈、往外暈開到`bg`色再到近黑`#05070c`（用`radial-gradient`還原+強化原本CSS的漸層手法）。沒選政黨（總）或非歷年分析模式時，光暈色退回預設深藍灰`#141b2b`。7處呼叫`mapBgColor()`的地方不用改，字串直接指定給`style.background`就會套用多層背景。

**格線版太老套，雷達同心圓也覺得怪，最後拿掉所有紋理**：紋理類的嘗試（棋盤格線→雷達同心圓+點陣）你都不滿意，判斷問題出在「用幾何紋理堆疊科技感」這條路線本身就不適合疊在地圖這種本來就有大量線條（選區邊界）的畫面上，容易互相干擾、看起來雜亂或突兀。

改成完全不用紋理，純粹靠**光暈本身的層次+動態**做科技感：
1. `mapBgColor()`加一層偏心的柔光亮點（`radial-gradient(circle at 32% 38%, rgba(255,255,255,0.14) 0%, transparent 32%)`），疊在主光暈上模擬光源角度，比對稱的正中心光暈多一點立體感、少一點「純色塊」感。
2. 主光暈`radial-gradient`衰減距離從55%收緊到45%，核心更快到飽和色、邊緣更快到黑，對比拉高，「發光」的感覺比原本那種漸層平緩的版本更明顯。
3. 各政黨的`glow`核心色整體調亮調飽和（例如國民黨`#1a4f9e`→`#2266c9`、民進黨`#137a3a`→`#16a34a`、民眾黨`#0d7a90`→`#0891b2`、其他`#5b34a8`→`#7c3aed`），配合上面收緊的衰減距離，霓虹感更明顯。
4. `#mp`本身新增`mpGlowBreathe`這個CSS動畫（6秒一輪，`filter: brightness/saturate`微幅起伏），讓整個地圖畫面帶一點持續在運作中的呼吸感，這是純用`filter`做的，不會影響地圖本身的顏色資訊判讀，也完全不需要疊加任何格線/幾何紋理就能感覺「活著」、更有科技感。

## v1.57 — 地圖背景改「兵棋推演桌」風格，紋路跟著滑鼠移動

你想法是把地圖背景做得更像兵棋推演桌，紋路還能隨滑鼠移動變化。這次疊了三個新東西在`mapBgColor()`裡：

1. **四角戰術定位括號**：`_cornerBracketLayer(color)`用inline SVG畫四個角落的L形括號（該黨核心色，跟按鈕/光暈同一套色），視覺上像戰術地圖/瞄準介面常見的邊角標記，是單次裝飾元素不是重複紋理，不會有格線那種「棋盤感」。
2. **紙面顆粒紋**：`_MAP_GRAIN_LAYER`用固定座標（不是`Math.random()`，每次重整位置都一樣，避免畫面閃動）灑15個不規則大小、8%透明度的白點，140px一個循環tile，模擬實體兵棋桌/紙質地圖的顆粒感，特意用不規則點位而非整齊格線/棋盤點陣（之前試過的格線、雷達同心圓都覺得不自然，問題可能出在太規則）。
3. **跟著滑鼠移動的亮點+紙紋視差**：`#mp`新增CSS變數`--mx`/`--my`（亮點位置）、`--px`/`--py`（紙紋位移），在`mapBgColor()`回傳的字串裡用`var()`引用而不是寫死數值，這樣JS只要用`style.setProperty()`更新這幾個CSS變數就能讓背景即時反應，不用每次滑鼠移動都重新組字串塞回`style.background`（效能好很多）。新增一個一次性的`mousemove`/`mouseleave`監聽器（掛在`#mp`上，`requestAnimationFrame`節流），滑鼠在地圖上移動時亮點跟著游標跑（像手電筒/雷達站掃照桌面），紙紋則反方向做很小幅度的位移（視差深度感），滑鼠離開地圖區域時平滑歸位到預設位置（40% 50%）。這個監聽器是全新加的，之前`#mp`完全沒有綁過`mousemove`，不會跟既有的地圖hover/tooltip邏輯衝突。

移除v1.56加的`mpGlowBreathe`呼吸動畫（`#mp`上`filter: brightness/saturate`微幅起伏那個CSS動畫）——你不要這個了，`@keyframes`定義跟`#mp`上的`animation`屬性都拿掉，CSS變數`--mx/--my/--px/--py`保留（滑鼠互動功能還在用）。

## v1.58 — 滑鼠跟隨光暈/紙紋視差改精細一點

**動作變滑順（lerp緩動取代瞬間跳位）**：原本滑鼠移動時，光暈位置(`--mx/--my`)跟紙紋位移(`--px/--py`)是每個`mousemove`事件直接寫死成當下滑鼠座標（用`requestAnimationFrame`節流，但一旦畫就是瞬間跳到位），滑鼠移快一點會有種頓頓的跳動感。改成用`curX/curY`每幀往`targetX/targetY`（滑鼠當下位置）做`EASE=0.09`的線性內插（lerp），大概要10幾幀才追到位，看起來比較像有慣性、細膩滑順地跟過去，而不是瞬移。滑鼠移動時持續跑`requestAnimationFrame`迴圈，追到位（誤差<0.02）就自動停止迴圈省效能，滑鼠再動或離開地圖區域（歸位到40% 50%）才會重新啟動。

**紋理跟光暈本身也做細一點**：紙面顆粒紋`_MAP_GRAIN_LAYER`從15顆疏鬆大點（半徑0.3~0.6，140px一個循環）改成43顆更密更小的點（半徑0.18~0.3，100px一個循環tile），紋理密度提高、顆粒感更細緻，不會一眼看到明顯的稀疏散點。滑鼠光暈`spotlight`從單一`transparent 30%`的大範圍柔光，改成三段式（核心16%不透明→中段5%→24%全透明），範圍收窄，看起來比較像有明確聚焦的手電筒光束，而不是一大片模糊的亮塊。

## v1.59 — 跟隨速度太慢、紋理還是太粗，再調

**跟手速度**：`EASE`從0.09（要10幾幀才追到位，反應慢半拍）調到0.3，改用線性同餘演算法（LCG，固定種子17，不是`Math.random()`，重整頁面座標不會變）產生偽隨機點，幾幀內就貼上滑鼠、更跟手。紙紋視差偏移量的乘數也從`-0.4`收到`-0.15`，避免追手速度變快之後，紙紋跟著甩動的幅度也一起被放大變得太誇張。

**紋理粗細**：紙面顆粒紋從43顆（半徑0.18~0.3、100px tile）再加密到90顆、顆粒縮到半徑0.06~0.19、tile縮到64px，用固定種子的LCG演算法批次產生座標（比之前手key一顆顆座標更均勻密集，也還是確定性、不會每次重整頁面紋理亂跳）。滑鼠光暈範圍再收緊，核心不透明範圍從14%縮到6%、完全透明的邊界從24%收到12%，讓「手電筒」的光束更集中銳利，不是一大片模糊亮暈。

## v1.60 — 光真的「照在地圖上」（新增疊在svg上層的手電筒圖層）

你點出前幾版的問題：v1.53~v1.59的光暈/紋理都是加在`#mp`的CSS `background`，而`svg#map`（畫地圖選區的地方）本身是不透明的，蓋在`#mp`的background「上面」——所以滑鼠光暈跟紙紋其實只有在選區之間的縫隙、地圖範圍外的空白處看得到，掃過地圖本體（實際的縣市選區色塊）時完全看不出來，跟「手電筒照在地圖上」的預期效果不符。

新增`#mp-light`這個div，直接插在`</svg>`後面（DOM順序在svg之後，疊在地圖上層），關鍵是兩個CSS技巧：
1. **`mix-blend-mode: soft-light`**：這層的內容（紋理+亮點）會跟底下svg選區實際的顏色做混合運算，而不是單純蓋住——選區顏色深，光照過去會提亮；選區顏色淺，效果比較內斂——才會有「光線照在有色物體表面」的真實感，而不是一塊死板的白色疊圖。
2. **`mask-image: radial-gradient(circle at var(--mx) var(--my), ...)`**：把這個div裁成一個跟著滑鼠位置(`--mx`/`--my`，跟背景那層共用同一組CSS變數)跑的圓形範圍，圓圈外完全裁掉（等於完全不影響圓圈外的地圖選區顏色），圓圈內才讓`soft-light`生效。這樣紋理+提亮效果就只會出現在手電筒光斑掃到的範圍，隨滑鼠移動即時跟著跑，不會整張地圖都被染色。

`pointer-events: none`確保這層完全不會擋到地圖原本的點擊進入選區/hover顯示tooltip等互動。因為`--mx/--my/--px/--py`是會繼承的CSS變數、父層`#mp`每幀更新，`#mp-light`的mask跟background只要初始化時設定一次（`initMapLightOverlay()`，在既有的滑鼠監聽器IIFE前呼叫一次）就會自動跟著即時反應，不需要每幀重新賦值。

## v1.61 — 光束收緊、拿掉兵棋角括號、背景改成貼地圖亮外圍暗

**光束再收緊**：`#mp-light`的`mask-image`圓形範圍從（實心0~8%、8~18%漸淡、18~30%全透明）收緊到（實心0~3%、3~8%漸淡、8~14%全透明），`initMapLightOverlay()`裡的`hotspot`跟`mapBgColor()`裡的`spotlight`也同步收緊到差不多的比例，手電筒的光斑範圍明顯變小、更聚焦。

**拿掉兵棋角括號**：v1.57加的`_cornerBracketLayer()`（四角戰術定位L形括號）你這次說不要了，整個函式跟`mapBgColor()`裡呼叫它的那一層都刪掉。

**背景改成貼地圖亮、往外暗**：`baseGlow`原本是`radial-gradient(ellipse at 40% 50%, glow 0%, mid 45%, #05070c 100%)`，色階變化平緩、對比不夠明顯。新增`_lightenHex(hex, amt)`小工具（把hex顏色往白色方向混），改成`glow`色先在中心加一段更亮的核心（`_lightenHex(glow,0.3)`）、接著才過渡到原本的`glow`（18%）、`mid`（40%），最後在72%就到底黑`#05070c`（原本要100%才到底）——衰減曲線更陡，看起來比較像「台灣所在的地方亮，離開地圖範圍很快就暗下去」，而不是整個背景均勻地慢慢淡出。

## v1.62 — 「貼地圖亮」你是指貼著台灣本島邊線，改用drop-shadow真的沿海岸線發光

v1.61做的其實是「background漸層置中在畫面某個固定座標」，跟地圖本身的形狀無關，你說的是要貼著台灣本島實際的海岸線/選區外框亮，這是兩回事。

改法：畫地圖選區色塊的SVG群組是`#g-base`（裡面是`.ft-ed`/`.ft-tc`/`.ft-vill`，依目前層級放全國選區、單一選區的鄉鎮、或村里的實際色塊path），新增CSS規則`#g-base { filter: drop-shadow(0 0 3px var(--edge-glow)) drop-shadow(0 0 10px var(--edge-glow)); }`。`drop-shadow`是根據該群組實際畫出來的形狀（alpha通道）算陰影/光暈，兩個相鄰選區都不透明、交界處挨在一起就不會有光——只有最外圍跟空白背景交界的地方（也就是海岸線、以及目前畫面最外圍的國界/選區界）才會亮。這樣光暈天生就會貼著實際畫出來的形狀走，不管全國/單一選區/單一鄉鎮哪個層級、不管有沒有zoom/pan，都自動對得上，不用手動計算/對齊座標。

顏色用新的CSS變數`--edge-glow`控制，掛在`#mp`上，值就是`mapBgColor()`裡本來就有算好的`glow`（該政黨的核心色，或預設色）——`mapBgColor()`本來就是每次redraw都會被呼叫，這次讓它順便多做一步`setProperty('--edge-glow', glow)`，不用另外再找七個呼叫點逐一加。原本`baseGlow`那層改成單純的四角環境底色（`#mp`空白角落用的暗色漸層），不再嘗試「貼地圖」，這件事完全交給新的`#g-base` drop-shadow負責。

（如果之後在全台村里這種path數量很多的層級發現地圖拖動/縮放有明顯變慢，很可能是`drop-shadow`模糊運算量大導致，屆時可以考慮只在全國選區層級套用、或把模糊半徑再調小。）

## v1.63 — 滑鼠跟隨光暈再加快；資訊欄棒狀圖加毛玻璃感

**滑鼠跟隨再快一點**：`EASE`從0.3調到0.5，幾乎一兩幀就貼到滑鼠位置，比上一版更貼手。

**資訊欄棒狀圖（候選人得票/政黨票/歷史比較）加毛玻璃感**：這頁裡棒狀圖軌道有三種class（候選人得票條`.bbg`、政黨票`.pl-bar-wrap`、歷史/2020/2016比較條`.hist-bar-wrap`），三個都補上`backdrop-filter: blur(6px) saturate(1.3)`+半透明邊框+內陰影，跟頁面其他玻璃感元件（例如導覽彈窗）用同一套手法。

填色的部分（`.bfill`/`.pl-seg`/`.hist-seg`）比較麻煩——這些條目的顏色很多是JS產生HTML時用inline `style="background:..."`直接寫死的（不是靠CSS class），如果直接在CSS class上加`background-image`做光澤效果會被inline style蓋掉。改用`::after`偽元素疊一層由上而下、由亮轉透明的白色漸層當作玻璃光澤——偽元素完全獨立於該元素自己的`background`，不管填色是inline style還是class設的都不會被蓋掉，三種都用同一條規則`.bfill::after, .pl-seg::after, .hist-seg::after`，不用一一去改產生HTML的十幾個地方。

**「民進黨 65.3% 勝選」這個膠囊徽章（`.win-badge`）也加了同一套**：`border`+`backdrop-filter: blur(4px)`+`inset`陰影做出玻璃邊框感，`::after`疊一層光澤（跟棒狀圖同招，不受inline style/`.bd-xxx` class設色影響）。這個徽章裡面直接有文字，光澤強度比棒狀圖版本收斂一些、範圍只留在上緣（35~50%就淡出，棒狀圖版本是45~65%），避免蓋到文字可讀性。

## v1.64 — 棒狀圖毛玻璃拿掉（設計備份保留）；整個資訊側欄改玻璃手機質感

**棒狀圖/badge的毛玻璃拿掉，但設計存起來**：你說先刪掉這些，如果之後要用要固定用這個設計。`.bbg`/`.pl-bar-wrap`/`.hist-bar-wrap`/`.bfill`/`.pl-seg`/`.hist-seg`/`.win-badge`都改回v1.63之前的樣子，原本的`backdrop-filter`/`border`/`box-shadow`/`::after`光澤紋路整段原封不動存成CSS註解放在`.bbg`旁邊，之後要重新套用直接複製貼上就好，不用重新設計。`.win-badge`的縮小尺寸（`padding:1px 6px`/`font-size:10px`）有保留。

**整個側欄（`#left-rail`，含收合的功能區+`#ic`資訊卡+底部圖例）改玻璃手機質感**：這個panel本來就有`backdrop-filter: blur(16px)`，這次做得更像一片實體玻璃：
- 背景從純色`rgba(5,5,10,0.55)`改成一道由左上到右下的斜向漸層（左上角接近透明、右下角較深），比較像玻璃因為厚度/角度造成的光影不均勻，不是均勻一片色。
- `backdrop-filter`模糊值從16px提高到26px、飽和度1.2→1.6，透視底下地圖顏色時更朦朧、更飽和，玻璃感更明顯。
- 邊框從只有`border-right`改成整圈`border`（左邊因為緊貼畫面邊界拿掉），`box-shadow`加兩層`inset`高光（頂部一條、右邊一條很淡的白線），模擬玻璃邊緣因為折光而產生的亮邊。
- 新增`#left-rail::before`一道固定的斜向反光帶（45度角、白色低透明度、由淡到亮再到淡），疊在panel最底層（比裡面的按鈕/文字更早畫，不會蓋住任何內容），就是手機玻璃背板常見的那種反光效果。
- `#ic`（裡面滾動的資訊卡內容區）背景從`rgba(18,24,38,0.72)`調淡到`0.38`，讓底下`#left-rail`的玻璃紋路/反光帶能透過來，加上下各一條極淡的白邊當作「玻璃卡片邊界」的分隔線，整個側欄看起來像疊了兩層玻璃（外層panel+內層資訊卡）而不是一塊純色面板。

## v1.65 — 上一版改錯元件了，真正的「資訊面板」（#ip）改成雙層玻璃+手機螢幕質感

來回確認了好幾輪：v1.64改的`#left-rail`其實是左邊的功能/按鈕收合區，不是你說的那個顯示「資訊面板／激戰選區／區域立委棒狀圖／勝選徽章」的面板——那個實際上是`#ip`（CSS裡本來就寫了註解「浮動在地圖上的毛玻璃面板」，`right:0`釘在畫面右側）。`#left-rail`已經改回v1.64之前的原樣。

你要的是「真的透明的背板、雙層玻璃、晶瑩剔透、資訊像手機螢幕一樣疊在上面」，這跟前幾版做的「霧面深色玻璃」是不同方向——改成真正兩層結構：

1. **外層：`#ip`本身，晶瑩剔透的玻璃殼**。背景改成幾乎透明的漸層（`rgba(255,255,255,0.16)→0.03→0.08`，不再是深色調的`rgba(18,24,38,0.55)`），`backdrop-filter`加到`blur(22px) saturate(1.9) brightness(1.08)`（提高飽和度+亮度，看穿玻璃時後面的地圖顏色會更鮮明清透，不是霧濛濛一片深色）。邊框從只有左邊一條線改成整圈`1.5px`、更亮的白（`rgba(255,255,255,0.38)`），`box-shadow`疊了四層`inset`（頂部亮邊、底部暗邊、左右各一條）模擬玻璃邊緣因為折光造成的高光/陰影落差，最外面再加一層向下飄的陰影讓整片玻璃有「浮起來」的感覺。連帶把`right:0`改成`right:8px`、四個角全部圓角`22px`（原本只有左邊兩角圓角，右邊直接貼死畫面邊界），讓它看起來像一片真的浮在地圖上、離開邊界一點距離的玻璃卡片，不是嵌死在畫面邊緣的側欄。

2. **內層：`#ip::before`，深色「手機螢幕」**。新增這個偽元素，用`inset:7px`（剛好等於`#ip`新加的`padding:7px`）內縮一圈，做出一塊圓角`16px`、深色（`rgba(8,11,18,0.62)`）、帶一點內陰影跟極淡青色光暈的矩形——所有實際資訊內容（標題、棒狀圖、候選人清單…）看起來就是疊在這片「螢幕」上面，螢幕跟玻璃殼邊緣之間留一圈剛好7px的玻璃「邊框」，很像真的手機面板嵌在透明背蓋裡。

技術上有個坑：`::before`是`position:absolute`加`z-index:auto`，照CSS疊放順序規則，這種偽元素其實預設會蓋在一般文件流內容「上面」（不是下面），如果不處理，深色螢幕背景會蓋住裡面的文字。修法是幫`#ip`的所有直接子元素（`#ih`/`#ic`/`#legend`…）都補上`position:relative;z-index:1`，明確讓它們的疊放層級比`::before`（層級0）高，才會正確疊在螢幕背景「上面」而不是被蓋住。

## v1.66 — 毛玻璃感加重；面板內部也改成能透出地圖染色的玻璃

**毛玻璃更明顯**：`#ip`外層玻璃殼的`backdrop-filter`模糊值從`22px`拉到`38px`（`saturate`也從1.9提高到2），原本比較接近「幾乎看不太出來在模糊」的清透感，這次让霧面模糊感明顯很多，同時背景漸層的白色比例也從`0.16/0.03/0.08`微調到`0.2/0.06/0.12`，玻璃本身更看得出「存在」，不是幾乎隱形。

**面板內部也能透出地圖染色**：之前v1.65把內層螢幕（`#ip::before`）設計成不透明深色底（模擬手機OLED螢幕），這次你說要面板裡面也跟外層玻璃一樣直接看得到地圖選區的染色顏色透過來，所以把這片內層螢幕也改成半透明+自己的`backdrop-filter: blur(16px) saturate(1.7)`（不是完全不透明的`rgba(8,11,18,0.62)`，改成`rgba(10,14,22,0.22)`+自己的模糊），變成「兩片玻璃疊在一起」而不是「玻璃殼包一塊不透明螢幕」。裡面滾動內容的`#ic`背景也從`0.38`降到`0.12`，讓地圖的選區顏色能更清楚地透過整個面板顯示出來，同時還留一點點深色黑霧讓白色文字不會完全失去對比、維持可讀性。

## v1.67 — 你傳的參考圖「透明手機」，照這個重做整個面板

你傳了一張「透明手機」的參考圖——重點是機身本身幾乎完全看穿（幾乎沒有模糊霧感、幾乎沒有色調），只靠邊框高光+一道斜向反光帶表現出「這是玻璃」，而畫面上的App圖示是清晰、不透明、顏色鮮明地浮在最上面，不是也跟著模糊。這跟v1.63~v1.66一路做的「霧面模糊玻璃」方向不一樣——之前愈調愈朦朧、愈像毛玻璃，這次要反過來做。

**`#ip`外層改成真的看穿**：`backdrop-filter`從`blur(38px) saturate(2)`砍到`blur(3px) saturate(1.15)`（幾乎不模糊，只留一點點柔化邊緣的效果），背景色從有色的漸層`rgba(255,255,255,0.2)`一路降到近乎透明的單一`rgba(255,255,255,0.035)`，這樣才會真的像參考圖那樣直接看穿玻璃看到後面（地圖）的東西，不是霧濛濛一片白。邊框高光（`inset` box-shadow那幾層）保留，這是玻璃「有實體」的視覺線索，參考圖裡機身邊緣也是這樣亮一圈。

**內層深色「手機螢幕」整個拿掉，改成單純的斜向反光帶**：v1.65/v1.66做的`#ip::before`（內縮一圈的深色/半透明底）整個換掉，改成一道`linear-gradient(125deg, ...)`的固定斜向反光帶，貼合`inset:0`（不再內縮7px），單純模擬參考圖玻璃機身那道對角高光，不再假裝有一塊「螢幕」。

**文字可讀性靠text-shadow顧，不是靠深色底**：機身變透明之後，白色文字直接疊在會不斷變色的地圖上，原本靠深色screen墊底的可讀性沒了。改成`#ip > *`（所有直接子元素，`text-shadow`會繼承到底下所有文字）加`text-shadow: 0 1px 4px rgba(0,0,0,0.85), 0 0 10px rgba(0,0,0,0.55)`，用文字本身的陰影跟地圖顏色拉開對比，而不是靠一塊擋在文字後面的深色板子——這樣文字才能像參考圖裡的App圖示一樣「自己夠銳利、浮在透明機身上」，不用犧牲機身的透明度。

## v1.68 — 玻璃透地圖顏色的效果加強

你問能不能做到參考圖那種「背後像照著一片玻璃，直接反光出地圖顏色」。技術上這個效果本來就是靠`backdrop-filter`（取樣元素背後實際畫面、加模糊/飽和度調整後再顯示）做的，`#ip`浮在`#mp`（畫地圖的地方）上面，理論上背後的地圖顏色會透過`backdrop-filter`顯示出來——v1.67給的參數（`blur(3px) saturate(1.15)`、背景`rgba(255,255,255,0.035)`）調得太保守，效果太微弱、幾乎看不出來。

這次大幅加強：`saturate`從1.15拉到2.6（背後地圖顏色透過玻璃時會被大幅增豔，而不是變得死白/褪色）、加`contrast(1.12)`跟`brightness(1.05)`讓穿透的顏色更有「光線通過玻璃」的層次感，`blur`從3px降到2px（玻璃本身該是清澈的、不是霧面漫射，模糊太多顏色會糊成一片看不出形狀）。`#ip`自己的背景色也從`0.035`降到`0.02`，`#ic`（面板內文字滾動區）背景從`0.12`降到`0.04`——這兩層本來就是疊在`backdrop-filter`那層「取樣結果」上面的一層固定色調，數值愈低代表愈不會蓋掉、稀釋掉透出來的地圖顏色。

## v1.69 — 斜向反光帶太假，拿掉

你說整體還是好假，先把v1.65加的那道固定斜向白色反光帶（`#ip::before`）整個刪掉——那是純裝飾、寫死角度跟位置，不會隨光源/視角變化，看起來確實比較像「貼上去的效果」而不是真的玻璃反光。拿掉之後順便清掉配套的`position:relative;z-index:1`疊放層級workaround（本來是為了讓內容蓋過那道反光帶，反光帶都沒了就不需要）。剩下的「看穿玻璃透地圖顏色」（`backdrop-filter`）跟文字用`text-shadow`墊底可讀性都保留。

## v1.70 — 不當「浮起來的手機」了，改成直接頂到頁面邊界的一片玻璃

你說做出來的還是沒有玻璃感、乾脆不要手機外框這個概念了——面板不要浮在畫面裡、離邊界一小段距離，改成直接頂到頁面右邊界，只有跟地圖交界的那一側（左邊）是鈍角圓角，其餘三邊都貼齊頁面邊界，看起來像鑲在畫面右側的一整片玻璃，不是一張獨立飄浮的卡片。

改法：`right`從`8px`改回`0`、`bottom`從`18px`改回`10px`，拿掉v1.65加的`padding:7px`+`box-sizing:border-box`（那是為了做手機邊框的留白，概念沒了就不需要）。`border-radius`從四角都圓（`22px`）改成只有左邊兩角圓、右邊直角（`26px 0 0 26px`，鈍角比之前更圓一點），`border`拿掉右邊那一側（`border-right:none`，反正右邊貼齊頁面邊界，畫一條邊框線在那裡沒有意義）。玻璃色澤改靠邊緣的高光/暗角層次做（頂部亮邊、底部暗邊、左邊界亮邊，模擬光從左上角打過來、玻璃邊緣因為折光產生的明暗落差），而不是貼一條寫死角度的假反光線。`backdrop-filter`看穿地圖顏色的部分維持v1.68的參數不變。

## v1.71 — 邊線還是太像手機邊框，拿掉實體border改純柔光

你說那圈邊線看起來還是很像手機邊框，不夠像透明玻璃。問題出在`border: 1.5px solid rgba(255,255,255,0.42)`——不管透明度調多低，一條寬度固定、四邊粗細一致的實體邊線天生就會讀成「裝置外殼的邊框」，這是`border`這個屬性的視覺本質，不是顏色/透明度能解決的。

改法：把`border`整個拿掉，邊緣的「這是玻璃」視覺線索完全交給`box-shadow`的`inset`陰影做——而且這次把原本銳利的1.5px實線陰影（`inset 0 1.5px 0`這種0模糊半徑的寫法，本質上也是一條線）都改成有模糊半徑的柔光（`inset 0 1px 6px`、`inset 0 -8px 16px`、`inset 6px 0 14px`），變成漸層式的柔和光暈，而不是輪廓分明的一圈線——光是從左上角灑進來、慢慢暈開的感覺，比較接近真的玻璃因為折光造成的柔和明暗過渡，不是一條看得出粗細的邊框線。

**v1.72**：你說方向對了、再明顯一點。四層`inset`柔光的不透明度跟模糊/擴散半徑都加大——頂部亮邊`0.22→0.38`、底部暗角`0.18→0.28`、左邊界亮邊`0.06→0.12`，模糊半徑也整體拉大（`6px→10px`、`16px→22px`、`14px→20px`），柔光的存在感更明顯，但因為都還是有模糊半徑的漸層（不是0模糊的實線），不會退回「像邊框」的觀感。

**v1.73**：再加大一輪。頂部亮邊`0.38→0.55`、底部暗角`0.28→0.4`、左邊界亮邊`0.12→0.2`，模糊/擴散半徑再拉大一截（`10px→16px`、`22px→32px`、`20px→30px`），外側的陰影（讓面板看起來浮在地圖上那層）也從`-10px 0 36px 0.45`加到`-14px 0 48px 0.55`。柔光範圍變寬、對比更明顯，還是靠模糊半徑撐開的漸層，沒有變回銳利邊線。

**v1.74**：v1.73加太多了，又變回你說的「太假」。退回v1.72的數值（頂部亮邊`0.38`、底部暗角`0.28`、左邊界亮邊`0.12`，模糊半徑`10px/22px/20px`），這組是你先前確認「有比較好」的版本。

## v1.75 — 邊緣反光改成更自然（集中在角落，不是整條邊均勻一條線）

你要邊邊的玻璃反光更自然。原本三層`inset`陰影都是純水平/垂直offset（`0 2px`、`0 -10px`、`10px 0`），效果是沿著整條邊「均勻一致」的亮/暗帶——這種等寬等亮度的直線光帶，物理上不太合理：真實光源是從某個方向打過來的，打在圓角玻璃邊緣上，亮點應該集中在光源那一側的角落附近、然後往外漸漸暈開變暗，不會整條邊framework一樣粗細相同。

改成對角線offset（`10px 10px`模擬左上角光源、`-14px -14px`模擬右下角背光陰影），並加上負的spread半徑（`-6px`/`-8px`）讓亮區/暗區集中在角落附近再往外暈開，不是貼著整條邊拉一條等粗的線。左邊界另外留一條很淡的邊緣光（`4px 0 10px`，透明度降到`0.1`）維持左側輪廓的存在感，但不再是主要的效果來源。整體看起來比較像「光從某個角度照在圓角玻璃上」，不是「四邊都裝了霓虹燈條」。

## v1.76 — 資訊面板毛玻璃整個取消；海岸線發光依政黨輸贏分區調亮/調暗

**資訊面板（`#ip`）毛玻璃取消**：v1.65~v1.75這一長串玻璃感實驗，你最後決定不要了，`#ip`跟`#ic`都改回v1.65之前的原樣（`#ip`背景`rgba(18,24,38,0.55)`+`backdrop-filter:blur(20px)`+左邊框、`#ic`背景`rgba(18,24,38,0.72)`），順便清掉沿路加的`text-shadow`可讀性補丁（不透明底色回來了，不需要靠文字陰影撐可讀性）。

**海岸線發光依選區輸贏分區調亮/調暗**：v1.62做的海岸線`drop-shadow`（`#g-base`那條CSS規則）是全島統一一種顏色/亮度，不管選了哪個政黨都是同一圈光。你要的是選了政黨之後，這個黨贏的選區沿海那段光調亮、沒贏的調暗，不要整島同一種光。

`#g-base`原本的CSS group-level filter（沒選政黨/總的時候用的預設統一光）保留不動，新增`updateEdgeGlowPerDistrict()`，選了政黨時逐一在每個`.ft-ed`（選區）本身用inline `style.filter`蓋掉那個統一的group filter（inline style優先權比CSS規則高）——亮/暗怎麼判斷直接借用`edColor()`已經算好的結果：`edColor()`在選了政黨時，選區沒被該黨拿下就會回傳v1.54定義的黑化色`'#333338'`，這裡只要檢查回傳值是不是這個黑化色，不用另外重寫一次「這個選區算不算贏」的邏輯。贏的選區用該黨的`glow`色（`3px`+`11px`兩層，跟原本統一光同一個量級）、沒贏的選區用很淡的白色柔光（`1.5px`+`3px`，幾乎看不見，這樣沿海線在非本黨版圖的地方會明顯黯淡下來，符合「調暗一些」的要求，但不會整個消失變全黑，維持地圖輪廓還看得出來）。清掉政黨選擇（回到「總」）時把inline filter清空，自動退回group的統一光。

只在`goED()`（全國選區總覽）這個層級生效（掛在`updateGlowAndBaseED()`尾端，這個函式本來就只在ED層級的redraw會跑），跟「台灣本島」這個框架一致——切到單一選區/鄉鎮層級後`.ft-ed`不在畫面上，這個效果自然不會有動作，也不需要額外處理。

**注意一個自然的副作用**：因為是逐選區個別套`drop-shadow`（不是把「贏/沒贏」各自併成一個大群組再各套一次filter），贏跟沒贏兩個選區交界的那條「陣營分界線」（不是海岸線、是選區內部邊界）現在也會因為兩邊套了不同的filter而稍微透出一點光影落差，等於順便把「這個黨的連續版圖範圍」的內部輪廓也隱約勾出來了——不只海岸線在變化，算是額外的一個效果，沒有特別去消除它。

## v1.77 — 改成滑鼠移到贏的選區才亮

你提議與其全島同時分區亮暗，不如改成滑鼠移過去哪個選區、那個選區是這個政黨贏的才亮起來，比較互動、也比較不會整張圖同時很多光源在搶注意力。

改法：`updateEdgeGlowPerDistrict()`裡選了政黨後，先把所有選區的filter都設成統一的淡光（`dimFilter`，跟之前「沒贏」用的同一組很淡的白色柔光），接著在每個`.ft-ed`上掛`mouseover.edgeglow`/`mouseout.edgeglow`（用命名空間`.edgeglow`，不會覆蓋掉原本`showEDPanel`用的那個`mouseover`監聽器，兩個各自獨立觸發）——滑鼠移進去時檢查`edColor(d.properties.ed)`是不是黑化色`'#333338'`，不是的話（代表這個政黨真的贏了這裡）才把這個選區的filter換成亮的`winFilter`；滑出去一律退回淡光。清掉政黨選擇時，連filter跟這兩個監聽器一起清掉，退回group預設的統一光，不會留下殘留的hover行為。

**v1.78**：你說預設那圈淡光透出去的範圍太細了。`dimFilter`兩層`drop-shadow`的模糊半徑從`1.5px`/`3px`拉大到`4px`/`9px`，光暈往外暈開的範圍變大；同時把透明度也從`0.05`/`0.03`微幅提高到`0.06`/`0.04`，避免範圍變寬之後光線被稀釋到幾乎看不見。

**v1.79**：範圍再加大不少。模糊半徑從`4px`/`9px`大幅拉到`10px`/`24px`，透明度也再提高一點（`0.07`/`0.045`）維持在這麼寬的範圍下還看得出來。

## v1.80 — 修卡頓：關掉重複疊加的group filter、預設淡光收斂成單層

你反應有點卡頓，抓到兩個問題：

1. **`#g-base`的group filter跟每個選區的per-path filter同時疊在一起跑**：v1.76只想著「per-path filter用inline style蓋掉group filter」，但這兩個其實是套在不同層級（一個在`#g-base`本身、一個在裡面每個`.ft-ed`），並不會互相蓋掉——選了政黨之後，等於畫面同時在跑「整個`#g-base`一次`drop-shadow`模糊運算」+「73個選區各自兩層`drop-shadow`模糊運算」，兩輪疊加，這是卡頓的主因。修法：`updateEdgeGlowPerDistrict()`裡選了政黨時額外把`gEl.style.filter = 'none'`，直接關掉`#g-base`自己那層group filter，只留per-path這層在跑；清掉政黨選擇時再把它設回空字串`''`，讓CSS的group filter規則接手。
2. **v1.79為了「範圍要大」把預設淡光的模糊半徑衝到24px、還疊兩層`drop-shadow`**：`drop-shadow`的運算成本跟模糊半徑大致成正比，24px模糊+兩層，乘上73個選區同時套用，成本相當高。收斂成單層、模糊半徑降到`5px`（比v1.78的`4px`/`9px`雙層更收斂，但比v1.77原始的`1.5px`/`3px`還是寬一些），效能跟範圍取一個折衷點。hover時才出現的`winFilter`（一次只會有一個選區在用）維持雙層沒動，那個本來就不是效能瓶頸。

## v1.81 — 還是卡，乾脆拿掉「73個選區同時都套一層」這件事本身

v1.80把模糊半徑收斂了，但問題根源其實不是「半徑多大」，是「不管多淡，73個選區同時各自套一層`drop-shadow`」這個做法本身就貴——就算單層、半徑只有5px，73次同時算還是有負擔，量測後這才是卡頓真正的主因，不是半徑數字的問題。

徹底改法：baseline完全不逐選區設filter了。`#g-base`本身那層CSS group filter（v1.62本來就有的，只對整個群組算一次模糊，跟選區數量無關，非常便宜）重新當作唯一的預設光源，一直開著、不再因為選了政黨就關掉。逐選區的`filter`只在滑鼠真的hover在那一個元素上的當下才短暫存在，滑開就直接拿掉（`style('filter', null)`，不是換回一個「淡」的filter，是真的完全不設，效能上跟原本沒有這個功能一樣）。這樣同一時間最多只會有1個選區在額外算drop-shadow（hover中的那個），不會再有「73個一起算」的情況，正常情況下（沒在hover任何選區）效能應該要跟v1.76之前完全一樣。

## v1.82 — 跑馬燈數字也依選定政黨變色/反黑

你問切政黨的時候，上面跑馬燈（民主進步黨/中國國民黨/民眾黨/無黨籍/其他政黨那排數字）要不要也跟著反應——原本不管選哪個政黨，跑馬燈都維持預設白色不動。

新增`applyTickerPartyHighlight()`：選了政黨（`_histSeatParty`）時，比對到的那個黨（`tk-label-g`=民進黨/`tk-label-b`=國民黨/`tk-label-w`=民眾黨(或2016的時代力量)/`tk-label-ind`=無黨籍/`tk-label-oth`=其他）維持`opacity:1`、數字顏色換成`HIST_PARTY_BTN_STYLE`裡那個黨的代表色（跟收合條按鈕、地圖染色用同一份色票，不用另外定義）；其餘四個整組（logo+名稱+數字）降到`opacity:0.35`+`grayscale(0.6) brightness(0.6)`濾鏡（你說的「反黑」——不是真的變黑色，是去掉飽和度+壓暗，看起來像關掉的燈）。選「總」或非歷年分析模式時全部還原成預設白色。

呼叫時機：直接掛在`applyHistSeatPartyToButton()`尾端——這個函式本來就是唯一一個「不管從哪個入口（選導覽最後一步的政黨、按右上角按鈕循環切換、切語言時同步）改了`_histSeatParty`都一定會呼叫」的共用函式（v1.52修bug時就是靠這個特性把按鈕文字/顏色統一起來），跑馬燈掛在同一個地方，不用在每個改動`_histSeatParty`的地方各自補call。

## v1.83 — ED→TC不管選不選政黨都卡；TC層光暈太強眼花——海岸線光暈改成只在看得到全國輪廓的層級才開

你確認了ED進TC層的卡頓「不管選哪個政黨還是沒選都會卡」，代表跟v1.76~v1.81那個逐選區hover發光的功能無關（那個只在選了政黨時才會動作），問題出在別的地方。同時你也說TC層的光暈太強、有點眼花。

抓到問題：`#g-base`的CSS group filter（v1.62做的海岸線發光，`filter: drop-shadow(...)`那條規則）寫死在CSS裡，不管在哪個層級都一直開著算——包括鑽進單一選區看鄉鎮（`goTC`）、鑽進單一鄉鎮看村里（`goVill`/`goVillAll`）這種畫面只剩一小塊局部的情況，也一樣在跑這個filter。這有兩個問題：(1) 效能上，這個filter每個畫面都要重新算一次模糊，跟目前顯示的內容有沒有用到它無關，鑽進小範圍畫面時完全是白耗；(2) 視覺上，同一圈固定模糊半徑，套在「全國」這種大範圍形狀上看起來剛好，套在「一個選區的幾個鄉鎮」這種小範圍形狀上，比例上就顯得過強、刺眼。

改法：新增`setCoastlineGlowActive(active)`，直接切`#g-base`本身的`style.filter`（`'none'`關掉／`''`清掉inline、退回CSS預設開啟）。在`goED()`、`goTCAllNational()`（全台鄉鎮總覽）、`goVillAllNational()`（全台村里總覽）——這三個看得到「全國輪廓」的層級——呼叫`setCoastlineGlowActive(true)`；在`goTC(ed)`（鑽進單一選區）、`goVill()`、`goVillAll()`（鑽進單一鄉鎮/選區村里）——這三個只看得到局部的層級——呼叫`setCoastlineGlowActive(false)`。鑽進去的時候完全不算這個filter，應該同時解決卡頓（少算一輪模糊）跟太刺眼（畫面裡完全沒有這圈光暈了）兩個問題。

**順便**：你另外提到滑鼠跟隨的探照燈（`#mp-light`）想試試`mix-blend-mode`讓光打在地圖上更像手電筒照在壓克力板上的折射感——這個屬性其實v1.60就已經加了（`mix-blend-mode: soft-light`），這次照你的建議換成`overlay`（比`soft-light`對比更強、更有「跟底下顏色做光影運算」的味道），其餘（`mask-image`裁範圍、跟著滑鼠跑）都沒動。

## v1.84 — hover贏的選區時，密集小選區（高雄市區）光暈太刺眼——改成依選區大小動態調整光暈範圍

`winFilter`原本是寫死的`4px`/`14px`模糊半徑，套在高雄市區這種選區密集、每個選區面積本身就小的地方，固定的光暈範圍相對選區大小比例過大，會直接糊成一整片很刺眼；相反地套在花蓮台東這種選區面積很大的地方，同樣的半徑反而顯得保守、不夠亮。

改法：拿掉寫死的`winFilter`常數，改成每次`mouseover`當下用`this.getBBox()`量這個選區（`<path>`元素）實際畫出來的大小，取寬高較小的那邊當作`size`，光暈的兩層模糊半徑改成`size`的固定比例（核心層`size*0.05`、外暈層`size*0.16`），並各自夾在一個上下限範圍內（核心`1.2px~4px`、外暈`4px~14px`）避免選區極端小/極端大時光暈完全消失或誇張過頭。這樣密集小選區的光暈會自動縮小、不會糊成一片，大選區維持原本差不多的亮度。

## v1.85 — 台中/高雄市區還是太刺眼，抓到根本原因：黨色光暈把原本的黃框glow整個蓋掉了

你說台中市區（國民黨）、高雄市區（民進黨）hover還是太亮太刺眼，「黃匡都快蓋掉了」——這句話點出真正的問題：`.ft-ed:hover`這個CSS規則本來就有一圈黃色描邊+黃色`drop-shadow`glow（`filter: drop-shadow(0 0 1.5px 黑) drop-shadow(0 0 4px 黃)`，這是選區hover的原有UI提示，跟這次的功能無關、原本就存在）。但v1.84的JS用`d3.select(this).style('filter', winFilter)`直接整個蓋掉這個屬性——inline style設`filter`的優先權比CSS的`:hover`規則高，這不是「疊加」，是「取代」，所以hover密集小選區時，你看到的那片刺眼強光其實不是黨色光暈疊加黃框glow變超亮，而是黨色光暈把原本比較收斂的黃框glow整個換掉、換成一個沒對齊過比例、看起來過曝的效果。

修法：把黃框那層filter原封不動複製成`yellowHoverFilter`常數，跟黨色光暈的兩層`drop-shadow`寫進「同一個」filter字串裡再一起設進`style.filter`——這樣兩個glow會同時疊加顯示（黃框glow保留原本的視覺、黨色光暈疊在旁邊當輔助強調），不再是互相取代。同時把黨色光暈自己的比例再收斂一截（核心層`size*0.05→0.025`、外暈層`size*0.16→0.07`，上限也從`4px/14px`降到`2.2px/6px`），讓它明確是輔助角色，不會蓋過本來就存在的黃框hover提示。

## v1.86 — 全島縮小檢視時，密集市區還是太亮；抓到是下限夾太高

你補充了一個關鍵線索：同樣是台北/台中/高雄市區，「放大看就OK，但沒放大、整個台灣島縮小檢視時就是太亮」。這代表問題不是比例算錯，是`Math.max(下限, ...)`那個下限夾得太高——縮小看全島時，這些密集市區選區`getBBox()`量出來的`size`可能只有個位數px，乘上比例（`size*0.025`、`size*0.07`）會是遠小於1的極小值，但先前的下限（核心`0.8px`、外暈`2px`）比這個算出來的極小值大上好幾倍，等於不管比例怎麼算，最後都會被夾回`0.8px`/`2px`這個下限——而`0.8px`/`2px`的光暈半徑，對一個本來只有幾px大的選區來說還是太大，直接糊成一片。放大檢視時選區本身變大、算出來的比例值超過下限，這時候用的是比例算出來的值，才會看起來正常。

修法：把下限大幅降低（核心`0.8px→0.15px`、外暈`2px→0.35px`），縮小看全島時，密集小選區會用非常接近比例算出來的極小值，不會被下限硬拉大，光暈自然變得很細很收斂；放大檢視時因為比例值本來就超過新的下限，效果不受影響、維持原本的樣子。

## v1.87 — 依bbox動態調的方向沒調準，改成單層固定小半徑

v1.86調完你說反而更刺眼了——`getBBox()`量出來的其實是選區在SVG「本地座標系」裡的大小，不是螢幕上目前縮放後看到的視覺大小（zoom多半是套一層transform縮放整個群組，`getBBox()`量到的值不會因為這層transform改變），這套「依大小動態算比例」的邏輯從一開始的假設可能就不成立，調下限調上限都只是在猜、沒有真的對準問題，才會一直調不準甚至調反方向。

放棄整套動態比例計算，改成單層、固定`1px`模糊半徑的`drop-shadow`，不管選區大小、目前縮放層級都是同一個很小的值——不是最精確的做法，但好預測，不會再有「某個特定情境（全島縮小/密集市區/特定選區）突然爆亮」這種意外，之後如果還是不合適比較容易抓到問題在哪（純粹只有一個數字可以調，不用同時想「比例」跟「縮放狀態」兩個變數）。跟黃框hover glow疊在同一個filter字串裡這件事維持v1.85的修法沒變。

## v1.88 — 滑鼠探照燈在密集市區自動縮小集中

你提了另一個角度：與其一直調per-district光暈的比例，不如讓滑鼠探照燈（`#mp-light`，v1.60做的那個跟著滑鼠跑的手電筒效果）在選區密集的地方自動縮小、集中光束範圍，這樣光就不會同時蓋住好幾個緊挨在一起的小選區。

這次用`getBoundingClientRect()`（會反映目前實際縮放層級下、選區在螢幕上真正的大小，跟v1.86發現「`getBBox()`量到的是SVG本地座標、不會隨縮放變化」不一樣，這是這次改用它的原因）量滑鼠正停在哪個選區上、螢幕上的大小，新增CSS變數`--torch-scale`（0.35~1之間，選區螢幕大小低於90px時開始等比縮小，最小縮到35%），套進`#mp-light`的`mask-image`（裁出探照燈範圍那個circle的百分比半徑都乘上這個變數）跟`initMapLightOverlay()`的核心亮點漸層。滑鼠移到高雄/台中/台北市區這種選區小的地方，探照燈自動縮小集中；移到花蓮台東這種選區面積大的地方維持原本大小。

實作上掛在`.ft-ed`既有的兩個`.join('path')`綁定點（`goED()`本體、2016↔其他年份切換時的Pingtung選區數量差異重綁），用命名空間`mouseover.torchscale`/`mouseout.torchscale`，不會跟原本的`mouseover`（`showEDPanel`）或v1.76加的`mouseover.edgeglow`互相覆蓋，三組各自獨立觸發。

**v1.89**：市區還要再縮小一點。門檻從`s/90`拉高到`s/140`（選區螢幕大小要更大才能到滿格`scale=1`，等於更多選區會落在縮小的範圍內），下限也從`0.35`降到`0.2`（密集市區可以縮得更小）。

## v1.90 — 區域立委/總統大選/不分區政黨票三條棒狀圖用一個框框起來

你問這三條棒狀圖要不要框起來方便一眼比較分裂投票，討論後你確認要「一個大框」（不是三個各自分開的框）。

新增共用函式`histTripleBoxWrap(html)`，邊框顏色直接用`#ip`資訊面板本身同一色（`rgba(255,255,255,0.08)`），跟現有視覺語言一致、不是另外發明一套。這三條棒狀圖的HTML其實是三個獨立函式各自組出來的（`histBoxHtml()`用於2024、另一個用於2020、`function`（無獨立命名，2016版）各自維護一份`regionBar`/`presBar`/`plBar`拼接邏輯，本來就沒有共用），所以在這三個函式各自的`return`那一行外面包一層`histTripleBoxWrap(...)`，不用去動裡面各自的組裝邏輯。

**v1.91**：框線再粗一點、棒狀圖區塊之間那條細分隔線整個拿掉。`histTripleBoxWrap()`的邊框從`1px solid rgba(255,255,255,0.08)`加粗加亮到`1.5px solid rgba(255,255,255,0.16)`。棒狀圖區塊（三條比較棒狀圖box）跟候選人得票棒狀圖清單之間那條細線，是共用全域常數`SEC_DIVIDER`（`<div style="border-top:1px solid rgba(255,255,255,0.06);margin:8px 0">`）畫出來的——這個常數在ED/TC/VC三個層級、2016/2020/2024三個年份總共7個地方都有用到，改這一個常數就等於全部一次改完，不用每個層級/年份各自找一次。拿掉`border-top`只留`margin`（保留間距、不留線）。

## v1.92 —「無效票比例」改成「有效票比例」，刪掉「選區有效票」數字

**無效票比例→有效票比例**：這個是`turnoutBadgeHtml(tr, ir)`這個共用函式產生的（投票率/無效票比例那兩顆膠囊，ED/TC/VC每個層級每個年份都共用同一份，跟`SEC_DIVIDER`一樣是單一函式），改法很單純：label從「無效票比例：」改成「有效票比例：」（英文`Invalid:`→`Valid:`），顯示的數字從`ir`（無效票率）改成`100 - ir`（有效票率＝100%減無效票率）。膠囊小圓點的顏色邏輯維持用原本的`ir`門檻判斷（`ir<2`綠、`ir<=5`黃、其餘紅）沒有跟著改，因為那本來就是「無效票率愈低愈好」的品質訊號，反映到「有效票率」上一樣是「愈高（愈接近100%）愈好」，邏輯本來就對得起來，不用重新設計門檻。

**刪掉「選區有效票」數字**：這個不像`turnoutBadgeHtml`/`SEC_DIVIDER`是單一共用函式，是17個地方各自用inline template literal寫死`<div>...選區有效票/有效票...${xxx.tot.toLocaleString()}</div>`（ED/TC/VC三層×2016/2020/2024三年×各種fallback分支，各自維護自己的一份，本來就沒共用），逐一在這17個地方把這個`<div>`那段拿掉，同時保留同一行/同一段落原本緊接著的`turnoutBadgeHtml(...)`（投票率/有效票比例那兩顆膠囊）不動——這兩個原本常常寫在一起，但邏輯上是各自獨立的兩塊資訊，只拿掉票數這塊。

## v1.93 — 投票率/有效票比例兩顆膠囊搬到標題右上角，取代「推算選舉人數」徽章的位置

討論完前一版「膠囊要放哪裡才不亂」之後，你直接提議：乾脆放棄標題右上角那個「推算選舉人數▲+2.7% +7,104人」徽章（跟前一年比，選舉人數估計變化），把那個位置讓給投票率/有效票比例這兩顆膠囊。

這個「推算選舉人數」徽章（`pvi-shift-badge`，只在有「跟前一年比較」的情境下才會出現，例如2024模式比2020、2020模式比2016）跟投票率/有效票比例膠囊分別存在於6個地方（ED/TC/VC三層×vs2020/vs2016兩種比較各一份，各自獨立維護、彼此之間沒有共用邏輯）。逐一在這6個地方：
1. 把標題列右上角原本顯示`XxxElvArrowBadge`（推算選舉人數徽章）的位置，改成顯示`turnoutBadgeHtml(...)`（投票率/有效票比例膠囊）。
2. 把原本膠囊出現的位置（通常在候選人清單後面）拿掉，避免同一份資訊出現兩次。

原本的「推算選舉人數」相關計算（`_edElvArrowBadge`/`_vcElvArrowBadge`等變數）保留在程式碼裡沒有刪除，只是不再被用到（純JS層面是安全的無害死碼，不影響執行），如果之後想要恢復或是移到別的地方顯示，數字都還在、隨時可以接回去用，不用重新計算。

## v1.94 — 兩顆膠囊跟選區title擠在一起，改成自己一行；文案微調

你反應這兩顆膠囊放在標題右上角（跟v1.93做的一樣）跟選區title擠在一起——原因是v1.93把它們塞進跟「選區名稱」同一個`justify-content:space-between`的標題列裡，狹窄的288px面板寬度扣掉選區名稱（有些選區名很長，例如「新北市第12選舉區」）之後剩下的空間，塞不下兩顆膠囊，才會看起來很擠。

改法：6個地方（跟v1.93改的同一批：ED/TC/VC三層×vs2020/vs2016）標題列右上角都改回只放PVI/開票分類徽章（`_edPviBadge`/`_tcPviBadge`/`_vcPviBadge`，原本就有的，跟這次的膠囊無關），兩顆膠囊搬出標題列，改成緊接在勝選徽章（`win-badge`，就是「民進黨65.3%勝選」那個）下面自己獨立一行、靠右對齊（`justify-content:flex-end`），不再跟選區名稱共用一行搶空間。`turnoutBadgeHtml()`本身也補上`justify-content:flex-end`，換行時兩顆膠囊會靠右對齊，不會有一顆貼右一顆貼左的狀況。

**順便**：「有效票比例」文案簡化成「有效票率」，比較口語。

## v1.95 — 兩顆膠囊調暗縮小，維持獨立一行不變

討論完後你確認：維持現在獨立一行的位置，但整體調暗、縮小字級，不用改成跟開票分類徽章同一行或改左對齊。`turnoutBadgeHtml()`裡的`pill()`：字級`10px→9px`、文字顏色`#bbb→#999`、padding/border-radius跟著等比縮小一點、背景/邊框透明度也調低（`0.05→0.04`、`0.1→0.08`）；小圓點另外加`opacity:0.75`稍微收斂存在感；最外層包一層`opacity:0.8`整體再壓暗一次。維持`justify-content:flex-end`右對齊、自己獨立一行的位置不變。

## v1.96 — 兩顆膠囊改左對齊

你問要不要改左對齊——標題、勝選徽章都是從左邊開始，右對齊反而讓這行動線跳來跳去。`turnoutBadgeHtml()`內層的`justify-content:flex-end→flex-start`，加上外層6個地方包住它的`<div style="display:flex;justify-content:flex-end;margin-top:2px">`也要一起改（內層決定膠囊之間怎麼排、外層決定整個膠囊區塊在那一行裡貼左還是貼右，兩層都要改才會真的左對齊），用同一段字串`justify-content:flex-end;margin-top:2px`在6個地方都一樣，一次`replace_all`改完。

## v1.97 — 內容區跟底部「點擊進入下一層/全選區村里」按鈕之間加一條細線

你問要不要在棒狀圖跟底部導覽按鈕（點擊地圖進入鄉鎮市區▶/⊞全選區村里）之間加條線——這條線跟v1.91拿掉的那條不一樣，那條是夾在兩塊「內容」（棒狀圖vs候選人清單）中間純粹增加雜訊；這條是分隔「內容」跟「操作按鈕」，性質不同，用細線區隔比較合理。

這個按鈕列的外層wrapper（`display:flex;gap:6px;margin-top:6px;align-items:center`）是整個檔案裡唯一只用在這個位置的固定樣式字串，在ED/TC/VC三層×各年份×各種fallback分支總共13個地方都是同一段，直接`replace_all`把這段字串加上`border-top:1px solid rgba(255,255,255,0.06)`+`padding-top:8px`（`margin-top`也從6px微調到8px配合），13個地方一次改完，不用逐一去找。

**v1.98**：線再粗再明顯一點，`1px solid rgba(255,255,255,0.06)`加到`1.5px solid rgba(255,255,255,0.16)`，13個地方同樣用`replace_all`一次改完。

## v1.99 — 地圖標題跟左側按鈕欄重疊，跟著跑馬燈一起右移

抓到原因：`#left-rail`（左側側邊欄）是`position:absolute`浮在地圖上面（不是flex排版把`#mp`往右推），`#mp`本身其實佔滿整個畫面寬度，`#left-rail`只是疊在它左邊那250px的區域上面。`#map-title`（地圖標題，`position:fixed;left:14px`）剛好也定位在畫面最左邊，跟`#left-rail`覆蓋的區域重疊；平常文字短（「台灣立委選舉儀表板」）看起來還好，但winner模式選了年份後標題會變成「2020 台灣立委選舉儀表板」這種帶年份前綴的較長文字，寬度增加後跟側邊欄裡的內容重疊更明顯。

改法：`#map-title`的`left`從`14px`移到`264px`（清出側邊欄250px寬度+14px留白），跟原本的跑馬燈（`#ticker`，本來就已經用`left:266px`避開側邊欄）同一個邏輯、同一個區域。但標題移過去之後會跟跑馬燈卡在同一段空間搶位置，所以跑馬燈也跟著從`left:266px`移到`left:430px`，讓標題文字（尤其是加了年份前綴變長的時候）有足夠寬度不會撞到跑馬燈。這兩個數字是憑估算抓的，實際效果麻煩你看一下，如果還是卡到再微調。

**v2.01**：v1.99用「往右推」的方式讓標題閃開跑馬燈，但你說右邊還要留空間給別的東西（`#top-right-controls`/`#seat-summary-float`），沒辦法無限往右推。改用「上下疊放」取代「左右並排」：跑馬燈維持原本的`top:16px;left:266px`（右邊空間讓出來，不用再往右塞），地圖標題改成放在跑馬燈正下方（`top:42px;left:266px`，跟跑馬燈同一個起始x座標，垂直隔開約26px的高度），兩者不再搶同一排的水平空間，也不用再一路把跑馬燈往右推。

## v2.02 — 模式切換改成懸浮展開選單（FAB Speed Dial），放在標題正下方

討論確認範圍後（只換模式切換按鈕，年份選單/滑桿等其他控制項留在左側欄），實作：

**拿掉左側欄最上面那五排「按鈕＋年份選單」**：原本`btn-winner`/`btn-comp`/`btn-third`/`btn-sim`四顆按鈕（歷年分析/綜合PVI/第三勢力/情境推演，政黨熱力圖沒有獨立按鈕、本來就是select本身兼作切換）整個從DOM刪掉——先確認過這四個id在檔案裡沒有被其他地方引用（`setMode()`裡`document.getElementById('btn-'+k)`那行有`if(el)`保護，刪掉按鈕後安全地變成no-op，不會噴錯）。年份選單（`winner-year-select`/`comp-year-select`/`third-year-select`/`heatmap-year-select`）、`party-heatmap-select`、`sim-sub-select`全部保留、id/onchange都沒動，只是外層從「按鈕+選單同一排」改成「純標籤文字+選單同一排」（`.mode-ctx-row`+`.mode-ctx-label`），並且五排各自包上`id="mode-ctx-{winner|comp|third|heatmap|sim}"`，預設只有`mode-ctx-winner`顯示、其餘四個`display:none`。

**新增FAB主按鈕+扇形選單**：`#mode-fab-wrap`用`position:fixed`固定在地圖標題正下方（`top:68px;left:266px`，跟標題/跑馬燈同一欄，不是塞在左側欄裡面，跟收合側欄的狀態完全無關），點主按鈕（圓形，`onclick="toggleModeFab()"`）展開/收合`.mode-fab-wrap.open`這個class，五個`.mode-fab-item`用CSS變數`--fx`/`--fy`（各自inline style設定）算出扇形展開後的座標——從水平（0°）到垂直向下（90°）四分之一圓弧，`transition-delay`錯開讓五個圖示依序彈出、不是同時跳出來。點擊圖示背後呼叫`chooseModeFab(key)`，還是呼叫原本的`setMode(...)`（政黨熱力圖那顆會讀`party-heatmap-select`目前的值，沒選過就預設`dpp`），收合選單。點選單外面任何地方也會自動收合（掛在`document`的click監聽器上判斷有沒有點在`#mode-fab-wrap`裡面）。

**同步邏輯**：新增`updateModeFabUI(m)`，掛在`setMode()`裡（跟`btn-winner`等按鈕原本toggle`.active`同一行後面），依目前`mode`決定FAB主按鈕要顯示哪個圖示、扇形選單裡哪個圖示要加`.active`樣式（發光邊框）、以及五個`.mode-ctx-row`裡哪一個要顯示、其餘四個要隱藏——只要`setMode()`有跑，這幾個UI都會自動同步，不用在每個切換模式的地方各自補call。

## v2.03 — 整個FAB Speed Dial跟標題位置調整都revert掉，回到最原本的版本

你說算了、改回最原本的樣子，標題跟左側資訊欄有一點點重疊沒關係。把v1.99~v2.02這一整段全部復原：

- `#map-title`的`position`從`top:42px;left:266px`改回最原始的`top:12px;left:14px`
- `#ticker`維持`left:266px`（這個本來在v1.99~v2.01之間调整過，最後停在跟原始值一樣的地方，不用再動）
- 整組FAB Speed Dial（`#mode-fab-wrap`/`#mode-fab-main`/`.mode-fab-item`相關CSS、HTML、`toggleModeFab()`/`chooseModeFab()`/`updateModeFabUI()`三個JS函式、`setMode()`裡呼叫`updateModeFabUI(m)`那一行）全部拿掉
- 左側欄最上面那五排控制項改回原本「按鈕＋年份選單」的樣子（`btn-winner`/`btn-comp`/`btn-third`/`btn-sim`四顆按鈕都復原），不再是v2.02那種「只顯示目前模式那一排、其餘隱藏」的`.mode-ctx-row`寫法，五排一直都同時顯示，跟最初的行為一致

搜尋確認過`mode-fab`/`mode-ctx`/`ModeFab`這幾個關鍵字在檔案裡已經完全找不到殘留。

## v2.04 — 選政黨這題擴充到2020/2016；時代力量獨立選項；無黨籍分類修正；常駐聲明ResizeObserver；跑馬燈政黨框+熱力圖模式也適用

這輪是一次處理好幾件事，逐項記錄：

**選政黨題目擴充到2020/2016**：原本「歷年分析模式選政黨」這題（`WINNER_ONBOARD_STEPS`最後一步）的選項是寫死的2024版（國民黨/民進黨/民眾黨/無黨籍/其他/總），2020/2016年份也會顯示同一份，但民眾黨在這兩年不該獨立成選項（2016根本還沒成立，2020雖然有但你要求時代力量優先）。新增`getPartyChoicesForYear(yr)`，2024回傳原本那組（tpp當第三勢力），2020/2016回傳把tpp換成npp（時代力量）的版本，其餘（kmt/dpp/ind/other/total）三年共用。`renderCompOnboardStep()`渲染選項那一行改成：如果是`chooseHistPartyAndFinish`這題（也就是歷年分析選政黨），就呼叫`getPartyChoicesForYear(winnerYear)`動態算，不是讀寫死的`s.choices`。右上角循環切換鈕（`cycleCampFilter`）的順序也改成從同一個函式取，不再各自維護一份。

**新增REAL_SEATS_2020/REAL_SEATS_2016**：依照已經在檔案裡、原本就用在半圓圖的`HIST_REGION_SEATS_2020`/`HIST_PL_SEATS_2020`/`HIST_IND_SEATS_2020`（2016同理）三張表加總算出來，不是憑空編數字。分類原則（討論確認過）：黨籍撐腰但登記無黨籍參選、實際當選的（2020：蘇震清/趙正宇/林昶佐/傅崐萁）算「無黨籍」；沒有另外開獨立選項的小黨（2020台灣基進1席、2016親民黨3席）算「其他」。兩年分別加總都對得上113席。新增`getRealSeatsForYear(yr)`統一取表，`updateSeatSummaryStrip()`兩處讀`REAL_SEATS_2024`的地方都改成呼叫這個函式，不用各自判斷年份。

**候選人黨籍顯示補上4個人的年度覆寫**：`getCandidateDisplay()`跟`_resolveCandKnownParty()`（本來就有陳超明/傅崐萁的2020特例）補上蘇震清/趙正宇/林昶佐三個名字的2020覆寫（→無黨籍），跟上面REAL_SEATS_2020的分類邏輯對齊，不會出現「表格說他是無黨籍，但候選人卡片/地圖染色還是顯示別的」這種不一致。連帶把`edColor()`/`tcColor()`/`vcColor()`的2020分支（原本直接讀原始`wp`欄位，跟v1.93修陳超明2024版的問題一樣）都改成先過`getCandidateDisplay()`解析出正確的`code`再傳給`winMarginFill()`，這樣選「無黨籍」時，2020年的地圖染色/hover發光才會正確覆蓋到這4個人的選區。2016不用改，因為2016的原始資料本來就已經正確標記（林昶佐2016是npp、蘇震清2016是dpp，都不是這次要覆寫的年份）。

**常駐聲明位置改用ResizeObserver**：`positionDisclaimerNote()`本來就有算`#left-rail`目前的下緣位置，但只在window resize跟收合側欄兩個地方手動呼叫，側欄裡任何其他會改變高度的互動（展開折疊區塊、情境推演參數面板開關）沒有觸發重新定位，容易跟側欄底部對不齊。新增一個`ResizeObserver`直接監看`#left-rail`本身尺寸，不管什麼原因造成高度變動都會自動重新算位置，不用每個互動點各自補呼叫。

**跑馬燈政黨標示擴充到政黨熱力圖模式，數字改成色框**：`applyTickerPartyHighlight()`原本只認`_histSeatParty`（歷年分析模式專用），這次擴充成也認`mode`本身是不是政黨熱力圖模式（`mode==='dpp'`等）——兩種來源都會觸發標示。命中的政黨數字從單純變色，改成`border`+`background`（用該黨`HIST_PARTY_BTN_STYLE`的顏色/底色）做出一個色框（你說的「logo:00」那種框起來的樣子），沒命中的維持原本的降低透明度+去飽和。`w`（民眾黨/時代力量共用同一個跑馬燈欄位）判斷改成「active是tpp或npp都算命中」，不用管究竟是哪個年代的第三勢力。掛在`setMode()`裡`_isHmMode`那段判斷式外面（不管是不是熱力圖模式都會呼叫，這樣切回非政黨模式時能正確清除殘留的色框/變暗效果）。

**沒做的部分**：「白卡簡寫數字直接從半圓圖讀，不要另外維護一份表」這個想法，我看了一下半圓圖（`updateHemicycle()`）的渲染邏輯，它是逐一個席位畫點、大量分支是給模擬情境推演/陣營(camp)用的，不是一個乾淨的「政黨→席次數」對照表可以直接拿來用，要整併成單一資料來源工程量不小、風險也高於這次其他項目。這次新增的`REAL_SEATS_2020`/`REAL_SEATS_2016`至少是從既有的`HIST_REGION/PL/IND_SEATS_20XX`表加總算出來、不是憑空另編，`REAL_SEATS_2024`維持原樣沒動。如果你還是想要真正的單一資料來源，這是一個獨立、需要另外排時間做的項目。

## v2.05 — 上一版誤把「框起來」做在跑馬燈，正確位置是收合四張卡的logo:數字badge；半圓圖點點反黑；onboarding文案修正

你用截圖澄清了：框框要做在展開收合條後那四張卡（區域立委/不分區政黨/原住民席次/立法院總席次）上面各自的政黨logo+數字（`renderSeatRatio()`產生的`.sr-badge`），不是跑馬燈；而且半圓圖裡屬於非選定政黨的點點要反黑。逐項修正：

**跑馬燈的框拿掉，只留顏色**：`applyTickerPartyHighlight()`把v2.04加的`border`/`background`/`borderRadius`/`padding`都拿掉，退回只改`color`（v1.82原本的行為）。

**新增共用判斷`_getActivePartyForHighlight()`**：跑馬燈、`.sr-badge`、半圓圖點點三個地方都要判斷「目前選的政黨是哪個」，抽成一個函式共用，不用各自重寫一次。

**`.sr-badge`（收合四張卡的logo:數字）加色框**：`renderSeatRatio()`裡，命中目前選定政黨的badge用該黨顏色加`border`+`background`框起來，其餘badge降低透明度+去飽和變暗。`short==='ind'`這個badge比較特殊——它實際上是`seats.other`（區域/總席次那兩張卡把「無黨籍」跟「其他」合併成同一個桶，資料本身沒有再細分），所以判斷時「選定政黨是ind或other都算命中這個badge」。

**半圓圖點點反黑**：新增`_campMatchesActive(camp, active)`（camp用的是green/blue/white/npp/tsr/magenta/other這套命名，跟active的黨籍代碼dpp/kmt/tpp/npp/ind/other不是同一套，這裡做對照表）跟`_hemiDotFill(camp, active)`（命中回傳原本的政黨色+0.9不透明度，沒命中回傳深灰`#333338`+0.7不透明度，深灰不是純黑，跟地圖選政黨後非該黨選區反黑用同一個色號，風格統一）。套用到`updateHemicycle()`裡四個畫點的地方：區域73席、不分區34席、總113席、原住民6席，這四個之前各自獨立呼叫`CAMP_C[camp]`上色，現在都改成先過`_hemiDotFill()`。`third`/`comp`模式走的模擬情境那條分支（`mode==='third'&&thirdSubMode==='pr'`）沒有動，因為`_getActivePartyForHighlight()`在那些模式下本來就回傳null，不會有變暗效果，維持原樣。

**確保選政黨當下半圓圖立刻反應**：`chooseHistPartyAndFinish()`跟`cycleCampFilter()`的winner分支都補上直接呼叫`updateHemicycle(0)`，不用等`redrawCurrentLevel()`剛好有沒有觸發到半圓圖更新。

**onboarding文案兩處修正**：
1. 「推算選舉人數」那一頁整個拿掉——這個徽章在v1.93已經搬走位置（改用那個位置放投票率/有效票率膠囊），繼續留著解釋一個已經不存在的UI元素沒有意義。
2. 選政黨那題原本寫死「2024年真實當選席次」跟「國民黨／民進黨／民眾黨／無黨籍／其他／總」這個政黨清單（但選項本身早就在v2.04改成依`winnerYear`動態算了，文案沒跟著動，2020/2016年份看到的文字還是講2024）。改成文案裡放`{{YEAR}}`佔位符、拿掉寫死的政黨清單敘述，`renderCompOnboardStep()`渲染時用正規表示式把`{{YEAR}}`換成當下的`winnerYear`。

## v2.06 — 2020選政黨要有民眾黨；跑馬燈「w」欄位不是民眾黨/時代力量共用；側欄按鈕排序調整

**2020選政黨補回民眾黨**：v2.04把`getPartyChoicesForYear()`寫成「2024用tpp，2020/2016都用npp」的二選一邏輯，但2020其實民眾黨（不分區5席）跟時代力量（3席）都有真實席次，不是只有時代力量——只有2016是真的沒有民眾黨（還沒成立）。改成：2024只給tpp、2020兩個都給（tpp+npp）、2016只給npp，不是簡單的新舊年份二分。

**跑馬燈「w」欄位判斷修正**：你抓到的問題——政黨熱力圖模式2024/2020都有時代力量%選項可以選，但跑馬燈的票數（`calcNationalVotes()`算的）只有2016才會把w這欄換算成時代力量的票，2024/2020的w欄位就是單純民眾黨的票，時代力量在這兩年的票數其實是併在「其他」裡面、沒有自己專屬的欄位。之前`applyTickerPartyHighlight()`寫成「active是tpp或npp都算命中w」，等於2020選時代力量時，跑馬燈會誤把民眾黨那欄當作命中對象點亮——改成只有`winnerYear==='2016'`時才用npp判斷命中w，其餘年份只認tpp，2020選時代力量時w欄位正確地不會亮（因為它本來就沒有自己專屬的跑馬燈欄位可以亮）。

**側欄按鈕排序**：政黨熱力圖從原本第4個移到歷年分析後面（第2個），綜合黨派投票指數往後移到第3個，第三勢力/情境推演順序不變。純粹搬動HTML區塊位置，id/onchange邏輯都沒動。

## v2.07 — 台灣基進真的抓不到（漏掉一個mode）；台灣基進/親民黨/無黨團結聯盟都獨立開選項；半圓圖camp對照表修正

**抓到真正的bug**：`_getActivePartyForHighlight()`判斷「政黨熱力圖模式目前選哪個政黨」的清單裡漏了`'tsr'`（台灣基進），選台灣基進%時這個函式直接回傳`null`（等於「沒選政黨」），框跟變暗全部不會出現——不是特例邏輯寫錯，是根本沒被這個函式認得。補上`'tsr'`，同時`HIST_PARTY_BTN_STYLE`補上`tsr`的顏色（沿用`CAMP_C.tsr`同一組色號）。

**台灣基進、親民黨、無黨團結聯盟都改成獨立選項，不再併入「其他」**：
- 台灣基進（2020區域1席）：跟政黨熱力圖模式的選項一致，2020的「選政黨」新增`tsr`選項，`REAL_SEATS_2020.other`從1改成0、新增`REAL_SEATS_2020.tsr:{region:1,...}`。
- 親民黨（2016不分區3席）、無黨團結聯盟／高金素梅（2016原住民1席）：2016的「選政黨」新增`pfp`/`npsu`兩個選項，`REAL_SEATS_2016.ind`從2（含高金素梅）改成1（只剩區域那位無黨籍）、新增`REAL_SEATS_2016.npsu:{ind:1,...}`跟`REAL_SEATS_2016.pfp:{pl:3,...}`、`REAL_SEATS_2016.other`歸零。2020/2024的高金素梅資料因為`HIST_IND_SEATS_2020`（聚合表）跟`indCamps2020`（逐席細表）兩張表本身就對不起來（一個算進blue、一個標成other），這次先不動，只處理資料乾淨的2016。

**半圓圖camp對照表(`_campMatchesActive`)重新設計**：原本的邏輯把'ind'/'other'寫死互相合併，這次台灣基進/親民黨/無黨團結聯盟都分出來後，改成每個active值各自比對專屬的camp（`tsr`→camp`tsr`、`pfp`→camp`pfp`、`npsu`→camp`magenta`），不再靠特例合併，邏輯更直接也更不容易再錯。另外修正一個「camp:'other'在不同卡片意思不一樣」的問題——區域/原民/總席次卡的`other`桶指的是黨籍撐腰但登記無黨籍參選（對應active='ind'），不分區政黨卡的`other`桶指的是真正的小黨（對應active='other'），兩者同名不同義，新增`section`參數（'region'/'pl'/'ind'/'total'）區分，`_hemiDotFill()`跟四個呼叫點（區域/不分區/原民/總共）都補上這個參數。

**收合四張卡的badge比對簡化**：拿掉原本`short==='ind'`那個特例（`active==='ind'||active==='other'`都算命中），改成直接`it.short===_srActive`——台灣基進/親民黨/無黨團結聯盟分出來後，這個特例已經不需要了，順便把`renderSeatRatio()`裡`short:'npu'`的舊拼法統一改成`short:'npsu'`（跟`KNOWN_PARTY_MAP`/色票/logo圖等其他地方用的拼法一致，`logoSVGs.npsu`本來就有對應圖示，不會找不到icon）。

**跑馬燈也同步**：`applyTickerPartyHighlight()`補上「active是'other'或'tsr'都算命中『其他』欄位」（因為`calcNationalVotes()`裡台灣基進的票數本來就沒有獨立欄位、是併在其他算的，這點沒有改，只是讓框的判斷跟這個既有事實對齊）。

## v2.08 — 選了親民黨，地圖上真正贏的村里沒有點亮

你抓到2016彰化縣第三選舉區南港村（親民黨贏的村里）選了親民黨模式沒有被點亮。原因是同一個bug模式：`_normPartyForHistFilter()`（決定地圖染色/hover發光時「這個選區/鄉鎮/村里的當選政黨算不算符合目前選的政黨」）沒有認得`'pfp'`跟`'npsu'`這兩個代碼，遇到就直接落到預設的`return 'other'`——南港村原始資料的當選黨籍是`'pfp'`，選了親民黨（`_histSeatParty==='pfp'`）時，`_normPartyForHistFilter('pfp')`被錯誤正規化成`'other'`，跟`'pfp'`本身兜不起來，就被當成「不符合」直接黑掉。補上`pfp`/`npsu`各自對應自己，不再落到`other`。這個函式是v1.54就存在、專門管地圖染色比對用的，跟這次新增的席次表/半圓圖/badge是不同的程式碼路徑，各自都要獨立檢查有沒有漏掉新加的政黨代碼。

## v2.09 — 台灣基進2016不該點亮跑馬燈「其他」；hover發光加粗；2016勝選跑馬燈的投票率沒有移到正確位置且抓錯年份資料

**跑馬燈`oth`欄位年份判斷**：`applyTickerPartyHighlight()`原本「active是'tsr'就算命中跑馬燈『其他』欄位」沒有分年份，但2016年台灣基進根本還沒成立、沒推候選人，那年的「其他」欄位票數完全跟台灣基進無關。改成`active === 'tsr' && !_tickerIs16`，只有2020/2024選台灣基進時才點亮「其他」。

**歷年分析模式hover發光加粗**：維持原本「只有滑鼠經過該政黨贏的區域邊線才發光」的行為（沒有要改成常駐），單純把`updateEdgeGlowPerDistrict()`的`winGlowFilter`從單層`drop-shadow(0 0 1px)`加粗成雙層`drop-shadow(0 0 3px) drop-shadow(0 0 8px)`，光線更明顯。（中途一度誤做成「不需要hover、常駐發光」，已改回原本的hover觸發版本。）

**2016勝選模式投票率/有效票率位置＋資料來源都修正**：2020/2024的ED面板都是「當選標籤」下面立刻接投票率/有效票率的pill，但2016這個區塊原本是掛在候選人名單全部跑完之後、快到底部才出現，跟其他年份不一致；而且資料來源錯誤——當時寫的是`s.tr`/`s.ir`（那是`ED_STATS`，2024年的資料），不是2016自己的投票率。2016的投票率/有效票率其實存在另一張獨立的表`TR_ED_2016[ed16]`（`ED_2016`本身的候選人物件沒有`tr`/`ir`欄位），改成從這張表讀，並搬到當選標籤正下方，跟2020/2024位置對齊。

## v2.10 — 2024時代力量在「其他」模式漏標；順手修2016投票率的兩個小bug（標題併進badge的嘗試做了又revert掉）

**標題併進badge，做了又改回來**：你原本提議把「屏東縣第2選舉區」這種標題文字直接併進win-badge本體裡（badge外框=標籤），試做了一版套用到ED/鄉鎮/村里三層級×2016/2020/2024三年份的歷年分析/政黨熱力圖面板，但實際看到效果後你說「有點不符預期」，改回標題/badge分開各佔一行的原本排版。這批call site（連同其中會用到的`winBadge()`共用函式）已經全部改回原樣，畫面上不會有變化；過程中新增的`winBadgeManual()`輔助函式跟`.win-badge-combo`/`.wbc-title`這兩個CSS class也一併移除，沒有留下用不到的死程式碼。

**順手修的兩個2016資料bug**（改上面那批badge的過程中發現，這兩個有留著沒有revert）：
- 鄉鎮層級2016的投票率一直沒有顯示過——`TC_2016`這個表本身根本沒有`tr`/`ir`欄位（只有ED層級的`TR_ED_2016`有），原本寫的`s16tc.tr`永遠是`undefined`，pill判斷式直接跳過不顯示。改成退而求其次借用`TR_ED_2016[ed16]`（同一選區底下每個鄉鎮數字會一樣，但至少有得看）。
- 村里層級2016（3個call site：VC_2016有資料/VC_SIM.v16 legacy fallback/ED層級fallback）的投票率pill原本也是掛在候選人名單跑完後面才出現，這次一併搬到badge正下方，跟其他層級/年份對齊。

**2024選「其他」漏掉時代力量贏的地方**：你抓到2024時代力量（時代力量2024沒有立委席次，理論上該分類進「其他」）在TC/村里層級選「其他」時沒有點亮。原因：`_normPartyForHistFilter()`把`'npp'`無條件正規化成`'npp'`本身，沒有考慮到2024年根本沒有npp這個獨立選項（`getPartyChoicesForYear('2024')`只有kmt/dpp/tpp/ind/other，不含npp）——選「其他」時拿`_histSeatParty==='other'`去跟`_normPartyForHistFilter('npp')==='npp'`比對，兜不起來，被當「不符合」直接黑掉。改成正規化完之後，再檢查這個政黨代碼在目前`winnerYear`是不是真的有獨立選項（用`getPartyChoicesForYear(winnerYear)`動態查），沒有的話併入`'other'`。這樣2024的npp、以及理論上任何「這個年份沒有自己選項的政黨代碼」都會正確併入其他，不用每個年份各自寫死例外。

## v2.11 — win-badge改成Ghost/Neon風格，不再是一塊實心色磚

你嫌win-badge（「民進黨 54.9% 勝選」那個）壓在標題正下方太重、太「磚頭感」，我提了三個方向（右側對齊／Ghost neon text／跟投票率並成一列），你選了neon text這個，理由是整體介面本來就走深色戰情室風格，跟滑鼠火把、海岸線光暈這些既有的發光效果最一致。

**做法**：`.win-badge`本身的實心底色拿掉，改成`rgba(255,255,255,0.06)`極低透明度底＋細邊框；每個政黨色不再是`.bd-*`那組「整塊塗滿黨色」的規則，另外用`.win-badge.bd-*`複合選擇器（兩個class都要中才套用，不會動到`.badge.bd-other`等其他地方共用的`.bd-*`原始規則）蓋出新版：文字本身變成該黨色、`text-shadow`做出發光效果、底色/邊框都只留極低透明度的同色。國民黨/民進黨/民眾黨/台灣基進/無黨團結聯盟/無黨籍/其他/時代力量/親民黨/世界和平黨/新黨每個都各自配一版。

知名小黨或高票無黨籍（例如高金素梅、某些notable independent）原本是JS內直接inline `style="background:${顏色}"`塗滿，這批沒有固定在CSS的class裡、顏色是動態抓來的，所以另外寫了`_hexToRgba(hex, alpha)`小工具把同一套「文字色+低透明度底+發光」邏輯用inline style刻出來，跟CSS那組視覺上一致。

## v2.12 — 選了政黨後，海岸線發光把整張地圖染成該黨色，蓋過黑化對比

你截圖抓到：國民黨模式下開激戰選區/翻盤選區，非國民黨選區照理該變黑（`_normPartyForHistFilter`那套黑化邏輯），但整張台灣地圖看起來還是一片藍，黑化的對比幾乎看不出來。根因：`mapBgColor()`裡，只要`mode==='winner' && _histSeatParty`有選政黨，就會把`--edge-glow`這個CSS變數設成該黨的glow色，而這個變數是`#g-base`（整個地圖群組）的`filter:drop-shadow(0 0 3px) drop-shadow(0 0 10px)`用的顏色——這是貼著台灣「整個外框海岸線」發光的group-level效果，不是逐選區判斷，選了政黨就整條海岸線＋選區交界處都暈開該黨色，直接蓋過旁邊被黑化(`#333338`)的非該黨選區，看起來像整張圖都被染色。

修法：選了政黨時只染遠處背景的環境光（`mid`，畫面四角照不到地圖的空白區塊底色，用來當情境提示），貼著海岸線的`glow`維持中性色`#1c2740`不跟著變。這樣「哪裡是這個黨贏的」還是只靠逐選區的黑化/染色對比來表達，海岸線那圈光暈退回單純的裝飾光，不會搶走對比的訊息。

## v2.13 — 三大棒狀圖／候選人長條圖的外框加淡光＋數字加neon發光，色塊本身不動

你問三大棒狀圖（區域立委/總統大選/不分區政黨票）跟候選人列表的長條圖要不要跟著win-badge一起改質感。這兩種都是「一條bar裡塞好幾個政黨的色塊」，色塊本身（`.hist-seg`／`.bfill`）故意不加發光——如果每個分色塊都neon化，綠藍交界會糊在一起，比例反而比現在難分辨，這是bar chart功能優先於美觀的地方。

**實際改的兩處**：
- `.hist-bar-wrap`（三大棒狀圖外框）／`.bbg`（候選人長條圖外框）：各加一圈`rgba(180,210,255,…)`中性淡光（`box-shadow`+`border`），不挑特定政黨色——因為一條bar裡混了好幾黨，沒有單一黨色可以代表整條，用中性藍白光當HUD面板邊框處理。
- 三大棒狀圖下面的數字（`.hn-g`/`.hn-b`/`.hn-w`/`.hn-npp`/`.hn-tsr`/`.hn-o`/`.hn-orange`）本來就各自有黨色文字，這次加上同色`text-shadow`，跟win-badge的neon文字質感呼應。

**候選人長條圖的`.bpct`（每位候選人自己的得票%數字）這次沒有動**：它現在的顏色邏輯是「當選/落選」對比（亮白vs灰），不是政黨色，要做成政黨色neon得先在JS那邊把候選人色傳進這個數字的inline style，範圍比較大，先沒有一起做——如果你想要這個也一起做效果，之後可以再處理。

## v2.14 — 政黨輪替分析選中按鈕的邊框改半透明+發光，不再是一圈實心橘

你說「政黨輪替分析」那格（搖擺地圖/翻盤選區這些按鈕）選中時看起來有點粗。查了一下，`#flip-analysis-box .btn.on`（按鈕選中狀態）的`border-color`原本寫死`#e06010`（滿版不透明橘），跟這一輪其他地方（win-badge、長條圖外框）已經統一改成的「低透明度色＋text-shadow發光」風格不一致，實心邊框旁邊圍著一堆半透明的東西，自然顯得特別粗重。改成`border-color: rgba(224,96,16,0.45)`半透明邊框＋文字加`text-shadow`發光，質感跟其他按鈕/badge統一。這次只動了`.on`（選中）狀態，一般跟hover狀態本來就已經是半透明配色，沒有動。

## v2.15 — 「政黨輪替分析」collapse header改橘色的嘗試，做了又改回來

v2.14修完按鈕邊框後，討論到「政黨輪替分析」header跟「資訊面板」用同一種青藍色+粗體是不是該區隔開，試改成跟`.btn.on`呼應的橘色、字重降一階，但套用後你說「不是啦」，改回原本的青藍`rgba(40,200,200,0.7)`＋`font-weight:700`＋外框`rgba(40,200,200,0.25)`。這個方向暫時擱置，畫面上不會有變化。

## v2.16 — 跑馬燈「其他」欄位不再幫台灣基進點亮，因為那個數字本來就不是台灣基進獨有的

v2.07/v2.09一路把「選台灣基進時跑馬燈『其他』欄位要不要點亮」修到「2020/2024點亮、2016不點亮」，這次你進一步指出：其實不管哪一年都不該點亮。原因是`calcNationalVotes()`計算「其他」這個桶時，是把台灣基進候選人的票數跟其他真正的小黨候選人（不是dpp/kmt/tpp/ind）全部加在一起算成同一個數字，不是台灣基進獨立的票數——選台灣基進模式時把這格點亮，等於暗示「這個數字就是台灣基進的成績」，但其實是台灣基進+其他小黨的混合數字，不精確。跟地圖染色/黑化那套邏輯不一樣（地圖是逐選區判斷、每個選區的贏家是精確已知的），跑馬燈這裡從資料源頭就沒辦法拆出台灣基進獨立的數字，所以乾脆不點亮比較誠實。`applyTickerPartyHighlight()`裡`(k==='oth')`的判斷式從`active==='other' || (active==='tsr' && !_tickerIs16)`簡化回`active==='other'`，台灣基進維持「沒有專屬跑馬燈欄位可以命中」的狀態，不分年份。

## v2.17 — 「深藍PVI/深綠開票」分類badge也改成neon風格，跟win-badge統一

你問綜合PVI模式的`classBadgeHtml()`（「深藍 PVI -9.3%」「深綠 開票 +14.2%」這種分類badge，實色底+白字，跟改neon前的win-badge是同一種做法）要不要一起做毛玻璃。我建議不要玻璃感（win-badge之前就試過毛玻璃版又拿掉，程式碼裡還留著那份備份），改成跟win-badge一致的neon文字發光，你確認要照這個方向。

**做法**：拿掉實色`background`（原本是`pviColor(pvi)`連續漸層色或簡化版綠/藍/灰，透明度0.65），改成極低透明度底＋同色細邊框＋文字本身變色階色＋`text-shadow`發光，套用`_hexToRgba()`（win-badge那批已經寫好的hex轉rgba小工具）。**分類色的來源也順便理順**：原本背景色是用連續漸層`pviColor()`算的，但顯示的分類文字（深綠/淺綠/接近/淺藍/深藍）是離散的5級——兩者對不太上（背景色深淺不一定跟文字的分類剛好對齊）。改成直接用`CATS`自己的`clr`（深綠`#22c55e`／淺綠`#4ade80`／接近`#94a3b8`／淺藍`#60a5fa`／深藍`#3b82f6`，都是neon版好讀的亮色），色塊跟文字保證一致。原本「接近(tossup，差距≤1%)」有一圈亮灰邊框特別標出來，neon版改成邊框透明度/發光強度各加強一階，維持「這裡差距很小」的提示功能。

## v2.18 — 黨派投票指數排行榜／鄉鎮市區分類清單也neon化；順手修一個「接近選區顏色跑掉」的bug

**擴大套用範圍**：你問「黨派投票指數」排行榜（`updatePVIMarginCard()`，卡片上「臺南市第5選舉區／綠+25.7%」那個列表）跟「鄉鎮市區分類／村里分類」清單（`combinedCategoryListHtml()`，展開`cmp-box`時看到的深綠/淺綠/接近/淺藍/深藍分欄清單）要不要比照win-badge/classBadgeHtml一起neon化。兩處原本都是同一種「實色底0.65透明度+白字」的舊pill樣式，這次一起拿掉實色底，改成前面已經定案的「低透明度底＋同色邊框＋文字發光」。`combinedCategoryListHtml`的CATS原本是拿去畫圖表用的暗色（深綠`#1b5e20`等），直接當neon文字色太暗，換成跟classBadgeHtml一致的亮版6級色階。

**順手修的bug**：你抓到「黨派投票指數」排行榜裡，PVI很小、該算「接近」的選區（例如澎湖）沒有按照深綠/淺綠/接近/淺藍/深藍分級變色，而是直接被塗成飽和的綠或藍，跟安全議席看起來一樣。原因是改neon時，選區名badge的顏色來源被我誤接到旁邊「綠/藍+X%」用的簡化二三色（`partyColor`，只分正負號），而不是原本`pviColor()`那套會依實際PVI數值分五級深淺的連續漸層——等於badge從「五級分類」被誤降成「二元分類」。修法：badge另外用一組跟`classBadgeHtml`同樣門檻(±8%/±2%)、同樣5級neon色階的`badgeClr`。

## v2.19 — 「黨派投票指數」排行榜同一列的badge跟文字顏色不一致，兩邊統一用5級色階

v2.18修完badge之後，你發現同一列裡badge（選區名）已經是5級色階、但旁邊「綠/藍+X%」文字還是用原本簡化的二三色（`partyColor`，只看正負號）——結果PVI很小、badge顯示「接近」灰色調的選區，旁邊文字卻是飽和的綠/藍，同一列兩種顏色來源對不起來。改成兩邊都用同一個`badgeClr`（5級門檻±8%/±2%），跟「鄉鎮市區分類」清單的做法一致，`partyColor`這個變數不再需要，一併移除。

## v2.20 — 「黨派投票指數」badge的±2%接近區間，改成跟鄉鎮市區分類一樣有淺綠/淺藍過渡，不是整段同一種灰

v2.19把badge跟文字都改用5級色階後，你進一步指出：±2%這段「接近」區間現在整段都是同一種灰，不像「鄉鎮市區分類」清單那樣，接近綠的一邊會帶一點淺綠、接近藍的一邊會帶一點淺藍，是有過渡感的。原因是`updatePVIMarginCard()`的`badgeClr`用的是`classBadgeHtml`那組5級門檻（深綠/淺綠/接近/淺藍/深藍，「接近」只有一種顏色），跟`combinedCategoryListHtml`實際上用的6級（深綠/淺綠/接近偏綠/接近偏藍/淺藍/深藍，中間拆成兩色）不是同一套。`classBadgeHtml`會用5級是因為它要顯示的文字標籤本身就只有5種（深綠/淺綠/接近/淺藍/深藍），但這裡的badge沒有文字標籤限制，改用跟`combinedCategoryListHtml`一致的6級門檻（±8%/±2%/0%）跟色階（`#22c55e`/`#4ade80`/`#86efac`接近偏綠/`#93c5fd`接近偏藍/`#60a5fa`/`#3b82f6`），中間的過渡感就對上了。

## v2.21 — 上一版方向錯了，改回跟`classBadgeHtml`（選區旁邊「接近PVI+0.4%」）一致的5級

v2.20改成6級之後，你說「喔不對」——你原本要的其實是跟選區旁邊那顆「接近 PVI +0.4%」badge（也就是`classBadgeHtml`）同一套染色邏輯，不是鄉鎮市區分類清單那套6級。這兩個都是「同一個選區的PVI」，理應對齊；鄉鎮市區分類清單分6級是因為那邊本來就是拿去分欄用的不同情境。`badgeClr`改回`classBadgeHtml`的5級門檻（±8%/±2%，「接近」單一灰色`#94a3b8`），`combinedCategoryListHtml`本身維持v2.18改的6級不動（那邊沒被說要改）。

## v2.22 — 綜合PVI模式ED層級：拿掉「歷史開票」折疊區塊，鄉鎮市區分類/選區趨勢改放回右側資訊欄（先只做ED-2024）

你發現一個結構性問題：綜合PVI模式下，「鄉鎮市區分類」跟「區域立委趨勢」折線圖其實不是畫在右側資訊欄（`#ic`）裡，而是`updateCardsForED()`把右上角浮動的「四張卡片」裡兩張（不分區政黨半圓圖那張、原住民席次那張）內容整個換掉，暫時顯示這兩塊分析。問題是：如果使用者把那四張浮動卡片折疊起來，這兩張被換過內容的卡片會一起被蓋住，鄉鎮市區分類/選區趨勢就完全沒有備援位置可以看，等於整段分析消失。

查code發現有趣的地方：`showEDPanel()`裡其實本來就有算好一份`_edCompInner`（鄉鎮市區分類+區域立委趨勢+動員率長條圖，都用`collapseSection()`包好），但這個變數算完之後根本沒被塞進`#ic`的innerHTML——是死程式碼，从來沒有真的顯示過，取而代之的是靠`updateCardsForED()`去偷換浮動卡片。

這次的改法（**範圍先限定在ED層級、compYear=2024，TC/VC跟2020/2016之後再看要不要一起改**）：拿掉「歷史開票」折疊區塊（`collapseSection('歷史開票', _edCandBars)`，候選人得票長條圖那塊），改用`_edCompInner`（本來就有算、只是沒用到的那份）放進同一個位置；同時拿掉`if(mode==='comp') updateCardsForED(ed)`這行呼叫，不再讓comp模式偷換浮動卡片內容——浮動卡片維持原本的半圓圖顯示，不會再被「暫時換成別的內容」，鄉鎮市區分類/選區趨勢固定顯示在資訊欄裡，不管浮動卡片收合與否都看得到。

## v2.23 — 「綜合黨派投票指數 權重調整」加折疊；拿掉跟「i」導覽重複的說明文字

你發現側欄「綜合黨派投票指數 權重調整」（滑桿那個區塊）底下那兩行＊說明文字（＊黨派投票指數 權重混合...＊/＊綜合黨派投票指數簡化...＊），跟按「i」按鈕看到的導覽（`COMP_ONBOARD_STEPS`「什麼是綜合PVI？」那一步）內容一模一樣，是重複的。

**拿掉重複文字**：`#pvi-note`的初始HTML跟`getPviNoteInnerHtml()`（mode切換時重新產生`#pvi-note`內容用的函式）這兩處的comp分支都拿掉這兩行＊文字，只留公式（`getCompFormulaHtml()`，本來就是可以點開/收起的折疊）。third（第三勢力）模式那兩行維持原樣沒有動，明天再一起處理。

**「權重調整」整塊加折疊**：原本標題是純文字，不能點；改成跟「政黨輪替分析」同一套折疊寫法（▶/▼箭頭、點標題展開/收合滑桿+說明），預設是展開狀態（畫面上不會有變化），但現在可以收起來，進一步減少側欄常駐的資訊量。

## v2.24 — 鄉鎮市區分類/區域立委趨勢/催票率/權重調整，四個折疊統一套用「政黨輪替分析」的外觀

v2.23只有幫「權重調整」加了個陽春的▶/▼文字折疊，你要求擴大：資訊欄裡的「鄉鎮市區分類」「區域立委趨勢」「催票率（候選人票÷選舉人總數）」也都要折疊，而且外觀要跟歷年分析模式的「政黨輪替分析」（`#flip-analysis-box`：青綠色邊框+毛玻璃底+粗體青綠標題）長得一樣，不是原本`collapseSection()`那種純文字、沒有邊框底色的極簡樣式。

**新增共用函式`boxedCollapseSection(label, content, bodyId, defaultOpen)`**：把`#flip-analysis-box`的視覺規格（邊框`rgba(40,200,200,0.25)`、背景`rgba(14,26,30,0.5)`、`backdrop-filter:blur(6px)`、箭頭+標題那套排版）抽成一個可重複使用的折疊容器，之後任何地方要跟「政黨輪替分析」外觀一致的折疊都可以直接呼叫這個函式，不用重複刻樣式。

**套用範圍（先只做ED層級2024，跟v2.22同一個範圍）**：
- 鄉鎮市區分類、區域立委趨勢：原本用`collapseSection()`，改成`boxedCollapseSection()`。
- 催票率：原本`mobRateBarHtml()`直接輸出、完全沒有折疊（連內容都固定展開），這次也包進`boxedCollapseSection()`。因為`mobRateBarHtml()`內部自己也有印一次「催票率」標題，會跟折疊外層的標題重複，幫函式加了第三個參數`skipTitle`，包折疊時傳`true`跳過內部那次標題，只讓折疊本身的header顯示一次。
- 「綜合黨派投票指數 權重調整」：v2.23那個陽春版也一起升級成`#flip-analysis-box`同款外觀（邊框+毛玻璃底），不再只是純文字箭頭。

## v2.25 — 「i」導覽「什麼是綜合PVI？」補充說明：借鏡Cook PVI、第三勢力怎麼被排除的

原本這一步只有兩行＊重點提示，你要求保留這兩行（順手拿掉「黨派投票指數」跟「權重混合」中間一個多餘的半形空格），並補充兩段說明：

1. 這個指數是模仿美國Cook Political Report「Cook PVI」概念做的實驗性嘗試，數值怎麼解讀留給讀者自行判斷，不是嚴謹的學術定論。
2. 為了避開第三勢力干擾，「藍綠對決」不管是區域立委得票還是不分區政黨票，分子分母都只算國民黨跟民進黨兩黨——這部分我對照`dppKmtFromVotes()`（綜合PVI實際用來算d/p比例的函式）確認過：其他政黨（民眾黨/時代力量/台灣基進等）跟沒有政黨背書的無黨籍候選人得票完全不計入，但如果無黨籍/其他政黨候選人實際上是被國民黨或民進黨背書參選（`PVI_BACKED_MAP`/`COMP_BACKED_MAP`/`BACKED_MAP_2016`這幾張表，例如洪慈庸/陳柏惟/林昶佐/蘇震清/趙正宇算民進黨、傅崐萁/李翁月娥/林國慶算國民黨），會算進背書政黨的得票，不當第三勢力排除掉。

## v2.26 — 左下角學術聲明框改成更亮的紅

你說底下「※本頁面純屬學術性空間資料視覺化與科學模型研究...」那個警示框（`#disclaimer-note`）要改成再亮一點的紅。文字色從`#f87171`（偏柔和的珊瑚紅）改成`#ff5252`（更飽和鮮明的紅）；你接著說背景框框也要一起改，底色/邊框的紅色基準也從原本`rgba(239,68,68,...)`一起換成跟文字同一組`rgba(255,82,82,...)`，透明度順便各拉高一點（底色0.08→0.12、邊框0.35→0.45），整個警示框從文字到邊框都更顯眼。

## v2.27 — 「i」導覽新增一步，逐項解釋選區資訊欄的PVI相關數字

你問「區域黨派投票指數 -8.3%」「政黨黨派投票指數 -22.2%」「加權（相對全國平均） -16.7%」「2020→2024區域立委板塊移轉 +4.5%」跟選區名旁邊「深藍 PVI -16.7%」「淺藍 開票 -7.1%」這些數字是怎麼算的，原本的「i」導覽只有一步「公式」，但那步只是提示「公式區塊可以點開看」，沒有逐項白話解釋每個標籤實際代表什麼。

在「公式」跟「地圖顏色」中間新增一步「選區資訊欄的各項數字」，逐項說明：
- **區域黨派投票指數**＝該選區區域立委兩黨得票減全國平均（公式的<i>d</i>）
- **政黨黨派投票指數**＝該選區不分區政黨兩黨得票減全國平均（公式的<i>p</i>）
- **加權（相對全國平均）**＝<i>d</i>、<i>p</i>依滑桿權重混合後的最終PVI，**跟選區名旁邊「PVI」badge是同一個數字**，只是分別顯示在清單列跟badge兩個地方
- **板塊移轉**＝這次的加權PVI減上一屆同一選區的加權PVI，正值比上屆更綠、負值更藍
- 選區名旁邊還有一顆**「開票」**badge——這不是PVI，是這個選區**原始、沒有扣掉全國基準**的兩黨得票差距，所以跟「PVI」badge數字通常不一樣（PVI是相對全國平均的表現、開票是這個選區自己的絕對表現，這點是我在對話裡先跟你確認過台東縣的例子，才動手寫進導覽的）。

## v2.28 — 地圖右下角圖例（亮度=投票人數多寡/激戰區域）包成獨立框框

你問地圖右下角「亮度＝投票人數多寡」「五五波...鐵票區」「激戰區域（差距≤5%，放大後顯示）」這三行圖例要怎麼改才不突兀——這批文字原本是直接疊在地圖上，沒有底色/邊框，地圖底色深淺不一時文字對比忽強忽弱，看起來像飄在畫面上的雜訊。討論了「獨立框框」跟「縮成icon點開」兩個方向，你選框框（風險低、跟這幾輪改的其他面板同一套視覺語言）。

`#map-legend`加上跟其他面板一致的淡底+細邊框+`backdrop-filter:blur(6px)`（`rgba(255,255,255,0.05)`底、`rgba(255,255,255,0.1)`邊框、8px圓角），右下角固定錨住不再飄；另外補一條`#map-legend:empty{display:none}`防禦性規則，內容真的是空字串時不會露出一個沒有內容的空框（目前程式邏輯下這個圖例幾乎總是至少有激戰區域那行，實務上不會觸發，純粹保險）。`pointer-events:none`維持不變，不會擋住底下地圖的hover/點擊。

## v2.29 — 綜合PVI/第三勢力PVI模式下，地圖圖例「五五波/鐵票區」文字是黑色看不到

v2.28把圖例包成深色半透明框之後，你發現綜合PVI跟第三勢力PVI模式下「五五波」「鐵票區」文字幾乎看不到。原因：`renderMapLegend()`裡有一段`isLightBg = (mode==='comp'||mode==='third')`，假設這兩個模式的地圖「海洋」背景是淺色，所以文字故意切成深黑色`#333333`；但實際上`mapBgColor()`不管哪個模式海洋都是深色（這是這幾週改版下來的現狀，不是v2.28造成的，只是v2.28把圖例包進一個本來就偏深的框之後，深黑字疊深色框幾乎完全隱形，問題才被看見）。拿掉這個依mode切換顏色的邏輯，圖例文字統一用淺灰`#cccccc`（跟標題`#8ec9ff`、激戰色`#FFB74D`一樣不再分mode），跟框本身的深色背景才有對比。

## v2.30 — 綜合PVI/第三勢力PVI模式隱藏頂部跑馬燈

你問綜合PVI模式邏輯上是不是不需要顯示頂部跑馬燈（民主進步黨/中國國民黨/台灣民眾黨/無黨籍/其他政黨那排數字）。確認過：跑馬燈的框選高亮功能（`_getActivePartyForHighlight()`）只在歷年分析（`_histSeatParty`）跟政黨熱力圖（`mode`本身就是政黨代碼）這兩種情境才會回傳非null值，綜合PVI/第三勢力PVI模式沒有「選政黨」這個概念，`_getActivePartyForHighlight()`在這兩個模式下永遠是null，跑馬燈完全不會有任何框起來的互動效果，純粹陳列全國絕對得票數——這個「絕對票數」的框架，跟綜合PVI「相對全國平均」的分析邏輯也對不太上，你確認第三勢力PVI模式一併隱藏。

`updateTicker()`開頭加一段：`mode==='comp'||mode==='third'`時把`#ticker`整條設成`display:none`並直接return（不用再花時間算票數/跑動畫），切回其他模式時改回`display:flex`。`updateTicker()`會在`setMode()`切換模式時的共用重繪路徑裡被呼叫到，所以切換進出comp/third模式時跑馬燈會即時顯示/隱藏，不用另外找地方掛勾。

## v2.31 — 修v2.30誤傷：第三勢力PVI的「不分區席次推演／關鍵少數」按鈕被跑馬燈隱藏邏輯連坐蓋住，搬出來跟「i」同一排

v2.30把整個`#ticker`隱藏之後，你發現第三勢力PVI模式的「不分區席次推演」「關鍵少數」這兩顆切換按鈕（`#third-sub-toolbar`）也一起消失了——查了HTML才發現這組按鈕原本就寫在`#ticker`容器裡面（跟民主進步黨/中國國民黨那幾個跑馬燈項目擠在同一個div），v2.30隱藏`#ticker`時被連坐蓋住，但這兩顆按鈕偏偏是第三勢力PVI模式操作介面的一部分，不該跟著跑馬燈的顯示邏輯走。

修法：把`#third-sub-toolbar`整塊從`#ticker`搬到`#top-right-controls`（原本放「總」按鈕、「i」按鈕、EN/中文切換那排），插在「總」跟「i」中間，你要求的「關鍵少數/不分區席次推演跟i按鈕同一排」。`#top-right-controls`本來就是`display:flex`的一排，直接搬過去不用額外調整版面。搬家後這兩顆按鈕的顯示/隱藏（`setThirdSubMode`那組邏輯）完全獨立於跑馬燈，不會再被`updateTicker()`的`display:none`波及。

## v2.32 — 綜合PVI模式2024拿掉投票率/有效票率紅綠燈

你要求2024綜合PVI模式的ED/鄉鎮/村里面板都拿掉投票率/有效票率那排紅綠燈小圓點（`turnoutBadgeHtml()`）。這三處（`_edTurnout`/`_tc24Turnout`/`_vc24Turnout`）原本只判斷「有沒有投票率資料」，現在多加一個`mode!=='comp'`的條件，綜合PVI模式一律不顯示；winner（歷年分析）模式維持原樣不受影響。**範圍只做2024**：2020/2016的comp模式面板（ED/TC/VC共6個call site）目前還是照樣顯示這個badge，之後如果也要拿掉再說。

## v2.33 — 「區域立委趨勢」「催票率」fold標題旁的公式小字，顏色改成跟fold本身一樣的青綠

v2.24把這兩個區塊改成`boxedCollapseSection()`（青綠色fold），但fold標題後面那段小字註解（「區域立委趨勢」後面的`DPP/(DPP+KMT)`、「催票率」後面的`（候選人票÷選舉人總數）`）當時沒有跟著改，還是留著舊的灰色`#bbb`，跟fold標題本身的青綠`rgba(40,200,200,0.7)`不一致。這次把這兩處小字的顏色也改成`rgba(40,200,200,0.7)`，跟fold標題同色。範圍一樣先只改ED層級2024這兩個call site；TC/VC層級跟其他年份（還在用`collapseSection()`舊樣式、不是`boxedCollapseSection()`）暫時沒有動，等之後那些也升級成boxed樣式時再一併處理。

## v2.34 — 「錯估席次與得票落差」卡片的國民黨藍色調亮

你說「錯估席次與得票落差」那張浮動卡片（`updateMismatchedCard()`）裡的藍色太暗看不清楚。查了一下，`predColor`/`actualColor`（標示PVI預測黨籍跟實際當選黨籍用的顏色）國民黨那邊還是用最原始的`#1555b4`深藍，疊在卡片深色底上對比不夠；改成跟這幾輪其他地方統一用的亮版`#60a5fa`。民進黨那邊的`#1b9e4b`維持不變，沒被提到就先不動。

## v2.35 — 「錯估席次」卡片候選人小圓點/百分比文字，國民黨藍色一併調亮

v2.34只調了卡片標題那排「PVI預測/當選」的藍色，你接著抓到候選人清單（例如「丁學忠 47.8%」那排）的小圓點跟百分比文字也是暗藍色。這批顏色來自`getCandidateDisplay()`共用的`disp.color`，這個函式的顏色很多地方（尤其地圖染色margin漸層混色）需要用原本較深的飽和色當基準，不能整個函式改亮，所以只在這張卡片自己的候選人小圓點/百分比文字上局部覆蓋：`disp.code==='kmt'`時用`#60a5fa`、`'dpp'`時用`#4ade80`，其他黨籍維持`disp.color`原樣，不影響`getCandidateDisplay()`本身或其他呼叫它的地方。

## v2.36 — 綜合PVI模式的「總」按鈕文字變回總了但顏色還是藍的

你抓到點進綜合PVI模式時，「總」按鈕（`camp-toggle-btn`，切換泛綠/泛藍/中間篩選那顆）文字雖然正確顯示「總」，但顏色卻是藍的。根因：這顆按鈕的外觀同步邏輯只有兩處會主動觸發——`m==='winner'`時呼叫`applyHistSeatPartyToButton()`、`_isHmMode`（政黨熱力圖）時有一段內嵌同步——如果是從歷年分析模式選了「國民黨」（`applyHistSeatPartyToButton()`把inline`style.color`設成藍色`#60a5fa`）直接切進comp/third模式，沒有任何程式碼會清掉這個殘留的inline顏色，即使別的地方後來把文字改回「總」，顏色還是留著藍的沒被同步。

修法：`setMode()`裡新增一段，`m==='comp'||m==='third'`時比照`cycleCampFilter()`的邏輯，依`campFilter`目前的值（不是依殘留狀態）重新套用一次正確的class/顏色/文字——這樣不管是從哪個模式切進comp/third，「總」按鈕都會被強制同步成乾淨、正確反映`campFilter`實際值的外觀，不會再有殘留顏色的問題。

## v2.37 — 2024綜合PVI的ED層級改法，擴大套用到TC（鄉鎮）跟VC（村里）層級

你要求把v2.22/v2.24/v2.32/v2.33這一系列在ED層級做的修改（催票率/區域立委趨勢/鄉鎮市區分類搬進資訊欄、改成`boxedCollapseSection`外觀、拿掉投票率badge、公式文字顏色），全部套用到2024綜合PVI的TC跟VC層級，而且要確保不再跟「錯估席次與得票落差」卡片重疊。

**TC層級（`showTCPanel`）**：發現跟ED層級一模一樣的bug模式——`_tcCompInner`（村里分類+區域立委趨勢+催票率）早就算好了，但從來沒被塞進`#ic`，實際顯示靠`updateCardsForTC()`偷換浮動卡片。更嚴重的是這個偷換**直接蓋掉`.stats-card.ind-card`**，跟「錯估席次與得票落差」（`updateMismatchedCard()`同樣用這張卡）正面衝突。修法：`_tcCompInner`改用`boxedCollapseSection()`（三塊都包，催票率原本完全沒有折疊）直接放進`#ic`；`updateCardsForTC()`函式開頭加`if(mode==='comp')return;`，comp模式不再偷換任何浮動卡片（third模式維持原樣不動）。

**VC層級（`showVCPanel`）**：`_vcCompInner`（區域立委趨勢+催票率，村里已經是最細層級沒有再分類）原本雖然有被用到，但外面又包了一層「查看選區分析」的巢狀`collapseSection`，跟ED/TC的排版不一致；`updateCardsForVC()`同樣會在comp模式偷換浮動卡片（雖然這裡换的是`.total-card`跟另一張卡，不是`.ind-card`，不會直接蓋掉錯估席次，但内容跟`#ic`裡的重複，一樣沒必要）。修法：`_vcCompInner`拆成兩個平行的`boxedCollapseSection`（不再有巢狀外層），直接放進`#ic`，跟ED/TC同一種排版；`updateCardsForVC()`同樣加`if(mode==='comp')return;`。

三個層級（ED/TC/VC）現在對comp模式（2024）都是同一套邏輯：內容固定顯示在資訊欄裡、外觀統一、不再偷換浮動卡片，「錯估席次與得票落差」卡片不會再被任何東西蓋掉。2020/2016跟third模式維持原樣，之後有需要再處理。

## v2.38 — 2024的做法擴大套用到綜合PVI 2020/2016，ED/TC/VC六個區塊全部更新

你要求把v2.37套在2024的做法直接沿用到2020/2016。逐一檢查了ED/TC/VC×2020/2016這六個comp模式區塊，發現每個區塊的起始狀態都不太一樣，不是單純複製貼上：

**`updateCardsForED()`加上內部guard**：2024當時是直接拿掉呼叫的call site，但2020/2016的ED區塊末尾也有呼叫`updateCardsForED(ed)`，逐一拿掉call site太瑣碎，改成在函式本體開頭加`if(mode==='comp')return;`（跟TC/VC的`updateCardsForTC/VC()`同一招），一次擋掉所有年份comp模式的偷換浮動卡片，third模式不受影響。

**ED層級2020/2016**：跟2024一樣有「歷史開票」折疊（`_hist20`/`_hist16`，候選人得票+投票率badge），但沒有鄉鎮市區分類/區域立委趨勢/催票率。拿掉「歷史開票」跟裡面的投票率badge，新增`_edCompInner20`/`_edCompInner16`（三塊都用`boxedCollapseSection`）放進`cmpBoxHtml()`後面。**2016的催票率選舉人數**：`ED_2016`本身沒有tr/ir，跟v2.09一樣借用`TR_ED_2016`這張獨立表。

**TC層級2020/2016**：原本兩個區塊都完全沒有鄉鎮市區分類/區域立委趨勢/催票率（連偷換浮動卡片都沒觸發到這塊內容），直接新增`_tcCompInner20`/`_tcCompInner16`。2016這裡原本已經有算好`_v16tc`（候選人票數物件，本來是要丟給`updateCardsForTC()`的options用）跟`_e16tc`（選舉人數，但舊算法是直接拿`s16tc.tot`當人數，不是真的用投票率反推）——搬到前面重用給催票率折疊，並改用跟ED層級一致的`TR_ED_2016`表重新計算，比舊版準確。

**VC層級2020/2016**：2020原本已經有`_vcCompInner20`（只有區域立委趨勢），但外面包了一層「查看選區分析」巢狀`collapseSection`，這次拆平成兩個平行的`boxedCollapseSection`（區域立委趨勢+新增的催票率），不再巢狀。2016原本完全沒有這塊內容（連死程式碼都沒有），新增在「有村里級資料」那個分支（`_vc16c`存在時），沒資料的fallback分支維持原樣不動。村里層級本身沒有再往下分類，兩個年份都只有區域立委趨勢+催票率兩塊，跟2024一致。

## v2.39 — 「⚡Battleground」badge斷字＋ED層級英文版PVI/開票badge被擠壓，兩個排版bug修正；「開票(泛綠)」英文不要逐字翻譯

**「⚡Battleground」斷字（每一層都有）**：你抓到英文版「⚡Battleground」badge（`.swing-tag`，出現在「Weighted PVI (vs. Avg.)」那一列）會被硬擠到中間斷字變成「Battlegro」/「und」兩截。原因是`.swing-tag`沒有`white-space:nowrap`，英文標籤本身比中文長、排版較擠時內部文字就會被強制換行斷開。補上`white-space:nowrap`，badge寧可稍微擠一點也不斷字；順便把這顆badge也一併做這幾輪的neon化（拿掉實心橘底`#e06010`，改成低透明度底+邊框+文字發光），跟`#flip-analysis-box`/`.btn.on`用同一組橘色。

**ED層級英文版PVI/開票badge被擠壓（只有ED層級有，TC/VC沒有）**：你抓到英文版「Solid K Results -9.1%」會被擠到win-badge（「DPP 63.2% Won」）同一列，而不是像中文版那樣好好排在「Solid K PVI」正下方。查了一下，TC/VC層級的PVI+開票兩顆badge本來就是疊在標題右邊同一欄（`flex-direction:column`），只有ED層級是分開放：PVI badge在標題列、開票badge擠在win-badge那一列（`justify-content:space-between`、沒有wrap）——英文版兩串字合起來常常比中文版寬很多，擠不下時就會被壓縮。改成跟TC/VC同一種排版，PVI+開票兩顆badge一起疊在標題右邊，win-badge自己獨立一列，2024/2020/2016三個ED區塊都改了。

**「開票(泛綠)」英文不要逐字翻成顏色**：你抓到收合條在綜合PVI模式選了陣營篩選（泛綠/泛藍）時，英文版顯示「Results (Green)」/「Results (Blue)」，逐字把「泛綠/泛藍」翻成顏色，看起來很奇怪。跟其他地方（例如`updatePVIMarginCard()`已經是用DPP/KMT不是Green/Blue）統一，改成「Results (DPP)」/「Results (KMT)」。

## v2.40 — 「⚡Battleground」改成「⚡Swing」，更短也跟其他地方用詞一致

你問有沒有比「Battleground」（12個字母）短、意思一樣的字。「Swing」（5個字母）意思對得上「搖擺」，而且地圖右下角圖例的「激戰區域」英文本來就已經是"Swing (≤5%, zoom to reveal)"——改成同一個字，除了更短、更不容易在英文版擠版面，也讓同一個概念（搖擺/激戰）在app裡前後用詞一致，不是兩個地方各自翻譯出不同英文字。`T('搖擺','Battleground')`兩處都改成`T('搖擺','Swing')`。

## v2.41 — 數字（+6.2%）跟「⚡Swing」還是會分成兩行，補上nowrap

你問「Swing」能不能跟前面的數字（例如+6.2%）同一行。v2.39幫`.swing-tag`加了nowrap，badge本身不會斷字了，但數字文字本身沒有nowrap，兩者中間版面不夠寬時還是會被拆成兩行——一行留數字、一行留⚡Swing，看起來還是分開的。這批數字+badge共用的容器是`.cmp-pvi`（`區域黨派投票指數`/`政黨黨派投票指數`/`加權`這幾列的值都用這個class，之前完全沒有對應的CSS規則，純粹靠inline style），補上`.cmp-pvi{white-space:nowrap}`，讓數字文字跟後面的badge維持在同一行，不會再各自斷開。

## v2.42 — 「i」導覽兩步文案精簡＋加粗關鍵字，「＊...＊」符號改成青綠圓點

**「什麼是綜合PVI？」**：三個修改——1) 開頭兩行的「＊...＊」符號拿掉，改成跟「下一步」按鈕同一組青綠色（`rgba(40,200,200,0.85)`）的圓點`●`，文字本身也直接加粗，比原本星號包起來好看。2) 「本指數是模仿美國Cook Political Report「Cook PVI」概念做的實驗性嘗試，數值該如何解讀，請自行判斷」改成「本指標旨在提供客觀的板塊結構參考，實際政治意涵請綜合評估」。3) 第三勢力排除的說明段落精簡（拿掉重複列舉的政黨/候選人名單，只留2個代表例子），關鍵字（只計入國民黨與民進黨兩黨得票、實際由兩大黨背書參選）改用`<b>`粗體標示。

**「選區資訊欄的各項數字」**：你說可以精簡，拿掉每項定義裡比較囉唆的重複用詞（例如「這個選區的」「（例如...板塊移轉）」這種說明性贅字），關鍵句（跟選區名旁邊「PVI」badge是同一個數字、未扣全國基準的原始）改用`<b>`粗體，整體變成比較精簡的條列式短句，內容跟原本一致沒有刪減資訊量，純粹是文字精簡。

## v2.43 — 泛藍/泛綠簡寫數字上色；政黨熱力圖切換按鈕上色；泛藍/泛綠地圖比照歷年分析非領先反黑

三個相關的要求，都是「泛藍/泛綠模式該有的視覺回饋，比照歷年分析政黨模式已經有的做法」：

**收合條簡寫數字上色**：`updateSeatSummaryStrip()`選了泛藍/泛綠時，`ss-region`/`ss-pl`這兩個數字原本完全沒有上色，補上跟其他地方一致的亮版neon色（綠`#4ade80`/藍`#60a5fa`）；順便發現切回「中間」跟「總」這兩個分支完全沒有清掉顏色，會殘留上一次選的陣營色，一併補上`style.color=''`重置。

**政黨熱力圖切換按鈕上色**：你說「總」按鈕切到某個政黨（政黨熱力圖模式，只有『總』跟某黨可選）時，文字應該要跟歷年分析模式選政黨一樣有該黨的顏色。查了`setMode()`的`_isHmMode`那段，原本是直接`campBtn.style.color = ''`清空顏色，跟winner模式`applyHistSeatPartyToButton()`用`HIST_PARTY_BTN_STYLE`上色的做法不一致。改成也用`HIST_PARTY_BTN_STYLE[m]`上色，兩種模式視覺統一。

**泛藍/泛綠地圖非領先反黑**：你說泛藍/泛綠地圖應該比照歷年分析模式選政黨後「非該黨選區反黑」的做法。新增共用函式`_mapPviColor(pvi)`：`campFilter==='blue'`時PVI≥0（民進黨領先）反黑成`#333338`，`campFilter==='green'`時PVI<0（國民黨領先）反黑，邏輯上跟`winMarginFill()`裡`_histSeatParty`的黑化判斷是同一個概念，只是這裡看PVI正負號不是當選政黨。`edColor()`/`tcColor()`/`vcColor()`三個地圖染色函式裡，comp模式所有呼叫`pviColor(...)`算地圖顏色的地方（共15處，2016/2020/2024都有）全部改呼叫`_mapPviColor(...)`；`pviColor()`本身完全沒動，其他用到它的地方（清單、badge、半圓圖點點，共7處）都刻意不動，因為那些情境不該被這個反黑邏輯影響。

## v2.44 — v2.43兩個修正沒生效的根因；常駐聲明改成跑馬燈下方的圓形按鈕+展開框

**政黨熱力圖按鈕顏色，v2.43沒修乾淨**：你抓到選了政黨（例如民進黨%）應該直接是綠字，不是要先切「總」再切回去才變色。查了才發現`camp-toggle-btn`的顏色同步邏輯其實散落在三個地方，v2.43只修了`setMode()`那一處：`cycleCampFilter()`裡點按鈕本身切換「這個政黨/總113席」的那段，以及語言切換（`setLang`）重新校正UI文字的那段，這兩處都還是無條件`style.color=''`清空顏色，沒有依`heatmapSeatView`決定要不要上色。三處這次統一改成同一套邏輯：`heatmapSeatView==='party'`時用`HIST_PARTY_BTN_STYLE[mode]`上色，`==='total'`時清空。

**泛藍/泛綠地圖反黑，v2.43也沒生效**：根因是`cycleCampFilter()`改`campFilter`之後只更新了收合條數字（`updateSeatSummaryStrip()`），完全沒有觸發地圖重繪——`edColor()`/`tcColor()`/`vcColor()`（裡面已經有`_mapPviColor`的反黑判斷）只有在地圖真的重新算一次fill色時才會被呼叫到，光改變數不會讓已經畫好的地圖polygon自動變色。補上`redrawCurrentLevel()`，點擊「總」按鈕循環到泛藍/泛綠時地圖會即時重繪反映反黑效果。

**常駐聲明重新設計**：你要求把常駐聲明從「浮在側欄下方、隨側欄高度動態定位」搬到跑馬燈正下方，改成一顆紅色圓形按鈕（放大、三角警示圖標），點下去像折疊尺展開一樣長出一個長方形框框顯示完整聲明文字，再點一次收合。原本靠`ResizeObserver`監看`#left-rail`高度變化、`positionDisclaimerNote()`動態算位置那一整套邏輯拿掉（跑馬燈位置是固定的`top:16px`，常駐聲明改成跟著它的固定`top:50px`定位，不用再動態算），新增`#disclaimer-wrap`包住按鈕跟展開框，`toggleDisclaimerNote()`控制`max-height`/`padding`/`border-color`做出展開動畫。`setMode()`裡顯示/隱藏的目標也從`#disclaimer-note`改成外層的`#disclaimer-wrap`。

## v2.45 — 「中間」模式K/D縮寫上色＋不逐字翻譯；常駐聲明收合時露出的紅色細線

**收合條「中間」模式的B/G改成K/D並上色**：你截圖抓到「中間」（campFilter='neutral'）模式收合條顯示「B37」「G36」，英文又是逐字把「藍/綠」翻成顏色縮寫（B/G），跟這幾輪已經在其他地方做的「不逐字翻顏色、講政黨」原則不一致；而且不管中英文，這個字母/字本身都是白色沒有上色，只有數字沒特別處理。改成跟其他地方一致用政黨縮寫（英文K/D、中文維持藍/綠），並且這個字本身也上對應顏色（K/藍用`#60a5fa`、D/綠用`#4ade80`）。

**常駐聲明收合時露出紅色細線**：v2.44新增的展開/收合是同一個`#disclaimer-note`元素身上同時做`max-height`裁切**又**做邊框/背景的顯示切換，收合到`max-height:0`時，邊框的transition效果在特定畫面/瀏覽器算圖時機下沒有跟裁切完全同步，露出一條紅色細線。改成「外層只負責`max-height`+`overflow:hidden`裁切（不帶任何邊框/背景），內層才是真正有邊框/底色/padding的視覺樣式、不隨開合狀態改變」——這樣收合時外層直接裁到0，內層不管長什麼樣都會被完全裁掉，不會有邊框卡在過渡中間露出來的可能。

## v2.46 — 拿掉「全台鄉鎮市區」按鈕旁邊的折疊箭頭，「全台村里」直接顯示

側欄「全台鄉鎮市區」按鈕旁邊原本有一個▶箭頭（`toggleNatAccordion()`），要點開才會展開下面的「全台村里」按鈕。你要求拿掉這層折疊，兩個按鈕直接並排顯示、不用多點一次。移除`#nat-accordion-icon`跟`#nat-accordion-body`這層包裝，`#btn-vill-nat`直接跟`#btn-tc-nat`放在同一個`flex-wrap`的列裡；`toggleNatAccordion()`這個函式也一併刪掉，沒有其他地方在用。

你說想要側欄收合時直接整個藏進按鈕裡，不要在左邊留一大塊（原本是28px寬）的收合殘留。`.sidebar-collapsed #left-rail`原本只把`width`縮到`28px`（裡面的內容`#sidebar-content`雖然淡出到`opacity:0`，但那28px的欄位本身還在、看得到背景/邊框），這次改成`width:0`，順便把`border-right`跟`box-shadow`也一起拿掉（不然就算寬度變0，那條邊框線可能還是會露出一條細縫），`padding`也清零。`#left-rail`本身既有`overflow:hidden`又有`transition:width 0.25s ease`，縮到0寬度時會平滑收合、不會有內容溢出。浮動的圓形收合鈕（`#sidebar-toggle`）本來就是獨立定位、不佔用側欄版面，收合後還是照樣浮在原本位置，可以再點開。

## v2.47 — 第三勢力PVI「不分區席次推演」拿掉區域立委內容、卡片覆蓋改搬進右側資訊欄

你確認第三勢力PVI的「不分區席次推演」（`thirdSubMode==='pr'`）子模式應該比照綜合PVI已經做過的處理：拿掉跟不分區主題無關的三大歷史開票棒狀圖、區域立委候選人長條、投票率/有效票率紅綠燈，並且把滑鼠經過鄉鎮市區/村里時會「偷換」浮動卡片內容（不分區政黨票／村里分類（不分區）／政黨票趨勢）的舊做法，改成跟綜合PVI一樣直接用`boxedCollapseSection()`折疊區塊放進右側資訊欄`#ic`本體——原因是浮動卡片收合時會直接蓋住地圖上的區域，這些卡片內容就完全看不到。

**ED層**（`showEDPanel`）：`_thirdDistrictBlock`在PR子模式下改成兩個`boxedCollapseSection`（鄉鎮市區分類（不分區）+ 政黨票趨勢），不再顯示原本的候選人棒狀圖跟投票率。

**TC層**（`showTCPanel`）：新增`_tcThirdPRInner`，包含三個`boxedCollapseSection`折疊（不分區政黨票／村里分類（不分區）／政黨票趨勢），組裝進`#ic`的邏輯從原本二選一（comp/其他）改成三選一（comp／third+PR／其他）。

**VC層**（`showVCPanel`）：2020跟2024兩處PR分支的內容都從`collapseSection`改成`boxedCollapseSection`（跟綜合PVI折疊同一套視覺：`rgba(40,200,200,0.25)`邊框、`backdrop-filter`毛玻璃、▶/▼箭頭），順便拿掉2024分支裡會多附加一段區域立委歷史開票內容的殘留程式碼，以及一段已經被取代、直接在`showVCPanel`裡面重複偷換卡片2/卡片4內容的冗餘區塊（~18行）。

**浮動卡片偷換邏輯**：`updateCardsForED`/`updateCardsForTC`/`updateCardsForVC`三個函式最上面的guard都加上`(mode==='third'&&thirdSubMode==='pr')`一起跳過（原本只擋`mode==='comp'`），`updateCardsForTC`裡原本專門處理PR子模式的偷換分支（~26行）整段刪掉，因為內容已經直接進`#ic`、不會再走到這裡。

## v2.48 — 「不分區席次推演」半圓圖34錯位；收合條/卡片簡寫文字調整；熱區排行文字發光

**「不分區選舉結果」卡的「34」跟半圓弧對不齊**：你截圖抓到PR子模式第一張卡（不分區選舉結果）下方的「34」明顯偏離半圓弧中心，第二張卡（第三勢力黨派投票指數估算）的「34」則是正常置中的。根因在`refreshRegionHemiDots()`：PR模式（`is34`分支）把dot座標換成`PL_SEATS`（設計給160×80座標系用的），但只更新了svg的`width`/`height`屬性成160/80，沒有跟著把`viewBox`從HTML寫死的`"0 0 180 90"`一起換成`"0 0 160 80"`——座標系跟viewBox對不上，半圓弧在畫面上就往左偏了一截，但「34」文字本身是靠`.hemi-center-lbl`的`left:0;right:0`絕對置中在容器正中央，兩者對不齊。補上`.attr('viewBox', '0 0 160 80')`（非PR分支同樣補上`'0 0 180 90'`，避免切換模式後viewBox卡在上一次的狀態）。

**收合條簡寫文字**：PR子模式收合條原本顯示「不分區 34」「第三勢力 34」，你要求簡寫改成跟四張卡片標題呼應的字——「不分區」→「選舉」（呼應「不分區選舉結果」）、「第三勢力」→「估算」（呼應「第三勢力黨派投票指數估算」）。

**第三張卡標題精簡**：「如果用漢迪法分配113席？」這句完整問句當卡片標題太長，改成直接顯示方法名稱「漢迪法」。

**政黨票戰略熱區排行——選區title改成文字發光**：原本選區名稱是「實心底色＋白字」的badge，你要求改成文字發光的呈現方式（染色規則不變，還是用`thirdPviColor(item.plPvi)`同一套PVI門檻對應色階），比照綜合PVI「黨派投票指數預測比例」卡選區badge的做法——淡底色（0.12 alpha）＋同色邊框（0.4 alpha）＋`text-shadow`發光（0.7 alpha），文字顏色直接用該色階顏色而不是白色。

**「白 +10.2%」跟「▲ +10.4%」擠成兩行**：你截圖抓到選區名稱字數多、badge換行變兩行高時，右邊「白 +10.2%」也跟著斷成「白」一行、「+10.2%」一行（跟「▲ +10.4%」的單行高度不一致，忽粗忽細）。這兩個span本身沒有`white-space:nowrap`，文字寬度不夠時會自己斷行，補上`nowrap`後不管左邊badge是不是兩行，右邊這串數字都固定維持單行；`.pvi-shift-badge`這個class也一起補上`white-space:nowrap`保險。

**第三勢力「i」說明文字符號統一**：`getPviNoteInnerHtml()`裡third模式的「＊第三勢力的定義是...＊」「＊以第三勢力目前最大黨...＊」兩行，符號從「＊...＊」改成跟v2.42已經套用在綜合PVI「什麼是綜合PVI？」那步一樣的青綠圓點`●`＋`<b>`粗體，文字內容不變。

**`updateCardsForVC`死碼清理**：v2.47拿掉PR分支之後，剩下的`if(mode==='third'){...} else {...}`裡，函式最上面的guard已經先擋掉了`mode==='comp'`跟PR子模式，走到這裡`mode`必定是`'third'`且不是PR，`if(mode==='third')`這個判斷式恆真，後面那個`else`分支（舊的comp模式村里催票率/區域立委趨勢內容）其實是永遠執行不到的死碼——這不是這次改動造成的，是更早以前三選一（PR/third/其他）被砍成二選一時遺留下來的殘影，順手拿掉，只留下`if(mode==='third')`的內容（拿掉不必要的`if`包裝，反正一定成立）。

## v2.49 — 收合條「選舉／估算／漢迪法」改顯示第三勢力自己的席次數（不是34/113總數）

你說「不分區選舉結果」簡寫「選舉」的數字應該是8（第三勢力政黨自己的席次數），「估算」也是同一個概念，新加的「漢迪法」也是。v2.48只改了簡寫文字，數字還是照搬`region-hemi-center`/`pl-hemi-center`的原始文字（34/34總席次），沒有換算成「扣掉藍綠兩大黨剩下的第三勢力席次」。

三個數字改成分別從三張卡片各自的資料來源重新算「總席次－DPP－KMT」：「選舉」用`getPLSimSeats()`（卡1「不分區選舉結果」badge/半圓圖同一份實際結果資料，34－green－blue）、「估算」用`getSimulatedPRSeats(compYear, wParty)`（卡2「第三勢力黨派投票指數估算」同一份PVI推演資料，34－dpp－kmt）、原本被綜合PVI模式借去放「錯估」的「原民」欄位，在PR子模式下借來放「漢迪法」，用`runDHondt(HIST_PARTY_VOTES[yr], 113)`（卡3同一份漢彌爾頓最大餘數法分配資料，113－dpp－kmt）。三個算法用的都是各自卡片已經在算的同一份資料，只是換算成單一總數顯示在收合條上，不是另外重算一套。

## v2.50 — 「不分區席次推演」TC/村里地圖染色＋鄉鎮市區分類清單，改跟資訊欄同一套PVI公式

你抓到一個「大問題」：TC層資訊欄（`thirdBoxHtml`）顯示的「區域第三勢力／政黨票第三勢力／加權白黨派投票指數／2016→2020第三勢力成長」，跟同一頁的「鄉鎮市區分類（不分區）」清單、還有TC/村里層的地圖染色，兩邊數字邏輯完全對不起來。

查了以後確認：`thirdBoxHtml`用的是「加權第三勢力PVI」（第三勢力得票率減全國平均、再依wParty滑桿混合區域票跟政黨票，這套邏輯跟「關鍵少數」子模式的地圖染色是同一個`getThirdPVIValue()`），但PR子模式專屬的`combinedCategoryListHtmlPR()`（鄉鎮市區分類清單）跟`tcColor()`/`vcColor()`的PR分支，用的是另一套完全獨立的`prCatColor()`/`prThirdPct16()`/`prThirdPct2x()`——直接拿「不分區第三勢力原始得票率」（0-100%絕對值）分5級（極弱<5%～極強35%+），不減全國平均、也不混區域票。兩套公式從一開始就是各算各的，數字當然對不起來，這不是巧合而是設計上的兩套系統並存。

問你要不要統一，你選了「清單/地圖改用PVI邏輯」（跟資訊欄同一套）。改法：

**`tcColor()`/`vcColor()`**：PR子模式（2016/2020/2024三個年份分支）原本呼叫`prCatColor(prThirdPct16/2x(...))`的地方，全部改成直接呼叫跟「關鍵少數」子模式完全相同的`thirdPviColor(getThirdPVIValue(lvl, key, yr))`——事實上ED層（`edColor()`）從頭到尾就沒有為PR子模式另外分支，本來就是共用`getThirdPVIValue`，這次等於是把TC/VC層補齊成跟ED層一致，不是三層都要重新設計。

**`combinedCategoryListHtmlPR()`**：每個村里/鄉鎮的分級依據，從「該村里/鄉鎮不分區第三勢力得票率」改成「該村里/鄉鎮的加權第三勢力PVI」（一樣呼叫`getThirdPVIValue('vc'/'tc', key, compYear)`），連帶把5級分類的門檻從「0-100%得票率」（5/15/25/35%）改成跟`thirdPviColor()`地圖染色同一套門檻（<0／0-2／2-5／5-8／8+，PVI相對全國平均的正負值），色階顏色也對應`thirdPviColor()`的灰白→淺青→深青漸層，圖例文字從「不分區第三勢力得票率」改成「加權第三勢力黨派投票指數（較全國平均）」。這樣清單裡「明顯偏高」的鄉鎮跟地圖上染色比較深的區域、跟資訊欄「加權白黨派投票指數」正值大小，三個地方現在講的是同一件事。

順便把改完之後完全沒有呼叫者的`prCatColor()`／`prThirdPct16()`／`prThirdPct2x()`三個函式刪掉（死碼，原本只有被剛剛拿掉的這幾個PR分支呼叫，`getPlVcAggr()`不受影響，還有其他地方在用，沒有動）。

**鄉鎮市區分類清單的小方塊也改成發光文字**：你說清單裡每個村里/鄉鎮的小方塊（`combinedCategoryListHtmlPR`裡`g.forEach`那段）也該比照政黨票戰略熱區排行同一套「發光文字」風格。原本是固定用整欄共用的`cat.bg`當實心底色、文字固定`#222`深灰；改成跟該項目自己所屬級距的顏色（`cls(it.pct).clr`）連動——淡底色（0.12 alpha）＋同色邊框（0.4 alpha）＋`text-shadow`發光（0.65 alpha），文字也直接用該級距色，不再是固定深灰字。

## v2.51 — 2024第三勢力PVI「不分區席次推演」TC/村里層還殘留win-badge跟投票率紅綠燈

你抓到2024年第三勢力PVI的「不分區席次推演」子模式，TC跟村里這兩層還會顯示「民進黨 55.8% 最多票」這種win-badge，以及「投票率：74.10% 有效票率：96.49%」紅綠燈——這兩個是v2.47說要拿掉的「選區層級行政資料」（跟ED層級一樣的道理），但ED層級當時拿掉了，TC/VC這兩層漏掉。

查了原因：TC層的`showTCPanel`裡`winBadge(s, s.wpct, true)`（族群最多票的badge）是無條件組裝進`#ic`的，完全沒有依mode/thirdSubMode判斷；投票率紅綠燈（`_tc24Turnout`）本身雖然已經有排除PR子模式的判斷式，但那只擋掉了投票率本身，win-badge沒擋到。VC層的`showVCPanel`2024 fallthrough區塊，win-badge（`winBadge(info, info.winner_pct, true)`）同樣無條件顯示，而且連投票率紅綠燈（`_vc24Turnout`）都忘記加PR子模式排除（跟TC層的判斷式少了`!(mode==='third'&&thirdSubMode==='pr')`這段）。

TC/VC兩層的win-badge都改成`thirdSubMode==='pr'`時輸出空字串；VC層的`_vc24Turnout`判斷式補上跟TC層一致的PR子模式排除。只動2024年這兩層，2020/2016的third+PR分支本來就已經正確用`thirdSubMode==='pr'?'':...`整段包起來排除掉這些內容，沒有這個問題。

## v2.52 — 第三勢力PVI「不分區席次推演」新增小黨跑馬燈（先做2020年）

你在考慮要不要幫第三勢力小黨（民眾黨以外的）也做跑馬燈，比照政黨熱力圖模式；門檻抓>=1%才獨立列，其餘併入「其他」。先幫你把各年度不分區政黨票（扣掉藍綠白）過1%門檻的小黨抓出來——直接解析`立委所得票明細及概況-2020`資料夾裡中選會官方逐投開票所得票數一覽表加總全國（2024年同理用`立委投票所得票明細2024`），不是憑印象估的，總數有對過`HIST_PARTY_VOTES`既有的dpp/kmt/tpp/npp數字（36.16/34.58/22.07、33.98/33.36/11.20/7.75，完全吻合）：2024年扣掉藍綠白後只有時代力量一家過2.57%（其餘都不到1%）；2020年有6家（時代力量7.75%/親民黨3.66%/台灣基進3.16%/綠黨2.41%/新黨1.04%/一邊一國行動黨1.01%）；2016年用`REAL_PL_2016`回推＋跟官方數字校正，抓到親民黨6.52%/時代力量6.11%/綠社盟~2.53%/台灣團結聯盟~2.51%。

跟你確認範圍後：**先只做跑馬燈本身**（不含點擊小黨重新替地圖染色，那個之後有需要再做），**先做2020年**（小黨數量最多、最有代表性），**沿用同一個`#ticker`位置**、third+PR子模式時切換內容（不额外占版面）。

新增`MINOR_PARTY_PR_2020`常數，存2020年這6個小黨的全國得票率（npp/pfp/tsr已經有`HIST_PARTY_BTN_STYLE`既有配色可以沿用，綠黨/新黨/一邊一國行動黨這三個app裡沒有既有配色，另外挑的）。`updateTicker()`原本對`mode==='comp'||mode==='third'`一律隱藏整條跑馬燈（因為跟「相對全國平均」的PVI框架不搭，見函式內註解），現在新增一個前置分支：`mode==='third'&&thirdSubMode==='pr'&&compYear==='2020'`時改成顯示這6個小黨的圓點+名稱+得票率，不再隱藏。跑馬燈的HTML原本是寫死在頁面裡的5個固定`tk-item`（民進黨/國民黨/民眾黨/無黨籍/其他），為了能夠切換回去，把這份原始HTML存成`_TICKER_DEFAULT_HTML`常數，用`tkEl.dataset.tkMode`記錄目前跑馬燈是「小黨版」還是「預設版」，離開2020年PR子模式時换回`_TICKER_DEFAULT_HTML`，避免原本讀`tk-gnum`等固定id的舊邏輯抓不到節點。

## v2.53 — 小黨跑馬燈加入台灣民眾黨、比照歷年分析模式可以點選+高亮

你說2016年小黨應該更多（先不處理，之後有空再補），先確認v2.52的跑馬燈方向可以，但要「比照歷年分析（勝選）模式」的選政黨概念做——歷年分析模式選了政黨之後，跑馬燈上對應那一項會亮、其他變淡（`applyTickerPartyHighlight()`），這裡也要一樣可以點選；另外要把台灣民眾黨也加進這個跑馬燈（原本v2.52只列了扣掉藍綠白的6個小黨，沒有台灣民眾黨本身）。跟你確認過這輪只做「選取＋跑馬燈高亮」，地圖染色不動（那是之後才要做的下一階段）。

新增`MINOR_PARTY_PR_2020_FULL`（民眾黨+原本的6個小黨，民眾黨用`HIST_PARTY_BTN_STYLE.tpp`既有色號`#26b5c4`）、狀態變數`_thirdMinorParty`（比照`_histSeatParty`的概念，null=未選）、`window.selectThirdMinorParty(key)`（點同一項再點一次取消選取，等於null）、`applyThirdMinorHighlight()`（選中項`opacity:1`，其餘`opacity:0.35`+`grayscale(0.6) brightness(0.6)`降飽和降亮度，樣式沿用`renderSeatRatio()`裡`_srActive`已經在用的同一套降權視覺語言，不是另外發明一套）。沒有沿用既有的`applyTickerPartyHighlight()`——那個函式的`TICKER_KEY_PARTY`/`_campMatchesActive`是綁死給g/b/w/ind/oth這5個固定id用的既有邏輯（歷年分析/政黨熱力圖專用），跟這裡動態產生的小黨清單是兩組不同的id集合，硬塞進同一個函式會讓那段邏輯變得難懂，所以另外寫一個小函式，語意獨立。跑馬燈每個`tk-item`補上`id="tk-label-<key>"`＋`onclick="selectThirdMinorParty('<key>')"`＋`cursor:pointer`，`updateTicker()`重繪小黨跑馬燈時也會呼叫一次`applyThirdMinorHighlight()`，確保重繪不會把選取狀態的高亮效果洗掉。

## v2.54 — 小黨選取改成比照composite mode泛藍/泛綠按鈕，不是點跑馬燈；跑馬燈縮小字體+加政黨logo

你說v2.53我理解錯了——可以點的應該是composite mode「泛藍/泛綠」那顆按鈕（`camp-toggle-btn`/`cycleCampFilter()`），不是跑馬燈本身；另外7個小黨擠在一起太雜亂，要縮小字體、加上政黨logo跟代表色。

**選取邏輯搬到camp-toggle-btn**：拿掉跑馬燈每個項目的`onclick`/`cursor:pointer`（跑馬燈恢復純陳列），改在`cycleCampFilter()`裡新增一個分支：`mode==='third'&&thirdSubMode==='pr'&&compYear==='2020'`時，這顆按鈕循環切換`_thirdMinorParty`（總→台灣民眾黨→時代力量→親民黨→台灣基進→綠黨→新黨→一邊一國行動黨→總），按鈕文字/顏色同步換成選中政黨的名稱跟代表色，邏輯結構比照上面`HM.includes(mode)`（政黨熱力圖）分支的寫法。`selectThirdMinorParty()`這個函式拿掉了（不需要了）。同時修正`setMode()`裡「進入third模式時camp-toggle-btn要重新同步外觀」那段，加一個分支：third+PR+2020時照`_thirdMinorParty`同步（不是照campFilter，第三勢力小黨沒有泛藍泛綠這個框架）；`setThirdSubMode()`離開PR子模式、`setCompYear()`離開2020年時，如果`_thirdMinorParty`還選著東西，主動清掉選取狀態+按鈕外觀，避免下次點擊時（此時已經改用campFilter邏輯）按鈕上還殘留著小黨名字的錯亂畫面。

**修正黨籍代碼/顏色**：v2.52倉促之下自己編了一組key（`tsp`/`ycg`）跟顏色，這次改成沿用app裡`getCandidateDisplay()`候選人黨籍對照表已經在用的正式代碼——台灣基進是`tsr`不是`tsp`、一邊一國行動黨是`oio`不是`ycg`；顏色也一併對齊既有慣例：綠黨`#8dd35f`、新黨`#FFDA00`、一邊一國行動黨`#009933`（原本是我自己亂挑的顏色，現在跟app其他地方看到的這些黨的顏色一致了）。改對key的附帶好處：`logoSVGs`剛好也是用同一套代碼命名，可以直接撈到這些小黨的黨徽圖檔。

**跑馬燈縮小+加logo**：7個政黨（民眾黨+6個小黨）擠在一條跑馬燈裡容易看起來雜亂，字體從跑馬燈預設大小（`.tk-num`13px）縮小到10-11px，每個項目前面加上`logoSVGs[key]`黨徽小圖示（12x12），跟app其他地方（候選人列表、席次badge等）用的是同一套圖，不是另外做新的圖示。

## v2.55 — 小黨跑馬燈拿掉「（全國不分區政黨票，≥1%）」備註

跑馬燈右側原本跟著一行小字說明數字來源，你說刪掉，直接拿掉那個`<span>`。

## v2.56 — 選小黨後地圖真的染色了；太小的黨可以下鑽到鄉鎮市區/村里看

前幾輪（v2.52-v2.55）先做了小黨跑馬燈的選取+高亮，這輪你說直接套用到地圖染色：選了某個小黨之後，ED/TC/VC三層地圖都改成「這個黨在當地的不分區得票率，有沒有超過它自己的全國平均」來染色——超過的地方用該黨代表色染色（愈高愈飽和），沒超過的反黑（`#333338`，跟`winMarginFill()`選政黨時非命中區域、`campFilter`泛藍/泛綠的反黑是同一個視覺語言）。如果選的黨太小、在選區層級看不太出深淺差異，本來就可以照現有的ED→TC→VC下鑽機制往下看，同一套規則三層通用，不用另外做「自動找哪一層」的邏輯。

**資料缺口**：TC/VC層需要「這個黨在當地的不分區得票數」，查了才發現app裡`PL_VC_2020`這份既有資料剛好已經有時代力量/親民黨/台灣基進/綠黨/新黨/民眾黨（npp/pfp/tsr/gp/np/tpp）逐村里的得票數（原本不知道，繞了一圈用Python重新解析中選會原始xls驗證出跟這份既有資料幾乎一致，算是意外的交叉驗證）——唯獨一邊一國行動黨沒有獨立欄位，被併在"o"（其他小黨）這個聚合桶裡拆不出來。用同一份官方原始xls（`立委所得票明細及概況-2020`資料夾）另外解析出一邊一國行動黨的逐村里得票數，存成新常數`PL_VC_2020_OIO`（vc11碼→得票數，~7700個村里，123KB），只補這一個政黨，其餘6個沿用`PL_VC_2020`既有欄位，不重複存一份幾乎一樣的資料。

**實作**：`getPlVcAggr()`（原本就是TC/ED層級從VC彙總不分區政黨票的既有函式，村里分類清單、政黨票趨勢折線圖等都在用）在`year===2020`時順便把`PL_VC_2020_OIO`的值併進聚合結果，這樣呼叫端不用管oio特殊在哪裡，都是同一個函式、同一種呼叫方式。新函式`minorPartyMapFill(pvcKey, level)`：算出選中小黨在該區的得票率，跟它自己的全國平均（`MINOR_PARTY_PR_2020_FULL`裡存的`pct`）比較，超過就用該黨hex色跟白色的混合（`ratio`從0.25到1，超過越多越飽和，公式抄`winMarginFill()`的`realColor`分支同一招），沒超過回傳反黑色。`edColor()`/`tcColor()`/`vcColor()`的`mode==='third'`分支各自加一行：`thirdSubMode==='pr'&&compYear==='2020'&&_thirdMinorParty`時優先呼叫`minorPartyMapFill()`，否則維持原本的`thirdPviColor(getThirdPVIValue(...))`。

**收尾**：`cycleCampFilter()`裡切換小黨的分支原本切完只更新跑馬燈高亮，沒有重繪地圖——跟`campFilter`泛藍/泛綠反黑當初的坑一樣（v2.44），補上`redrawCurrentLevel()`；圖例（`renderLegend()`）原本third模式固定顯示「加權三勢力PVI／兩黨獨佔↔第三勢力強」這組通用圖例，選了小黨之後改顯示該黨自己的圖例（黨名+代表色漸層+「未過全國平均／明顯高於平均」），不然圖例跟畫面顏色講的不是同一件事；`cycleCampFilter()`切換小黨時也補上呼叫`renderLegend()`。

## v2.57 — 台南第6選區、新竹縣第1/2選區選任何小黨都不染色的根因

你抓到台南市第6選舉區、新竹縣第1/2選舉區這三個選區，不管選哪個政黨地圖都是白的（無資料狀態），不是特定某個黨的問題。

查了`getPlVcAggr()`（v2.56新的地圖染色在ED層級靠這個函式把村里資料彙總到選區）在`level==='ed'`時是直接比對`VC_ED[vc].ed===key`來篩選屬於這個選區的村里——用`VC_ED`裡「新竹縣選舉區」底下的村里實際檢查了一下，這些村里的`.ed`欄位全部都還是寫著重劃前的舊選區名「新竹縣選舉區」（單一選區，沒有分1/2區），沒有一個村里的`.ed`是「新竹縣第1選舉區」或「新竹縣第2選舉區」；台南市第6選舉區同理，底下村里的`.ed`欄位還留著重劃前的「臺南市第4/5選舉區」。這是舊資料留下的已知現象（CLAUDE.md裡本來就有一條筆記說台南/新竹縣的重劃是對的、不要因為這樣去改動別的行為，但這次踩到的是另一個沒被涵蓋到的地方：`getPlVcAggr`的ED層級彙總直接靠字串比對`.ed`，重劃區這樣比對永遠比不中，彙總結果是空的，地圖就整個選區顯示無資料）。`combinedCategoryListHtmlPR`的ED層級清單原本就沒有這個問題——因為它是透過`TC_STATS`（選區→鄉鎮的對照表，这個沒有過期）先列出選區底下有哪些鄉鎮，不是直接比對`.ed`。

把`getPlVcAggr()`的ED層級邏輯改成跟`combinedCategoryListHtmlPR`一樣的做法：先用`TC_STATS`找出這個選區底下的鄉鎮清單，對每個鄉鎮遞迴呼叫自己的'tc'分支彙總（'tc'分支是用`vc.substring(0,8)===tcCode`比對，純結構性、不依賴`.ed`欄位，沒有過期問題），三個年份（2016/2020/2024）共用同一段新邏輯，不用各自修一次。原本三個年份分支各自寫的`level==='ed'`比對分支都變成死碼，一併清掉。

**順便說明TC層很多黑的是不是bug**：不是，是預期行為。用同一份`PL_VC_2020`資料實際統計了一下六個黨在全國368個鄉鎮市區的分布：台灣民眾黨只有29.9%的鄉鎮贏過全國平均、時代力量24.2%、親民黨33.7%、台灣基進32.9%、綠黨18.2%、新黨16.3%——每個黨都是「多數鄉鎮低於全國平均」，這是得票率分布本來就右偏的正常現象（少數幾個票倉把平均值拉高，導致大多數地方其實都低於平均），不是資料或染色邏輯有問題。

## v2.58 — 2020第三勢力PVI地圖選了小黨之後按鈕死機（效能問題）

你截圖抓到2020第三勢力PVI地圖選了小黨之後，地圖按鈕整個沒反應。查了以後不是邏輯錯誤，是效能問題：`getPlVcAggr()`（v2.56新的地圖染色在ED/TC層級都靠這個函式把村里資料彙總）裡，'tc'跟'vc'這兩個層級原本的寫法是每次呼叫都重新`Object.keys(VC_ED)`把全國約7700筆村里資料從頭掃一遍，只為了找出屬於某一個鄉鎮或某一個村里的資料——'vc'層級（單一村里）明明可以直接`src[key]`一次查到，卻跟'tc'層級一樣掃全表；這個寫法在`getPlVcAggr`原本只有少數幾個地方呼叫時還好，但v2.56把它接進地圖染色（`minorPartyMapFill`）之後，redraw時會對畫面上每一個ED/TC/VC的polygon各呼叫一次——尤其是切到全台村里這種一次要畫七千多個村里的畫面，等於七千多次×七千多筆全表掃描，數千萬次以上的重複運算讓瀏覽器主線程卡住，看起來就像當機。

改成兩個方向優化：(1) 'vc'層級直接`src[key]`一次查表（O(1)），不用再掃全表；(2) 'tc'層級新增一個共用的村里→鄉鎮分組快取`_vcListByTc()`，只在第一次呼叫時把全國村里依前8碼鄉鎮代碼分組建好索引，之後每次查某個鄉鎮只查自己那一小份村里清單，不用再全表掃描。兩個改動都只是加速查詢方式，計算結果（彙總出來的數字）完全不變，純粹解決效能問題。

## v2.59 — 鄉鎮市區村里分類、政黨票趨勢也跟著小黨選取更新

你問「鄉鎮市區村里分類等還有政黨票趨勢是不是也該隨著地圖邏輯更新」，還要求政黨票趨勢選了小黨之後只顯示那一個黨自己的單線折線圖。確認過這是合理的延伸——地圖染色（v2.56）已經改成看選中小黨的當地得票率vs它自己的全國平均，但資訊欄的鄉鎮市區分類清單、政黨票趨勢折線圖還停在原本「不分區第三勢力總和」那套邏輯，跟地圖講的不是同一件事，這輪把這兩個地方也接上選中小黨的邏輯。

**鄉鎮市區/村里分類清單**（`combinedCategoryListHtmlPR`）：新增`itemPvi()`，選了小黨時改用`getPlVcAggr()`算「這個鄉鎮/村里該黨的不分區得票率－該黨全國平均」（跟`minorPartyMapFill()`地圖染色用同一份資料源、同一個算法），沒選小黨時維持原本`getThirdPVIValue()`的加權第三勢力CP值。5級分類的色階也跟著換——選了小黨時用該黨代表色調出5級深淺（新函式`_partyTierClr()`，用跟`minorPartyMapFill()`同一種色彩混合算法），沒選小黨時維持原本的青色系，圖例文字也會換成「[黨名]得票率（較全國平均）」。

**政黨票趨勢**：發現ED層級跟TC層級原本用的是兩個不同的函式——TC層級用`plVcTrendHtml()`（本來就有拆時代力量/親民黨/台灣基進/綠黨等個別政黨的折線），ED層級卻是另一個更陽春的`distTrendChartHtmlPR()`，只畫民進黨/國民黨/第三勢力總和3條線，沒有拆到個別小黨——兩層級行為本來就不一致。把ED層級也改成呼叫`plVcTrendHtml()`（用`getPlVcAggr(ed,year,'ed')`彙總三個年份餵進去），跟TC層級統一。`plVcTrendHtml()`新增`onlyKey`參數，選了小黨時只留那一條線（其餘政黨的線、「合計」虛線參考線都不畫，畫面乾淨很多）；順便補上一邊一國行動黨、新黨這兩個原本沒畫進這張圖的政黨線（一邊一國行動黨只有2020年一個資料點，2016/2024還沒有這個黨，折線圖本來就會自動略過沒有資料的年份，不是缺陷）。

**小插曲**：改的過程中發現ED層級的`distTrendChartHtmlPR()`現在只剩一處呼叫，而且那處呼叫本身是`updateCardsForED()`裡已經因為guard提早return而永遠執行不到的死碼（更早以前v2.47migrate的殘留）——這個函式定義先留著沒刪，怕死碼萬一以後被誤觸發時找不到函式報錯，之後有機會可以連同那段死碼一起清掉。

## v2.60 — 鄉鎮市區分類選了小黨後全部都顯示低於全國平均（v2.59帶進去的bug）

你抓到選了小黨之後，鄉鎮市區分類清單裡的鄉鎮全部都掉進「低於全國均值」這一級，一個都沒有分到其他級——這是v2.59的bug，不是預期的資料分布（之前查TC層地圖染色時statistics顯示每個黨大概還是有16-34%的鄉鎮贏過全國平均，不會是0%）。

根因：`getThirdPVIValue('tc', key, ...)`跟`getPlVcAggr('tc', key, ...)`這兩個函式雖然都吃`level==='tc'`，但`key`參數格式不一樣——`getThirdPVIValue`要吃`TC_STATS`那種「選區__鄉鎮」組合鍵，`getPlVcAggr`只吃純8碼鄉鎮代碼（v2.58剛把它改成靠`_vcListByTc()`查表，這個索引是用純8碼鄉鎮代碼建的）。v2.59新增的`itemPvi()`在ED層級的鄉鎮迴圈裡，選了小黨時呼叫`getPlVcAggr(k, 2020, 'tc')`，`k`卻是`TC_STATS`迴圈給的組合鍵（例如"南投縣第1選舉區__10008010"）——這個字串當然查不到任何村里，`getPlVcAggr`回傳空結果，`itemPvi()`就照著「無資料」的後備邏輯回傳「明顯低於平均」，所以每個鄉鎮都被歸類成同一級。

`itemPvi()`改成同時接收`thirdKey`（給`getThirdPVIValue`用，'tc'層級要組合鍰）跟`aggrKey`（給`getPlVcAggr`用，'tc'層級要純鄉鎮代碼）兩個參數，ED層級的呼叫點改成`itemPvi('tc', k, tc)`分開傳這兩種格式；'vc'層級因為兩個函式吃的都是純11碼村里代碼、格式一樣，維持`itemPvi('vc', vc, vc)`共用同一個值。順便確認了地圖染色（`minorPartyMapFill`）跟這個bug無關——`tcColor()`呼叫`minorPartyMapFill()`時本來就有正確做`key.split('__')[1]`取純鄉鎮代碼，只有這次新加的分類清單踩到這個鍵值格式不一致的坑。

## v2.61 — 折疊開合狀態沒記住、政黨票趨勢數字位置跟點點對不上、補親民黨/新黨2024資料

一次處理三個你抓到的問題。

**折疊點開之後滑鼠移開又自動關閉**：查了以後發現「不分區政黨票」這個折疊（id `_pcr`，TC層級`_tcThirdPRInner`裡的第一個、VC層級2020分支裡的那個）從一開始（v2.47那輪migrate）就沒有跟`_pca`（村里/鄉鎮市區分類）、`_pct`（政黨票趨勢）、`_mob`（催票率）一樣做「重繪前記下目前是開是關、重繪後還原」這件事——面板本來就是滑鼠移到地圖上不同選區/鄉鎮/村里時透過`mouseover`重新整個innerHTML重建的，`_pca`/`_pct`/`_mob`每次重建都有把「使用者剛剛有沒有點開過」的狀態存起來、重建完再套回去，只有`_pcr`這個折疊漏了這道手續，所以每次滑鼠移動觸發面板重建，`_pcr`都會被打回預設的關閉狀態，跟其他折疊行為不一致。TC層級（`showTCPanel`）跟VC層級2020分支（`showVCPanel`）都補上跟`_pca`/`_pct`同一套`_pcrWas`記錄+還原邏輯。

**政黨票趨勢折線圖數字位置跟點點對不上**：你抓到親民黨/新黨這種2024年還在但這次畫面沒有點的（下面會解釋），還有一邊一國行動黨（2021年解散，只有2020一個資料點）這種情況，數字標籤的位置是錯的。查了`plVcTrendHtml()`，原本標籤固定畫在圖表最右邊（也就是2024年的x座標），邏輯上假設「每個政黨最後一個有效資料點一定是2024年」——這個假設對大部分政黨成立，但對已經解散、或某年份沒有拆出獨立欄位的政黨就不成立，變成「數字釘在2024的位置，點點卻畫在2020」這種對不上的畫面。改成先找出這條線「最後一個真的有資料的年份」是哪一個索引，數字標籤就跟著那個點的x座標畫（往右偏移5px），不再假設一定是最右邊那一年。

**親民黨/新黨2024年確實沒有拆出來——這次補上了**：你問「親民黨新黨2024依然在但是沒有點點」是不是資料問題——查了確認是真的資料缺口，不是我算錯：`PL_VC_2024`這份既有資料完全沒有`pfp`/`np`這兩個欄位（親民黨/新黨2024年的票都被併進"o"其他小黨聚合桶，沒有像時代力量/台灣基進/綠黨那樣拆成獨立欄位）。既然2024年這兩個黨都還有實際跑（親民黨全國0.51%、新黨0.29%，不是真的0票），用跟v2.56一邊一國行動黨同一招——解析`立委投票所得票明細2024`資料夾裡中選會官方逐投開票所得票數一覽表，把親民黨/新黨的逐村里得票數單獨拆出來，存成新常數`PL_VC_2024_PFPNP`（vc11碼→{pfp,np}，村里加總跟全國0.51%/0.29%對得起來，驗證過）。`getPlVcAggr()`2024分支、`plVcTrendHtml()`非彙總（單一村里）分支都補上跟這份資料合併，親民黨/新黨2024年的點現在會正常顯示。

## v2.62 — 折疊還是會關：v2.61漏改了ED層級（真正的根因在這裡）

你說折疊還是會自動關，重新檢查後發現v2.61抓錯地方了。TC層級跟VC層級2020分支的`_pcr`確實原本沒有Was/restore、也確實補上了，但那不是主要問題——**ED層級（`showEDPanel`）的`mode==='third'`整個分支，從一開始就完全沒有做「重繪前記狀態、重繪後還原」這件事**，不是只漏了`_pcr`一個，是`_pca`（鄉鎮市區分類）跟`_pct`（政黨票趨勢）兩個折疊全部都沒有——這整個分支從PR子模式在v2.47剛搬進`#ic`的時候就沒接上這套機制，TC/VC層級有、ED層級沒有，三個層級本來就不一致，只是你之前主要在ED層級（選區層級，滑鼠最常停留的地方）測試才會一直碰到。

補上`_edPcaWas`/`_edPctWas`（進入`mode==='third'`分支時就先記錄，因為PR跟key兩個子模式共用同一個分支、同一個return出口），面板重建完（`updateCardsForED(ed)`呼叫之前）還原回去。這次應該是真的抓到根因了。

## v2.63 — 切換小黨時折疊也保持不關

你問「可以讓在不同政黨切換時折疊也不會關嗎」——這是另一種情境：v2.62修的是「滑鼠移到地圖上不同選區/鄉鎮/村里」觸發的面板重建，這次是「點camp-toggle-btn切換小黨」觸發的重建，兩種觸發方式都會重建面板，但走的是不同的程式碼路徑，所以要另外處理。

查了`cycleCampFilter()`切小黨的分支，重繪地圖是靠`redrawCurrentLevel()`，這個函式底層呼叫的是`goED()`/`goTC()`/`goVillAll()`這系列函式——這些函式除了重繪地圖，還會把`#ic`整個換成「移到地圖查看資料」這種佔位提示文字（這是切換陣營/切換年份時本來就有的既有行為，不是這次才發現的新問題），連帶把裡面所有折疊的DOM元素整個沖掉，折疊開合狀態當然也跟著消失。

修法：切小黨時，重繪地圖之前先把目前面板裡`_pca`/`_pct`/`_pcr`/`_mob`這幾個折疊的開合狀態記下來（`_foldWas`），`redrawCurrentLevel()`重繪完（面板變成佔位提示）之後，用`curEDPanel`/`curTCPanel`/`curVC`這幾個「上次顯示面板時是哪個ED/TC/VC」的追蹤變數，重新呼叫一次對應的`showEDPanel()`/`showTCPanel()`/`showVCPanel()`，把面板換回原本在看的那個選區/鄉鎮/村里（順便讓面板內容反映新選的政黨，不用使用者自己重新hover一次），最後再把記下來的折疊狀態貼回去。新增了`curTCPanel`追蹤變數（在`showTCPanel()`一開始就記錄目前的key，跟既有的`curEDPanel`同一個概念）；VC層級不需要新變數，因為`showVCPanel()`本來就會在一開始把`curVC`設成目前這個村里，已經有現成的「上次顯示面板時的村里」可以用。

## v2.64 — 選了小黨後，滑鼠經過贏過平均的選區邊線也會發亮（比照歷年分析選政黨）

你問可不可以比照歷年分析（hist）模式選政黨的效果——滑鼠經過「這個政黨贏的地方」邊線會發亮，反黑的地方維持反黑不發亮——加到第三勢力PVI選小黨的情境。查了歷年分析模式這個效果原本是`updateEdgeGlowPerDistrict()`這個函式做的：只在`mode==='winner'&&_histSeatParty`（選了政黨）時生效，滑鼠經過每個選區時先檢查`edColor(ed)`是不是反黑色`#333338`，是的話不發亮，不是的話疊加一層該黨代表色（`HIST_PARTY_BTN_STYLE[...].glow`）的`drop-shadow`光暈+既有的黃框hover效果。

新增一個條件`isThirdMinorActive`（`mode==='third'&&thirdSubMode==='pr'&&compYear==='2020'&&_thirdMinorParty`），成立時也讓這個函式生效，發光顏色改用`MINOR_PARTY_PR_2020_FULL`裡選中小黨的`color`。判斷「這個選區算不算贏」的邏輯完全不用改——直接沿用同一行`edColor(d.properties.ed)==='#333338'`，因為`edColor()`在選了小黨時本來就是呼叫`minorPartyMapFill()`，沒贏過該黨全國平均的選區本來就會回傳`#333338`，兩種情境（選政黨/選小黨）用的是同一套「反黑=不發亮」判斷，不用另外寫一次。這個函式已經是`goED()`重繪流程的一部分（透過`updateGlowAndBaseED()`），v2.63切小黨時呼叫的`redrawCurrentLevel()`本來就會觸發到，不用另外接線。

## v2.65 — 村里層政黨票趨勢也要跟著選黨過濾；ED/TC層選黨後保留第三勢力合計虛線

兩個問題一起處理：(1) 村里層（VC）的政黨票趨勢折線圖，v2.59那輪只顧到ED跟TC層接上`onlyKey`（選了小黨只顯示那一條線），VC層級的兩個呼叫點忘記傳`_thirdMinorParty`，還是照樣顯示全部政黨的線，沒有跟著過濾。(2) 你這次額外要求：ED/TC層選了小黨、只留那一條線之後，「合計」（第三勢力整體得票率）那條參考虛線不要跟著拿掉，好對照這個黨自己表現跟第三勢力整體的落差；但VC層級選了小黨後這條參考線還是不要，維持乾淨的單線。

`plVcTrendHtml()`新增第4個參數`showTotalLine`，跟`onlyKey`分開控制——原本`onlyKey`一設定就把「合計」虛線整個拿掉，現在改成只有`onlyKey`設定**且**`showTotalLine`沒設定時才拿掉。ED層級、TC層級的呼叫都補上`true`當第4個參數（選黨後保留合計線），VC層級的兩個呼叫點都補上`_thirdMinorParty`當第3個參數、不傳第4個參數（選黨後只留單線、不留合計線）。

## v2.66 — 折疊在ED/TC/VC三層之間切換也要保持開啟（根本改法）

你說折疊在不同地圖層級（ED選區↔TC鄉鎮↔VC村里）來回切換時應該也要維持開啟。v2.62/v2.63修的是「同一層級內滑鼠移到別的選區/鄉鎮/村里」「切換小黨」這兩種情境，用的是「重繪前讀一次DOM現在的開合狀態、重繪後手動貼回去」這招，每個面板重建的地方都要各自寫一次——但**換層級**（例如從ED選區點進TC鄉鎮）中間會先經過`goTC()`這類函式，把`#ic`整個換成「移到鄉鎮查看資料」這種佔位提示，這個當下`_pca`/`_pct`等節點根本不存在，沒辦法用「重繪前讀DOM」這招去記狀態——這是第三種、之前兩輪都沒處理到的情境，而且「重繪前讀一次、重繪後貼回去」這個模式本質上就是每加一種新的觸發情境就要多補一處，治標不治本。

這次改成根本解法：折疊的開合狀態不再依賴「重繪前的DOM長什麼樣子」，改成一份全域狀態`_foldOpenState`（用bodyId，例如`_pca`/`_pct`/`_pcr`/`_mob`，當key），點擊折疊時透過新的`window._toggleFold()`同步更新這份全域狀態；`boxedCollapseSection()`每次渲染折疊區塊時，直接讀這份全域狀態決定一開始是開是關，不再吃`defaultOpen`參數決定的固定值（`defaultOpen`只在`_foldOpenState`裡完全沒有這個bodyId的紀錄——也就是使用者從頭到尾都還沒點過這個折疊——時才當初始值）。這樣不管面板是被滑鼠移動、切換小黨、還是換ED/TC/VC層級的哪一種原因重建的，只要`boxedCollapseSection()`被呼叫到，就會自動套用「使用者上一次設定的開合狀態」，不用每個面板重建的地方各自記一次、各自還原一次。v2.61-v2.63那批`_pcaWas`/`_pctWas`/`_pcrWas`/`_mobWas`的散落程式碼保留沒刪（現在等於是重複動作，但無害——反正`boxedCollapseSection()`渲染出來的狀態已經是對的，那些舊程式碼頂多是把同一個正確狀態再設一次），之後有空可以整理掉。

## v2.67 — TC層級的「不分區政黨票」跟「政黨票趨勢」合併成一個折疊，比照VC層級排版

你說喜歡VC層級「不分區政黨票分析」這個折疊的排版——長條圖（不分區政黨票）跟趨勢折線圖（政黨票趨勢）合併在同一個折疊裡（用`sec-title`分兩小節），不是兩個各自獨立的折疊，希望TC層級也改成這樣。

TC層級（`showTCPanel`的`_tcThirdPRInner`）原本是三個獨立折疊：「不分區政黨票」(`_pcr`，長條圖)、「村里分類（不分區）」(`_pca`)、「政黨票趨勢」(`_pct`，折線圖)。改成跟VC層級`showVCPanel()`兩處組裝`_prInner`的寫法一致：把長條圖跟趨勢折線圖合併進同一個`_pcr`折疊（標題改成跟VC層級同名的「不分區政黨票分析」），`_pct`這個獨立折疊拿掉（折線圖現在是`_pcr`折疊內部的第二小節，不再是自己的折疊）。「村里分類（不分區）」(`_pca`)維持獨立折疊不變——VC層級沒有這個東西可以比照，因為村里已經是最細的層級，沒有更下層可以分類，只有ED/TC層級才需要這個功能。

## v2.68 — TC層「村里分類（不分區）」搬到「不分區政黨票分析」上面

v2.67合併完之後順序是「不分區政黨票分析」在上、「村里分類（不分區）」在下，你說村里分類應該要在上面，兩個`boxedCollapseSection`呼叫的串接順序對調過來。

## v2.69 — 收合條數字選了小黨後改成該黨自己的席次，比照歷年分析選政黨

你說收合條（四張卡折疊起來時看到的「選舉8 估算5 漢迪法24」這排數字）選了小黨之後應該要跟著換成那個黨自己的席次數，跟歷年分析模式選政黨後看到的是「這個黨自己的區域/不分區/原民/總」席次數同一個概念——v2.49做的時候還沒有小黨跑馬燈這個功能，數字算的是「總席次－DPP－KMT」（也就是第三勢力全體，不分是哪個小黨）。

`updateSeatSummaryStrip()`裡`isThirdPR`分支改成看`_thirdMinorParty`有沒有值：沒選（`_thirdMinorParty`是null）維持原本「第三勢力全體」的算法；選了的話，改成分別從三個既有的席次函式（`getPLSimSeats()`／`getSimulatedPRSeats()`／`runDHondt()`，跟卡片上badge/半圓圖同一組資料源）裡查那個黨自己的欄位。三個函式的欄位命名不統一（`getPLSimSeats()`裡民眾黨的欄位叫`white`不是`tpp`），用一張對照表`_fieldMap`分開查每個黨在三個函式裡各自對應哪個欄位名稱。親民黨/台灣基進/綠黨/新黨/一邊一國行動黨這5個小黨2020年得票都在5%政黨門檻以下，三種算法本來就分不到席次，選這幾個黨時三個數字都會是0——這是正確答案（門檻以下依法沒有席次），不是查不到資料。順便發現`cycleCampFilter()`切小黨的分支原本沒有呼叫`updateSeatSummaryStrip()`，數字根本不會更新，一併補上。




## v2.70 — 小黨跑馬燈加5%政黨門檻虛線標示；2024補上親民黨

你說5%是不分區政黨門檻（沒過5%依法分不到席次），想讓跑馬燈能一眼看出哪些黨過門檻、哪些沒有。跑馬燈本身是`updateTicker()`裡`_isThirdPRMinor`分支組出來的一排`tk-item`（logo+名稱+得票率文字），本來完全沒有長條圖或分隔線可以掛。兩份小黨清單（`MINOR_PARTY_PR_2020_FULL`/`MINOR_PARTY_PR_2024_FULL`）本來就是依pct由大到小排序，所以不用針對兩個年份寫死插入位置，改成在`.map()`裡逐項檢查「上一項還有5%、這項沒有5%」的分界點，符合就插一個白色虛線分隔元件（`border-left dashed`+一個白色「5%」小字標籤），不符合就不插——2020年會插在時代力量（7.75%）跟親民黨（3.66%）中間，2024年插在民眾黨（22.07%）跟時代力量（2.57%）中間，兩個年份共用同一段邏輯自動算出正確位置。

順便補上2024年的親民黨——原本`MINOR_PARTY_PR_2024_FULL`只列了5個在`PL_VC_2024`裡本來就有獨立欄位的政黨（民眾黨/時代力量/小民參政歐巴桑聯盟/台灣綠黨/台灣基進），親民黨（0.51%）沒列。查了確認親民黨其實不用額外解析資料——v2.61已經做過`PL_VC_2024_PFPNP`這份補充資料（親民黨/新黨2024年份的村里得票），`getPlVcAggr()`也已經有merge這份資料的邏輯，加進跑馬燈清單就能直接用，地圖染色/下鑽層級都正常。

原本也想順便加雙語無法黨（0.33%，比親民黨還少）——查了才發現這個反而更不需要額外解析：`PL_VC_2024`原始資料裡其實已經有獨立的`"tbip"`欄位（全國44768票，用中選會逐投開票所原始資料另外重新加總驗證過，跟這個數字對得起來），`getPlVcAggr()`的`tc`/`ed`分支都是逐欄位加總、不是只挑固定幾個key，所以`tbip`本來就會自動彙總，不用補任何資料。但實際加進去試了之後，跑馬燈變成7個政黨，字數太長（尤其「小民參政歐巴桑聯盟」跟「臺灣雙語無法黨」都是長黨名），整排寬度蓋到右邊「不分區席次推演／關鍵少數」分頁按鈕——2020年剛好因為黨名字數都比較短（沒有雷同「小民參政歐巴桑聯盟」這種長度的），7個政黨照樣放得下，2024年放到第7個就爆版了。先把雙語無法黨拿掉、只留親民黨，把原因寫在陣列上面的註解裡，之後真的要補的話資料已經現成，直接把這個party物件加回陣列即可。




## v2.71 — 新增「地方樁腳分析」：找候選人歷屆得票異常集中的村里

你問council.html的「最高候選人」模式能不能原封不動搬來這裡標示可能的地方樁腳。查了發現機制上不能直接搬：council的多席次SNTV選舉裡「誰是單一最高票」跟「誰當選」是兩件不同的事，值得特別標示；但這裡的區域立委是單一席次制，最高票=當選人，直接搬只是「換個方式標示已經知道的當選人」，不是新資訊。真正能標示樁腳的是另一種訊號——同一個人在同一個村里，連續好幾屆得票率都遠高於他自己那一屆的選區平均，這種地理上高度集中、跨屆持續的異常，比單一年度「誰最高票」更接近地方樁腳/家族網絡的真實樣子。

資料面確認2016(`VC_2016`)/2020(`VC_SIM[vc].v20`)/2024(`VC_ED`)三屆在村里層級都有完整的逐候選人得票（`cands:[{n,p,v}]`或`votes:{name:{p,n}}`），且都是直接用村里代碼(vc)當key——不像council.html的縣市議員選區，立委選區重劃在這裡完全不影響比對，因為比對單位是村里本身（地理上穩定），不是選區（會重劃）。

核心邏輯（`_computeCandidateStrongholds(name)`）：抓出候選人在2016/2020/2024裡有參選的每一屆，算出他那一屆的「選區平均得票率」（自己在整個選區的得票總和/選區總票數），再算他在每個有參選的村里裡「村里得票率 − 選區平均」，挑出連續每一屆都超前15百分點以上的村里當候選熱點。用馬文君（南投縣第1選舉區，2016/2020/2024連續三屆參選）測試：選區平均落在53-55%之間，但仁愛鄉（南投的山地原住民鄉）裡有4個村（力行村/榮興村/都達村/萬豐村）連續三屆都超前25-40個百分點——這正好對上台灣政治常識裡山地原住民鄉常見的、跟部落/在地網絡高度綁定的集中投票模式，不是隨機雜訊，验证了这个信号是有意义的。

視覺呈現刻意不碰既有的`redrawSwing()`那套搖擺區疊圖機制——那套邏輯跟目前導覽狀態（`curED`/`curTC`/`level`/`winnerYear`/各種選區重劃對照表）深度耦合，是為了「畫出目前視角範圍內符合條件的鄉鎮/村里」而設計，不是為了「不管目前在哪裡，畫出某個候選人在他自己選區的所有村里」這種需求。改成更輕量的做法：搜尋候選人時直接呼叫既有的`goVillAll(ed)`導覽到他的選區＋全選區村里總覽，然後在`vcColor(vc)`最前面加一個判斷式——樁腳模式開啟時，直接回傳依margin算出的強度色（紫色色階，跟現有的PVI綠/藍、政黨熱力圖區隔開來），略過後面所有原本的模式判斷。退出時清掉狀態、重新呼叫一次`goVillAll(curED)`即可恢復正常染色，不需要额外维护一个独立的SVG圖層。

UI是左側工具列新增一個可收合的「🔍 地方樁腳分析」區塊，姓名輸入框搭配`<datalist>`原生自動完成（候選人清單只在第一次focus時建立一次，掃描全部村里資料建索引，避免每個字元輸入都重算），下方列出符合門檻的村里（依margin排序，最多列前20個）。面板裡明講兩個限制：(1) 只比對完全相同姓名，換人選（家族接棒）偵測不到；(2) 15百分點的門檻是人為選定，不是統計顯著性檢定，只是初篩參考，不是嚴謹結論——這兩點是你在提出這個功能時已經接受、之後要自己另外查證的部分。




## v2.72 — 把「激戰選區（差距≤5%）」搬進「政黨輪替分析」box裡

你想把原本獨立在資訊面板最上面的「激戰選區（差距≤5%）」搬到「政黨輪替分析」（#flip-analysis-box）裡，跟搖擺地圖/翻盤選區/翻盤鄉鎮市區/翻盤村里放在一起。中途你先說要拿掉翻盤選區按鈕，我照做了，你馬上說講錯了要加回來，我原封不動復原（用同一段HTML直接貼回去，沒有重新設計）。

搬動前先查了「激戰選區」跟「政黨輪替分析」box目前各自的顯示條件不一樣：政黨輪替box只在`mode==='winner'`時顯示；激戰選區原本除了`mode==='comp'`會被單獨隱藏之外，在第三勢力/政黨熱力圖等其他模式都看得到、可以用。搬進box裡之後，激戰選區的顯示權就會被box自己的外層`display:none`蓋過去，等於只剩winner模式能用——這是搬動本身必然帶來的副作用，不是我自己另外加的限制。跟你確認過這個影響可以接受（兩個功能本來都是在講「跟真實2020→2024開票結果的比較」，winner模式限定其實概念上也算一致），才動手搬。

技術上刻意讓新按鈕保留原本的`id="swing-mask-row"`（外層）／`id="chk-swing-mask"`（按鈕本身）——這樣comp/sim/不分區地圖模式底下另外幾處各自獨立控制這兩個id顯示/停用狀態的程式碼完全不用改，只有：(1) HTML從`<input type="checkbox">`+`<label class="switch-toggle">`改成跟翻盤選區同款的`<button class="btn">`；(2) `toggleSwingMask()`內部從讀`.checked`改成自己反轉`showSwingMaskOnly`布林值＋`classList.toggle('on',...)`；(3) 唯一一處直接讀`.checked`的地方（comp模式強制關閉激戰選區時）改成讀`showSwingMaskOnly`變數。CSS的`#flip-analysis-box .btn.on`規則本來就已經存在（給翻盤選區用），新按鈕搬進box裡直接套用，不用另外寫樣式。verified：按鈕會正確顯示「on」高亮狀態、地圖疊圖確實生效，切到comp模式時整個box（含激戰選區）正確隱藏，沒有console錯誤。




## v2.73 — 把council.html的連任/新選上/回鍋/連任失敗標籤移植到區域立委選區面板

你要求把v2.69-2.71在council.html做的候選人連任標籤系統原封不動搬過來。查了才發現「原封不動」在機制上做不到：council.html的縣市議員是多席次SNTV，「誰是最高票」跟「誰當選」是兩件要分開標示的事；這裡的區域立委是單一席次制，最高票=當選人，沒有「最高票標示」這個額外需求，直接搬概念就好，但render部分完全沒有共用函式可以搬——`showEDPanel()`內建了2016/2020/2024三個年份各自獨立、結構重複的`renderRow`閉包（外加各自的「其他N位候選人」collapsed detail子閉包，一共6個模板位置），不像council.html的`renderCouncilCandRow()`是單一共用函式。跟你確認過還是要做完整3年份，才動手。

比對邏輯完全對應council.html：用「同縣市＋同姓名」而非「同選區代號」，因為區域立委選區在2016→2024之間會因人口變動重劃（縣市本身穩定，選區編號/範圍不穩定）。新增`_legCcFromEd(ed)`（用選區名稱字串開頭比對22縣市名稱，反查縣市代碼——`EDB`裡沒有現成的ed→cc對照表）、`_legWinnersInCounty(cc,year)`（先用`_legEdWinnersByYear()`把村里逐候選人得票依選區聚合、找出每個選區的最高票，再把整個縣市所有選區的贏家收成一個Set，跟council.html的`_councilWinnersInCounty()`同一個概念）。三個年份的資料來源不同：2024用`VC_ED[vc].votes`+`.ed`欄位；2020用`VC_SIM[vc].v20.cands`+`.v20.ed`；2016的`VC_2016`沒有`.ed`欄位，額外寫了`_legBuildTc2016EdMap()`透過`TC_2016`的key（`ed__tc`格式）反查每個鄉鎮對應的2016選區，只建一次快取。

`_legTermBadgeSlot()`回傳的badge視覺樣式/顏色/文字（金色×N／綠色新／紫色回鍋／紅色落N）跟council.html完全一致，一樣放在`.blb`（姓名欄，同一份`style.css`寫死70px+ellipsis裁切）外面的獨立flex slot，一樣2016年（資料集起點）不顯示任何badge。把這個函式呼叫塞進6個renderRow模板：2020(major+detail)、2016(major+detail)、2024(major+detail)——每個都在`isElected`/`check`/`ns`算完之後、`return`模板字串組裝之前加一行`const termBadge = _legTermBadgeSlot(...)`，模板裡`${check}${translateCandidate(...)}</div>`後面插入`${termBadge}`。showTCPanel()裡也有幾乎一樣的重複模板（另外4處），但範圍只談到ED層選區面板，這幾處故意沒動。

驗證用真實已知案例，不是隨便挑選區：南投縣第1選舉區馬文君（2016/2020/2024連續三屆當選，2024模式顯示×3、2020模式顯示×2、2016模式無badge，全部正確）；台北市第4選舉區高嘉瑜（2020當選、2024落選，眾所皆知的敗選案例，顯示「落1」／"OUT×1"）；同選區李彥秀（曾當選、中間斷過、2024又選回來，顯示「回鍋」／"BACK"，也是實際政治生涯跟資料吻合的案例）。中英文都測過，沒有console錯誤。




## v2.74 — 修復地方樁腳分析／連任標籤在重劃縣市選錯選區的bug

你回報搜尋陳美雅／黃柏霖時，兩人的樁腳分析選區結果對調了。查了直接發現根因：`_computeCandidateStrongholds()`跟`_legEdWinnersByYear()`（v2.71/v2.73新加的兩個功能）都直接讀`VC_ED[vc].ed`／`VC_SIM[vc].v20.ed`當作村里所屬選區，但這個檔案裡已經有明確記載、多處都在處理的已知問題——高雄/台南/新竹縣/屏東這幾個2020年後重劃過的縣市，村里資料的`.ed`欄位本身是重劃前留下的舊值，直接讀會把村里分到錯的選區。陳美雅／黃柏霖剛好各自有村里落在高雄同一個重劃鄉鎮，沒修正的話兩人的村里就會被分到彼此的選區去，變成「選區對調」的現象。

修法：新增`_legResolveVcEd(vc, rawEd)`，內部呼叫這個檔案本來就有的`resolveCurrentEd(tc8, vc)`（`goVillAll()`/`buildFlipCacheTown()`等處已經在用同一顆，不重新發明第二套修法），解不出來才退回原始`.ed`值。把v2.71的`_computeCandidateStrongholds()`（2024/2020兩個分支）跟v2.73的`_legEdWinnersByYear()`（同樣2024/2020兩個分支）裡所有直接讀`.ed`的地方都改用這個函式。2016不受影響——2016的重劃問題本質不同（是「邊界definition本身不一樣」不是「資料欄位寫舊值」），且`_computeCandidateStrongholds()`本來就不會為2016算`ed`。

驗證方式：修復後陳美雅/黃柏霖分別對應到高雄市第6/第5選舉區，直接跟`ED_STATS`（`showEDPanel()`實際使用、比對過確認正確的資料源）交叉核對兩人確實分別出現在對應選區的候選人名單裡，不是憑印象猜的。另外重跑南投縣馬文君（非重劃縣市）確認沒有回歸問題，term count仍然正確算出3。沒有console錯誤。




## v2.75 — 修復2016年小黨跑馬燈模式，高雄市第9選舉區／屏東縣第3選舉區（等2020重劃後消失的舊選區）地圖不染色的bug

你回報2016小黨模式下，屏東縣第3選舉區、高雄市第9選舉區這幾個選區地圖沒有顏色，後續補充是親民黨、也補充不只親民黨（其他小黨也一樣）。

查了發現這兩個選區的資料本身是存在的（`ED_2016`裡查得到、有正常票數），問題出在`getPlVcAggr(key, year, 'ed')`的2016分支——這個分支直接用`VC_ED[vc].ed===key`篩村里，這個做法原本是有意設計的：v2.73的紀錄裡提到台南/新竹縣這幾個2020重劃過的縣市，`.ed`欄位還留著重劃前（也就是2016年）的舊選區名，直接拿來查剛好符合需要。但這個假設對高雄市第9選舉區／屏東縣第3選舉區不成立——這兩個選區在2020重劃時不是「改名」而是直接「拆併消失」（第9選舉區被拆進第5/6/7/8選舉區、第3選舉區大部分併入第1/2選舉區），現行`VC_ED[vc].ed`欄位裡幾乎不會留著這兩個已經不存在的舊選區名（用grep直接查`data_vd_v64.js`/`test.html`裡的原始資料確認：這兩個縣市底下真正屬於舊選區9/舊選區3的鄉鎮，村里`.ed`欄位現在清一色是第5/6/7/8或第1/2，只有5筆／35筆是資料本身的雜訊個案，不是真正的選區範圍）。用這個篩法找到的村里數量太少，加總出來的票數低到被當成「這個選區沒有小黨資料」，`minorPartyMapFill()`拿到`null`就整選區塗成無資料的`#e0e0e0`灰色。

修法比照這個函式自己在2020/2024分支已經在用的做法——那兩個分支不靠`.ed`欄位比對，而是先用`TC_STATS`（選區→鄉鎮的結構性對照表，不會有欄位過期問題）查出選區底下有哪些鄉鎮，再逐鄉鎮呼叫同一函式的`'tc'`分支彙總（`'tc'`分支本身是用`vc.substring(0,8)===tcCode`比對村里代碼前綴，純結構性、不吃`.ed`欄位）。2016分支改成一樣的模式，只是把`TC_STATS`換成`TC_2016`（這個檔案本來就有、key是`舊選區名__鄉鎮代碼`格式，是照2016年真實重劃邊界建的表，不會有過期問題）。

驗證：修復後`getPlVcAggr('高雄市第9選舉區',2016,'ed')`回傳的總票數從近乎0變成198,853票（跟一個正常選區的量級相符），親民黨在地方得票率4.68%；`getPlVcAggr('屏東縣第3選舉區',2016,'ed')`回傳136,831票，親民黨4.35%——兩個數字都低於親民黨2016年5%門檻，正確顯示成低於門檻的深色，不是無資料的灰色。進一步用全部11個2016小黨（時代力量/新黨/綠社盟/台灣團結聯盟/信心希望聯盟/民國黨/軍公教聯盟黨/無黨團結聯盟/樹黨等）逐一跑過`minorPartyMapFill()`，兩個選區在每個黨底下都拿到有意義的顏色（超過門檻的漸層色或低於門檻的深灰，兩種都是正常染色狀態），不再是`#e0e0e0`；也直接呼叫`edColor()`確認跟高雄市第1選舉區這種沒有重劃問題的正常選區回傳同一類型的顏色值。沒有console錯誤。




## v2.76 — 鄉鎮市區／村里層滑鼠hover的黃色框，比照全台選區層跟不分區地圖統一起來

你問能不能把不分區地圖（縣市）模式鄉鎮市區層那個滑鼠移過去會出現的黃色框加到其他5大模式（歷史勝選/政黨熱力圖/綜合PVI/第三勢力PVI/情境推演）——先只問鄉鎮市區層，後續補充「還有村里層」，確認範圍是兩層都要。

查了發現這個檔案裡黃色hover框其實已經有兩套各自獨立的實作，只是沒有涵蓋到鄉鎮/村里層：(1) 全台選區層（`goED()`最上層鳥瞰視角）的`.ft-ed:hover`這個CSS class規則，用`#ffd900`金黃色+`drop-shadow`光暈；(2) 不分區地圖模式的`_addPLTownshipLayer()`，用JS在mouseover/mouseleave裡手動改`stroke`成`#facc15`。但5大模式共用的鄉鎮市區層（`goTC()`/`goTCAllNational()`，都用`.ft-tc`這個class）跟村里層（`goVill()`/`goVillAll()`/`goVillAllNational()`，都用`.ft-vill`）各自的`:hover`規則從一開始就只寫了一個幾乎看不出來的深色描邊（`.ft-tc:hover`是`rgba(0,0,0,.55)`寬0.8px，`.ft-vill:hover`是`rgba(0,0,0,.5)`寬0.6px）——不是bug，是最初這兩層根本沒設計hover視覺回饋，跟選區層/不分區地圖模式的待遇不一致。

`.ft-tc`/`.ft-vill`這兩個class全部渲染鄉鎮/村里的地方（`goTC()`、`goTCAllNational()`、`goVillAll()`、`goVillAllNational()`……）都共用同一份，不用逐一改渲染函式，只要在`style.css`把這兩個class的`:hover`規則換成跟`.ft-ed:hover`同一套視覺語言（`stroke:#ffd900`+雙層`drop-shadow`），全部呼叫點自動套用。村里形狀比鄉鎮小很多，`stroke-width`故意比照原本兩層粗細比例縮小（鄉鎮1.6px、村里1.2px，跟原本0.8px/0.6px的比例一致），不是照抄鄉鎮的數字，避免村里這種細小形狀被邊框蓋過去看不出原本輪廓。

驗證：改完後瀏覽器载入的仍是快取的舊`style.css`（`document.styleSheets`裡讀到的還是舊版`rgba(0,0,0,.55)`規則），對`<link>`的href加cache-busting query重新載入才確認新規則生效——這是瀏覽器快取問題，不是程式碼沒生效。用`document.elementFromPoint()`直接讀游標位置實際命中的DOM元素、確認`matches(':hover')`為true且`getComputedStyle().stroke`是`rgb(255, 217, 0)`，鄉鎮層／村里層／切到`third`模式（不同底色）都測過，沒有跟地圖填色顏色混在一起分不清楚的狀況。

## v2.77 — 連任標籤（×N／新／回鍋／落N）從只有ED選區層，往下補齊到鄉鎮市區層跟村里層

你說v2.73做的連任標籤鄉鎮市區層跟村里層也要一樣有——v2.73當時明確把`showTCPanel()`/`showVCPanel()`排除在範圍外（原因記在v2.73的紀錄裡：只談ED層選區面板），這次補上。

`showTCPanel()`裡2024（現行年度）候選人長條圖是自己獨立一段`_tc24CandBars`，直接有`s.ed`可用，插入方式跟v2.73在`showEDPanel()`做的完全一樣（`isElected`/`isWinnerMode24`算完之後、`return`模板組裝之前塞一行`_legTermBadgeSlot()`）。2020／2016兩個年度比較特殊——`showTCPanel()`這兩個分支不是像`showEDPanel()`那樣各自獨立寫一份renderRow，而是共用同一個外部函式`_compHistCandBarsHtml(candsArr, tot, yr, winnerName)`，原本沒有`ed`參數可以傳給`_legTermBadgeSlot()`算縣市。加一個第5個參數`ed`（呼叫端2020傳`s.ed`、2016傳`_dispED16w`，兩個都是這兩個分支裡本來就有算好的現行選區名稱），函式內部原本的`_showCheck`變數（`!!winnerName`）本來就等同「現在是不是winner模式」，直接拿來當`isWinnerModeThisYear`用，不用另外算。

`showVCPanel()`結構跟`showTCPanel()`類似但沒有共用函式——2020/2016都是各自inline的renderRow（跟`showEDPanel()`比較像），2024是`_vc24CandBars`。三處都比照同樣的模式插入，`ed`分別用`_ed20`（2020分支既有變數）、`info.ed`（2016分支——這個欄位在重劃縣市可能是舊選區名，但`_legCcFromEd()`只取縣市名稱前綴、不管選區編號，舊名稱一樣能正確反查出同一個縣市代碼，不影響結果）、`_vc24EdForWinner`（2024分支既有變數）。

驗證：南投縣第1選舉區馬文君（跟v2.73同一個已知案例）分別在鄉鎮市區層（草屯鎮）、村里層（中正里）的2024模式面板都正確顯示`×3`金色標籤，樣式、位置（`.blb`姓名欄外面的獨立36px flex slot，跟v2.73同一個排版）都跟ED層一致，長條圖沒有跑版或裁切。2020模式（`_compHistCandBarsHtml`共用函式改完後）鄉鎮/村里兩層都測過有正確顯示badge；2016模式維持空白（`_legTermBadgeSlot()`本身就對`yearLabel==='2016'`回傳空的36px佔位div，不用額外處理，跟ED層行為一致）。沒有console錯誤。

## v2.78 — 修復高雄市大寮區光武里2024年資料異常（VC_ED幽靈村里+TC_STATS選區總計被加倍）

你說高雄市大寮區的光武里不是改名，是民國111年（2022年）6月1日因眷村改建、都市計畫導致人口減少，正式被裁併、併入忠義里——2024年（2024年1月選舉，在裁併之後）的資料不應該再有光武里這個獨立村里。

查了先確認council.html的2022年（Nov 2022選舉，同樣在裁併之後）資料本來就是對的——`COUNCIL_2022_VILL`裡完全查不到`64000140024`（光武里的村里代碼），忠義里的2022年得票數（354票）也跟2018年（391票）差不多，沒有異常暴增，合理（人口在正式裁併前就已經因為眷村改建大量流失，到2022年選舉時光武里剩餘選民可能已經所剩無幾）。但test.html的`VC_ED`（2024年立委資料）裡`64000140024`卻還有一筆總票數64,100票的資料——單一村里64,100票是不可能的數字（隔壁忠義里同一年只有305票），細查發現這筆「資料」其實是把大寮區其他24個真實村里的得票總和（也是64,100票，逐候選人拆開比對：林岱樺41,535/陳若翠22,565，兩邊完全一樣）整筆複製貼上塞進光武里這個已經不存在、原始資料查不到的村里代碼裡。

往上一層查`TC_STATS`（鄉鎮層級彙總）裡大寮區（"高雄市第4選舉區__64000140"）的選區總計顯示128,200票——正好是真實總票數64,100票的整數兩倍，逐欄位比對（林岱樺83,070=41,535×2、陳若翠45,130=22,565×2）確認是同一套「整個被加倍」的問題，不是巧合。用真實世界常識交叉驗證：大寮區人口約11萬人，128,200票的投票數已經超過總人口，不可能；而64,100票對應大寮區人口的比例（約58%），跟同選區其他三個行政區（林園區/仁武區/鳥松區）的票數對人口比例（都在58-62%之間）高度一致，反過來確認64,100才是合理數字。往上一層的`ED_STATS`（整個「高雄市第4選舉區」選區總計，涵蓋林園區+大寮區+仁武區+鳥松區四個行政區）本身沒有這個問題（185,262票，正好等於三個正常行政區加上大寮區的正確值64,100加總），代表這個「加倍」bug只出現在大寮區這一個TC_STATS條目，沒有往上污染到選區總計，範圍侷限。

沒有用猜的或用內部數字互相比對就下手改，而是直接查中選會官方原始選舉資料庫（`votedata/votedata/voteData/2024總統立委/區域立委/`的elbase/elcand/elctks/elprof.csv，標準中選會格式）逐欄位核對：elbase.csv裡高雄市（縣市代碼64）大寮區（鄉鎮代碼140）的村里清單只到023、025（跳過024），村里代碼024（光武里）完全不存在——直接證實2024年真實選舉資料裡光武里確實已經裁併消失，不是資料庫漏建，是行政區劃本身就沒有這個村里了。elctks.csv大寮區（縣市64/選區04/鄉鎮140）的選區層級加總列（村里別0000）逐候選人得票：林岱樺41,535票、陳若翠22,565票，總計64,100票，跟修正後的數字完全吻合；忠義里（村里代碼0018）單獨的加總列：林岱樺142票、陳若翠163票、總計305票，跟`VC_ED`裡忠義里現有的資料完全一致，本來就是對的，不用改。elprof.csv同一個彙總列的投票率(69.81%)/無效票率(2.97%)也跟`TC_STATS`裡既有的`tr`/`ir`欄位完全吻合，這兩個是比例值（不是加總的票數），加倍問題不影響它們，維持不動。

修法：(1) `VC_ED`裡`64000140024`這筆整條刪除——光武里在2024年不是「查無資料」（那樣會顯示灰色），是「這個村里已經不存在」，刪掉之後`vcColor()`既有的`if (!info) return '#e0e0e0'`保護邏輯會自動接手，跟其他任何年份合理缺資料的村里一樣顯示灰色，不用額外寫特殊處理。(2) `TC_STATS`裡大寮區那筆的`votes`/`tot`/`gov_n`/`opp_n`四個票數相關欄位除以2改回真實值（41,535/22,565/64,100/41,535/22,565），`dist_gov_pct`/`dist_pvi`/`party_gov_pct`/`party_pvi`/`wpct`這幾個比例值算過confirm加倍前後數學上完全不變（64.8%不管是41535/64100還是83070/128200都四捨五入到同一個值），加上`tr`/`ir`本來就是對的，這6個欄位維持原樣不動，只改真正錯的4個。2014/2018/2020年的光武里村里級資料（本來就有、本來就正確，分別138/140/116票，規模跟忠義里同期347/391/352相近，是真實小村里的合理數字）完全沒有動。

驗證：`VC_ED['64000140024']`現在是`undefined`；`TC_STATS`大寮區條目`tot`確認變成64100、`林岱樺`/`陳若翠`分別41535/22565；忠義里`VC_ED['64000140018'].total`確認維持305沒被誤動；`vcColor('64000140024')`回傳`#e0e0e0`（無資料灰色，不是報錯或顯示殘留舊色）；`showVCPanel('64000140024')`不會拋錯，`showVCPanel('64000140018')`（忠義里）正常顯示含陳若翠的候選人資料；鄉鎮層面板（大寮區）確認不再出現128,200這個數字。沒有console錯誤。

## v2.79 — 修復臺東市11個已裁併村里的幽靈資料（VC_ED）；順便發現全臺東市村里層資料嚴重低估

你列了一份臺東市（臺東縣臺東市）11個已經被裁撤、整併進其他村里的舊村里名單（民權里→文化里、四維里→中華里、鐵花里/興國里→中山里、仁愛里/大同里→中正里、復興里/復國里→成功里、強國里→建國里、富豐里→富岡里、建業里→分別併入建興里及知本里），另外還有2個之後才拆出新村里（新生里拆出仁和里、豐榮里拆出豐安里/豐盛里）先擱著晚點處理。

查中選會2024年立委原始資料（`elbase.csv`）確認這11個舊村里名稱在縣市代碼10、鄉鎮代碼014、村里清單裡完全查無記錄——2024年1月選舉時這些行政區劃確實已經不存在，跟你列的名單完全吻合；同時確認新生里、豐榮里（未來會拆分的那兩個村里）本身依然存在，還沒拆分，跟你說「先擱著」的判斷一致。

比對`VC_ED`裡這11個舊村里代碼的資料，發現一個比光武里更明顯的錯誤：11筆資料的候選人得票、總票數（都是3839）、甚至`gov_n`/`opp_n`這種選區層級才該出現的巨大數字（23624/14227，遠超過各自申報的總票數3839，數字本身就兜不起來）全部一字不差、完全相同——不是像光武里那樣「複製別的村里湊數」，比較像是某個乘載「找不到這個村里、用一個固定佔位值填補」邏輯的副作用，11個空缺全部套用同一組寫死的數字。

驗證這個假設時，用其他完全沒有牽涉裁併、名字沒變過的村里（馬蘭里/寶桑里/康樂里）當對照組，本來預期會是正常資料，結果查中選會原始逐投開票所資料（`elctks.csv`村里彙總列）才發現：這幾個「乾淨」的村里在`VC_ED`裡的數字也一樣是錯的——馬蘭里真實得票1,282票，`VC_ED`裡只有121票；寶桑里真實715票，只有68票；康樂里真實714票，只有67票，全部低估到只剩真實值的一成左右，而鄉鎮層級的`TC_STATS`（46,073票）本身反而是對的。合理推測：臺東市整個村里層的得票分配這一步驟有系統性問題，大部分票數（約42,000票）沒有正確分配到各村里，其中一部分被錯誤地整批塞進這11個「查無此村里」的空位（前述固定數字的由來），其餘34個左右的「真實」村里也只拿到一小部分、遠低於實際數字。

跟你確認過範圍：這次只先處理你原本要求的「11個已裁併村里不該出現」這件事——刪掉這11筆幽靈資料，做法跟v2.78光武里同一套（整條刪除，不留任何佔位值，讓`vcColor()`既有的無資料灰色`#e0e0e0`邏輯接手）。臺東市其餘30幾個真實村里普遍低估的問題範圍大得多（需要逐村里重新解析中選會原始投開票所資料才能修正），你說晚點再另外處理，這次沒有動。

驗證：11個村里代碼（`10014010018`等）在`VC_ED`裡現在全部是`undefined`；沒被動到的馬蘭里（`10014010010`）維持原本121票（已知偏低但這次故意不動）；`TC_STATS`臺東縣選舉區的鄉鎮層總計維持46073不變（這次的錯誤只在村里層，鄉鎮層本來就是對的，不用改）；另外交叉確認council.html的2022年村里資料（`council_2022_vill.js`）本來就沒有這11個幽靈村里（Nov 2022選舉本來就在裁併之後，資料源頭正確），2014/2018年資料（`council_2014_vill_data.js`/`council_2018_vill_data.js`）則正確保留這11個村里、且各自資料彼此不同（不是同一組複製貼上的假資料），確認council.html完全不用改動。沒有console錯誤。

## v2.80 — 已裁併村里2024年地圖跟著目標村里一起染色，不再是無資料灰色破洞

v2.78/v2.79把光武里、臺東市11個已裁併村里在`VC_ED`裡查無資料的假資料清掉之後，這些村里在2024模式的地圖上變成無資料灰色`#e0e0e0`——你說這樣看起來像地圖上破了一個洞，這塊地理範圍明明已經併入隔壁村里，應該跟著隔壁一起染色，不是顯示成缺資料。

地圖形狀（VD拓樸）本身是現行行政區劃的靜態快照，不會因為切換年份而增減村里形狀，所以光武里這塊形狀在2024模式底下還在，只是查無2024年自己的選票資料。解法是幫這些已裁併村里的`VC_ED[vc]`補上一份目標村里資料的淺拷貝（`{...VC_ED[targetVc]}`，不是共用同一個物件參照，避免任何潛在的原地修改互相影響）——這樣`vcColor()`/`showVCPanel()`/`getPlVcAggr()`等所有原本就是直接讀`VC_ED[vc]`的地方完全不用逐一修改，會自動吃到目標村里的染色跟候選人資料；村里自己的名稱（`NM.v[vc]`，來自另一張獨立的表，沒有被動到）維持顯示原本名稱，不會被覆蓋成目標村里的名字。

臺東市建業里比較特殊——民國111年戶籍整編時分別併入建興里跟知本里兩個目標，但地圖形狀只有一塊，沒辦法真的切成兩半分屬兩個顏色。跟你確認過，選規模略大的知本里代表整塊形狀的染色/資料來源。

只補資料本身還不夠——如果使用者滑到這些村里、看到跟隔壁一模一樣的顏色跟候選人數字，卻沒有任何說明，會誤以為是程式又出了新的資料錯誤（跟這次連續兩輪修復的bug同一種既視感）。在`showVCPanel()`外面包一層：面板渲染完之後，只要這個村里代碼在裁併清單裡，就在最上面插一句橘色提示「此村里已併入「OO里」，2024年地圖染色／候選人資料沿用「OO里」」。刻意包在外層而不是鑽進`showVCPanel()`內部十幾個分支各自加判斷——原函式依`winnerYear`/`compYear`/`mode`分好幾個分支，要在正確的分支插入提示很容易漏掉某個分支，外層包裝不管哪個分支渲染都一定會補上這句話，而且這句提示本身在任何年份都是事實陳述（不管現在看的是2024還是2016年資料，這個村里「已經併入OO里」都是真的），不會因為顯示的是舊資料而失真。

驗證：`VC_ED['64000140024'].total`跟`VC_ED['64000140018'].total`兩者現在都是305（完全複製，非巧合）；`vcColor()`在2024模式對這12組（含光武里）配對回傳的顏色逐一比對後完全相同；切到2020模式後再比對同一組配對，回傳顏色正確變回不同（光武里v20總票數131、忠義里375，各自維持原本歷史資料沒被覆蓋，證實只有2024年（`VC_ED`）被複製，2020/2016年資料完全沒被動到）；`showVCPanel('64000140024')`渲染出的面板確認在最上方看到橘色提示框，內容正確寫「此村里已併入「忠義里」」，下方候選人得票數字（國民黨53.4%最多票）跟忠義里自己面板顯示的數字一致。沒有console錯誤。

## v2.81 — 重建臺東市全部35個真實村里的2024年得票資料（不再是普遍低估到剩約十分之一）

v2.79發現臺東市11個已裁併村里以外，其餘真實村里的得票也普遍被低估到只剩真實值的一成左右（見v2.79紀錄裡的對照組驗證：馬蘭里真實1,282票、原本只有121票）。你說先重建臺東市（不假設全國其他縣市也有同樣問題，那些之後再另外抽驗）。

直接分析中選會官方逐投開票所資料（`elctks.csv`村里彙總列，跟這次連續好幾輪修復用的同一套解析方式）拿真實票數，逐村里比對確認：全部35個真實村里（46個原始村里代碼扣掉v2.79已刪除的11個已裁併村里）加總得票剛好是46,073票，跟`TC_STATS`鄉鎮層總計（已確認正確，v2.78就核對過）完全吻合，逐一抽查（馬蘭里1,282、寶桑里715、康樂里714、富岡里833、文化里876）也跟v2.79驗證時查到的原始資料一字不差。

處理原始資料時額外發現：中選會現行(2024年)村里清單裡其實有46個「舊」村里代碼以外的3個新代碼——仁和里(0047)、豐安里(0048)、豐盛里(0049)，正是你先前提到「先擱著晚點處理」的分割增設村里（新生里分出仁和里、豐榮里分出豐安里/豐盛里）。這3個確實有自己的真實得票，但test.html的地圖拓樸（VD）跟名稱對照表（NM）都還沒有這3個村里的形狀，沒辦法真的畫出獨立區塊。刻意不建立這3個代碼的獨立VC_ED條目（建了也沒有地圖形狀可以顯示、變成永遠不會出現的死資料），改成把仁和里的票數併回新生里、豐安里/豐盛里的票數併回豐榮里——這兩個舊代碼在現行地圖上還是單一未分割的形狀，維持這樣才不會漏算這兩塊區域真正的完整得票（如果直接丟棄仁和里等3個新代碼的票，新生里/豐榮里反而會變成新的「低估」案例，等於在修A bug的同時製造一個新的同類bug）。

`gov_n`（比較兩大黨基準用的「本土/在野」二分欄位）/`dist_pvi`兩個衍生欄位沒有直接照抄票數比例算，而是先拿同選區（臺東縣選舉區）另外兩個沒有壞掉的鄉鎮（成功鎮/關山鎮）村里當對照組反推公式：opp_n精確等於黃建賓(國民黨)得票；gov_n精確等於賴坤成(民進黨)+劉櫂豪(無黨籍但這個選區慣例併入本土陣營計算)兩人得票加總——用兩個候選人權重明顯不同的對照村里各自驗證過，不是巧合湊出來的。dist_pvi則是gov_n/(gov_n+opp_n)百分比減去一個反推出來的全國基準值（三個對照村里反推結果一致落在50.73，比直接讀當下`_NAT24_DIST_2P`更準——後者是即時從全部VC_ED加總算出來的，這個選區資料本身還沒修好之前拿current live值來對照本身就會有偏差）。`party_pvi`（不分區政黨票版本的PVI）嘗試比對VC_SIM的政黨票資料反推公式，三個對照村里反推出來的隱含基準值不一致（40.2/43.3/43.6，不像dist_pvi那樣三個吻合到小數點後兩位），沒有把握確定正確公式；你說寧可跟dist_pvi設成一樣的值，也不要繼續用「用錯誤票數算出來的party_pvi」（反正原本上線的數字本來就是根據錯誤票數算出來的，本來就不準，改成跟dist_pvi一樣至少不會比現狀更糟，也不會留一個看起來煞有其事、實際上公式沒把握的數字）。`tr`（投票率）/`ir`（無效票率）改成用真實選舉人數/有效票數重新計算，不是簡單沿用村里彙總列上已經四捨五入過的百分比欄位（尤其是新生里/豐榮里這兩個因為併入分割村里票數而需要跟合併後的真實選舉人數重新對齊分母）。

驗證：重建後全部35村里加總46,073票，跟`TC_STATS`鄉鎮層總計完全吻合（`sumUnique`排除v2.80新增的12個併村複製條目後精確等於46073，沒有算重複）；新生里(`10014010007`)確認吃到仁和里併入的票數後總票數變成3,665（原本被低估的舊數字更小）；`10014010047`（仁和里）確認`VC_ED`裡沒有這個key（刻意不建立，等地圖拓樸支援後再處理）；地圖切到2024年、全選區村里總覽檢視臺東縣選舉區，各村里顏色亮度呈現正常依票數多寡分布的漸層（不再是v2.79修復前那種一片相同色塊或後來的無資料灰色破洞）。沒有console錯誤。

## v2.82 — 新模式：政黨基本盤（跨屆，混合2016/2020/2024三屆估算）

你問可不可以做一個獨立mode估算各政黨的基本盤，經過幾輪釐清定案：混合三屆（2016/2020/2024）「區域候選人得票」跟「政黨票」，用意是濾掉單一屆的短期波動（換候選人、單一政治事件），抓出比較穩定的長期基本盤。分類邏輯你後來加碼成兩軸：區域票領先誰、政黨票領先誰——兩邊一致才依混合margin分深/中/淺/搖擺/完全搖擺，兩邊不一致（分裂投票）獨立標記，不管margin多大都不算「伯仲之間」，因為那代表選民本來就是分開投的。

**資料上限制，先講在前面**：2016/2020的政黨票只有選區(ED)層級資料，沒有村里層級（2024的VC_SIM才有村里級`pl_g/pl_b/pl_w/pl_o`），所以這個模式只能做到選區層級，比其他政黨熱力圖模式（可以到村里）粗——不是刻意簡化，是資料本身的極限。TPP（民眾黨）2019年才成立，2016年這個政黨根本不存在，三年平均遇到2016直接跳過該年，不會誤算成0%（算0%等於誤把「政黨不存在」當成「政黨支持度是0」，會不公平拉低它的基本盤）。總統票（你另外問的）目前還沒接進來——2024的`pres_2024`資料確實存在且已載入(`data_edb.js`)，但2016/2020的總統票資料還沒確認，先擱置，之後再處理。

核心函式：`classifyBaseLean(ed)`（`_baseLeanRaw`讀三屆原始票數→`_baseLeanAvg`逐年正規化後跨年平均→回傳`{type:'lean'|'split', party, tier, margin, ...}`）、`_baseLeanColor(ed)`（分類轉顏色，深/中/淺用`_mixWhiteHex()`淡化黨色，搖擺/完全搖擺灰階，分裂投票用跟跨屆比較彈窗一致的紫色`#b06bff`，避免跟任何黨色混淆）。新增左側工具列「政黨基本盤（跨屆）」按鈕，`setMode('baselean')`；`edColor()`最前面加一行攔截（這個模式完全不看`winnerYear`，放在2016專屬分支之前，不然會被攔截進不來）；`window.isHeatmap`判斷式加入`'baselean'`（沒加之前地圖染色函式雖然算對了，但`.ft-ed`的fill attr被`isHM`門檻卡住，整層地圖還是顯示成統一深色`#16161c`——花了一輪單獨debug才抓到）。

**過程中抓到兩個真的bug，都是自己第一版verify時發現的**：(1) 政黨票欄位命名沒有跟區域票軸統一（來源資料是`g/b/w/o`，區域票軸用的是`dpp/kmt/tpp/other`），導致`_baseLeanAvg()`依`dpp/kmt/tpp/other`這幾個key去平均政黨票軸時全部讀不到值、變成全部是0——連帶讓每個選區都被誤判成「分裂投票」（因為`Object.entries({dpp:0,...}).sort()`在全部平票時穩定回傳插入順序的第一個`'dpp'`，巧合式地讓一部份選區看起來「對」，其實只是猜對）。(2) 上面提到的`isHeatmap`門檻漏掉新模式，色彩算對了但畫不出來。兩個都在同一輪debug對照已知選區（臺南市第1選舉區=深綠鐵票區、花蓮縣選舉區=深藍鐵票區）驗證時發現。

**選區資訊面板**（`_baseLeanPanelHtml`）：點選任一選區顯示分類headline（例：「深民進黨，三屆混合平均領先30.4pt」，分裂投票則顯示「區域票領先X／政黨票領先Y」）＋逐屆逐軸明細表（2016/2020/2024×區域票/政黨票，各自顯示領先政黨跟得票率），底部附資料侷限性說明。

**全台總覽分類統計**（`_baseLeanNationalSummaryHtml`）：你後來加碼要「算出每一分類的票數range」——切進這個模式時，全台總覽面板改顯示一張表：每個分類（深綠/中綠/淺綠/…/分裂投票）有幾個選區、這些選區「區域+政黨票混合平均」得票率的最低～最高range。這個表原本設計成`goED()`回到全台總覽時才刷新，實測發現直接點模式按鈕切換（不觸發`goED()`）不會更新，面板停留在舊模式殘留文字——後來發現`setMode()`同一輪呼叫裡後段還有邏輯會覆寫`#ic`的內容，直接同步寫入會被蓋掉，改用`setTimeout(fn,0)`延到下一個事件迴圈執行（並在執行時重新檢查`mode`還是不是`'baselean'`，避免使用者手速快、這段延遲觸發前又切到別的模式時蓋錯面板）才穩定生效。

**新竹縣第2選舉區2016年資料缺口**：你回報這個選區顯示不出2016年數據。查證後發現不是bug，是真實的選區重劃——2016年新竹縣還沒有分成「第1/第2選舉區」，`ED_2016`裡只有籠統的「新竹縣選舉區」一個key（同樣情況全國還有臺南市第6選舉區，共3個選區受影響）。你提議「用2020的範圍抓2016 data」——改成`ED_2016[ed]`查無資料時，改用村里級`VC_2016`資料＋現行(`VC_ED[vc].ed`)的村里→選區對照表反推：抓出屬於這個選區（依現行劃分）的每個村里，把各村里2016年村里級candidate票數加總，等於「如果照現在的選區重劃方式回推，2016年這個範圍會開出的票」。政黨票軸沒有對應的村里級2016資料可以這樣反推，這3個選區的2016年政黨票仍然是缺值（跳過該年，不影響平均，只是樣本少一年）。驗證：新竹縣第2選舉區`_baseLeanRaw()`的`dist.y2016`從null變成有實際數字。

驗證：對照兩個已知選區（臺南市第1選舉區深綠、花蓮縣選舉區深藍）分類結果跟現實認知吻合；地圖染色呈現有意義的空間分布（南部整片綠、東部/北部部分藍、都會/競爭區塊分裂投票的紫色群聚，符合對台灣政治地理的既有認知）；選區面板、全台總覽分類統計表、圖例三處UI都截圖確認正常顯示。深/中/淺（20/10/5/2pt）門檻是預設值，還沒讓使用者實際看過所有選區分布後微調；總統票（2016/2020）整合、圖例文字最後潤飾留待下一輪。

## v2.83 — 政黨基本盤加碼：每個分類的估算票數＋Sankey流向圖

延續v2.82，你要求把每個分類的「範圍」進一步換算成實際票數估算，甚至做一張Sankey圖。

**估算票數**：`_baseLeanEdTotalVotes(ed)`彙總`VC_ED`拿到每個選區2024年實際區域票總票數（真實數字，不是虛構），「估算」只發生在下一步——把三屆混合算出的領先政黨得票率(%)套用在這個真實的2024總票數上，同分類底下所有選區加總，得到「深民進黨15個選區估算約180萬票」這種具體數字。分裂投票／搖擺／完全搖擺這幾類沒有單一政黨可以套用估算（分類定義本身就是「沒有哪個黨獨占優勢」），改顯示這些選區2024年實際投票總數（標「共」字首，跟其他列的政黨估算票數區隔開，不要讓人誤以為那也是某黨的得票估算）。全台總覽分類統計表新增第4欄「估算票數」。

**Sankey流向圖**：`_baseLeanSankeySvg()`，左欄＝各選區「區域票軸」領先的政黨、右欄＝「政黨票軸」領先的政黨，流量寬度＝該組合涵蓋選區的2024實際總票數加總。同黨對同黨（左右一致，例如區域票跟政黨票都是民進黨領先）的直向流用該黨自己的顏色；跨黨（分裂投票，例如區域票領先國民黨但政黨票領先民眾黨）的斜向流一律用分裂投票的紫色`#b06bff`——不用等看文字列表，一眼就能看出哪些政黨組合之間的分裂投票量最大。手刻SVG（沒有用D3 sankey plugin），沿用council.html`_renderSourceSankeySvg()`同一套「手算節點高度比例＋三次貝茲曲線畫流帶」的技法，但那邊是單一來源（民調vs歷史)固定2條帶子流向1個節點，這裡是4×4的一般化版本，需要為每個節點各自維護一個「目前已經畫到哪個高度」的游標，按固定政黨順序（dpp/kmt/tpp/other）依序疊加每個流入/流出的區段，避免同一節點的多條流帶互相重疊蓋住。插入全台總覽面板，接在分類統計表下方。

驗證：全台總覽面板截圖確認估算票數欄位顯示合理數字（深民進黨約180萬、分裂投票標「共」+實際總票數317萬）；Sankey圖截圖確認呈現預期樣式——民進黨/國民黨兩條粗的同色直向流帶（基本盤穩定的大宗），中間夾雜可見的紫色斜向流帶（分裂投票），節點大小跟流帶寬度比例看起來合理，沒有節點溢出或流帶跑到畫布外的情況。

## v2.84 — 政黨基本盤三連發：時代力量獨立分類、逐縣市明細、跨屆Sankey

同一輪對話裡你陸續加了三個要求，因為都動到同一段核心分類邏輯，合併一次做完。

**1. 時代力量(NPP)獨立列為第四個追蹤政黨，不再併入「其他」。** 區域票軸三屆都有完整npp標記資料（`KNOWN_PARTY_2016`/`ED_2020`/`VC_ED`的候選人`p`欄位都能標到npp），直接比照tpp的做法追蹤。政黨票軸只有2016年有拆開的npp數字（`REAL_PL_2016`本來就有），2020/2024的來源（`PARTY_VOTES_2020`/`VC_SIM`）只有g/b/w/o四桶，npp的票混在「其他」裡拆不出來——這裡刻意不虛構0%去湊，而是把`_baseLeanAvg()`的平均邏輯從「整個軸共用同一組有效年份」改成「逐政黨各自判斷哪些年份真的有資料再平均」，npp在2020/2024政黨票軸就直接跳過那兩年，只用2016年真實的單一數字，不會被虛構的0%稀釋。新增`BASE_LEAN_PARTIES`共用常數陣列取代原本四處重複寫死的`['dpp','kmt','tpp','other']`，`BASE_LEAN_PARTY_HEX`/`_LABEL`/`_GROUP_ORDER`跟圖例都補上npp（深/中/淺時代力量，黃色`#FBBE01`跟這個app其他地方用的npp代表色一致）。副作用（預期中，非bug）：npp在全台總覽目前實際上不會出現「深/中/淺時代力量」這個分類——因為分類規則要求區域票軸跟政黨票軸領先同一黨，而npp的政黨票軸幾乎總是被歸進「其他」，兩軸幾乎不會同時判給npp，npp支持度強的選區多半會被分類成「分裂投票」而不是「深時代力量」。這是分類設計本身合理的結果，不是要另外修的東西。

**2. 全台總覽面板改成逐縣市展開，不再只有一個全國混合數字。** 你先前確認過「這是全台灣的，我要的是22縣市各自的」——原本的分類表/票數估算/Sankey全部只做了全國彙總一份。改成`_baseLeanSummarize(edList)`/`_baseLeanSankeySvg(edList)`都接受可選的選區清單參數（不傳就是全國），面板結構變成：最上面留一份全國總覽當快速參考，下面用原生`<details>`逐縣市展開（22個，預設收合，不用額外寫JS開合邏輯），每個縣市自己的區塊有：分類統計表（該縣市內幾個選區屬於哪個分類＋range＋估算票數）、分類票數長條圖（下面第3點）、跟該縣市自己範圍內的區域vs政黨票Sankey。

**3. 分類票數長條圖，比照council.html mayor 2026預測模式的視覺慣例。** 你指定「跟2026縣市長類似」——找到`_mcRangeRowsHtml()`（範圍帶+中位數刻度+左右端點百分比數字那套視覺語言），新增`_baseLeanRangeBarsHtml(groups)`套用同一套呈現方式，只是這裡的「範圍」不是蒙地卡羅模擬的10-90百分位，是該分類所有選區三屆混合平均得票率的最低～最高值，中間刻度用平均值（不是中位數，跟原版語意不同但視覺呈現一致）。分裂投票/搖擺類沒有單一政黨的%可以畫條，這幾類在長條圖裡直接跳過（表格裡還是看得到，只是不會出現在長條圖）。

**4. 跨屆Sankey：2016→2020→2024政黨版圖流向。** 你要求「顯示三個cycle分類的流向」——這是全新的第二種Sankey，跟原本那張（同一屆內部區域票vs政黨票吵不吵）是不同的故事：這張看的是「同一個地方隨時間怎麼變」。新增`_baseLeanYearLeader(ed, year)`算出單一選區單一屆自己的領先政黨（同一屆內一樣混合區域+政黨票各半，只有一軸有資料就退回那一軸），再用`_baseLeanCycleSankeySvg()`畫三欄式Sankey（2016→2020→2024），權重統一用該選區2024年實際票數（歷屆確切選民規模不完全可比，用現行規模當固定份量比較有意義）。中間欄（2020）身兼「左邊轉場的終點」跟「右邊轉場的起點」兩個角色，各自維護獨立的游標追蹤畫到哪個高度，不會互相干擾。同色直向流＝這個地方跨屆領先政黨沒變，橘色（跟分裂投票的紫色刻意用不同顏色區分語意）斜向流＝換黨；任一端缺資料的選區（該屆查無這個選區名稱），那個轉場直接跳過，不畫假流向。這張圖放在全國總覽層級（不是逐縣市，時間跨度的意義在全國層級比較完整，縣市內部樣本太小畫三欄Sankey意義有限）。

驗證：整個面板改版後在瀏覽器裡測過一輪確認沒有JS錯誤——22個`<details>`區塊（對應22縣市）、24個SVG（22個縣市各自的Sankey+2個全國層級的Sankey，數字對得上）；展開南投縣（2個選區）確認顯示正確的縣市範圍分類表（淺國民黨2選區、45.2-46.4%、估算123,458票）跟對應的長條圖（範圍帶+平均值刻度）；圖例確認新增深/中/淺時代力量三個黃色色塊。總統票Sankey你自己也還沒想清楚要怎麼呈現（提到「總統、立委兩個獨立」的想法），先不猜測動工，留待下一輪確認方向。

## v2.86 — 政黨基本盤(baselean)下放到村里層

延續v2.85的縣市/鄉鎮兩層設計（`_base5PLAggrFor(geoKey, year)`分派縣市名字串/8碼鄉鎮代碼），新增第三層：村里(11碼vc代碼)。跟council.html這次同步在做的縣市長base model比對是同一批底層函式移植過去用的，但村里層這件事test.html自己原本也沒做過（`getTCPLAggr`本來就有，`getVillPLAggr`是全新補的）。

**`getVillPLAggr(year)`**：不像縣市/鄉鎮那樣需要加總——`PL08_VC`/`PL_VC_2020`/`PL_VC_2024`等原始資料本來就是逐村里(vc)key，只要對2016年做g/b→dpp/kmt正規化、直接回傳村里自己那筆記錄即可，比`getTCPLAggr`簡單。`_base5PLAggrFor`依序試縣市→鄉鎮→村里三張表，三種key格式（中文字串/8碼/11碼）互不衝突。

**「空氣人」也下放到村里層。** `_base5RealElig2024(geoKey)`原本鄉鎮層要在`TC_STATS`裡找`ed__tc`複合key、可能要跨多個選區加總才能拿到真實投票率/廢票率/總票數反推選舉人數；村里層更簡單——`VC_ED[vc]`本身就直接帶這個村里2024年區域立委自己的`tr`/`ir`/`total`欄位，一個村里只會屬於一個選區，直接查表不用像鄉鎮層那樣多筆加總。`_base5Elig2024IsDerived`不用改（`PRES_TURNOUT_COUNTY['2024'][geoKey]`對vc碼一定查不到，本來就會正確落到「反推」分支）；`_base5EligGapRowHtml`裡「空氣人：無法估算」的原住民鄉鎮警語跟「（選舉人數為推算值）」標籤，改成依`geoKey.length===11`動態切換「本村里/This village」還是「本鄉鎮/This township」措辭。

**地圖／面板／導覽**：`_baseLeanVillPanelBody`/`Html`完全比照鄉鎮層的`_baseLeanTownPanelBody`/`Html`（13分類detail/camp3/camp4彙總、空氣人缺口列全部沿用同一套渲染），一樣不顯示總統交叉比對Sankey（跟鄉鎮層同一個理由：`PRES_COUNTY_2008/2012`從頭就只有縣市級官方數字，村里層缺得更徹底，不要假裝有精確度）。新增`_baseLeanGoVillage(tc)`/`_baseLeanBackToTowns()`：直接用`VD.objects.villages.geometries`裡屬於這個鄉鎮的原始村里形狀個別畫出來（不像鄉鎮層要`topojson.merge()`疊合，村里本來就是VD最小原生單位，不用合併），`_baseLeanGoTown()`原本鄉鎮形狀只有`mouseover`沒有`click`，補上點擊進村里層的handler。

驗證：南投縣→草屯鎮→27個村里形狀個別渲染、顏色依各自分類正確著色；玉峰里面板內容（深/中/淺民進黨/國民黨/民眾黨明細+空氣人缺口列）跟直接呼叫`_baseLeanVillPanelHtml('10008030001')`比對一致；detail/camp3/camp4三態切換、`_baseLeanBackToTowns()`回上一層都測過無誤；全國抽樣336個村里（每23個取1個）呼叫`_baseLeanVillPanelHtml`零錯誤，其中300個正常顯示空氣人缺口、35個正確顯示「無法估算」（原住民鄉鎮村里，符合預期）。

## v2.87 — 地板/天花板年份從「全國固定一組」改成「每個地理範圍各自找自己的」

你問鄉鎮市區/村里層能不能各自獨立算地板/天花板年（不要固定套全國那組），先在瀏覽器裡直接跑數字驗證再決定要不要真的改：拿council.html的縣市長催出率當量測工具，比對高雄市38個鄉鎮「用全國年份」vs「用自己局部年份」算出來的陳其邁催出率。結果：23/38鄉鎮兩種算法選到完全一樣的年份（跟全國趨勢差不多的地方，改不改都一樣）；但楠梓區/橋頭區/仁武區/左營區/鼓山區這幾個持續成長區明顯不一樣——全國DPP天花板年固定在2016（全國歷史最高的那屆），但這幾個鄉鎮2008一路成長到2024，2024票數早就超過2016，套全國天花板年會低估這些地方自己真正的上限（楠梓區催出率106.9%→修正後104.0%）。偏遠山區/離島（茂林/桃源/那瑪夏那種288-369%催出率）改成局部年份沒有明顯改善——那些是分母(基本盤票數)本身太小的問題，不是年份選錯的問題。

**改法**：`baseLean5FloorCeil(party)` → `baseLean5FloorCeil(geoKey, party)`，原本掃22縣市加總找全國最低/最高的那一屆，改成只看這個geoKey自己（縣市/鄉鎮/村里，經`_base5PLAggrFor`通用分派）5屆裡票數最低/最高的那一屆。覆蓋率只會變好不會變差——舊版需要「剛好是全國那兩個特定年份都有資料」，新版只需要「任兩個有資料的年份」。

**全國總覽面板的年份標籤怎麼處理**：原本`baseLean5FloorCeil(party)`那個全國加總版本保留下來、改名`baseLean5NationalFloorCeilLabel(party)`，只給`_base5NationalRowsHtml()`的年份標籤顯示用——因為全國總覽現在的deep/mid/light數字是22縣市「各自局部年份」segments加總，已經沒有一組「全國共同年份」可以誠實對應那串數字了，標籤改顯示「（各縣市各自地板/天花板年）」而不是硬湊一組可能跟實際計算對不上的年份。縣市/鄉鎮/村里面板則直接讀`seg.floorY`/`seg.ceilY`（`baseLean5PartySegments`回傳值裡本來就有），不用另外呼叫`baseLean5FloorCeil`。

下游所有消費者（`baseLean5PartySegments`、`_base5CountyAtoms`、`classifyBaseLeanCounty`、`_base5CountyCampSegments`等）都是透過`baseLean5PartySegments`間接讀年份，只有這一個call site真的需要改，其餘全部自動連動生效。council.html有自己一份移植過去的複本，同步套用同樣的修正（council.html沒有顯示年份標籤的UI，不需要那份national label函式）。

驗證：22縣市+全國抽樣155個村里呼叫面板零錯誤；全國總覽面板的「各縣市各自地板/天花板年」標籤正確顯示；直接查`baseLean5PartySegments('64000040','dpp')`（楠梓區）確認`ceilY`從2016變成2024；council.html端同步驗證（22縣市市長面板+194個抽樣村里+74個抽樣鄉鎮+22縣市議員面板）零錯誤，楠梓區市長面板畫面截圖確認催出率從106.9%修正成104.0%、理論上限票數從舊的2016年數字變成正確的2024年46,838票。

## v2.85 — 政黨基本盤全面重寫：從「2016-2024三屆ED層級混合區域+政黨票」換成「2008-2024五屆縣市層級純政黨票地板+回升分類」，新增三張獨立Sankey＋逐政黨明細表

你的構想：政黨票（不分區）當基本盤指標，KMT在2016低潮期還留住的票算深藍（地板），2020/2024回升的部分跟2008/2012（KMT較強年份）的高點對照分出中藍/淺藍，同理套用到DPP/TPP/NPP，再逐縣市算出每種分類的票數range+percentage，另外做區域立委跟總統的Sankey，也要有政黨票的Sankey。整個v2.82-2.84的「政黨基本盤」面板（分類表/長條圖/Sankey）全部換成這套新設計，v2.84結尾留的「總統票Sankey怎麼呈現」懸案這次也一併解決。

**資料層：2008/2012兩屆的縣市層級資料原本完全不存在，新增`parse_2008_2012.py`解析中選會原始CEC資料。** 在`votedata/votedata/voteData/`下找到`2008立委`（不分區政黨/總統/區域）跟`20120114-總統及立委`（不分區政黨/總統/區域立委）的原始`elbase/elcand/elctks.csv`，格式跟現有腳本（`build_mayor_tc.py`等）用的標準CEC格式一致，縣市層級加總列（town='000', village='0000'）中選會本來就有算好，不用自己逐村里加總。老代碼（2008是六都合併前的舊制，臺北縣/臺中市+臺中縣/臺南市+臺南縣/高雄市+高雄縣分開；2012已經是合併後）用縣市「名稱」（不是數字代碼，兩個年代數字代碼完全不相容）對應到現在的縣市名稱（`edToCounty()`用的格式，"臺"已轉「台」）。**政黨票(不分區)這部分後來發現完全不用自己解析**——test.html裡`getCountyPLAggr(year)`本來就已經支援`yr===2008/2012`分支，讀的是已經存在、村里代碼已經對到現行VC_ED的`PL08_VC`/`PL12_VC`（7381/7463筆，涵蓋率跟既有PL_VC_2020/PL16_VC的已知缺口同一量級，不是新問題）。真正新增的只有總統跟區域立委兩種資料，各22縣市，寫死成`PRES_COUNTY_2008/2012`、`ED_COUNTY_2008/2012`四個const（縣市層級%+總票數，不需要每次動態算，2008/2012本身不會再變動）。驗證：跟自己手動用`PL08_VC`/`PL12_VC`重新彙總的縣市層級數字互相對照（例如台北市2008 kmt=50.54%/dpp=33.15%完全一致），跟真實歷史常識比對（2008馬英九大勝、2012馬蔡對決、金門/連江因為藍綠常年不提名區域立委跑出高比例「其他」）全部吻合。

**分類公式：「地板+回升比例三分位」，不寫死年份，每個政黨自動抓自己的地板年/天花板年。** `baseLean5FloorCeil(party)`掃過該政黨「真的有資料」的每一屆（tpp只從2020起、npp只從2016起，不存在的年份不會被當成0%去稀釋），算全國得票率取最低/最高的一屆——KMT地板自動抓到2016、天花板2008，DPP方向相反（地板2020、天花板2016，實測結果，不是原本猜測的2008/2012，比手動猜對的自動偵測價值就在這裡）。`classifyBaseLeanCounty(county)`是「贏家版」——先看該縣市2024政黨票哪黨最高（margin<2pt算完全搖擺、<5pt算搖擺），贏家的2024%落在自己地板~天花板範圍的哪個三分位決定深/中/淺。**踩到一個關鍵bug並修正：如果2024自己剛好是某政黨的全國地板或天花板年（tpp還在成長、npp還在衰退，2024正好是各自的極值年），分母(ceil-floor)每個縣市都會退化成「該縣市2024的值-自己」=0，讓ratio被算式強制卡在0或1，22縣市100%全部變成同一個tier，完全看不出縣市間差異**——解法是`baseLean5FloorCeil`只從2008/2012/2016/2020四屆(排除2024本身)挑地板/天花板候選年，2024永遠只當「目前回升到哪裡」的觀察年，不會自己當自己的地板/天花板。

**沒有「分裂投票」分類了。** 舊版混合區域+政黨票兩軸才會有「兩軸領先不同黨」這種情況，新版只有政黨票單一軸，你在這次對話中途也主動要求拿掉這個分類——地圖/面板/圖例全部確認沒有殘留。

**逐政黨獨立分類（4黨×22縣市=88組），不是只看贏家。** 你回頭要求「記得要做每一分類」，因為`classifyBaseLeanCounty()`只挑贏家會讓TPP/NPP永遠是0個縣市（政黨票在全台22縣市沒有一個贏過藍綠）——新增`classifyBaseLeanCountyParty(county, party)`，每個政黨在每個縣市各自獨立套用同一套地板+回升三分位，不看誰是該縣市的贏家，不設搖擺/完全搖擺門檻（那是「贏家」概念，套用在單一政黨自己分類上沒意義）。`_base5SummarizeByParty()`彙總這88組，同一個縣市會同時出現在多個政黨分類底下（縣市數加總不等於22是預期行為）。全台總覽面板現在有兩張表：原本贏家版（跟地圖染色邏輯一致）+ 新的逐政黨版，逐政黨版的長條圖沿用`_base5RangeBarsHtml()`（跟council.html mayor 2026預測模式`_mcRangeRowsHtml()`同一套範圍帶+刻度+端點數字視覺語言，跟v2.84的做法一致）。

**逐政黨版揭露兩個誠實但不亮眼的真相，刻意不用造假變化去掩蓋：** (1) **TPP因為只有2020一屆(排除2024後)可以當地板/天花板候選年，數學上就是不夠形成有效範圍**，`baseLean5FloorCeil('tpp')`回傳null，22縣市的TPP分類全部顯示「資料不足」，不是硬套一個假的deep/mid/light。(2) **NPP在2024年的政黨票，實測結果是全台22縣市無一例外全部低於自己2016年的地板**，ratio被`Math.max(0,...)`夾在0，22縣市因此100%都落在npp_deep——這不是bug，是時代力量2024年真的全面崩盤到比自己2016低潮期還低的真實反映，跟前面那個「2024自己當地板/天花板」的bug不同（那個bug是全部政黨在特定情境下都會出現的計算缺陷，這個是NPP數據本身的真實走向）。

**三張獨立Sankey（政黨票/區域立委/總統），縣市層級、5欄(2008→2012→2016→2020→2024)，泛化成通用N欄渲染函式`_base5CycleSankeySvg(years, unitList, leaderFn, weightFn, parties)`，取代舊版寫死3欄的`_baseLeanCycleSankeySvg()`。** 你先前確認「區域立委選區Sankey要縣市層級匯總」（不是逐ED追蹤——2008是單一選區制元年、2020又重劃一次，選區邊界完全對不上，縣市層級才穩定），三張圖因此統一縣市粒度。政黨票Sankey（`_base5PLSankeySvg`）直接用縣市2024政黨票領先黨（`baseLean5PLLeader`），跟分類邏輯共用同一套贏家判斷。區域立委/總統Sankey（`_base5EdSankeySvg`/`_base5PresSankeySvg`）新增`getCountyEdAggr`/`getCountyPresAggr`（2008/2012讀新增的`ED_COUNTY_*`/`PRES_COUNTY_*`，2016讀`ED_2016`/`PRES_VC_2016`，2020讀`ED_2020`/`PRES_VC_2020`，2024讀`VC_ED`/`VC_SIM`的`pres_g/pres_b/pres_w`），統一只分kmt/dpp/other三桶（2008/2012原始候選人資料沒辦法拆出tpp/npp，5屆才能公平比較同一套分類）。三張圖彼此獨立（同一屆政黨票/區域立委/總統領先政黨可能不一致），橘色斜流＝換黨，任一端缺資料的轉場直接跳過不畫假流向。

**清掉的舊code：** `_baseLeanRawCounty`/`_baseLeanAvgCounty`/`_baseLeanCountyCache`（縣市版三屆混合，只有`classifyBaseLeanCounty`用，換掉後整組變死code）、`_baseLeanSankeySvg`（區域vs政黨票單屆Sankey）、`_baseLeanYearLeader`/`_baseLeanCycleSankeySvg`（舊版3欄跨屆Sankey）——三個都只在舊版`_baseLeanNationalSummaryHtml`/`_baseLeanCountyPanelHtml`裡被呼叫，換掉呼叫端後直接刪除整個函式，不留著當死code。ED層級的`classifyBaseLean`/`_baseLeanColor`/`_baseLeanPanelHtml`（點擊單一選區時顯示的舊版3屆混合面板）保留不動——那是選區細節下鑽用的獨立功能，這次沒有動它。

**地圖染色跟圖例同步更新：** `_baseLeanCountyColor`改用新分類（不再有split顏色分支）；`renderMapLegend()`裡`mode==='baselean'`那段圖例文字/深淺說明整段重寫（舊版寫死「≥20pt/10-20pt/5-10pt」margin門檻+分裂投票色塊，新版沒有固定pt門檻，深淺是「2024落在該黨自己地板~天花板範圍的相對位置」，也拿掉分裂投票那行）；工具列標題`'2016-2024 政黨基本盤估算地圖'`改成`'2008-2024 政黨基本盤估算地圖'`（原本漏改，在瀏覽器裡肉眼比對截圖時才發現）。

**驗證方式：** 先用esprima（pip裝的，環境沒有node）個別解析兩段新增/修改的code區塊確認語法正確（原生瀏覽器`?.`optional chaining語法esprima舊版解不動，所以只抽取自己改的區塊測，不測全檔）；本機`python3 -m http.server`起server用claude-in-chrome實際跑`setMode('baselean')`，console.log確認零JS error、面板innerHTML零NaN/undefined、22縣市`<details>`+3個Sankey SVG都渲染出來；逐一抽查苗栗縣/花蓮縣/新竹縣(應該是kmt/deep)、台南市/高雄市(應該是dpp/deep或mid)、台北市(應該是swing，民眾黨在雙北支持度高導致藍綠差距縮小)，結果全部符合對台灣政治版圖的常識預期；截圖比對地圖染色（22縣市合併形狀，不是73個選區）、面板文字、圖例文字，三者一致沒有互相矛盾的地方。

## v2.88 — 新功能：政黨得票趨勢折線圖（2008-2024，縣市/鄉鎮/村里三層通用）

延續v2.87的地板/天花板修正，你問「政黨基本盤維持看到2024，但額外做出縣市/全台/村里等範圍各種分類的折線圖」——地板/天花板/鐵中淺是「這個政黨史上最低點跟最高點各是多少」的靜態快照，沒辦法直接看出「怎麼從A走到B」，折線圖補的正是這一塊：每屆(2008/2012/2016/2020/2024)真實原始得票數畫成一條時間序列，形狀（穩定衰退 vs 暴起暴落 vs 真實成長）一眼就看得出來。

**分組跟著目前的detail/camp3/camp4模式走，不另外發明一套新分類。** `_base5TrendLines(geoKey)`依`_baseLeanDisplayMode`回傳不同的線組：detail模式＝每個追蹤政黨(dpp/kmt/tpp/npp/tsr/tsu/spgp/gp)自己一條線＋「其他(游離)」一條；camp3＝藍營/綠營/第三勢力三條(第三勢力＝tpp+npp+tsr+tsu+spgp+gp+other殘值加總)；camp4＝泛藍/泛綠/中間三條＋「其他」一條(泛綠＝dpp+npp+tsr+tsu+spgp+gp，中間＝tpp獨立)——跟現有長條圖/地圖染色的陣營定義完全共用，不用維護第二套分類規則。

**政黨還沒成立的年份算0，不是跳過不畫。** 你明確要求「如果政黨沒出現就是0」——`_base5YearlyPartyVotes`用`baseLean5CountyPLVotes(...)||0`，跟鐵/中/淺算法(故意排除掉政黨不存在的年份、不然會被當成0拉低平均)是完全不同的處理方式：折線圖要看的是「這個政黨從無到有怎麼長出來」，2019年才成立的民眾黨在2008/2012/2016應該畫成貼齊0的三個點，不是留白斷線，這樣視覺上才看得出「這條線真的是從0開始往上長的」。

**縣市/鄉鎮/村里三種範圍共用同一個modal，不是三個獨立入口。** 你確認選這個做法。scope下拉切「縣市/鄉鎮市區/村里」，unit下拉列出對應範圍的選項——縣市22個、鄉鎮約368個都可以直接塞進一個下拉；村里全國7713個直接列會太長，改成先選鄉鎮(跟鄉鎮scope共用同一份368個選項)、選定後才動態列出該鄉鎮底下的村里(通常幾十個，可以接受)。開modal時如果滑鼠當下正懸浮在某個縣市(`_baseLeanCurCounty`有值)，直接帶入當初始選項，不用每次重選。

**SVG折線圖手刻**(`_base5TrendChartSvg`)：5個x座標點對應2008-2024，y軸自動抓當前所有線的最大值當上限，3條y=0/50%/100%的格線輔助讀值，每條線自己的顏色沿用`BASE_LEAN_PARTY_HEX`/`BASE5_CAMPS_3`/`BASE5_CAMPS_4`既有色票，全程沒有虛構任何數字，5個點都是`baseLean5CountyPLVotes`直接查表的真實原始得票數。全程0票的線自動濾掉不顯示圖例(例如某縣市第2屆前完全沒有第三勢力紀錄的極端案例)。

驗證：22縣市×3種顯示模式(detail/camp3/camp4)+抽樣鄉鎮+抽樣村里，共169組呼叫`_base5TrendChartSvg`零錯誤；高雄市detail模式視覺確認民進黨/國民黨兩條主線走勢符合認知(民進黨2016高點、國民黨2016低點)，民眾黨線正確從2008-2016貼齊0、2020才開始有值，時代力量線2016成立後先升後降(2020高點、2024下滑)，肉眼可讀出這個「先盛後衰」的完整故事；切到camp3模式確認三條線(綠營/藍營/第三勢力)重新聚合正確，第三勢力線從2008的0穩定爬升到2020；切到村里scope、選楠梓區→東寧里，確認能畫出單一村里等級的折線圖，且視覺上跟先前(2026-08-27) council.html那邊獨立發現的「楠梓區持續成長」結論完全吻合(綠營/第三勢力雙雙上升、藍營下滑)，兩個完全不同的分析路徑(local floor/ceiling修正 vs 逐年折線圖)在同一個地方得出一致的結論，互相佐證。

## v2.89 — 選舉人數校正(population/eligible-voter correction)：從鼓山區聚落demo到全國性即時切換功能

起點是council.html的鼓山區聚落案例(`GUSHAN_SETTLEMENTS`)：老聚落(哈瑪星/巖仔/內惟) volatile share穩定收斂在34-36%，新開發區(凹仔底)卻高達53.6%——這個結果太乾淨，反而是懷疑的理由。驗證後確認：快速成長村里(龍水里12年人口+67%、橋頭新莊里4年+73%)的深/中/淺分類，把「單純人多了」跟「真的政治游離」混在同一個數字裡算，校正後龍水里51.6%→21.3%、新莊里67.2%→12.7%，兩個獨立行政區同一個模式重現。

**真正的資料源不用外部抓取——本專案本來就在解析的`elprof.csv`(不分區政黨，2008/2012/2016/2020/2024)裡，選舉人數欄位就在票數欄位旁邊。** 新增`build_legis_elig.py`→`data_legis_elig.js`(`LEGIS_CC_ELIG`/`LEGIS_TC_ELIG`/`LEGIS_VC_ELIG`)。過程中修了兩個解析bug（2012的巢狀引號清洗順序錯誤、2016的檔名其實是`elprof_T4.csv`不是`elprof.csv`），並且靠對照test.html自己既有的`PRES_TURNOUT_COUNTY`縣市層真實選舉人數，反推出2008/2012兩年舊制縣市代碼→現行代碼的完整對照表（2008需要3組真的合併：高雄市+高雄縣、台中市+台中縣、台南市+台南縣；2012單純重新編號），全部落差在0.3%以內。**範圍明確只到縣市層——鄉鎮/村里層的舊代碼對照沒有解，村里層特別牽涉真實邊界合併，留白不假裝解決。**

**上線方式：`baseLean5PartySegments`保留同名，內部改成thin dispatcher，依`_base5EligCorrected` flag決定呼叫原始版(改名`baseLean5PartySegmentsRaw`)還是新的`baseLean5PartySegmentsElig`。** 校正版內部先把地板/天花板/近屆改在「每千選舉人」比率上算(避免母體大小放大票數)，最後再乘回目前顯示年份的實際選舉人數換算回票數單位——這樣所有既有的畫bar/算模型盲區的下游程式碼完全不用改一行，只有這一個dispatcher的分支邏輯是新的。新增「選舉人數校正」切換鈕(EN/中文切換旁邊)。缺資料的年份/geoKey(2008全部、2012村里層)透過既有的`validYears.length>=1`退化邏輯自動跳過，不用寫新的缺口處理。

**全國22縣市驗證：DPP游離度34.2%→13.2%(61%是人口雜音)，KMT 43.7%→29.2%(33%)，第三勢力53.2%→52.8%(幾乎沒變，符合這些政黨天生沒有深地板可以被誇大)。** 用2016+2020反推2024(vs.真實結果)做本專案第一次真正的樣本外backtest：36組county×party，原始版9.81%平均誤差 vs 校正版9.61%(校正版21/36勝)——小幅但一致的改善，誠實框架成「方向對」不誇大成「顯著提升」。另外測試並**明確否決**了進一步用有效票數(投票率)校正的假說——backtest反而變差到10.74%，判讀是選舉人數是純分母雜訊(拿掉安全)，投票率本身帶有真實政治訊號(拿掉會丟掉訊號)，這個負向結果記錄下來、不要重試。

高雄2022市長選舉當作真實案例對照：陳其邁催出率100.6%→98.0%(天花板因為2016的每千人支持率被換算到2024更大的選民基數上，反而變大)，柯志恩83.6%→100.0%(幾乎精確打到校正後的藍營天花板，KMT真正的每人支持率高峰也從2012校正成2020)。延伸抽查另外6個2022選戰，發現同一個模式(綠營普遍低於天花板、藍營普遍超過)在現任者跟新人身上都一致出現——不是現任者優勢，是2022年全國性的藍營浪潮年，模型意外抓到了這個訊號。完整敘事寫成portfolio案例研究：`/Users/wangshien/Desktop/sean-wang-portfolio/case-study-population-correction.md`。

## v2.90 — 不分區獨立頁面：party_list.html

你（延續前一天的討論）決定把不分區(第三勢力/不分區地圖)相關功能從test.html拆成獨立頁面，理由是test.html的側欄同時塞了「區域立委」跟「不分區」兩套完全不同的分析目標，介面越來越擠。原本評估真正的程式碼拆分（把`mode==='third'`相關邏輯搬出test.html），但探勘後發現這條邏輯散落在約84處、跨50個函式，牽一髮動全身，改成採用低風險做法：**party_list.html是test.html的完整副本，兩邊共用同一份程式碼跟資料，只靠CSS `[data-section]`屬性決定哪些按鈕在哪個頁面顯示**——`test.html`隱藏`[data-section="partylist"]`，`party_list.html`隱藏`[data-section="district"]`。綜合PVI（`comp`模式）留在test.html不搬，因為它本質上是選區層級的指標（跟美國Cook PVI一樣是算lean district），只是公式裡混了不分區票的權重。三個頁面（含council.html）左上角新增共用的「區域／不分區／地方選舉」三顆按鈕導覽列，之後要加總統版也是同一個模式往右加。

**同一天稍晚，你進一步要求party_list.html要有自己獨立的主要按鈕**（不是靠隱藏test.html按鈕帶出來）：「歷年分析」欄位改標成直接觸發不分區縣市地圖（`setPLCountyMode`），移除原本掛在警示燈旁邊的舊版不分區地圖按鈕；「第三勢力黨派投票指數」也搬進主按鈕欄，鎖定只顯示`thirdSubMode==='pr'`（不分區席次推演）半邊；test.html對稱地把「關鍵少數」（原本third-force模式的`key`半邊）獨立成自己的按鈕，也搬進主欄。過程中意外修好一個真實的既有bug：按Escape跳過首次導覽視窗時，整段mode相關UI（警示燈、模式按鈕）會卡住不顯示——這正是你一開始回報「看不到第三勢力按鈕」的根本原因，不是新拆分introduce的問題。

驗證：兩頁面反覆切換模式、語言、`?nocache`強制重整，確認按鈕顯示邏輯跟綜合PVI互不干擾；console無錯誤。

## v2.91 — 村里邊界「缺片」系統性抓漏：18個村里

你陸續回報了7個具體村里（忠誠里、國光里等）在地圖上顯示黑洞——查證後發現不是全新的bug，是這個session稍早那次村里地圖全面重建時留下的同一類問題：官方shp檔案裡這些村里其實是2片以上的MultiPolygon（真實的分離地塊，不是雜訊），但topology建置腳本當時只取了面積最大的一片。修復流程延用已經建立好的pipeline：解出缺的那一片、用既有的affine+IDW校正投影到現有座標系、跟鄰近村里的既有arc做`shapely.ops.snap()`貼齊，再插回既有拓樸——逐一比對CEC原始2024票數驗證正確性。

**你提醒「應該不只這幾個，我只是找到這幾個」，於是做了一次全國性系統掃描**（比對官方shp每個村里的real片數 vs. 目前VD的片數，排除離島縣市的真實碎礁雜訊），一次抓到**12個新增案例**（4個剛好對到你回報的地方，8個是掃描主動找到、你還沒看到的）：東石鄉網寮村/掌潭村、瑪家鄉排灣村、大安區南埔里（後來確認是雜訊、沒有修）、綠島鄉公舘村、頭城鎮龜山里、臺西鄉五港村、萬榮鄉萬榮村，加上你原本回報的4個。兩輪加總共18個村里修復，全部用CEC原始資料逐一核對確認是真實地塊而非數位化雜訊，僅1個（南埔里，12公尺寬的4點細條）確認是雜訊、保留原樣未修改。

驗證：全部18個村里`VD`確認正確保持多塊狀態；`NM.v`/`VC_ED`票數資料原本就存在（只有幾何資料缺失），未受影響；三個檔案（test.html/party_list.html/council.html）語法乾淨、console無錯誤。

## v2.92 — 縣市邊界（CD）飛地挖洞：兩輪修復

**第一輪**：你回報台北市、嘉義市滑鼠移過去不會亮（`_setCountyDim()`縣市hover遮罩機制）。查證發現CD（縣市拓樸）建置腳本用`max(rings, key=area)`只保留面積最大的環，把內部飛地（台北市是新北市裡的一個洞、嘉義市是嘉義縣裡的一個洞）的洞孔環也一併砍掉了——沒有洞，遮罩就會整片蓋過飛地，滑鼠移上去自然不會亮。修法：改成保留第一個環（外框）+ 所有非退化的額外環（洞孔），逐一驗證每個額外環確實幾何上被外框包住（不是誤判成洞的獨立離島）才採信。

**第二輪**：上述修法太寬鬆，某次伴隨全國性重拓樸重建後，縣市dissolve產生的接縫雜訊被誤判成大量假洞——全國22個縣市裡14個中招，最誇張的雲林縣有363個假洞、台東縣293個、台中市209個。改用面積門檻（>10個local單位，真洞的量測值是146-693，雜訊洞全部在2以下，門檻留有巨大安全邊界）——全國砍掉1512個假洞，剩下正好4個真洞（台北市/嘉義市之外，另外驗到新竹市、台中市各一個看起來是真的但沒有獨立命名確認的小洞，先保留）。

驗證：兩輪都在瀏覽器實測台北市/嘉義市hover正確排除在遮罩外；第二輪額外確認南投縣（原本87個假洞）視覺乾淨、無雜訊斑點。

## v2.93 — 「撕裂線」bug：三輪攻堅才真正解決

v2.91修完18個村里後，你陸續在party_list.html、council.html（議員/縣市長模式）、政黨基本盤等多個畫面回報鋸齒狀的破圖線——先從你回報的萬榮鄉、延平鄉、達仁鄉開始，後來確認雲林、高雄、台南都有，範圍逐漸擴大成全國性問題。

**第一輪**（不夠）：發現`TC_GJ`/`ED_GJP`（預先算好的鄉鎮/選區地圖）在v2.91修村里時沒有跟著重建，是舊資料——重建後意外抓到建置腳本自己的一個潛藏bug（合併前把每個村里MultiPolygon砍到只剩最大一塊，這個bug一直都在，只是v2.91新增的村里剛好踩到），順便救回其他本來就該是多塊卻被默默砍掉的村里。

**第二輪**（看起來修好、其實沒有）：你持續回報同樣的問題，追查發現整個VD拓樸本身18個新村里的邊界片段跟鄰居沒有真正共享arc（只是座標貼近，不是同一條線）——app裡14處以上即時呼叫`topojson.merge()`的地方全部都會踩到。做了一次全國重新拓樸建置，但**驗證方法是錯的**：只測了`shapely.unary_union().is_valid`（純幾何聯集，完全不管arc是不是真的共享），結果錯誤地回報「已解決」。

**第三輪**（真正解決）：你持續回報同樣的鋸齒（延平鄉/達仁鄉/雲林/高雄/台南），於是寫了一個真正比對arc index共享狀態的檢查腳本（`village_boundary_fix/check_arc_sharing.py`）——一測發現全國363個鄉鎮裡58個明顯異常、**全國中位數共享率只有48.7%**，證實是普遍性的品質問題，不是幾個地方壞掉。深入查證根本原因：不是座標精度問題（幾何上重合的點確實是逐位元相同），是不同處理歷史的村里（原始密度 vs. 曾經被簡化過）在共同邊界上插入了不同數量的中繼頂點，導致topojson要求的「整段點序列逐點相同」永遠對不上。修法：明確找出被2個以上村里共用的精確交界點，把每段邊界切開、取所有擁有者的點聯集、只簡化一次再寫回所有擁有者——用結構保證共享，不再賭運氣。

驗證（這次用4種獨立方法交叉確認）：arc共享比例檢查58→25個異常鄉鎮、中位數48.7%→53.6%；長度加權的全國逐對邊界檢查（更精確的量測，排除大範圍海岸線鄉鎮造成的統計失真）196.2→13.2個單位（93%下降，剩全國總長度的0.028%）；直接用瀏覽器實測`topojson.merge()`跑達仁鄉6個村里，修復前是invalid+29個洞+GEOS例外，修復後valid+0個洞+跟真實聯集完全一致；額外用council.html自己實際在用的`_councilBuildTCFeats()`（就是產生原始截圖那條鋸齒線的同一段程式碼）再次確認。**誠實保留2個已知殘留**：全國0.28%的村里邊界對仍未完全共享（多是短線段，agent自己的防護機制主動放棄處理避免破圖）；雲林縣一個鄉（10009040）有另一類「單點親吻」拓樸問題，不在這次修復範圍。

## v2.94 — 2008/2012區域立委：第一階段（資料準備，尚未接上網站）

你詢問是否能快速加入2008/2012年的區域立委地圖，起初評估是大工程（可能需要重建那兩年的實際選區邊界），但你指出「2008/2012應該都跟2016一樣，只有2008因為五都改制（高雄市/縣合併等）選區編號不同」——實測比對2012年raw CEC資料的縣市選區數 vs. test.html現有的2016年結構，**全22縣市逐一比對完全吻合**，證實2012可以直接沿用現有2016邊界。2008同樣真實選區跟2016相同，只有五都改制的5個縣市（台北縣→新北市、台中市+台中縣、台南市+台南縣、高雄市+高雄縣）選區編號不同，需要從2008原始CEC資料反推正確分組。

第一階段完成：解析出`ED_2008`/`TC_2008`/`ED_2012`/`TC_2012`（候選人層級票數，比照現有`ED_2016`/`TC_2016`格式）、`TC_TO_ED_2008`（5個合併縣市的鄉鎮→2008年選區對照表，2012直接沿用現有`TC_TO_ED_2016`）、`ED_GJ_2008`（只需重建5個合併縣市的選區地圖，其餘17個縣市沿用現有`ED_GJ_2016`）。過程中抓到真實的名稱陷阱：高雄縣三民鄉合併後改名那瑪夏區（避免跟高雄市三民區撞名），naive字串比對會誤判成同一個地方；也處理了板橋市這種2008年選票剛好接近50/50分裂兩個選區的邊界案例（地理上複製進兩個選區、票數則正確依CEC原始逐選區分組，不用猜）。數字校驗：2008全國57國民黨/13民進黨席（吻合史實的國民黨大勝格局）、2012全國44/27席（吻合較接近的選情）。

**第二階段（把這些資料接進test.html約60處分散的年份判斷邏輯、加下拉選單選項）尚未開始**——刻意等v2.93的拓樸修復完全穩定驗證後才動工，避免2008專屬的合併縣市地圖資料因為底層村里/鄉鎮形狀又變動而作廢重做。

同一天也順帶查證：官方村里界歷史圖資（data.gov.tw dataset 130549，這個session稍早重建2024年地圖的同一個資料源）**最早只到106年(2017)**，2008/2012/2014完全沒有官方歷史邊界圖資可用——確認Phase 2（每個年份用當年真實邊界渲染）在這幾個目標年份上目前不可行，不影響上述用現有邊界回推的做法。

## v2.95 — 導覽列/UI一致性收尾：5顆按鈕、綠光按鈕、邊界粗細，順便揪出一個潛藏很久的CSS優先權bug

延續v2.90拆分成三個獨立頁面（區域立委/不分區立委/地方選舉）之後，這次把導覽列擴充成完整5顆：加入「2026縣市長預測」（連到council.html並用`?goto=2026predict`直接觸發`setMayor2026PredictMode()`，不用使用者自己再點一次側欄）跟「政黨基本盤」（連到test.html並用`?goto=baselean`直接觸發`setMode('baselean')`）快捷按鈕，超出側欄寬度改成橫向捲動（`overflow-x:auto`）並補上可見的粗捲軸樣式＋滾輪垂直轉橫向的JS，修正原本只能精準拖曳極細捲軸才能捲到後面按鈕的問題。**council.html的導覽列這次才發現漏掉了`overflow-x:auto`**——task-28擴充5顆按鈕時只確實套用到test.html/party_list.html，council.html還停在舊的`overflow:hidden`，代表超出可視範圍的按鈕根本無法點到，是真的功能性bug，不只是體驗問題。

**黨基本盤跳頁bootstrap時序bug**：`?goto=baselean`快捷按鈕點進去後偶爾會先閃一下正確畫面、接著跳出一個不相干的「選哪個政黨角度看」引導視窗，關掉後畫面整個變回區域立委歷史勝選模式而不是政黨基本盤。根因：`window.onload`裡原本無條件先呼叫`setMode('winner')`（註解說是為了同步UI鎖定狀態），但`setMode('winner')`自己的引導視窗判斷用的是`_winnerPartyAskedThisLoad`這個**每次讀頁都會重設**的旗標（不是localStorage），導致每次新載入都會觸發引導視窗並提早`return`；緊接著的`setMode('baselean')`雖然有把畫面正確切過去，但那個還沒關掉的舊引導視窗，被使用者按Escape關掉時會呼叫自己的收尾邏輯把mode蓋回`'winner'`，把已經切好的畫面又蓋掉。修法：用既有的`_skipOnboardGateOnce`旗標，只在偵測到`goto=baselean`時讓那次同步用的`setMode('winner')`跳過引導視窗判斷，正常（沒有`goto`參數）進站時的首次引導視窗行為完全不受影響（已在瀏覽器重新驗證兩種路徑）。

**「全選區村里」綠光按鈕跟「2026縣市長預測」紫色按鈕，追出一個活了很久的CSS優先權bug**：這兩顆按鈕的HTML原本就正確寫著各自的自訂顏色（frosted-glass綠、淡紫），但畫面上顯示卻是統一的灰色——源頭是共用的`style.css`裡有一條全站通用的`.btn { background/border/color: ... !important }`規則，而原本用來讓「全選區村里」跳脫這條規則的例外樣式，選擇器寫的是`button.btn[style*="1b9e4b"]`（比對inline style裡有沒有出現這個十六進位色碼字串）——但按鈕實際用的是十進位的`rgba(27,158,75,...)`寫法，字串比對永遠對不上，這條例外規則形同虛設，多半在專案更早期就已經存在。追蹤方法是直接在瀏覽器用`document.styleSheets`列出所有真正命中這個元素的CSS規則，而不是憑肉眼讀`style.css`猜——「2026縣市長預測」按鈕還多一層優先權陷阱：一條專門給「按鈕+年份下拉選單」膠囊容器用的通用樣式(`#toolbar div[style*="border-radius:6px"][style*="overflow:hidden"] .btn.active`)靠著`#toolbar`這個祖先ID疊加兩個屬性選擇器，優先權比單一ID的例外規則還高，修法是把例外規則也補上`#toolbar`前綴湊到兩個ID才蓋得過去。兩顆按鈕的例外樣式改成同時比對舊寫法(hex)跟新寫法(rgba)兩種字串，並且改成跟其呈現目的一致的「霧面模糊+外光」frosted-glass風格，不是實心滿版色塊。

**不分區地圖（party_list.html）鄉鎮/村里邊界顏色跟粗細，補齊跟test.html/council.html一致**：鄉鎮邊界原本用`rgba(0,0,0,.15)`(太淡)，村里邊界原本錯用白色`rgba(255,255,255,.35)`（跟其他兩個頁面的黑色邊界完全不同調），統一改成黑色系、粗細也對齊。另外「全選區村里」（縣市層級一次攤開所有村里）模式原本完全沒有鄉鎮邊界疊圖層，只看得到一片密密麻麻的細村里線、分不出鄉鎮界線在哪——補上跟test.html/council.html的`goVillAll()`同款的`.ft-tc-border`疊圖層（更粗更深的線疊在村里細線之上）。

驗證：三個檔案（test.html/party_list.html/council.html）逐一osascript語法檢查全過；瀏覽器實測5顆導覽按鈕橫向捲動+滾輪轉橫向；`?goto=baselean`跟一般進站兩種路徑分別確認引導視窗行為正確、無殘留mode不同步；`getComputedStyle()`直接確認兩顆按鈕改色後的實際渲染值（不只是看inline style原始碼），跟council.html本身既有的「全選區村里」按鈕逐像素比對一致；花蓮縣全選區村里模式視覺確認鄉鎮邊界清楚可辨。

## v2.96 — 政黨基本盤選舉人數校正：補齊鄉鎮/村里層2008/2012資料缺口（全國性，非新竹市個案）

你回報新竹市「選舉人數校正」開啟後，縣市層顯示藍營小贏，底下三個行政區（東區/北區/香山區）卻全部顯示第三勢力大贏，問是不是有問題。查證後確認：縣市層KMT的地板/天花板計算看得到全部5屆（2008-2024），正確抓到2012是真正的天花板年（每千選舉人361.6票，比其他年份都高出一截）；但鄉鎮層選舉人數資料（`LEGIS_TC_ELIG`）完全沒有2008/2012——查了全國368個鄉鎮，兩年都是0筆，只有縣市層(`LEGIS_CC_ELIG`)有完整涵蓋。鄉鎮層天花板只能停在2020年，系統性低估KMT，且不限新竹市——任何政黨真正的地板/天花板剛好落在2008或2012年的地方，鄉鎮/村里層都會出現同樣的縣市層/鄉鎮層不一致。既有的防呆機制(`_base5MajorPartyDataMissing`)只擋得住「完全查無資料」，擋不住這種「有資料但少了2屆」的部分缺口，地圖不會顯示灰色警示，而是直接畫出一個看起來正常、其實少算兩屆的顏色。

**根因**：`build_legis_elig.py`的縣市層crosswalk（比對`PRES_TURNOUT_COUNTY`同年總數）只解到縣市層，鄉鎮/村里層的舊代碼從來沒有對照過，一直是文件裡誠實記錄的已知缺口。這次補上：CEC的`elbase.csv`跟`elprof.csv`放在同一個資料夾，前者有每個代碼對應的**真實中文地名**——不用再靠代碼運算或票數量級猜測，直接用地名精確比對（配上「鄉/鎮/市/區」字尾去除後的次要比對，處理縣轄市→鎮/鄉等純字尾升格），鄉鎮層兩年都做到368/368全比對成功、零模糊。過程中先試過「拿選舉人數量級去比對最接近的現代鄉鎮」，98%正確但另外2%悄悄比對錯——例如高雄縣三民鄉(併入高雄市後改名那瑪夏區，是刻意改名避免跟既有的高雄市三民區撞名)，量級比對法完全猜錯成撞名的三民區，這正是這個app已經在別處踩過一次的同一個陷阱，這次靠地名比對法（加一個手動override）徹底避開。村里層同樣用地名比對，但不做字尾模糊比對（村里邊界真的有歷史合併/改名，模糊比對容易撞錯到不同的地方）——2012涵蓋94.6%、2008涵蓋82.0%，查無比對的村里誠實留白，不硬猜。

驗證：縣市層資料維持完全不變（remap邏輯沒動）；鄉鎮層現在兩年都是368/368筆；抽查已知案例——新竹市東區2008 elig=133729、2012=144118（跟舊代碼直接查詢的原始值完全吻合）、那瑪夏區(64000380)拿到正確的小鄉鎮數字、原本會撞名的三民區(64000050)數字不受影響；重跑`_base5CountyCampTotalsSorted`確認新竹市東區/北區現在正確顯示KMT領先（跟縣市層一致），香山區顯示第三勢力以不到3個百分點些微領先——這次是真實的地方差異，不再是全區一致的系統性失真。`data_legis_elig.js`重新生成後osascript語法檢查通過。

## v2.97 — 8月28/29日修復的48個村里，補上政黨基本盤逐屆政黨票資料

8月28日那次「修復48個2024年缺失村里」的工程（`d9c1eab`），只把這些村里的2024區域立委資料（`VC_ED`/`TC_STATS`/`ED_STATS`）補了回來，政黨基本盤(baselean)用的是完全獨立的另一組資料表(`PL08_VC`/`PL12_VC`/`PL16_VC`/`PL_VC_2020`/`PL_VC_2024`)，這次沒有一起補，記錄在8月29日的memory待辦裡。你回報高雄市鼓山區延平里在政黨基本盤模式查無資料，查證後確認正是這個已知待辦——不只延平里，全部48個村里的5屆政黨票資料都是空的。

**抓資料的方法**：CEC不分區政黨的`elctks.csv`裡的候選人編號是「當屆實際登記政黨」的臨時流水號，不是`elpaty.csv`裡固定的政黨登記證號（同一個政黨每年編號都不一樣）——直接靠`elcand.csv`（候選人編號→政黨全名）查得到名稱，但要換算成這個app用的政黨代碼(kmt/dpp/tpp/...)還是需要交叉比對。解法：挑約13個全國各地已經有完整資料的鄉鎮村里當「羅塞塔石碑」，用它們在`elctks.csv`裡每個候選人編號的實際得票數，逐一比對回它們在既有`PL0X_VC`表裡對應政黨代碼的已知票數，交集多個村里的比對結果後，2016/2020/2024三年全部零模糊、每個候選人編號都精確解出對應代碼（2016年特別處理：這年的schema把民進黨/國民黨票數存進`g`/`b`兩個陣營合計欄位，不是`dpp`/`kmt`個別欄位，沿用既有慣例）。

**2008/2012兩年格式不同**：這兩年的CEC原始檔案還在用舊式縣市/鄉鎮編碼（不是這個app現行的代碼），沿用今天稍早修選舉人數校正時已經建好的地名比對crosswalk（`village_boundary_fix/vc_crosswalk_08.json`/`vc_crosswalk_12.json`），先把目標村里跟參照村里都轉換成舊代碼再去查`elctks.csv`。

**結果**：48個村里裡，27-28個成功補到全部5屆資料；剩下約20個（集中在新北市/桃園市）只有2024年資料——查證這不是bug，是這些村里在2020年之前根本還沒被劃出來（行政區劃分割產生的新村里，真的沒有更早的歷史可查），跟這個app其他地方「民眾黨2019年才成立、2014/2018沒資料合理回傳null」是同一種誠實留白，不是資料抓漏。10013280006(三和村,瑪家鄉)刻意排除在這次範圍外——它的基礎地理圖形本身都還沒修好（連現行官方shapefile都查無這個村里），沒有地圖可以掛資料上去。

新增`backfill_pl_vc_48villages.py`（永久保留在專案根目錄，附完整方法論註解），輸出存到`village_boundary_fix/pl_vc_backfill_48villages_result.json`；三個檔案(test.html/party_list.html/council.html，各自獨立內嵌一份`PL0X_VC`)都同步patch了157筆新資料（27+28+27+27+48）。驗證：延平里(64000020033)重跑`_base5MajorPartyDataMissing`從true變false，`classifyBaseLeanCounty`正確分類成民進黨深色，地圖不再是灰色查無資料；全數48個目標村里逐一檢查，28個完全解除「查無資料」警示，剩餘20個維持誠實的灰色（跟預期一致，不是殘留bug）；三個檔案osascript語法檢查全過。

**當天稍晚，你憑在地知識抓到一個我沒查到的個案**：台南市關廟區龜洞里，你指出這個村里2018年被台南市府跟田中里合併改名「南雄里」，因為在地居民連署正名成功，2022-01-01才恢復「龜洞里」舊名。這解釋了為什麼一開始的48村里清單裡，龜洞里只補到2012跟2024兩屆——不是「2020年後才劃出來的新村里」（跟其他20個真的查無更早資料的村里完全不同性質），而是**2016年它還是獨立村里、只是當年用的是舊編號(村里代碼015，不是現行的020)**，我原本的程式直接查現行代碼所以沒查到；2020年則是真的無法拆——當年它的票數整個併在「南雄里」名下，官方原始資料本身就沒有分開列，沒辦法回推切割。查證後補回2016年資料(用當年的舊代碼015重新查)，2020年維持誠實留白(無法可靠拆分)，2008年本來就查無對照，也維持留白。

**接著你要求幫龜洞里這類「有特殊歷史原因缺屆」的村里，跟另外20個「單純是新村里」的，在地圖面板上做出清楚區分的標示說明**，不要讓使用者以為都是同一種「資料漏抓」。新增`BASE5_VILLAGE_DATA_NOTES`（個案專屬說明，目前只有龜洞里一筆，逐字解釋合併/正名/哪幾年缺資料跟為什麼）跟`BASE5_NEW_VILLAGE_NOTE`+`BASE5_NEW_VILLAGE_VCS`（20個新村里共用同一段「這是2020年後劃出的新行政區，本來就不存在更早資料」說明），套用在村里懸浮面板`_baseLeanVillPanelHtml`裡，跟現有的通用「本村里缺2008或2012資料」提示疊在一起顯示（不衝突，通用提示講camp3/camp4模式為什麼不顯示陣營加總，這個新提示講「為什麼」缺資料，兩個回答不同問題）。council.html本身沒有baselean功能（只有連到test.html的導覽捷徑），所以這次只改test.html/party_list.html兩個檔案。

驗證：`_base5VillageDataNoteHtml('67000290020')`含龜洞里專屬說明文字，20個新村里之一(`68000010080`)含新村里共用說明，一個完全正常補齊的村里(`64000020033`延平里)回傳空字串（不顯示多餘提示）；瀏覽器截圖確認龜洞里面板正確顯示黃底警示框，文字清楚呈現合併/正名/缺屆年份與原因；兩個檔案osascript語法檢查通過。

## v2.98 — 網站瘦身：83-88MB → 13-15MB，四個頁面共用資料改成外部檔案

使用者回報上面導覽列切換頁面（區域立委/不分區立委/地方選舉/2026即時開票）明顯很慢，但同一頁裡面的功能按鈕（模式切換、下鑽選區）反應正常——這個對照本身就是重要線索：如果D3渲染邏輯是瓶頸，頁內操作也會慢；只有「換頁」specifically慢，指向的是換頁時要重新下載的資料量，不是JS執行效能。

**先量體積，不是先猜**：寫了一個Python工具掃過test.html所有頂層`const`宣告、量出各自的實際大小。結果：85MB裡有73MB是資料（縣市/村里邊界拓樸、五屆選舉逐候選人票數），真正的app邏輯只有12MB。前14個變數就佔了73MB裡的絕大部分，單一最大的一個（鄉鎮邊界GeoJSON）就有26.75MB。

**驗證「一樣」不能只憑變數名字一樣就假設**：四個檔案（test.html/council.html/party_list.html/live2026.html）裡都有同名的這些變數，直覺上應該是同一份資料重複貼了四次——但沒有直接假設，逐一對每個候選變數在四個檔案裡的值算SHA256 hash比對。結果14個裡有13個確認逐位元完全一致，安全合併成一組共用外部檔案；第14個（`VC_ED`，村里層2024區域立委票數）在test.html/party_list.html跟council.html/live2026.html兩個家族之間真的有差異——直接diff出實際的得票數字落差，這是一個獨立的資料正確性問題，跟效能瘦身完全是兩回事，沒有為了合併方便就武斷選一份覆蓋另一份，這個變數保留成兩份分開的外部檔案（`data_vc_ed_legis.js`/`data_vc_ed_council.js`），差異本身留待之後另外查證根因。

**精準切割85MB文字裡的資料區塊**：一開始用純Python逐字元掃描（追蹤字串邊界/大括號深度）來找每個值的真正結尾，正確但在這個檔案大小上跑到逾時。改用Python內建`json.JSONDecoder().raw_decode()`（C實作）——反正這些值本身就是合法JSON，不用自己再刻一次parser，速度快了將近100倍，正確性不變。

**搬移過程踩到一個自己造成的bug**：把驗證過的資料寫進新的外部檔案後，把原本HTML裡的`const`宣告位置換成`<script src="...">`。第一次執行完瀏覽器噴一堆「Unexpected token '<'」——追查發現這些大型`const`宣告原本是包在同一個既有、還沒關閉的`<script>`標籤裡面，插入的`<script src>`等於在JS原始碼中間硬塞了一段HTML標籤，被當成程式碼解析當然報錯。修法是在插入點前後各補一個`</script>`跟`<script>`，把原本那個大區塊切開、讓外部檔案標籤自己獨立成一個`<script>`元素，不影響後面接續的JS執行順序（同一份文件裡後面的`<script>`區塊本來就看得到前面區塊宣告的全域變數，切開不影響變數可見範圍）。

**結果**：test.html/party_list.html從83MB降到13MB，council.html/live2026.html從86-88MB降到15MB。共用的3個外部檔案（`data_map_v64.js`約55MB、`data_vd_v64.js`約7MB、`data_region_v64.js`約6.5MB）四個頁面共用同一份，瀏覽器第一次訪問後就會快取——直接對應到使用者原本回報的「換頁很慢」，因為換頁時這70MB共用資料完全不用重新下載，只剩每頁自己13-15MB的部分。

驗證：抽出的資料重新hash比對回原始值確認逐位元相同；改寫後的5個新增外部檔案跟4個HTML檔案都用osascript真實JS引擎語法檢查過；瀏覽器實測四個頁面——年份切換、選區/鄉鎮/村里逐層下鑽、2026預測模式全部點過一輪，確認渲染結果跟改動前完全一致、無console錯誤，才把改動swap進正式檔案。順手發現並修正一個獨立的`.gitignore`遺漏：`emblems/`（56個政黨logo圖檔）跟這次新增的外部資料檔從來沒被放行過，導致上線網站的政黨icon全部404，一併補進白名單。

## v2.99 — 全站英文翻譯缺口：導覽列/政黨基本盤/即時開票模式，以及根因townshipTranslationMap全站是空字典

使用者連續回報好幾處英文模式沒有正確顯示英文：導覽列按鈕、政黨基本盤（baselean）全台總覽面板（含逐分類票數/Sankey流向解說等詳細內容）、即時開票（live2026.html）整頁、以及多處鄉鎮市區/縣市名稱本身。逐一查證後發現這些其實是幾種不同的根因，不是同一個bug到處出現：

**導覽列按鈕**：`區域立委`/`不分區立委`/`地方選舉`等6顆按鈕的文字直接寫死在HTML裡，完全沒有`data-en`可以切換，四個檔案都補上雙語標籤。

**政黨基本盤/即時開票模式沒有語言切換重繪**：底層文字其實都已經用`T(zh, en)`包好雙語，但`applyLang()`（語言切換觸發的重繪函式）完全沒有對應這兩個模式的分支——跟council.html之前修過的即時開票applyLang()缺重繪是同一個bug類別，只是這次出現在test.html的baselean模式跟live2026.html自己的即時開票全台總覽層。額外發現政黨基本盤全台總覽的「逐分類票數」詳細內容實際上是寫進`#hemi-row`（取代原本73選區半圓餅的位置），不是`#ic`——原本的重繪修法只碰到`#ic`，漏了`#hemi-row`，所以看起來像沒修好，其實是修對了地方卻沒碰到真正顯示內容的DOM節點。

**縣市/鄉鎮名稱在政黨基本盤模式直接印中文原始字串**，沒有經過`translateCounty()`/`translateTownship()`——這類是單純漏呼叫既有翻譯函式，逐一補上。

**根因（範圍比預期大很多）**：追查鄉鎮市區名稱為什麼完全沒有英文時，發現`townshipTranslationMap`這個字典**全站從頭到尾都是空的**——不是baselean模式單獨的問題，是`translateTownship()`這個函式在所有模式下從來沒真的翻譯過任何鄉鎮市區名稱。原因是程式碼原本想從地圖資料本身自動抓鄉鎮英文名(`VD.objects.towns`)，但實際的TopoJSON資料裡根本沒有這個結構（只有`VD.objects.villages`）。縣市名之所以還看得到英文，是因為另外手動寫死了22個縣市的中英對照表當備援；鄉鎮市區（全台368個）從來沒人補這份備援清單。

**修法沒有手動填368筆**：比照專案裡已經在用、同樣「查表查不到就即時轉羅馬拼音」的候選人姓名翻譯備援機制（`_pinyinNameFallback`，處理2600+位候選人不可能逐一手填的問題），寫了同款`_pinyinTownshipFallback()`——市/鎮/鄉/區自動對應英文慣用單位詞尾(City/Town/Township/District)，其餘字元用pinyin-pro即時轉羅馬拼音、音節直接相接（跟既有`villageTranslationMap`的"Zhongmen Village"命名慣例一致）。test.html/party_list.html原本沒載入pinyin-pro的CDN，這次一併補上（council.html/live2026.html已經有，是候選人姓名翻譯在用）。

驗證：抽測多個鄉鎮市區（士林區→Shilin District、頭份市→Toufen City、三峽區→Sanxia District等）皆與官方英文譯名一致；四個檔案瀏覽器實測語言切換來回，導覽列/政黨基本盤全台總覽（含逐分類票數詳細內容）/即時開票整頁/縣市與鄉鎮市區名稱皆正確切換，無console錯誤。
