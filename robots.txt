# Personal, non-commercial technical portfolio project.
# Not ready to be publicly discoverable yet — disallow all crawlers for now.
# (Note: robots.txt only asks well-behaved crawlers not to crawl; the real
# "don't show up in search results" mechanism is the <meta name="robots"
# content="noindex"> tag on each page, which works even for a page that
# does get crawled. Both are used together here.)
User-agent: *
Disallow: /
